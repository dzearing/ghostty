---
date: "2026-08-05"
---

# Daily digest — Tuesday, August 5

## Yesterday

Yesterday was a **reliability day**, and a good one — 17 tasks landed, almost
all of them the kind a user actually feels:

- **Making a window taller no longer deletes your scrollback** — the most
  user-visible bug of the week, root-caused to the terminal core importing
  history rows that the Windows console child then repaints over.
- **A crash no longer loses the windows it left running**, and two restored
  windows no longer fight over focus while the screen is locked. Together with
  "a launch command is no longer eaten by the windows coming back", session
  restore is now trustworthy in the three ways it was quietly broken.
- **Closing a finished pane no longer kills a working one** next to it.
- The `+read` command stopped reporting an empty-but-healthy terminal as a
  broken one — every failure message now names a distinct real state.
- The loop's own supervisor became visible on the dashboard, revives itself,
  and no longer mistakes a relaunched session for a dead one. The
  intermittent test-lane crash was honestly concluded **not reproducible and
  not caused by the code** — it is parked as an armed watch that captures a
  full stack by itself if it ever returns (your call on D12 confirmed that).

## Today's focus

- This morning the watchdog learned not to nudge a session that is merely
  busy, and a fresh **P0 was filed from your own report: dragging a nested
  split divider makes it jump**. That is exactly the kind of feel-defect that
  should stay at the head of the queue.
- The queue's next stretch is dominated by **test-lane flakiness** — five of
  the six ready P0s are intermittent agent-test failures (spin-waits, timing
  assumptions, a temp-dir race). More on that below.
- One task is mac-seat (`T30`, the IPC modal-block fix) and sits high in the
  queue; this box cannot validate it, so it waits for the Mac seat.

## Reflection

**What keeps recurring:** the same three themes account for most of the last
week — (1) session persistence edge cases, now largely paid down; (2) the
loop's own liveness plumbing (watchdog, locks, probes), which has needed
repair four times; (3) **agent-test flakiness**, which is now most of the P0
queue. When five open P0s are all "this test is timing-sensitive under load",
the sound move is not five fixes — it is one **deflake pass** over the agent
test harness: a shared "wait for condition, never a fixed yield count"
helper, plus a soak runner that runs a lane N times under deliberate CPU load
so a timing assumption fails in review, not in the queue. I would rate that
the highest-leverage infra work available right now.

**A directive worth propagating:** your D11 note — *no Claude Code specifics
in a general terminal app beyond skills that make it work with Ghoztty* — is
bigger than the decision it answered. The pane-probe work that identified
"a pane running claude" should be re-examined: anything in the product that
pattern-matches one particular tool's UI belongs in the loop's own scripts,
not in Ghoztty. I would file a task to sweep for Claude-isms in product code
and move them out.

**Is the UX clean?** The chrome geometry is asserted, but two known rough
edges remain user-visible: the win32 title bar still interpolates the raw
`needs_input` token (T465), and the divider-drag jump you hit yesterday. The
viewer story on Windows is behind the Mac by exactly one feature — the git
diff pane (T463/T464) — which is also the biggest CLI-visible parity gap.

**Gaps between here and merging back into main:** (1) the 24 open P0s, half
of them flakes; (2) the win32 diff viewer; (3) the mac-seat backlog this box
cannot touch — it needs a session on the Mac to drain, or it will become the
long pole; (4) no standing **divergence inventory** against upstream ghostty
— before a merge can be planned, something has to enumerate what this fork
touches that upstream also moved. I would add a task to generate that
inventory mechanically (files changed here vs upstream since the fork point)
so the merge-back stops being an unknown.

**Process:** decisions now carry structured pros/cons with mitigations and a
recommended pick — you cleared all nine open ones within minutes of the
format landing, which suggests the bottleneck was the format, not your
attention. The digest you are reading is the same bet applied to the day as
a whole.

## Decisions

- All nine open decisions were resolved today (D8–D16) — nothing awaits your
  call this morning.
- Your D11 note (no Claude-specifics in the product) and D9 answer (move the
  scrollback fix into core resize semantics with a plumbed flag) both imply
  follow-up tasks; D9's is real engineering and should enter the queue with a
  design pass first, per its mitigation.
