---
id: D75
title: "An uncommitted CLAUDE.md to docs/claude/ refactor is sitting in the working tree - commit it, or leave it for whoever started it?"
task: "T467"
kind: "assumption"
status: "open"
created: "2026-08-13T05:28:48Z"
assumed: "Left it entirely untouched and committed only the files this task changed, by explicit path. Nothing was staged, discarded or rebased."
options:
  - key: o1
    label: "Leave it for its author, commit only the task\u0027s own paths (Recommended)"
    pros:
      - "a half-finished refactor is never published under another task\u0027s name"
      - "a live session\u0027s tree is not yanked out from under it"
      - "the change is still there, exactly as its author left it"
    cons:
      - "the work stays unpushed, so it dies with the box"
      - "every future turn sees a dirty tree and has to re-decide"
    mitigation:
      - "this receipt, plus a log entry naming the refactor, so the next turn does not have to re-derive what it is"
  - key: o2
    label: "Discard it and restore CLAUDE.md"
    pros:
      - "unambiguous clean tree"
    cons:
      - "destroys work no one has reviewed"
      - "irreversible - the files are untracked, so there is no reflog to recover them"
  - key: o3
    label: "Commit and push it as its own docs: commit"
    pros:
      - "the work becomes durable and visible to the other seat"
      - "the tree is clean for every later turn"
    cons:
      - "publishes a refactor nobody verified is finished - docs/claude/ is untracked, so a missing file would break every routing link in CLAUDE.md"
      - "if a session is mid-edit it would be committing a snapshot of a moving target"
    mitigation:
      - "read all seven docs/claude files end to end and check every link CLAUDE.md\u0027s routing table names before committing"
answer: null
answeredAt: null
note: null
---

# D75 - An uncommitted CLAUDE.md to docs/claude/ refactor is sitting in the working tree - commit it, or leave it for whoever started it?

## Why this needs a call

The tree was already dirty when this turn claimed the loop: CLAUDE.md is down 2626 lines, seven untracked files sit in docs/claude/, and a dozen source and script files have had their CLAUDE.md cross-references rewritten to point at them. No task in the tracker covers it and next -Claim reported NEXT rather than RESUME, so it is not a stale claim either. go.md says an unpushed commit dies with the box, which argues for committing it; the standing rule against touching another session's work argues against. T467 needed to write its audit into a doc, and the natural home (docs/claude/win32-ui.md) is one of the untracked files - it went into docs/design/win32-design-system.md instead, which is tracked and is the rulebook the routing table points at anyway.
