---
id: D12
title: "T443 has no reproducible crash left - park it as an armed watch instead of hunting it further?"
task: "T443"
kind: "assumption"
status: "resolved"
created: "2026-08-05T00:28:34Z"
assumed: "Left T443 open but stopped hunting; wrote the evidence up, filed T480, and pointed the next turn at T477 instead. Nothing was reverted or disabled."
options:
  - key: o1
    label: "Park T443 as an armed watch (Recommended)"
    detail: "Pros: the floor lanes already capture a full minidump and all-thread stacks on the next real occurrence, so waiting forfeits no evidence; the crash-day binaries and analysis stay archived; nothing is dishonestly closed. Cons: a possible live memory bug stays open with no owner and could resurface at a worse time. Mitigation: a standing rule that the next captured crash promotes T443 straight back to P0 with the new dump attached, plus a periodic agent-lane soak so occurrences are actively hunted rather than only awaited - file the soak as a task if this stands."
  - key: o2
    label: "Keep hunting at P0"
    detail: "Pros: guarantees continued attention on a possible memory-corruption bug. Cons: no named experiment remains that could produce new information - the trigger did not reproduce in 79 runs, the byte-identical binary soaked clean, and CPU contention and reboot are both excluded - so further hunting re-runs excluded hypotheses instead of testing new ones; this is an evidence problem, not an effort problem. Mitigation: none exists until a new hypothesis or a new crash appears, which is exactly what the armed watch waits for."
  - key: o3
    label: "Treat it as fixed and close it"
    detail: "Pros: none beyond a shorter open-task list. Cons: dishonest - nothing was fixed - and closing discards the crash-day binaries and the analysis a recurrence would need, so the next occurrence starts the investigation from zero. Mitigation: none; the discard is the option."
answer: "o1"
answeredAt: "2026-08-05T14:24:59Z"
note: null
---

# D12 - T443 has no reproducible crash left - park it as an armed watch instead of hunting it further?

## Why this needs a call

Every experiment this turn came back clean, including a soak of the byte-identical binary that produced this morning's dump. A binary that cannot be changed by a commit cannot be fixed by one, so the source bisect this task was queued for cannot find the trigger; CPU contention and a machine reboot are both excluded too. That leaves no experiment anyone has named that is cheaper than waiting for the next occurrence, which the harness now captures by itself.

## Resolution

Chosen: Park T443 as an armed watch (Recommended)

Resolved 2026-08-05T14:24:59Z.
