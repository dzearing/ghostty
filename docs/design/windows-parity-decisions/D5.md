---
id: D5
title: "Should T449 (is the machine or the toolchain at fault?) outrank T443 (bisect our own code)?"
task: "T449"
kind: "assumption"
status: "open"
created: "2026-08-04T10:54:24Z"
assumed: "Filed T449 ahead of T443 in this turn\u0027s write-up and told T443 not to bisect until T449 rules, but did not reorder the Current-priorities list itself - that is the user\u0027s call."
options:
  - key: o1
    label: "Do T449 first"
    detail: "One turn spent on memory tests and a second zig binary. If the box is at fault, it retroactively explains every flaky result since 2026-08-01 and saves T443 entirely; if it is clean, T443 resumes with one hypothesis eliminated."
  - key: o2
    label: "Keep T443 first"
    detail: "Keeps the momentum of two contexts of narrowing. But those contexts assumed the corrupter was in code the win32 and agent lanes share, and a crashing compiler shares none of it - so the bisect may be searching a space the bug is not in."
answer: null
answeredAt: null
note: null
---

# D5 - Should T449 (is the machine or the toolchain at fault?) outrank T443 (bisect our own code)?

## Why this needs a call

T444 found that zig.exe itself takes access violations on this box, three times in two days at three different fault offsets. T443's founding premise is that the corrupter lives in code the win32 and agent lanes share and the none lane does not; the compiler shares none of that code. Nothing in this turn could distinguish a machine fault from a toolchain bug from our own code, and the experiment that would is cheap now that the repro is 6/6 in under a minute.
