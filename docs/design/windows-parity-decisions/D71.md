---
id: D71
title: "Should the win32 filter boxes fold Unicode case the way macOS does, and if so with Windows\u0027 linguistic search or an in-tree fold table?"
task: "T790"
kind: "assumption"
status: "open"
created: "2026-08-12T10:26:20Z"
assumed: "T288 hoisted both filter boxes onto one ASCII fold (text_search.containsIgnoreCase) and documented the limit there, so behaviour is unchanged today and the widening is now a one-function change whenever this is answered."
options:
  - key: o1
    label: "Use Windows\u0027 own linguistic search, FindNLSStringEx with LINGUISTIC_IGNORECASE (Recommended)"
    pros:
      - "matches what macOS localizedCaseInsensitiveContains actually does, locale and all"
      - "it is the platform\u0027s own answer, so it tracks Windows\u0027 casing rules for Turkish i, final sigma and the rest without us owning a table"
      - "no case data vendored into the tree to go stale"
    cons:
      - "an OS call, so the fold itself leaves the pure none lane"
      - "each keystroke converts the needle and every candidate row to UTF-16"
      - "behaviour varies with the user\u0027s locale, which is harder to pin in a test"
    mitigation:
      - "keep the policy (empty needle matches, trim rules, which fields are searched) pure and tested as now, with only the fold behind one function - the same pure/impure split ViewerWorktreeProbe uses; convert the needle once per filter pass rather than per row"
  - key: o2
    label: "Keep the fold ASCII deliberately and close T790"
    pros:
      - "nothing to maintain, nothing to convert, the fastest and most predictable of the three"
      - "machine names and Windows process names are overwhelmingly ASCII in practice"
    cons:
      - "a real, permanent divergence from the Mac at two mirrored surfaces, which is the shape of gap this project explicitly does not ship"
      - "the user who hits it sees an empty list and no explanation, which reads as a broken filter rather than a limitation"
    mitigation:
      - "none that keeps the behaviour - at most the filter box could say why it found nothing, which is a worse fix than folding correctly"
  - key: o3
    label: "Vendor an in-tree Unicode simple-case-fold"
    pros:
      - "stays pure, so it keeps testing in the none lane exactly as today"
      - "deterministic across locales, which makes an acceptance arm trivial to write"
      - "no per-keystroke UTF-16 conversion"
    cons:
      - "simple case folding is not what the Mac does, so the two platforms would still disagree on the locale-sensitive letters"
      - "a fold table is case data we then own and have to refresh with Unicode revisions"
      - "more code in the tree for a search box"
    mitigation:
      - "generate the table from the Unicode data file with the generator checked in, so a refresh is a re-run rather than hand editing"
answer: null
answeredAt: null
note: null
---

# D71 - Should the win32 filter boxes fold Unicode case the way macOS does, and if so with Windows' linguistic search or an in-tree fold table?

## Why this needs a call

The two win32 filter boxes hand-rolled an ASCII fold each; T288 merged them into one function and had to record whether ASCII is the intended alphabet. Checking the Mac sources showed it is not: both mirrored filters there use localizedCaseInsensitiveContains, so Windows silently finds nothing for needles the Mac matches. Which way to close that is a product and architecture call - the platform-native answer costs the pure test lane, and the pure answer does not fully match the Mac - so it is filed rather than assumed.
