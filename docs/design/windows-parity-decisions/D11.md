---
id: D11
title: "Should the pane probe identify Claude Code by its transcript GLYPHS, or only by its chrome wording?"
task: "T440"
kind: "assumption"
status: "open"
created: "2026-08-04T23:11:46Z"
assumed: "Added the glyphs (U+25CF bullet, U+23BF tool-result connector) as a fallback after the word markers, plus \u0027tokens)\u0027 for the busy line. Rule 1 - a shell prompt at the bottom of the pane - still outranks both, so a claude that exited is still classified as a shell."
options:
  - key: o1
    label: "Keep the glyph fallback"
    detail: "A non-Claude full-screen TUI that draws U+25CF could be classified as \u0027claude\u0027. The watchdog would then TYPE the resume prompt at it instead of running the shim - wrong, but the safe direction: it cannot execute anything."
  - key: o2
    label: "Word markers only"
    detail: "Every marker is version-specific chrome. It has drifted twice already, and the failure mode is silent: an unrecognised pane reads \u0027unknown\u0027, and the watchdog then types a shell command path into a live TUI - the T241 defect, which is the loud direction."
answer: null
answeredAt: null
note: null
---

# D11 - Should the pane probe identify Claude Code by its transcript GLYPHS, or only by its chrome wording?

## Why this needs a call

Probing this very pane on 2026-08-04 returned 'unknown': every existing marker belongs to the IDLE composer, and a Claude Code that is working scrolls all of them off. Goal 3 of T440 depends on the classifier answering correctly for a live claude pane, so shipping without a fix would have shipped a feature that does not work on this box. The choice is between a marker set that drifts with each Claude Code release and a structural signal that has not.
