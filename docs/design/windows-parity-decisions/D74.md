---
id: D74
title: "Where should T463\u0027s viewer documentation live while another session\u0027s CLAUDE.md split is still uncommitted?"
task: "T463"
kind: "assumption"
status: "resolved"
created: "2026-08-13T04:39:21Z"
assumed: "Wrote the diff-pane documentation into docs/claude/viewers.md (where the content now lives) and left it UNSTAGED, so T463\u0027s commit carries only code, tests and tracker files; the full prose is also recorded in T463\u0027s own task file."
options:
  - key: o1
    label: "Leave it in the untracked docs/claude/viewers.md and let the refactor commit carry it (Recommended)"
    pros:
      - "one source of truth stays one"
      - "the refactor lands the paragraph in the right file with no merge"
      - "nothing of another session\u0027s half-finished work is committed by me"
    cons:
      - "if the refactor is abandoned the paragraph is lost with it"
      - "the doc update is not in T463\u0027s own commit, so a reader of that commit sees no docs change"
    mitigation:
      - "the same prose is written into docs/design/windows-parity-tasks/T463.md, which IS committed, so it can be re-applied from there"
  - key: o2
    label: "Wait for the other session to commit, then update the file"
    pros:
      - "no duplication and the update is durable"
    cons:
      - "blocks this turn on another agent\u0027s timing, which THE CONTEXT RULE forbids"
      - "that session may take hours, and an unpushed turn is invisible work"
  - key: o3
    label: "Commit docs/claude/viewers.md on its own"
    pros:
      - "the doc update is durable and lands with the feature"
    cons:
      - "HEAD would then hold BOTH the old CLAUDE.md text (macOS only so far) and the new file that contradicts it - two sources of truth, which is the thing this project refuses"
      - "it commits a fragment of another session\u0027s in-flight refactor without the router that makes it reachable"
answer: "o1"
answeredAt: "2026-08-14T18:02:45Z"
note: "Resolved by Claude (user delegated technical calls, 2026-08-14). Rides D75: the refactor commit carries docs/claude/viewers.md, and the same prose is already in the committed T463 task file so nothing can be lost either way."
---

# D74 - Where should T463's viewer documentation live while another session's CLAUDE.md split is still uncommitted?

## Why this needs a call

Another Claude session is concurrently splitting CLAUDE.md into docs/claude/*.md; that whole tree is untracked and the working-copy CLAUDE.md is already their router version, so the section T463 has to update exists only inside their uncommitted work. Editing HEAD's CLAUDE.md is impossible without fighting them for the file, and committing their new file alone would leave the repo asserting two different things about win32 diff panes.

## Resolution

Chosen: Leave it in the untracked docs/claude/viewers.md and let the refactor commit carry it (Recommended)

Resolved by Claude (user delegated technical calls, 2026-08-14). Rides D75: the refactor commit carries docs/claude/viewers.md, and the same prose is already in the committed T463 task file so nothing can be lost either way.

Resolved 2026-08-14T18:02:45Z.
