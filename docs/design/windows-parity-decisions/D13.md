---
id: D13
title: "Should an empty terminal pane make +read succeed with empty output, or fail with its own distinct error?"
task: "T181"
kind: "assumption"
status: "resolved"
created: "2026-08-05T01:11:59Z"
assumed: "Succeed with empty output, like tail of an empty file - and give every OTHER failure its own message so none of them can be mistaken for this one."
options:
  - key: o1
    label: "Succeed with empty output (Recommended)"
    detail: "Pros: matches the standard mental model (tail of an empty file), so no caller needs Ghoztty-specific knowledge; callers stop recording a phantom failure as a verdict; and with every remaining error renamed to a distinct real state, exit 1 now always means something is genuinely wrong. Cons: a caller that wants the one-bit signal 'this pane has printed nothing' must check output length itself. Mitigation: the length check is one line in any caller, and a --fail-if-empty opt-in flag can be added later without breaking anyone if a real caller turns out to need the old signal - file it as a task if one appears."
  - key: o2
    label: "Fail with a distinct message (e.g. 'pane is empty')"
    detail: "Pros: keeps a nonzero exit for the empty case, so exit-code-only callers still get a machine signal. Cons: that signal is wrong - empty is the normal state of every pane for its first fraction of a second and the permanent state of anything silent, so exit-code-only callers keep treating a healthy brand-new pane as broken, the exact hazard the task exists to remove. Mitigation: none; the failure is inherent in making a normal state an error."
answer: "o1"
answeredAt: "2026-08-05T14:25:09Z"
note: null
---

# D13 - Should an empty terminal pane make +read succeed with empty output, or fail with its own distinct error?

## Why this needs a call

A pane that has printed nothing is the normal state of EVERY pane for the first fraction of a second of its life, and the permanent state of a pane running something silent. The old behaviour reported it with the same message as a missing or wedged pane, so a caller that reads once records 'no output' as a verdict. Both options remove the ambiguity; they differ in whether an empty pane is still an error condition at all.

## Resolution

Chosen: Succeed with empty output (Recommended)

Resolved 2026-08-05T14:25:09Z.
