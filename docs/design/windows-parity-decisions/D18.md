---
id: D18
title: "Should live orphaned agent sessions ever age out, or stay pinned forever?"
task: "T521"
kind: "assumption"
status: "open"
created: "2026-08-06T14:06:28Z"
assumed: "Shipped only the visibility half (T411 log line); filed T521 for opt-in aging and did not build any reaping - today\u0027s keep-forever behavior is unchanged."
options:
  - key: o1
    label: "Opt-in TTL, default keep-forever (Recommended)"
    pros:
      - "today\u0027s guarantee stays the default"
      - "a user who accumulates orphans gets a remedy that never needs manual hunting"
      - "the clock resets on attach so daily-use sessions can never age"
    cons:
      - "adds a config knob and an agent-side timestamp to reason about"
      - "a misconfigured short TTL could reap something the user meant to keep"
    mitigation:
      - "default 0 = keep forever, and the chooser (T520) plus the restore log (T411) make what would be reaped visible before it ever is"
  - key: o2
    label: "Age by default (e.g. 30 days)"
    pros:
      - "the leak heals itself for every user with no configuration"
    cons:
      - "silently destroys a running process the user was told persists - breaks the session-persistence promise"
      - "a shell left for a long trip would die"
  - key: o3
    label: "Keep forever, no aging ever"
    pros:
      - "simplest possible contract - a persistent session is never destroyed by policy"
      - "zero risk of reaping a wanted session"
    cons:
      - "a leaked live session holds a real shell and one of 256 slots until found by hand"
      - "the 256 cap has already been hit once (T278 episode)"
answer: null
answeredAt: null
note: null
---

# D18 - Should live orphaned agent sessions ever age out, or stay pinned forever?

## Why this needs a call

T411 proved live pinned orphans accumulate invisibly (4 live sessions, layout held 3, one orphaned since 2026-08-02). The pin exists so sessions survive their viewer; whether an unattached one may EVER expire is a product-promise question only the user can settle, including the default N.
