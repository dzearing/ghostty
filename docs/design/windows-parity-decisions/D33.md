---
id: D33
title: "How should a Windows release be cut, given main has no Windows source at all?"
task: "T577"
kind: "assumption"
status: "open"
created: "2026-08-08T00:58:49Z"
assumed: "Widened release-windows.yml to also fire on \u0027win-v*\u0027 tags and cut win-v1.31.0 on users/dzearing/windows-amd64, so the workflow runs today off the only ref that can build a Windows terminal. The existing \u0027v*\u0027 trigger is untouched, so a tag cut from main takes over automatically once the branch merges back."
options:
  - key: o1
    label: "Tag the Windows branch with win-vX.Y.Z (Recommended)"
    pros:
      - "it is the only option that can build at all - the tag\u0027s tree carries both the workflow and src/apprt/win32"
      - "ships the Windows source that actually exists, features included, so the release matches what the branch has been building for weeks"
      - "needs no change to main, so nothing here can turn a macOS release red"
      - "the win-v tag and the win-v release tag become the same object, which is what installed builds already scan for updates"
      - "reverts to itself at merge-back: the untouched v* trigger takes over with no rewiring"
    cons:
      - "cutting a Windows release is a second deliberate act rather than riding the macOS tag push"
      - "the version number ties the two platforms by convention, not by automation, so they can drift if someone forgets"
    mitigation:
      - "the version-number tie is asserted by the website acceptance script, which already fails when the tag version and the filename version disagree; a follow-up can add a release-time check that the newest win-v matches the newest v"
  - key: o2
    label: "Wait for the branch merge-back"
    pros:
      - "fewest moving parts, and the end state arrives without an intermediate design"
    cons:
      - "the lag is unbounded and 729 commits deep"
      - "Windows users keep being offered a July build for however long that takes"
      - "it leaves the release path unproven right up to the moment it is load-bearing"
  - key: o3
    label: "Keep publishing by hand with scripts/publish-windows-release.ps1"
    pros:
      - "it is the on-box path T38 kept working and it needs no CI at all"
    cons:
      - "it is exactly the needs-a-human-to-remember process T38 was written to replace"
      - "it has not been run since 2026-07-19, which is the whole reason this task exists"
      - "it produces releases from whatever happens to be in a working tree, with no record of the ref"
  - key: o4
    label: "Cherry-pick the release path onto main"
    pros:
      - "one tag push would publish both platforms, which is the end state everyone wants"
    cons:
      - "it cannot work today - main\u0027s apprt enum is gtk/none/embedded, so the build step fails before it starts"
      - "it would make every future macOS release show a red Windows run, which trains everyone to ignore the signal"
      - "any fix that makes it build means checking out a different ref than the tag, so the published version would not describe the source it came from"
answer: null
answeredAt: null
note: null
---

# D33 - How should a Windows release be cut, given main has no Windows source at all?

## Why this needs a call

release-windows.yml was written to ride the same vX.Y.Z tag push as the macOS release, and that turns out to be impossible before merge-back: a workflow runs from the tree of the ref that triggered it, and origin/main carries no Windows frontend whatsoever. So the release either happens from the Windows branch or it does not happen. What is genuinely arguable is whether to publish a Windows build off a branch that has not merged back - the version number will match a macOS release whose core source is different.
