---
id: D15
title: "When a launch command and a session restore both apply, should Ghoztty do both, or should the command win?"
task: "T406"
kind: "assumption"
status: "resolved"
created: "2026-08-05T04:47:05Z"
assumed: "Implemented 'do both': the requested window is created first (so it is the surface that consumes initial-command), restore rebuilds the rest, and the requested window is raised to the front."
options:
  - key: o1
    label: "Do both (shipped) (Recommended)"
    pros:
      - "The only option that drops nothing the user asked for - the command typed just now runs AND the windows they left behind come back"
      - "Opening the requested window first guarantees a restored pane can never swallow the command; raising it afterwards keeps the thing they asked for in front"
    cons:
      - "A -e launch that also restores shows extra windows"
      - "-e implies quit-after-last-window-closed, so the app now lives until every restored window closes too"
    mitigation:
      - "Scope the -e quit rule to the requested window itself (quit when IT closes, regardless of restored windows) as a follow-up task if the longer lifetime proves surprising in practice"
  - key: o2
    label: "Command wins, skip restore"
    pros:
      - "A -e launch stays a clean one-shot with exactly one window"
    cons:
      - "The previous windows silently fail to come back on that launch - sessions stay alive in the agent so no work is lost, but the windows are missing with no sign why, which reads as data loss"
    mitigation:
      - "A visible notice explaining where the windows went would blunt the surprise, but the restore still has to happen on some later launch, so the confusion is deferred rather than removed"
  - key: o3
    label: "Keep today's behavior and document it"
    pros:
      - "No code change and no new behavior to stabilize"
    cons:
      - "The typed command is still silently dropped - no window, no error, no log line"
      - "A documented defect is still a defect the user must discover by reading docs at the exact moment something confusing happened"
    mitigation:
      - "None; the silent drop is the thing being fixed"
answer: "o1"
answeredAt: "2026-08-05T14:29:30Z"
note: null
---

# D15 - When a launch command and a session restore both apply, should Ghoztty do both, or should the command win?

## Why this needs a call

T406 asked to answer this from the Mac side first. The Mac side cannot answer it: it has the SAME defect (AppDelegate opens the initial window only when there is no pending restore, and the shared core rule hands initial-command to whichever surface is app.first - on a restoring launch, a restored pane). So there was no precedent to cite and the call had to be made here. I picked 'do both' because it is the only option that drops nothing the user asked for: the command was typed just now, and the windows are real work they left behind.

## Resolution

Chosen: Do both (shipped) (Recommended)

Resolved 2026-08-05T14:29:30Z.
