---
id: D50
title: "T532 did not reproduce - keep hunting it under the incident\u0027s real conditions, or park it as an armed watch?"
task: "T532"
kind: "assumption"
status: "resolved"
created: "2026-08-09T12:30:38Z"
assumed: "Left T532 open at the head of the queue with the eliminated ground recorded in its progress log, and landed the two liveness arms (F8b, C6d) so a recurrence is caught by a test rather than by the user. Filed T652 to sweep the same gap out of the remaining restore scripts."
options:
  - key: o1
    label: "Keep hunting, one named variable at a time (Recommended)"
    pros:
      - "the variables are named and finite, not a fishing trip"
      - "the alternate-screen path is genuinely different code (skip_replay), so it is a real hypothesis rather than a guess"
      - "a P0 that loses a user\u0027s live sessions deserves the contexts"
    cons:
      - "could burn several turns and still end in an armed watch"
      - "the harness needs new capability - putting a ConPTY session on the alternate screen and aging its ring - before the first experiment can run"
    mitigation:
      - "build the harness capability as its own task so the effort is reusable by every future session-persistence test, not spent only on this hunt"
  - key: o2
    label: "Rebuild the incident wholesale - restore the exact two builds and drive a real Claude Code session for hours"
    pros:
      - "the only approach that reproduces every variable at once, including the ones nobody has thought of"
    cons:
      - "no isolation - a reproduction proves the freeze exists without saying which variable caused it"
      - "needs the older agent build restored and hours of wall clock per attempt"
      - "would run against the user\u0027s own install lineage, which the endpoint-isolation rule exists to prevent"
    mitigation:
      - "none that keeps the isolation guarantee; would need a full second lineage staged from source"
  - key: o3
    label: "Park it as an armed watch, the way T443 was under D12"
    pros:
      - "the arms that would catch a recurrence are already in place, so parking costs no detection"
      - "stops a P0 sitting at the head of the queue soaking up turns that produce no fix"
    cons:
      - "T443 earned its parking after being hunted hard across several contexts; this has had one, with named leads still unexplored"
      - "the user hit this once and lost their working state - parking reads as deciding it will not happen again"
    mitigation:
      - "park only after the alternate-screen variable is tried, so the watch is armed on evidence rather than on fatigue"
answer: "o1"
answeredAt: "2026-08-09T13:31:21Z"
note: "Overtaken by events, recorded rather than left open: the hunt was continued exactly as this option describes (one named variable at a time) and it landed. The alternate screen was eliminated by a new fixture that actually reaches the skip_replay branch, the build skew was eliminated by a diff showing the attach and streaming code identical across the two builds, and the answer turned out to be inside variable 1 - a resume point recorded against a byte stream that no longer exists. Fixed and reproduced on demand; T532 is closed. No answer was needed from you, so none is being invented - this is the record of what happened."
---

# D50 - T532 did not reproduce - keep hunting it under the incident's real conditions, or park it as an armed watch?

## Why this needs a call

The reported freeze - restored panes paint correctly and are then dead to input and output - does not reproduce at HEAD on either re-attach path, including the hard kill the upgrade script performs. The two agent-side suspects named at filing are refuted: both unbind paths refuse to clear a bridge that is not theirs, and have since June and July. What is left is a set of conditions the harness cannot yet create: sessions hours into their rings (43MB and 7.4MB in the incident), almost certainly a TUI on the alternate screen, an agent one build older than the app, and a 17-second gap with no viewer.

## Resolution

Chosen: Keep hunting, one named variable at a time (Recommended)

Overtaken by events, recorded rather than left open: the hunt was continued exactly as this option describes (one named variable at a time) and it landed. The alternate screen was eliminated by a new fixture that actually reaches the skip_replay branch, the build skew was eliminated by a diff showing the attach and streaming code identical across the two builds, and the answer turned out to be inside variable 1 - a resume point recorded against a byte stream that no longer exists. Fixed and reproduced on demand; T532 is closed. No answer was needed from you, so none is being invented - this is the record of what happened.

Resolved 2026-08-09T13:31:21Z.
