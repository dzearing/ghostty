---
id: T153
title: "ghoztty +list --pid=<pid> never matches on a session-persistence box"
order: 32
deps: ["T89d"]
status: "done"
priority: "P0"
triage-reason: "+list --pid, a documented verb, always fails on a persistence box - i.e. the default config. Every pane reports pid 0."
tags: ["fix", "test"]
commits: ["fb0edb5f1"]
---

# T153 — ghoztty +list --pid=<pid> never matches on a session-persistence box

## Summary

**`ghoztty +list --pid=<pid>` never matches on a session-persistence box** — found 2026-07-29 during T139 while mapping claude pids to panes. CLAUDE.md documents `--pid` as *"the tty-less way for a process inside a pane to discover its own pane"*, but `+list --pid=$PID` and `--pid=<a real pane's claude pid>` both fail with `IPC request failed` (exit 1) against the live release app, and plain `+list --json` shows why: **every** terminal leaf reports `"pid":0` (and `"tty":""`), because agent-backed panes' shells are children of `ghoztty-agent.exe`, not of the app, so the app has no pid to walk ancestry from. Persistence is on by default, so this is the normal case, not an edge one — the documented fallback route is dead on every pane. T113's `$GHOZTTY_PANE_ID` is the better answer and works, so the user-facing impact is small, but a documented CLI verb that always fails is worse than one that says it cannot help. Do: have the app ask the agent for each session's child pid when the pane is agent-backed (the agent already reports `pid` in `+sessions --json` — session `c00af3d8…` correctly reported pid 1008), fill `pid` in `+list --json`, and make `--pid` walk ancestry against it; if a pane genuinely has no pid, `--pid` should exit with a message naming `$GHOZTTY_PANE_ID` as the route to use. Note T98 (bogus local-session pid) is adjacent — check whether that fix and this one are the same plumbing. Validation: extend `pane-id.ps1` with a section that resolves this pane via `--pid` on a persistence-on box, plus a `+list --json` assert that `pid` is non-zero for an agent-backed pane

## In plain terms

A program running inside a Ghoztty pane is supposed to be able to ask "which
pane am I in?" by giving Ghoztty its own process id. On Windows with the
default configuration this always failed with an unhelpful generic error,
because the panes' shells run under a background helper process and the app
didn't know their process ids. The lookup itself was fixed by adjacent work;
this task verifies that on the real setup, makes the failure message tell you
the better route (the `GHOZTTY_PANE_ID` environment variable every pane
already has), and locks it all in with automated checks.

## Goals

- [x] `+list --json` reports a real, non-zero pid for agent-backed panes
- [x] `+list --pid=<pid>` resolves a process inside a pane to its pane name
- [x] A no-match failure names `$GHOZTTY_PANE_ID` as the route that always works, instead of a bare `IPC request failed`
- [x] Automated coverage in `test/win32/pane-id.ps1`

## Validation criteria

- [x] `+list --json` leaf `pid` is non-zero for an agent-backed pane on a persistence-on box — pane-id.ps1 A7 (ALL PASS run, 2026-08-06), plus a live check on the debug build (pid 14796, shell a child of zig-out ghoztty-agent.exe)
- [x] `+list --pid=<pid of a process in the pane>` prints that pane's name, exit 0 — pane-id.ps1 A8, plus live: `--pid=<shell pid>` and `--pid=<ping child pid>` both printed the pane UUID with rc=0
- [x] `+list --pid=<unrelated pid>` exits 1 with a message naming `$GHOZTTY_PANE_ID` — pane-id.ps1 A9, plus live `--pid=4` showing the full message
- [x] Floor lanes green and P1–P3 ALL PASS — `floor-lane.ps1 -Lane all`: none/win32/agent all PASS; ipc-p1/p2/p3 ALL PASS, all re-run on the final binary

## Progress log

- 2026-08-06 00:00: claimed; work starting.
- 2026-08-06 00:15: root cause resolved by prior work — the agent already reports each session's child pid (OPENED/ATTACHED/RELAUNCHED), `Remote.publishProcessInfo` caches it, and T41 (2026-08-02) gave `Surface.shellPid` a `.remote` branch that returns it for local agent panes. Verified live on a persistence-on debug build: leaf pid non-zero, `--pid=<shell pid>` and `--pid=<ping child pid>` both resolve to the pane name.
- 2026-08-06 00:25: remaining gaps fixed — the win32 no-match error now explains itself and names `$GHOZTTY_PANE_ID` (IpcHandlers.zig), and the CLI prints the server's error message instead of swallowing it into a bare `IPC request failed` (cli/list.zig). pane-id.ps1 grew asserts A7–A9. Both paths re-verified live on the rebuilt binary.
- 2026-08-06 00:02 [session 742fef8c-6234-44d1-b149-3f95f23868f8]: Investigation: the pid plumbing already exists end-to-end - agent reports pid in OPENED/ATTACHED/RELAUNCHED (server.zig), Remote.publishProcessInfo caches it in child_pid, and T41 (2026-08-02, after this task was filed) moved shellPid onto Surface with a .remote branch that returns child_pid for local agent panes. So the core defect is likely already fixed; remaining work = live verification on a persistence-on debug build, the no-match error message naming GHOZTTY_PANE_ID, and the pane-id.ps1 validation section (which has no --pid coverage today).
