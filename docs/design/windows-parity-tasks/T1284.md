---
id: T1284
title: "The chooser close-chord check fails its own positive control, so it can say nothing about the chord"
order: null
deps: ["T1100"]
status: "done"
commits: []
seat: "win"
priority: "P1"
triage-reason: "A positive control that fails means the script proves nothing at all, and this one guards a chord that can close a pane full of work - the expensive failure it was written for."
tags: ["test", "infra"]
---

# T1284 - The chooser close-chord check fails its own positive control, so it can say nothing about the chord

## In plain terms

There is a check that Ctrl+W closes the machine chooser without also closing a
terminal pane behind it - the mistake that would throw away someone's work. On
the hidden test desktop the keystroke never seems to arrive, so the check's own
warm-up fails and it can say nothing either way. Either make the keystroke land
there, or have the check say plainly that it could not press the key.

## Goals

- [x] the positive control passes on the background desktop, or the missing capability is declared
- [x] `chooser-close-chord.ps1` is green there, or skips with a named capability
- [x] the red is re-attributed in writing: harness or product

## Summary

chooser-close-chord.ps1 fails its mandatory positive control (Ctrl+W closed one pane (2 -> 2)) on the background test desktop - the shape of a chord that never arrived rather than one that was mishandled. Decide whether the posted-chord route can carry it there; if it cannot, declare the missing capability so the script SKIPs (lib\DesktopCapability.ps1) instead of reporting a red against T603.

## Validation criteria

- [x] The suite red is REPRODUCED on demand, not guessed at: `chooser-close-chord.ps1
      -ForceConfirm` (`confirm-close-surface = always`) produces the 2026-09-02 suite
      log assertion for assertion - pane count frozen at 2, the chooser still opening,
      the owner window never re-enabling. Verified by running it.
- [x] The chord DOES reach the background desktop, so no capability is missing and
      nothing skips. Verified: `chooser-close-chord.ps1` ALL PASS (16 assertions) on
      the test desktop, with the two new assertions among them.
- [x] The modal no longer spreads. Under `-ForceConfirm` the run scores 2 FAILURE(S)
      (14 passed), both inside the positive control, and sections 2 and 3 are green -
      where the same condition previously reddened all three sections.
- [x] The idle precondition is named in the output rather than assumed:
      `both panes' shells are idle before the chord (2 shell(s) idle)`.
- [x] Floor: `scripts\floor-lane.ps1 -Lane all` green; P1-P3 green; the six audits
      `guard-due` named for the edit (isolation-meta, launch-preflight, verdict-exit,
      cleanslate, stderr-capture, desktop-launch) ALL PASS.

## Progress log

- 2026-09-02 10:05: claimed; work starting.
- 2026-09-02 10:23: root cause found and reproduced: the positive control's Ctrl+W raises Surface.close's MODAL confirmation when the pane's shell is not descendant-free, which freezes the pane count and leaves the owner disabled - so one wrong assertion became three. -ForceConfirm (confirm-close-surface=always) reproduces the 2026-09-02 suite log assertion for assertion. Not a desktop capability gap: the posted chord reaches the background desktop fine. Fix: lib\PaneIdle.ps1 (idle oracle + dialog reader), waited-for idle precondition, and the dialog named and dismissed.
- 2026-09-02 12:00: status: in-progress -> done
