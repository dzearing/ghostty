---
id: T680
title: "test\\win32 sweep: scripts that dial IPC with no private endpoint (context-menu-real-input reads the user\u0027s panes)"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P1"
triage-reason: "A test that dials the user\u0027s own IPC endpoint can disturb their live panes, which the standing rule forbids. The fix is harness-only, so it stays out of M1."
tags: ["test", "infra"]
---

# T680 - test\win32 sweep: scripts that dial IPC with no private endpoint (context-menu-real-input reads the user's panes)

## In plain terms

Some of the automated tests talk to whichever Ghoztty happens to be running
instead of the one they just built — which, when a person runs them, is the
terminal that person is sitting in. At best the test grades the wrong build and
reports a pass about it; at worst it reads the contents of the user's own panes,
or opens and closes windows in them. Every test should drive its own private
copy, and today a handful still do not.

## Goals

- [x] Every `test\win32` script that makes an IPC call claims a private
      endpoint before the first one
- [x] `context-menu-real-input.ps1` in particular stops reading whatever panes
      it finds
- [x] The rule is checkable, not remembered — a script that regresses is caught
      by something other than a human noticing

## Summary

Filed 2026-08-09 from T89i/T485. T485 fixed `session-persistence.ps1` (it now
calls `Set-GhozttyTestIsolation` before its pre-flight) and asked for the sweep
its own note describes: "any other script under `test\win32\` that runs a CLI
call before dot-sourcing `CleanSlate.ps1` or calling
`Set-GhozttyTestIsolation` has the same latent defect."

A first pass over all 80+ scripts found **seven that never establish a private
endpoint at all** and still drive the CLI. Ranked by what a run actually does
to the box:

| Script | What it does unisolated |
|---|---|
| `context-menu-real-input.ps1` | `+list --json` **and `+read --name=…`** against whatever answers, plus a `Start-Process $exe` that loses the single-instance race — reads the user's pane contents |
| `log-append.ps1` | two `+list` runs; measures the wrong instance's log |
| `agent-autostart.ps1` | launches an app instance with no suffix |
| `path-selfheal.ps1` | ditto |
| `update-check.ps1` | ditto, plus `+version` |
| `crash-diagnostics.ps1` | ditto |
| `cli-shim-redirect.ps1` | `+version` only — no IPC, lowest risk, listed for completeness |

`test-desktop-spike.ps1` also has no isolation call of its own but runs under
`lib\TestDesktop.ps1`, which drops the variable — verify rather than assume.

## Details

The evidence above is a TEXT-ORDER scan (first `$Exe`/`+verb` line vs. first
isolation line), which is why it is stated as a candidate list and not a
verdict: the same scan flags ~13 more scripts as "LATE" purely because they
define a `Run-Cli` helper above the isolation call, which is correct code. Any
sweep has to check EXECUTION order — the first CLI *invocation*, not the first
mention.

That is also the argument for goal 3. The durable fix is not editing seven
files; it is making the property checkable, e.g. an `Assert-GhozttyIsolated`
that every script inherits, or a meta-test that greps for a CLI verb in a
script with no `Set-GhozttyTestIsolation`/`CleanSlate` and fails the suite. A
list curated by hand goes stale the next time someone adds a script.

## Validation criteria

- [x] Each of the seven scripts above either claims a private endpoint before
      its first CLI call, or carries a comment saying why it cannot (with what
      protects it instead) — verified by reading each in execution order, then
      by the isolation-meta scan going green over the whole tree
- [x] `context-menu-real-input.ps1` run from a Ghoztty pane demonstrably reads
      its OWN pane: `Assert-GhozttyIsolated` passes in it — the assert is now
      called in `Start-Gui` after every launch; proven from this Ghoztty pane
      with the identical launch shape + suffix (private endpoint before launch,
      answering exe is zig-out's, caller's pane absent from the tree). The full
      mouse run was blocked twice by a foreground input lock (Windows Terminal
      held it), which aborts the script at its own SETUP gate before any input;
      the mouse assertions themselves are unchanged by this task
- [x] A meta-check exists that fails when a `test\win32` script makes a CLI
      call with no isolation, and it is proven to bite — reverting
      `agent-autostart.ps1` to HEAD made `isolation-meta.ps1` exit 1 naming
      exactly that file; its section A re-proves the bite on synthetic
      fixtures every run, and the `isolation-meta` guard row makes any edit to
      `test\win32\*.ps1` re-demand a run
- [x] The affected scripts still pass after the change — isolation-meta,
      cli-shim-redirect, log-append, path-selfheal, agent-autostart,
      update-check, deliver-windows-build, crash-diagnostics all ALL PASS on
      the box; all 17 edited files parser-clean; msi-upgrade not runnable
      (throwaway MSI fixtures need the Docker msitools image, Docker down =
      SKIP path) — its change is an env-set before any logic and is covered by
      the parser check + the scan

## Related

- T485 (the same defect in `session-persistence.ps1`, fixed), T441
  (`lib\Isolation.ps1`), T118 (`CleanSlate.ps1`), T350 (build-mode isolation).

## Progress log

- 2026-08-16 20:25: claimed; work starting.
- 2026-08-16 20:40 [session ce06d683-15ef-4426-80b9-62a2232bddff]: Verified all 7 candidates by reading execution order. Real offender: agent-autostart.ps1 (no suffix; +list/+sessions dialed the pane's baked endpoint = the user's app). Fixed with Set-GhozttyTestIsolation + Assert-GhozttyPrivateEndpoint. path-selfheal launches now suffixed; crash-diagnostics was a false positive (no CLI at all) and carries the isolation:none marker. update-check/log-append/cli-shim-redirect/context-menu-real-input upgraded from hand-rolled fixed suffixes to per-PID Set-GhozttyTestIsolation; context-menu-real-input also asserts Assert-GhozttyIsolated after each launch. New meta-check test\win32\isolation-meta.ps1 (presence scan + built-in bite fixtures) found 8 MORE scripts: msi-upgrade + deliver-windows-build got real isolation (+version dials the user's app from a pane); agent-pipe/sessions-agent-build (hermetic via LOCALAPPDATA), morning-refresh/guard-due/harness-exitcode-audit (comment-only verbs), ipc-fake-server (server half) carry reviewed isolation:none markers. Guard row 'isolation-meta' covers test\\win32\\*.ps1. Bite proven: reverting agent-autostart made the scan go red naming it.
