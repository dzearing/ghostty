---
id: T581
title: "win32\u0027s divider fallback is a fixed #808080 while Mac derives it from the background"
order: null
deps: []
status: "done"
commits: ["a69a237b1"]
seat: "win"
priority: "P2"
triage-reason: "P2: the fixed grey only shows against unusual backgrounds; T251 lifted the floor that actually mattered"
tags: ["polish"]
---

# T581 - win32's divider fallback is a fixed #808080 while Mac derives it from the background

## In plain terms

If you never set a divider color, the Mac picks one that suits your terminal's
background — a slightly darker version of it — while Windows always draws the
same mid gray. On most themes nobody notices; on a light theme the Windows gray
is a heavier line than the Mac's, and it is a different line for the same
settings.

## Summary

Found while doing **T251**. `Window.paintDividers` falls back to a hardcoded
`#808080` when `split-divider-color` is unset, while Mac's `splitDividerColor`
(`macos/Sources/Ghostty/Ghostty.Config.swift:535`) derives its default from the
terminal background: `darken(by: 0.08)` on a light background, `darken(by:
0.4)` on a dark one. Two seats, one config, two different lines.

This is cosmetic rather than a legibility hole — since T251 the win32 fallback
goes through the same 3:1 floor as a user color, so it can never vanish. What
it is NOT is parity.

Worth noting before porting Mac's formula verbatim: darkening a background
toward black is exactly what produces an invisible divider on a black terminal,
which is the shape of the bug T251 fixed. Whatever lands here should go through
`split_geometry.dividerPaint` like every other divider color, so the floor
catches it.

## Where

- `src/apprt/win32/Window.zig` — `paintDividers`, the `0x80` literal.
- `macos/Sources/Ghostty/Ghostty.Config.swift:535` — the Mac formula.
- `src/apprt/win32/split_geometry.zig` — `dividerPaint`, which the fallback
  must keep going through.

## Validation criteria

- [x] With no `split-divider-color` set, the win32 divider is derived from the
      terminal background the same way Mac's is (or a recorded decision that
      the fixed gray is deliberate).
- [x] The derived fallback still clears the 3:1 floor on a black background and
      on a white one — unit tests in `split_geometry.zig`.
- [x] `test/win32/split-divider.ps1` run 2's fallback assertion updated to the
      new expectation rather than deleted.

## Progress log

- 2026-09-06 00:20: claimed; work starting.
- 2026-09-06 00:35: root cause: Window.dividerConfiguredColor fell back to split_geometry.FALLBACK_COLOR (0x808080). Fix: split_geometry.fallbackColor(bg) ports Mac's splitDividerColor (darken 8% light / 40% dark; darken(by:) scales HSB brightness == per-channel multiply), and Window derives it from config.background. Still routed through dividerPaint so the 3:1 floor catches the black-on-black case. Unit tests added; split-divider.ps1 run 2 now expects the derived 90,89,89 rest / 115,114,114 hover at tolerance 6.
- 2026-09-06 01:06: validated: split_geometry.zig unit tests green (zig test, 78 tests) - the formula (255->235 on white, per-channel 0.6 on a dark tint) and the PAINTED floor on black/white/#1e1e2e/#faf4ed in both states. floor-lane -Lane all: lib/none/win32/agent all PASS. test\win32\split-divider.ps1 ALL PASS (73) with two new assertions: the gap holds the derived 90,89,89 at tolerance 6, and the fixed 0x808080 is gone; the T233 hover pair re-anchored to 90/115 still discriminates. Also filled a coverage gap in the same turn: split_geometry.zig had no guard row, so a divider-color edit was tied to no harness - added the split-divider row to guard-due.ps1 and the stamp block to the harness; guard-due check is clean but for the three pre-existing advisories. gate-negatives.ps1 ALL PASS (58).
- 2026-09-06 01:07: status: in-progress -> done [commit a69a237b1]
