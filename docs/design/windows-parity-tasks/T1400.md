---
id: T1400
title: "viewer-narrow-pane.ps1 fails in the repo working directory but passes in a fresh worktree on identical code"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P1"
tags: ["test", "infra"]
---

# T1400 - viewer-narrow-pane.ps1 fails in the repo working directory but passes in a fresh worktree on identical code

## Summary

`test\win32\viewer-narrow-pane.ps1` fails its assertion

```
FAIL  E: narrowing never ADDS ink to the strip
```

when run from `D:\git\ghoztty`, and passes (`ALL PASS (63 assertions)`) when the
SAME COMMIT is checked out into a fresh worktree and run from there. Measured
2026-09-06 during T1392, repeatedly, both ways.

The control that makes this a finding rather than a suspicion: the repo's own
source files were reverted to HEAD, rebuilt in place, and the script re-run from
`D:\git\ghoztty` — still red. So it is not the code under test, and it is not
whatever change happened to be in the tree. Something about *where the run
happens* decides the verdict.

`test\win32\viewer-feedback.ps1` showed the same split on the same day (red in
the repo, green in the worktree, identical code). That one had a second,
independent defect underneath — a window lookup with no wait loop — which T1392
fixed, and it is green in both places now. This card is the half of the symptom
that survived that fix, and the shared shape is what suggests one cause rather
than two coincidences.

## In plain terms

A test that checks how the viewer's toolbar shrinks says "broken" when you run it
from the main folder and "fine" when you run the exact same code from a copy of
that folder. Until we know why, neither answer can be trusted — and the check
that is supposed to say "somebody verified this" can never go green.

## Why it matters now

The harness guard `viewer-narrow-pane` clears only on a clean green run, and the
seat that changes viewer chrome works in `D:\git\ghoztty`. So this makes a guard
permanently unclearable from the only place the work is done — which is worse
than a red test, because the next turn's honest options are to leave a guard red
indefinitely or to reach for the `-NoGuardDue` hatch.

## What it actually was

Not the working directory — that framing was an artifact of small samples. The
same directory produces both verdicts: four whole-script runs from
`D:\git\ghoztty` on unchanged code scored PASS, FAIL, PASS, PASS.

The measurement in section E scans fixed-pitch boxes across the WHOLE bar and
counts a box as "inked" when it differs from the band background. The address
field is a child `Edit` sitting in that scan, so its own background counted as
ink — and a child control's pixels are not reliably part of a synchronous
`PrintWindow` capture. Eight captures of ONE unmoved bar, constant width,
constant `GetWindowRect` on the field:

```
PROBE[55.1] bar 351 edit 176 [170..346] slots 8  ink: 0=14 1=14 2=15 3=14 4=35 5=35 6=35 7=35
PROBE[55.3] bar 351 edit 176 [170..346] slots 4  ink: 0=14 1=14 2=15 3=14 4=0  5=0  6=0  7=0
```

Boxes 0-3 (what the bar paints itself) never move; boxes 4-7 are exactly the
field's rect, and they carry 35 full-height rows or nothing. Every ratio with a
field flickered (55, 72, 80); the two ratios too narrow for a field (86, 92)
never did. A wider band that happened to sample "field absent" then measured
FEWER inked boxes than the narrower band after it, which is precisely
"narrowing ADDS ink".

The fix bounds the scan at the field's left edge, so only what the bar paints
itself is counted. That is also what the assertions claim to be about: the
leading cluster and the "…" overflow that closes it. The field's own claim
("legible or absent, never a stub") already came from `GetWindowRect`, which is
synchronous and never flaked.

## Leads, in the order worth trying (as filed; all three were wrong)

1. **A live repo-built ghoztty.** `Stop-RepoInstances` kills instances running
   from THIS repo's `zig-out`. In a fresh worktree that matches nothing; in the
   main repo it may be racing something the go-loop or the session itself has
   open. Check what `Stop-RepoGhoztty -Exe <repo>\zig-out\bin\ghoztty.exe`
   actually killed in each case.
2. **Window geometry.** The pane geometry differed noticeably between the two
   locations in the sibling script (`page top` 275 in the worktree vs 658 in the
   repo on adjacent runs), and this assertion is about ink across five WIDTH
   bands. A window that opens at a different size measures different bands.
3. **Per-exe-path app state** under `%LOCALAPPDATA%\ghoztty` — a saved layout or
   config keyed on the executable path would differ between the two locations.

## Goals

- [x] Name the mechanism
- [x] Green from the repo working directory, with the guard stamped
- [x] Still green from a fresh worktree
- [x] Check whether the other acceptance scripts share it

## Validation criteria

- [x] The cause is NAMED, not worked around - and the premise is corrected: the
      working directory never decided anything. Demonstrated by the one thing
      that does decide it, toggled at constant geometry: a probe copy of the
      script captured the same unmoved bar eight times per width and printed the
      per-box ink; the boxes over the field read 35 rows or 0 with nothing else
      changing, and the boxes over the bar's own paint never moved.
- [x] `powershell -NoProfile -File test\win32\viewer-narrow-pane.ps1` is green
      from `D:\git\ghoztty` - four consecutive runs, byte-identical band
      numbers (4 / 2 / 1 / 1 / 1 inked boxes) where the same four runs before
      the fix scored PASS, FAIL, PASS, PASS with three different numbers. The
      `viewer-narrow-pane` guard stamps.
- [x] Still green from a fresh worktree: `D:\git\ghoztty-t1400` at the same
      commit, built there - ALL PASS, with band numbers identical to the
      repo's (4 / 2 / 1 / 1 / 1).
- [x] `-NegativeControl` still fails with exactly the four failures its header
      specifies, so the fix did not defang the section.
- [x] The finding is written into `docs/claude/testing.md` as a harness-wide
      rule (measure a child through its rect, bound ink scans at its edge, and
      suspect this shape whenever an assertion is intermittent at CONSTANT
      geometry). The audit of the other scripts is below.

## The other acceptance scripts

Twenty scripts look up a child `Edit`/`Button`; the exposure is only where a
capture of the PARENT is measured over the child's pixels. Two do both:

- `viewer-narrow-pane.ps1` - this one, fixed.
- `viewer-find.ps1` - captures the find card and asserts `>= 3 distinct
  colours`. The card paints its own background, border and label glyphs, so the
  threshold survives the field's pixels being absent; left as it is, and named
  here so the next intermittent there starts from the answer.

The rest (`activity-monitor`, the chooser scripts, `pane-banner`, `caption-bar`,
`chrome-theme`) capture windows that paint their whole content themselves, with
no child control inside the region they measure.

## Progress log

- 2026-09-06: filed from T1392, which hit it while clearing the guards its own
  change made due. Both control runs (reverted sources in the repo; unchanged
  sources in a worktree) are recorded above.
- 2026-09-06 14:59: claimed; work starting.
- 2026-09-06 15:15: root cause NAMED and it is not the working directory: the ink scan counted the address field's own background, and a child EDIT's pixels are not reliably part of a synchronous PrintWindow capture. Eight captures of one unmoved bar at constant width: field's 35 full-height rows present in 6, absent in 2, at every ratio that has a field (55/72/80); ratios with no field (86/92) never varied. Fix: bound the slot scan at the field's left edge so only what the bar paints itself is counted. 4/4 repo runs now byte-identical; -NegativeControl still exactly 4 failures.
