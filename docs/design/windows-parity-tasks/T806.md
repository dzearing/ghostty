---
id: T806
title: "guard-due covers only the go-loop harness, so every other unrun acceptance script is still invisible"
order: null
deps: ["T783"]
status: "done"
commits: ["e7502c6bd"]
seat: "win"
priority: "P2"
triage-reason: "the mechanism exists and one row is wired; the same silent-red failure is still open for every acceptance script outside the P1-P3 floor"
tags: ["test", "infra"]
---

# T806 - guard-due covers only the go-loop harness, so every other unrun acceptance script is still invisible

## Summary

T783 built the change-gate mechanism (scripts\guard-due.ps1) and wired ONE row: the go-loop harness. Every other acceptance script outside the P1-P3 floor still has the failure T783 exists to catch - it can go red against code that moved under it and nobody finds out until somebody remembers to run it. Add rows for the harnesses whose subject is a specific script family and whose coverage globs are unambiguous: guard-due itself (scripts\guard-due.ps1 + test\win32\guard-due.ps1), the tracker scripts (parity-tasks/parity-sweep/parity-decisions), the delivery scripts (launch-upgrade/upgrade-ghoztty-windows/deliver-windows-build vs upgrade-staleness.ps1 + deliver-windows-build.ps1), and the test-harness libraries under test\win32\lib\ that have their own audits. Each new row needs its harness to stamp on a clean green run, and the acceptance script for guard-due must then pass -Guard for its fixture arms (a multi-row table makes a single-row fixture read as DUE for every other row).

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-09-06 06:27: Already fixed by a later commit, not a new one: guard-due.ps1's coverage table now carries ~50 rows including every family this card names (guard-due itself, the tracker scripts parity-tasks/parity-sweep/parity-decisions, the delivery scripts upgrade-staleness/deliver-windows-build, and the test\win32\lib audits). e7502c6bd (T892) is the commit that first extended the table past the single go-loop row; it has grown with each harness since. The validation this card was owed is landed: test\win32\guard-due.ps1 ALL PASS (100 assertions) on 2026-09-06, and it does pass -Guard for its fixture arms, which is the specific requirement the Summary called out.
- 2026-09-06 06:27: status: todo -> done (by 2026-09-06 triage already-fixed sweep) [commit e7502c6bd]
