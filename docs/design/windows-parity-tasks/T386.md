---
id: T386
title: "selection.js's toolbar font stack has no Windows entry, so the viewer selection popover renders in Arial"
order: 157
deps: ["T375"]
status: "done"
priority: "P1"
milestone: "M1"
triage-reason: "viewer selection popover renders in Arial instead of Segoe UI; one-line visual polish of a shipped surface"
commits: ["93b7107d0"]
seat: "win"
---

# T386 - selection.js's toolbar font stack has no Windows entry, so the viewer selection popover renders in Arial

## In plain terms

Select some text in a viewer pane and a little Quote/Copy popover appears over
it. On Windows that popover — and the document text underneath it — was drawn
in Arial, and code blocks in a typewriter font, because the styling only ever
named Mac fonts. Everything around them is Segoe UI, so the pane read as a web
page dropped into the app rather than part of it. After this, viewer text is
set in the same system font as the rest of the window.

## Goals

- [x] The selection popover uses the system UI font on Windows (Segoe UI), and
      still San Francisco on macOS, from ONE shared file
- [x] The rendered document body and its error card do the same
- [x] Code blocks fall back to a real Windows monospace, not the generic
- [x] A unit test fails if a future edit puts the Mac-only spelling back in
      front

## Validation criteria

- [x] `zig build test -Dapp-runtime=none` and `-Dapp-runtime=win32` green,
      including the two new font-stack tests — `scripts\floor-lane.ps1 -Lane
      all`, 2026-08-08 03:39: `none#1=PASS` (300s), `win32#1=PASS` (338s)
- [x] `zig build test-agent` green — FAILED on the first pass and **not on
      this task**: the host-floor test flaked on T400's cache-dependent
      fetch-count assertion (win32 lane `nav_cost=0` PASS vs agent lane
      `nav_cost=1` FAIL, same source five minutes apart). Filed **T596** with
      the evidence. Re-run `-Lane agent` 03:57: `PASS` in 180s.
- [x] P1-P3 acceptance scripts ALL PASS — 2026-08-08 04:0x against the freshly
      built `-Doptimize=Debug` `zig-out\bin\ghoztty.exe`: `P1 ACCEPTANCE: ALL
      PASS`, `P2 ACCEPTANCE: ALL PASS`, `P3 ACCEPTANCE: ALL PASS`
- [x] The fonts are MEASURED on a live page, not asserted from the CSS — a
      canvas-width probe added to `ViewerPane.zig`'s "host floor" live-runtime
      test runs `ExecuteScript` against the rendered bundled template and
      compares the toolbar stack and the document body's computed stack
      against Arial and the Segoe faces. It logs
      `font probe: "true,true,true,true"` — not-Arial and a-Segoe-face, for
      both — in the win32 AND the agent lane. `getComputedStyle` alone would
      only have echoed back what we typed; the width comparison says which
      face the font matcher actually picked on this box.

## Summary

`src/viewer/selection.js:40` sets the Quote/Copy toolbar's font to
`-apple-system, BlinkMacSystemFont, sans-serif`. Neither of the first two
resolves on Windows, so the toolbar falls through to the generic `sans-serif`
family — Arial — while every other piece of win32 chrome is Segoe UI Variable
(`type_ramp.zig`, `title_font.zig`). T375 made this visible for the first time
by injecting the toolbar into real pages on Windows.

Its colors are NOT part of this: the file already has a
`@media (prefers-color-scheme: dark)` block (`selection.js:61`), and T373's
`put_PreferredColorScheme` drives it, so the popover already follows the window
appearance on Windows.

## Details

The fix belongs in the SHARED file, not a Windows copy — P1's rule is that
`selection.js` stays a single copy, and T375's
"selection.js is embedded VERBATIM" test enforces it byte-for-byte. Putting
`system-ui` at the head of the stack resolves to San Francisco on macOS and
Segoe UI Variable on Windows, so one edit is right on both platforms and
nothing forks:

```css
font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
```

Check the same line in `viewer.css` while there — T90e renders the document
body through it, and CLAUDE.md pins body text to "the system font (SF Pro via
`-apple-system`)", which has the identical hole on Windows. If it does, this
task covers both or files the second.

*Validation:* a win32 GUI probe is overkill for a font family; read it back
from the page instead. Once T90e/T160 land, `getComputedStyle` on the toolbar
button through an `ExecuteScript` round trip is the honest oracle. Until then,
a unit test on the shared CSS text (the stack begins with `system-ui`) plus a
screenshot in the task file.

## Progress log

- 2026-08-08 03:21: claimed; work starting.
- 2026-08-08 03:39: fix: system-ui leads the stack in the SHARED selection.js + viewer.css (San Francisco on mac, Segoe UI here), with Segoe UI / Cascadia Mono / Consolas behind the SF entries; no fork. Tests: two CSS-text unit tests (viewer_bridge.zig, viewer_content.zig) plus a canvas-width font probe in ViewerPane's live host-floor test that measures the real stacks against Arial on this box. Filed T595 for diff.css (win32 has no diff mode yet; rides T463).
- 2026-08-08 03:57: lanes: none PASS, win32 PASS, agent FAIL. The failure is NOT this task: ViewerPane's host-floor test flaked on T400's cache-dependent fetch-count assertion (win32 lane nav_cost=0 PASS, agent lane nav_cost=1 FAIL, same source 5 min apart). Filed T596 with the evidence and three fix options. The T386 font probe printed 'true,true,true,true' in BOTH lanes: the toolbar stack and the document body measure as a Segoe face and not as Arial, through a live WebView2 on this box.
- 2026-08-08 04:04: status: in-progress -> done [commit 93b7107d0]
