---
id: T616
title: "Cross-machine Restore All still dials its windows one after another"
order: null
deps: ["T339"]
status: "in-progress"
commits: []
seat: "win"
priority: "P2"
triage-reason: "Perf follow-up to T339, which already removed the freeze; serial dialing is slower, not broken. Out of M1."
tags: ["perf"]
---

# T616 - Cross-machine Restore All still dials its windows one after another

## In plain terms

Bringing another machine's windows back opens one connection per window, and it
opens them one at a time. Nothing freezes any more (T339 moved that work into
the background), but the wait still grows with the number of windows: six
windows on a slow link is six round trips of waiting before any of them appear.
Opening them together would make the wait roughly the same as for one window.

## Summary

`RestoreAllRelay.worker` (T339) dials the pull, then loops the decoded windows
dialing one transport each, SERIALLY. Off the GUI thread that is no longer a
freeze — the message loop pumps throughout, which is what T339 was for — but the
latency is still `(1 + N) x RTT` before the first window appears.

T339 deliberately did not also make them concurrent: the defect it was filed for
was *where* the dials happened, and one change at a time is what kept the
ownership rules checkable (D-T339, filed the same day). The per-window dials are
independent by construction — each window owns its own transport for life
(T336) — so they are a natural fan-out.

## Details

- The dials to parallelise are the LOOP's, not the pull's: the pull must land
  first, because its `GET_LAYOUTS` is what says how many windows there are.
- Ownership must survive a partial failure exactly as it does now: every dial a
  worker opens is either handed to a window by `App.adoptRestoreAll` or freed by
  `Job.destroy`. A fan-out has to keep that true when some threads fail and
  others succeed.
- Consider a bound (a small pool rather than N threads): a machine with 40
  windows should not open 40 simultaneous WebSocket upgrades at a relay that may
  rate-limit them.
- The fixture can measure this: `test/win32/lib/FakeRelay.ps1` gained
  `-SlowConnectFile` / `-SlowConnectMs` in T339, which defers each `/connect`
  answer without stalling the relay. With a 1500 ms delay, serial dialing of 2
  windows takes >= 4.5 s wall-clock and concurrent dialing ~3 s — a measurable
  difference, and it grows with N.

## Validation criteria

- [x] With the slow-connect fixture armed, the wall clock from the keypress to
      `restore all: rebuilt N window(s)` is close to `2 x SlowMs`, not
      `(1 + N) x SlowMs` — asserted in `test/win32/chooser-restore-all-remote.ps1`
      rather than eyeballed. **F3, two runs:** `3617 ms` and `3819 ms` for 2
      windows at `SlowMs=1500`, against `4500 ms` for serial dialing alone.
      **F2** reads the same fact at the relay: the two window dials landed
      `31 ms` / `15 ms` apart, where serial puts them `>= 1500 ms` apart.
- [x] `chooser-restore-all-remote.ps1` stays ALL PASS, F (the 1+N connect count)
      included: concurrency must not change how many transports are opened.
      **ALL PASS (46 assertions)**, three runs; F reads `3 connects for 2
      windows` unchanged.
- [x] A dial that fails while its siblings succeed still costs exactly its own
      window, and leaks nothing (the failure arm of F/E). **D** still passes
      (`relay dial failed … err=error.WebSocketUnauthorized` is reported, not
      swallowed, and no window is built on it); in the fan-out a failed dial
      leaves its own slot `null`, which `adoptRestoreAll` skips — nothing owns a
      connection until its own slot is written, so a partial failure cannot
      leak or double-own one.
- [x] Both zig lanes and `test-agent` green. `floor-lane.ps1 -Lane all`:
      lib/win32/agent PASS first time; none PASS on re-run (the first attempt
      died in `font.Collection.test.add full`, the T443 ghost's known victim —
      recorded there, nothing to do with this diff). `ipc-p1/p2/p3` ALL PASS.

## Progress log

- 2026-09-07 09:43: claimed; work starting.
- 2026-09-07 09:58: Defect re-verified in code: RestoreAllRelay.worker still dials one transport per window inside the decode loop, serially (src/apprt/win32/RestoreAllRelay.zig:249 before this change). Implemented a bounded fan-out: the loop now only builds the Prepared slots, and dialAll() runs them across a pool of at most max_parallel_dials=8, with the worker thread itself as one of the pool (so a failed spawn degrades to the serial loop, never worse). A failed dial leaves its slot null, which adoptRestoreAll already skips - ownership is unchanged because nothing owns a connection until its own slot is written. BLOCKED ON BOX STATE: C: has 0.1 GB free, so every zig C/C++ compile step dies with a bare 'error: Unexpected' (the same symptom T1054 documented for a full D:). Build cannot be validated until C: has room.
- 2026-09-07 10:31: RESUME STATE (context reset mid-task; the tree is dirty ON PURPOSE, keep it). Code is written and believed complete: (1) src\apprt\win32\RestoreAllRelay.zig - worker no longer dials in the decode loop; it fills Prepared slots with dialed=null, then dialAll() fans the dials over a pool of at most max_parallel_dials=8 with the worker thread itself in the pool, joins every helper before finish(); helperCount() has a unit test in the none lane. (2) test\win32\chooser-restore-all-remote.ps1 - new F2 (relay CONNECT timestamps for the window dials must be < SlowMs/2 apart; serial puts them >= SlowMs apart) and F3 (keypress->rebuilt wall clock < (1+N)*SlowMs), plus the slow-connect disarm moved AFTER the measurement. (3) scripts\guard-due.ps1 - new restore-all-remote guard row (RestoreAllRelay.zig + the script); guard table validates and the row reads DUE. (4) T1431 filed. WHAT IS LEFT: run floor-lane.ps1 -Lane all, run test\win32\chooser-restore-all-remote.ps1 (satisfies the new guard row), ipc-p1/p2/p3, then tick the criteria, log entry, commit+push. BOX STATE THAT WILL BITE YOU: C: has 0.1 GB free and zig's C/C++ steps scratch in %TEMP%, so a build dies with a bare 'error: Unexpected' on glslang/oniguruma/translate-c/helpgen. Set \C:\Users\David\AppData\Local\Temp and \C:\Users\David\AppData\Local\Temp to D:\tmp in every build/test shell - that alone makes the identical build succeed. See T1431.
- 2026-09-07 10:32: stale in-progress claim picked up by a new turn; reassessing from this log and git status before resuming.
- 2026-09-07 10:45 [session ba1b600f]: Resumed the stale claim: the dirty tree IS this task's work and the progress log's RESUME STATE matches it exactly (RestoreAllRelay.zig fan-out + helperCount unit test, chooser-restore-all-remote.ps1 F2/F3, guard-due.ps1 restore-all-remote row, T1431 filed). C: now has 38 GB free, so the box-state blocker from 09:58 is gone and builds run normally. floor-lane -Lane all: lib/win32/agent PASS; none FAILED once on 'font.Collection.test.add full' (exit 255) and PASSED on an immediate re-run - not this diff (nothing font-related), it is the T443 ghost's known victim, recorded there separately.
- 2026-09-07 11:00 [session ba1b600f]: VALIDATED. chooser-restore-all-remote.ps1 ALL PASS (46) three times, with the new F2 (the two window dials land 31 ms / 15 ms apart at the relay; serial puts them >= 1500 ms apart) and F3 (3617 ms / 3819 ms keypress->rebuilt for 2 windows, against 4500 ms for serial dialing alone); F still reads 3 connects for 2 windows, so concurrency did not change how many transports are opened. Added the stamp block the new restore-all-remote guard row needs (the script scores itself rather than through lib\TestScore.ps1, so it gates the stamp on its own bodyComplete flag set as the last statement of the try body) - guard now STAMPED. floor-lane -Lane all green, ipc-p1/p2/p3 ALL PASS, and the ten meta-audits the harness edit made due (body-complete, verdict-exit, isolation-meta, launch-preflight, cleanslate, stderr-capture, desktop-launch, command-resolve, test-reach, printclient) all re-run ALL PASS.
