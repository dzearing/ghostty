---
id: T1419
title: "The Activity panel names a direct-host machine two different ways depending on how it was opened"
order: null
deps: []
status: "done"
commits: ["e5ee2eb6e"]
seat: "win"
priority: "P1"
tags: ["fix"]
---

# T1419 - The Activity panel names a direct-host machine two different ways depending on how it was opened

## Summary

Opened from a remote window's palette the panel titles itself 'Activity - 127.0.0.1'; switched to from its own carousel it titles itself '127.0.0.1:47913'. Same machine, same panel, two names - and sections A and B of test\win32\activity-monitor-remote.ps1 are RED on it.

## In plain terms

Open the Activity Monitor on a window that is running on another machine and
the panel is titled `Activity — 127.0.0.1`. Move its machine carousel to Local
and back, and the same panel — same machine, same connection — is now titled
`127.0.0.1:47913`. Two names for one box, a click apart. On a host running more
than one agent the shorter name cannot even tell them apart.

## Goals

- [x] One name for a direct-host machine, whichever way the panel was opened.
- [x] `test/win32/activity-monitor-remote.ps1` sections A and B green again.

## Evidence

Measured 2026-09-07 while validating T614, on `activity-monitor-remote.ps1`:

```
FAIL  A the panel is titled for the MACHINE (got 'Activity — 127.0.0.1')
      state: source=127.0.0.1 total=359 shown=1 root=18728
FAIL  B the sample is attributed to the remote source
...
PASS  G2 the panel switched BACK to the machine (source=127.0.0.1:47913)
```

Where it came from: before **T610** (`f0fa80620`, 2026-09-07) the palette path
opened the panel with `.remote = .{ .id = id }` and no `name`, so `Source.label`
fell back to the id — `host:port`. T610 moved the entry onto the window and
added `.name = self.machineDisplayName()`, and `RemoteMachine.displayName`
(T1390) answers the bare host for a `.tcp` machine, deliberately: the close
confirmation wants the box the user typed, not a port. The carousel path still
labels by the id, so the two entries now disagree.

## The shape of the fix

Decide which of the two names an Activity panel should carry for a direct-host
machine, then make BOTH entry points produce it — the point is one derivation,
not a second special case:

- keep `displayName` as it is for the pill and the close confirmation, and give
  the panel the identity string (`host:port`) from both entries; or
- give the carousel the display name too, and accept that two agents on one host
  are named identically in the panel.

Either way the harness's A/B expectation is the contract to restate, and
`remote-pill.ps1` names the pill's own label — check it does not move.

## What landed

One derivation, at both entry points. `Window.activityPanelSource()` is new: it
runs `activity_borrow.sourceId` - the same key the carousel derives for this
window's card (`activity_machines.refreshWindowMachines`) and the same key
`borrowFrom` matches on - and hands the panel that string as BOTH its `id` and
its `name`. `openActivityMonitor` no longer formats the identity itself (it had
its own copy of the `host:port` switch, which is how the two drifted), and no
longer names the panel `machineDisplayName()`.

`machineDisplayName()` is deliberately untouched: the pill and the close
confirmation keep the bare host, because what they answer is "which box did I
type". The panel answers a different question - which of the agents on that box
am I looking at - and only the identity can tell two apart.

The gap underneath the gap: `activity-monitor-remote.ps1` was tied to no guard
row, so T610's edit to the naming site made nothing due and the harness sat red
for a day. It has a row now (`activity_borrow.zig`, `activity_machines.zig`,
`Window.zig`, the script), and the script stamps on a green run like the others.

## Validation criteria

- [x] `test/win32/activity-monitor-remote.ps1` ALL PASS - 96 assertions, on the
      box, over a Debug build rebuilt for this change. Sections A and B, which
      were the RED that filed this task, now read
      `A the panel is titled for the MACHINE` and
      `B the sample is attributed to the remote source (127.0.0.1:47913)`.
- [x] `test/win32/remote-pill.ps1` ALL PASS (11 checks) - the pill's label is
      unmoved, which is what `machineDisplayName()` being left alone buys.
- [x] The panel opened from the palette and the same panel returned to via the
      carousel carry the identical title. Section A reads the palette-opened
      panel's window text; G2 reads the panel after a round trip out to Local
      and back through the carousel, and both name `127.0.0.1:47913`.
- [x] Unit test `T1419: an Activity panel names a machine by its identity, both
      entry points` in `Window.zig` - both machine kinds, the deliberate
      divergence from `displayName()`, agreement with `activity_borrow.sourceId`
      (the key the carousel derives), and the refuses-rather-than-truncates
      path. Green in the `win32` lane.
- [x] Standing floor green on the box: `floor-lane.ps1 -Lane all`
      (lib / none / win32 / agent all PASS) and `ipc-p1` / `ipc-p2` / `ipc-p3`
      ALL PASS.
- [x] Every guard the change made due was RUN, not excused: `viewer-close`,
      `close-confirm`, `remote-disconnect`, `printclient-audit`, `test-reach`,
      `body-complete`, `isolation-meta`, `launch-preflight`, `verdict-exit`,
      `cleanslate`, `stderr-capture`, `desktop-launch`, `command-resolve` - all
      ALL PASS and re-stamped.

## Progress log

- 2026-09-07 04:16: claimed; work starting.
- 2026-09-07 04:26: root cause: T610 gave the palette entry .name = machineDisplayName() (bare host for .tcp) while the carousel's window-derived card labels by the source id (host:port). Fix: one derivation - new Window.activityPanelSource() built on activity_borrow.sourceId, used by openActivityMonitor, so id AND name are the identity at both entry points. Kept displayName for the pill/close confirmation. Unit test added in Window.zig; win32 lane green.
- 2026-09-07 05:01: status: in-progress -> done
