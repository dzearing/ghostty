---
id: T866
title: "win32 agent integrations (2/8): the hardened skills and hook scripts are not bundled in the Windows build, and the hook payload parsing still assumes jq"
order: 2
deps: []
status: "done"
commits: ["59388143f"]
seat: "win"
priority: "P0"
tags: ["feature"]
---

# T866 - win32 agent integrations (2/8): the hardened skills and hook scripts are not bundled in the Windows build, and the hook payload parsing still assumes jq

## In plain terms

The Mac app carries the Ghoztty skill files and the two helper scripts (the
banner updater and the busy/idle tracker) inside itself, so it can install
them for a coding agent with one click. The Windows app carries none of them,
and the scripts as written need a unix tool (`jq`) that a normal Windows
machine does not have. This task packs the up-to-date files into the Windows
build and settles how the scripts read their input without jq.

## Goals

- [x] the four asset files ride inside the Windows exe, byte-for-byte the
      Mac's current copies, with any drift from the Mac versions caught
      loudly by a test
- [x] the helper scripts no longer need `jq` — a clean Windows machine runs
      them with nothing installed beyond Ghoztty itself
- [x] proven end to end on this box with jq deliberately hidden: a session
      start and a user prompt flow through the banner script and the banner
      really appears on the pane

## Summary

Second slice of the T598 split. Two halves:

**1. Vendor the assets.** On `origin/main` they live at
`macos/Resources/Ghoztty/`: `hooks/ghoztty-banner.sh`,
`hooks/ghoztty-activity-state.sh`, `skills/ghoztty/SKILL.md`,
`skills/process-feedback/SKILL.md`. Take the tip-of-main copies — T598's
"vendor the HARDENED copies" table (06d50037a mkdir-mutex + mktemp +
self-heal + markdown-escaping, 1b632b812, 4b07859e4, 4df779938) is already
folded into them. Windows equivalent of the Mac app bundle: `@embedFile` into
the exe (a `GhosttyAssets`-shaped Zig module returning the text), which beats
loose files beside the exe — nothing to go missing or stale, and the
portable-zip manifest stays untouched. Keep the assets as repo files under a
shared path (e.g. `src/apprt/win32/assets/` or a platform-neutral
`resources/ghoztty/`) so the Mac and Windows builds can converge on one copy
later; note whatever you choose in the file header.

**2. The jq problem.** The scripts parse hook payloads (stdin JSON) with jq.
On-box facts measured 2026-08-15: `jq` exists here only via winget (not
guaranteed); `bash` resolves to WSL's `System32\bash.exe` on PATH, though
Claude Code on Windows ships/requires Git-for-Windows bash for its own hook
execution; Copilot CLI is absent entirely. Options to settle EMPIRICALLY
(check what the currently-working 0.8.0 plugin banner script does on this
box, measure `powershell -NoProfile` cold-start against a tool-tick hook's
budget):

- (a) Keep bash scripts, replace jq with a `ghoztty +hook-payload`-style
  native parse (the app's own JSON, per T598's guidance) — hooks stay
  one-liner `bash` commands, parsing is native and fast.
- (b) Rewrite the scripts in PowerShell (`ConvertFrom-Json` is built in) —
  no bash dependency at all, but measure startup latency: tool-tick hooks
  fire on every tool call and a 300-800ms `powershell.exe` spawn per tick is
  user-visible in agent responsiveness.
- (c) Keep bash + require jq — REJECTED already: a clean box lacks jq, and
  the Mac flow's "install jq with brew" nag is exactly the manual wiring
  this feature exists to remove.

This is a mechanism call (D46: decide, record in this file, do not file a
decision). Whichever lands, the choice must serve BOTH hook consumers the
same way (Claude today, Copilot when present), and the scripts' behavior
must stay byte-compatible with the Mac state files where the formats are
shared (banner state JSON under the app state dir).

## Decision record (mechanism call, D46: decided here, not filed)

**Chosen: (a) keep ONE shared bash script set; replace jq with native
`ghoztty +json` helpers** (`get` / `merge` / `encode`, `src/cli/json.zig`).
The verb lives in the shared CLI enum, so it exists identically on both
platforms by construction — no CLI divergence, and the Mac picks it up the
moment the script change is upstreamed (follow-up task below).

- On-box measurements (2026-08-15): `powershell -NoProfile` cold start
  132–155ms; `jq` spawn ~60ms; `ghoztty` CLI spawn 112–162ms. Option (b)
  (PowerShell rewrite) saves nothing per spawn and forks the implementation
  from the Mac's; rejected. (c) require-jq was pre-rejected by the task.
- The render path batches its eight state reads into ONE `+json get --each`
  spawn, so the fork's hook-path cost lands at or below the old jq
  version's (~12 spawns × 60ms vs ~5 × 112ms).
- `+json merge` honors the scripts' `<file>.lock` mkdir-mutex protocol
  (stale reclaim included), so an older installed script and this one can
  race the same tty-keyed state file safely — the plugin-upgrade case the
  Mac comments call out. It self-heals corrupt state to `{}` and publishes
  via unique same-dir temp + atomic rename.
- Parse inputs tolerate a UTF-8 BOM (measured: PowerShell 5.1 piping to a
  native exe prepends one; unit-tested).
- The fork drops upstream's cached-pane `+list` validation (its one jq
  filter too structural for `+json get`): with no `$GHOZTTY_PANE_ID` —
  legacy app builds only, never this box — `resolve_pane` now calls
  `+list --tty` each render instead of validating a cached name.
- `ghoztty-activity-state.sh` needed nothing: it parses with bash regex on
  the hot path by design (its own comment: a fork per tool call is real
  cost).
- For T867/T868: hooks must invoke the scripts through **Git for Windows
  bash**, never a bare `bash` lookup — PATH `bash` from a win32 process is
  WSL's `System32\bash.exe` (measured). And `banner_help` inside the script
  names `~/.config/ghoztty/hooks/` as the install path — T867's directory
  decision should either match it (`%USERPROFILE%\.config\ghoztty\hooks`
  reads the same under Git Bash) or upstream a placeholder.

## Validation criteria

- [x] The four assets are embedded in the win32 build and retrievable through
      a `GhosttyAssets`-equivalent API, with a missing/mismatched asset a
      typed error. Verified: `src/apprt/win32/GhosttyAssets.zig` unit tests
      (skill lookup + `error.UnknownAsset` + marker/fork tripwires) run in
      every lane via `src/apprt.zig`; floor `-Lane all` green.
- [x] The vendored copies are byte-identical to tip-of-main's
      `macos/Resources/Ghoztty/` versions, with drift loud. Verified:
      `test\win32\hook-json.ps1` section A compares the pristine
      `upstream/` mirror blob-for-blob against `origin/main` (git
      hash-object vs rev-parse) and the three unforked live copies against
      the mirror. The ONE deliberate divergence — the jq-free
      `hooks/ghoztty-banner.sh` fork — is pinned by tripwire assertions
      (no `jq` token, `+json` present, LF-only, no BOM) here and in the
      GhosttyAssets unit tests; the pristine banner copy stays in
      `upstream/` so the delta is reviewable and collapses on upstreaming.
- [x] The payload-parsing decision is recorded in this file with the on-box
      measurements (above), and the chosen path runs a SessionStart and a
      UserPromptSubmit payload end-to-end on this box without jq. Verified:
      `test\win32\hook-json.ps1` section C — jq SHADOWED by a sentinel stub
      that fails loudly if invoked (and an assertion that the shadow really
      was first on PATH), SessionStart wipe + session record, prompt
      paraphrase seeded with escapes decoded, banner read back from
      `+list --json`, both envelope shapes (Claude nested, Copilot flat).
      ALL PASS (37 assertions), stamped `hook-json`.
- [x] Unit tests green in both lanes via `scripts\floor-lane.ps1 -Lane all`.
      Verified: lib/none/win32/agent all PASS (run sequentially per T401).

## Progress log

- 2026-08-15 15:41: claimed; work starting.
- 2026-08-15 15:52 [session 2801ff1b]: Decision (mechanism, D46: recorded not filed): keep ONE shared bash script set, add native 'ghoztty +json' CLI helpers (get/merge/encode) in shared src/cli so the verb exists identically on both platforms by construction; rewrite banner.sh's jq plumbing on them. Measured on-box: powershell -NoProfile cold start 132-155ms, jq spawn ~60ms, ghoztty CLI spawn 112-162ms. PS rewrite (b) rejected: forks the implementation from Mac and saves nothing per-spawn. jq (c) already rejected. Native merge will honor the script's mkdir-lock protocol at <file>.lock so an old plugin script and the new script can race safely against the same tty-keyed state file. Assets: src/apprt/win32/assets/ghoztty/ with an upstream/ pristine mirror (drift check vs origin/main) and a patched hooks/ghoztty-banner.sh fork; upstreaming the fork to main = mac-seat follow-up task.
- 2026-08-15 16:42 [session 2801ff1b]: Built: GhosttyAssets.zig (@embedFile of 4 vendored assets under src/apprt/win32/assets/ghoztty/, pristine upstream/ mirror alongside), +json CLI action (get/merge/encode, shared src/cli/json.zig, mkdir-lock interop with the bash mutex), and the banner.sh fork rewritten on +json (zero jq references; activity-state.sh + both SKILL.md byte-identical to main). All four floor lanes PASS (lib 42s, none 321s, win32 389s, agent 344s). NOTE: floor-lane.ps1 exits -196608 with no output when launched from the harness PowerShell tool but runs fine from bash - transient harness interaction, worked around by running via bash; not filed (not reproducible as a repo defect). Next: acceptance script + jq-shadowed end-to-end proof.
- 2026-08-15 17:13: status: in-progress -> done [commit 59388143f]
