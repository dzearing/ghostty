---
id: T868
title: "win32 agent integrations (4/8): no hook registration exists - port ClaudeHookSpec (settings.json merged fragment) and CopilotHookSpec (dedicated hooks file) with native JSON"
order: 4
deps: ["T865", "T866"]
status: "done"
commits: ["8103126a1"]
seat: "win"
priority: "P0"
tags: ["feature"]
---

# T868 - win32 agent integrations (4/8): no hook registration exists - port ClaudeHookSpec (settings.json merged fragment) and CopilotHookSpec (dedicated hooks file) with native JSON

## In plain terms

Hooks are what let a coding agent tell Ghoztty "I'm busy", "I need input",
or "update my status banner" automatically. On the Mac, Ghoztty writes those
hook registrations into each agent's settings for you — carefully, without
disturbing anything you configured yourself. Windows gets that machinery
here: the part that edits Claude Code's shared settings file safely, and the
part that drops Copilot's dedicated hook file.

## Goals

- [x] The per-event hook command builder (verb + `--runtime=` + quoted
      script path) exists on Windows with the exact Mac command shape.
- [x] Claude's fragment can be merged into, detected in, and removed from a
      populated `settings.json` without touching the user's own hooks.
- [x] Copilot's dedicated hook file renders byte-stably and matches the Mac
      schema.
- [x] The component refuses to clobber an unparseable settings file and
      backs the original up before its first rewrite.
- [x] Both zig lanes green via the floor wrapper.

## Summary

Fourth slice of the T598 split. Port `HookSpec.swift`, `ClaudeHookSpec.swift`,
`CopilotHookSpec.swift`, `HookComponent.swift`. All JSON via `std.json` (the
task's standing rule: native parsing, no jq, no shelled-out tools).

- **HookPurpose / HookCommand**: nine purposes — banner three (`session-start-
  hook`/`prompt-hook`/`stop-hook` verbs) + activity six (`session-start`,
  `pause`, `tool-tick`, `agent-start`, `agent-stop`, `settle`). The command
  string invokes the T866-decided script flavor with the verb and
  `--runtime=<claude|copilot>`; payload arrives on stdin, NEVER interpolated.
  Single-quote shell-quoting of the script path (or the T866 equivalent).
- **ClaudeHookSpec** (merged fragment into `%USERPROFILE%\.claude\
  settings.json`): the event map is Mac's exactly — SessionStart (banner
  element matcher `startup|clear` + unmatched activity sweep element),
  UserPromptSubmit, PreToolUse (matcher `AskUserQuestion|ExitPlanMode` →
  pause), Notification, PermissionRequest (→ pause), PostToolUse (tool-tick),
  SubagentStart/SubagentStop, Stop (banner + settle). Each command carries
  `"timeout": 10`. Ownership signature = the hooks DIRECTORY substring in a
  deterministically-serialized element (sorted keys, literal slashes) — the
  directory, not the banner path, so activity elements match too. `merge`
  replaces only signature-matched elements within our events;
  `removeFragment` prunes empty events and the empty `hooks` map;
  `fragmentState` compares OUR elements as sets, installed/outdated/absent.
- **CopilotHookSpec** (dedicated `%USERPROFILE%\.copilot\hooks\ghoztty.json`):
  version-1 envelope, `_comment` = marker token, camelCase events —
  `sessionStart` (banner + activity), `userPromptSubmitted`, `agentStop`
  (banner stop + settle), `"timeoutSec": 10`, `"bash"` command key. Mac
  deliberately wires only the three empirically-verified events; keep that
  (guessed event names silently never fire — worse than absent).
- **HookComponent**: dedicated-file path = ManagedFile write/state/remove.
  Merged-fragment path: absent-or-empty settings → empty base; present but
  non-object/unparseable → typed refusal, NEVER clobber (and state reports
  OUTDATED, not not_installed — a destructive "Set up" must not be offered);
  one-time `.ghoztty.bak` backup before the first rewrite; write through the
  reparse-refusing atomic writer. Deterministic output: sorted keys, pretty,
  unescaped slashes — byte-stable across install runs (std.json stringify
  options; verify determinism in a test).
- Keep Mac's known debt (H9): the merged-fragment branch may call the
  concrete Claude spec directly; generalize only when a second
  merged-fragment runtime exists.

## Validation criteria

- [x] Merge into a populated settings.json preserves every user hook/key,
      replaces stale Ghoztty elements, and round-trips byte-stably (unit
      tests mirroring Mac's ClaudeHookSpecTests coverage) — verified by the
      `ClaudeHookSpec.zig` test block (11 tests, all of Mac's non-plugin
      cases plus a reinstall-dedupe case) and `HookComponent.zig`'s
      "installing twice is byte-stable" on-disk test.
- [x] removeFragment leaves a user-only file untouched shape-wise and prunes
      empty containers; fragmentState reports the three states correctly —
      "removeFragment drops only Ghoztty's entries", "activity-state
      elements … fully removed" (hooks map pruned), and the
      installed/outdated/not_installed cases across the spec tests.
- [x] An unparseable settings.json: state = outdated, install = typed error,
      file byte-identical after; a top-level array likewise — two
      `HookComponent.zig` tempdir tests assert `error.UnparseableConfig` and
      byte-identical content (and that no backup is taken on refusal).
- [x] First rewrite creates `settings.json.ghoztty.bak` once; later rewrites
      never touch the backup — "first rewrite backs up the original once"
      re-installs after an uninstall and asserts the backup still carries
      the ORIGINAL bytes.
- [x] Copilot rendered file matches the Mac schema (version, `_comment`
      marker, three camelCase events, timeoutSec) — full-file golden test in
      `CopilotHookSpec.zig` plus a parse-and-inspect round-trip test.
- [x] Both zig lanes green via `scripts\floor-lane.ps1 -Lane all` —
      lib/none/win32/agent all PASS (2026-08-15), and the standing floor
      P1 (25), P2 (20), P3 (16) acceptance scripts ALL PASS after.

## Progress log

- 2026-08-15 17:43: claimed; work starting.
- 2026-08-15 17:48 [session 36484d8d-ae06-425d-a868-637777e61956]: Design settled: port = hook_spec.zig (HookPurpose/HookCommand), stable_json.zig (sorted-keys compact+pretty serializer over std.json.Value - Zig's stringify keeps insertion order so determinism needs our own), ClaudeHookSpec.zig (merge/removeFragment/fragmentState over Value, signature = hooks DIR), CopilotHookSpec.zig (renderedFile golden), HookComponent.zig (dedicated=managed_file, merged=readJSON typed-refusal + .ghoztty.bak + writeAtomicNoFollow). Plugin detection confirmed OUT of scope (T869 owns installed_plugins.json parse). Ground truth checked: plugin's working hooks.json on this box proves bash '<path>' verb commands run under Windows Claude Code; banner path composed home+.config/ghoztty/hooks with forward slashes.
- 2026-08-15 18:06 [session 36484d8d-ae06-425d-a868-637777e61956]: Implementation complete: stable_json.zig (sorted-keys deterministic serializer, compact+pretty), hook_spec.zig (HookPurpose/HookCommand, bash '<path>' verb --runtime=<rt>), ClaudeHookSpec.zig (hooksBlock/merge/removeFragment/fragmentState, directory signature), CopilotHookSpec.zig (golden-tested rendered file), HookComponent.zig (dedicated=T865 writer, merged=typed refusal + one-time .ghoztty.bak + writeAtomicNoFollow), hook_scripts.bannerPathAlloc (forward-slash bash-facing path). All standalone zig test runs green incl. Mac-mirrored coverage + goldens. Registered in apprt.zig; floor -Lane all running.
- 2026-08-15 18:18: status: in-progress -> done [commit 8103126a1]
