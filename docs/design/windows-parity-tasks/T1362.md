---
id: T1362
title: "Four remote-machines design notes still tell a reader to run a script that no longer exists"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P3"
tags: ["docs"]
---

# T1362 - Four remote-machines design notes still tell a reader to run a script that no longer exists

## Summary

T550 deleted scripts/deploy-windows-agent.sh (the Mac-side build-and-drop-on-the-share deploy loop for the standalone agent). Five documents still instruct a reader to run it: docs/design/remote-activity-monitor.md, remote-machines-CONTINUATION.md, remote-machines-CONTINUATION-2.md (three places) and remote-machines-progress.md. They are historical continuation notes rather than live runbooks, which is why they were left alone in the T550 change, but somebody following one gets 'command not found' with nothing telling them what replaced it. The replacement is the ordinary Ghoztty install: the agent ships inside it. Either add a one-line note at each site or mark the documents as superseded.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
