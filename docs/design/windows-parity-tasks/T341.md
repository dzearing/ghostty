---
id: T341
title: "Action.cval leaves the C union\u0027s bytes undefined for void-payload actions"
order: 133
deps: []
status: "in-progress"
priority: "P1"
milestone: "M1"
triage-reason: "void-payload actions hand embedded apprts undefined union bytes; this is what made T29's bug random"
commits: []
---

# T341 - Action.cval leaves the C union's bytes undefined for void-payload actions

## In plain terms

Deep inside the app, every "do something" message (open a window, quit, toggle
fullscreen…) is handed to the platform code in a small box that can carry extra
details. For messages with no details, the box used to be left full of leftover
memory garbage — and one past bug (a "process exited" bar popping up over a
perfectly live terminal, at random) happened because code read that garbage.
After this change the box is always wiped clean first, so the same class of
mistake shows a harmless, predictable nothing instead of random behavior.

## Goals

- [x] The C-ABI action union is fully zeroed before the active payload is set,
      so void-payload actions hand apprts all-zero bytes.
- [x] The pointer-vs-zero question is answered and recorded: null is better
      than garbage (deterministic fault vs silent wrong read).
- [x] Unit tests pin both the zeroing and a populated round-trip.

## Validation criteria

- [x] None-lane unit test: every void-payload `Action.Key`'s `cval()` bytes are
      all zero, read via `std.mem.asBytes` (never a typed member). Verified:
      `"Action.cval zeroes the union bytes for void-payload actions"` in
      `src/apprt/action.zig`, run via `floor-lane.ps1 -Lane none -Filter
      "Action.cval"` → PASS (2026-08-07).
- [x] None-lane unit test: a populated `size_limit` survives `cval()` intact.
      Verified: `"Action.cval round-trips a populated payload"`, same run.
- [x] Full floor green: `floor-lane.ps1 -Lane all` (none, win32, agent) →
      `FLOOR SUMMARY: none#1=PASS win32#1=PASS agent#1=PASS` (2026-08-07).
- [x] P1–P3 acceptance scripts ALL PASS (2026-08-07, against a fresh
      `-Doptimize=Debug` build containing the fix).

## Evidence

- Fix: `Action.cval` and `Target.cval` (same file, same hazard — `Target`'s
  void `app` member overlaps a *pointer*) now build their `CValue` from
  `std.mem.zeroes(CValue)` and assign only the active member, so every byte
  the payload does not cover is a defined zero.
- Pointer question (from the proposal): zeroing is the right trade. A wrong
  reader that dereferences a zeroed pointer faults deterministically at
  address ~0 — loud and boring; garbage could point at readable memory and be
  silently believed. No Debug-only poison needed.
- Tests added in `src/apprt/action.zig`: void-payload sweep over the whole
  `Action` union (bytes via `std.mem.asBytes`), `size_limit` round-trip, and
  a `Target.cval(.app)` zero-bytes test.

## Summary

`Action.cval` (`src/apprt/action.zig:496`) converts the core's tagged union
into the C ABI union handed to embedded apprts:

```zig
const value: CValue = switch (self) {
    inline else => |v, tag| @unionInit(CValue, @tagName(tag), ...),
};
```

For a **void-payload** action (`toggle_window_decorations`,
`toggle_tab_overview`, `quit_timer`, `quit`, `new_window`, … — most of the
enum) that initializes a zero-size member and leaves **the rest of the union's
bytes undefined**. Every embedded apprt then reads that union through a `C`
struct whose members overlap, and the C side has no tag-payload binding to
protect it.

**T29 is what that costs when a reader picks the wrong member**: the macOS
switch read `child_exited.timetime_ms` for `toggle_window_decorations`, i.e.
uninitialized memory, and a nonzero read popped a "process exited" bar over a
live surface. T29 fixed *that reader*. This task removes the *hazard*: make
the bytes deterministic so the same class of mistake reads a defined zero and
fails visibly-boring instead of randomly.

## Why it is its own task

It is shared core code on the path of **every** action to **every** embedded
apprt (macOS, GTK's C API surface, and win32's own embedded uses). That is a
different blast radius from a one-file Swift switch, and it is testable in the
`-Dapp-runtime=none` lane on the Windows box, where T29's Swift half is not.

## Fix (proposed)

Zero-initialize `CValue` before the `@unionInit`, or build it as
`var value: CValue = std.mem.zeroes(CValue);` then set the active member —
whichever keeps the extern-union layout and costs nothing measurable (these
are a handful of words, dispatched at human speed).

Check first, before writing the fix: whether `CValue` contains any pointer
member for which all-zeroes is a *worse* lie than garbage (a null a reader
might dereference where garbage would have crashed loudly). If so, prefer a
`0xAA`-style poison in Debug and zeroes in release, or say plainly in the task
why zeroing is still the right trade.

## Validation

- A new unit test in the none lane: for each void-payload `Action.Key`, build
  the action, `cval()` it, and assert the union's bytes are all zero — read
  via `std.mem.asBytes` on the `CValue`, not through a typed member (reading
  an inactive member is the very thing under test).
- Assert the populated case still round-trips: a `size_limit` action's four
  fields survive `cval()` unchanged.
- Both lanes + `test-agent` + P1–P3.

## Related

- **T29** — the reader-side defect this enabled.
- **T340** — the Mac-side runtime verification of T29.

## Progress log

- 2026-08-07 02:47: claimed; work starting.
- 2026-08-07 02:50: fix in: cval() now zeroes CValue (std.mem.zeroes) before setting the active member; zeroing pointers judged BETTER than garbage (null deref faults deterministically at ~0 vs garbage silently reading plausible memory), so no debug poison needed. Two none-lane tests added: all-void-payload bytes-are-zero via asBytes, and size_limit round-trip. Targeted filter run PASS; full floor next.
