---
id: T620
title: "chooser-resume.ps1 can no longer build an orphaned session, so it fails at setup"
order: null
deps: []
status: "done"
commits: ["622cdf9e1"]
seat: "win"
priority: "P1"
milestone: "M1"
triage-reason: "a standing acceptance script for the chooser's resume flow is red at setup, so nothing it covers is being checked"
tags: ["test", "fix"]
---

# T620 - chooser-resume.ps1 can no longer build an orphaned session, so it fails at setup

## In plain terms

There is a test that proves you can take over a terminal session that is still
running but has no window on screen — the whole point of the machine chooser's
session list. That test could no longer set itself up, so it stopped before it
checked anything, and nobody would notice if the feature broke. Nothing was
wrong with the feature itself; the test's way of *creating* a window-less
session stopped working (the app got better at automatically recovering such
sessions at launch, which is good for users and fatal for this fixture). The
test now asks the app to skip that automatic recovery for one launch, and it
also learned to follow the session list's new sort order instead of assuming
rows never move — which was silently making two of its later checks
meaningless.

## Goals

- [x] the resume test builds its window-less session again and runs to the end
- [x] every check it makes is real (no check can pass when nothing happened)
- [x] sibling tests using the same trick are confirmed safe

## Summary

`test/win32/chooser-resume.ps1` builds its fixture the way the comment says a
user does: run panes under the agent, kill the APP (not the agent), delete the
local layout manifest so nothing auto-restores, and relaunch. The agent then
holds live children with no viewer.

That no longer produces an orphan. Launch-time restore has a SECOND source since
T194 — the agent's own layout-blob store (`local-agent-debug\layouts.json`, read
live over `GET_LAYOUTS`) — so the relaunched app rebuilds the fixture window from
the agent and re-attaches both sessions. Observed 2026-08-08 against
`89eb3857a` + the T333 change (which cannot touch this path):

```
info(win32): session-restore: recovered 1 crash-orphaned window(s) from the agent (0 in the local manifest)
info(win32): session-restore: attached 2 pane(s); 0 live agent session(s) unattached
```

and the script's own output:

```
  PASS the sessions outlived the app (2 alive)
  FAIL the roster has the orphans plus the new pane (2 rows)
  FAIL a live row with no viewer is rendered (index -1 of 2)
SETUP FAIL: no orphaned live session
```

`exit 1` at that point, so sections 2, 3 and 4 — the cursor paint, the resume
itself, the tombstone refusal and its positive control — have not run since. The
log records the script ALL PASS (21) on 2026-08-02, so the break is somewhere
after that.

Deleting `layouts.json` in the fixture does NOT fix it: the agent is deliberately
kept alive across the app kill, it owns that file, and `GET_LAYOUTS` answers from
memory (`lib/CleanSlate.ps1` says as much — "callers that keep the agent ALIVE
must not use this").

## The fix

Find a fixture that still yields a live session with no viewer. The candidate
that needs no product change: build the orphan with a SECOND app instance on its
own `GHOZTTY_PIPE_SUFFIX` (both instances share the one `-debug` agent), then
kill only that instance. The first app never relaunches, so launch-time restore
never runs, and its chooser sees the dead instance's session as alive and
unattached — which is also closer to what a user hits than a manifest delete.

Whatever shape it takes, keep the section-4 positive control: "nothing happened"
must stay distinguishable from "the keys never arrived".

While in there, check whether the same assumption is baked into any sibling
script (`chooser-sessions.ps1`, `chooser-restore-all.ps1` reset `layouts.json`
with the agent already dead, so they look safe — confirm rather than assume).

## Validation criteria

- [x] `test\win32\chooser-resume.ps1` reaches section 4 and ends ALL PASS, twice
      in a row. (Two consecutive runs, ALL PASS (26 assertions) each,
      2026-08-16.)
- [x] The orphan the fixture builds is verified from OUTSIDE the app:
      `ghoztty +sessions --json` reports it `alive` and not `attached` before the
      chooser is opened. (Sections 1's orphan set and section 2's before/after
      attach reads all come from `+sessions --json`, which dials the agent
      directly.)
- [x] The section-4 positive control still fails when the keys are not delivered
      (prove it once by pointing the send at the wrong window). (Proven: with
      the Return sent to the terminal surface instead of the chooser, section 4
      failed both its assertions; reverted.)
- [x] Any sibling script relying on "delete the manifest ⇒ no restore" is either
      confirmed safe or fixed in the same change. (`chooser-sessions.ps1` builds
      no orphan — it kills app+agent together and resets state;
      `chooser-restore-all.ps1` was already rewritten to EXPECT the T194
      launch-time recovery and asserts it.)

## Related

Found while validating T333, which needed the same script and had to ship its
own (`test/win32/chooser-roster-refetch.ps1`) because this one cannot start.

## Progress log

- 2026-08-16 17:12: claimed; work starting.
- 2026-08-16 17:19 [session 788dbb9c]: Root cause confirmed: T194 launch restore adopts agent layout blobs, so the kill-app/drop-manifest fixture re-attaches everything. The task's proposed second-instance fixture doesn't survive contact: the manifest (session-layout-debug.json) and the agent layout store are shared across pipe-suffix instances, and reconcile() has no attached-elsewhere filter, so whichever instance launches second adopts the first's window and steals its session (agent rebinds to newest ATTACH). Production is safe (IpcServer pipe bind is the single-instance lock; second launch forwards+exits), so this is test-only. Decision (mechanism, mine per D46): add a loud test seam GHOZTTY_RESTORE_SKIP in App.restoreSessionLayout mirroring T657's GHOZTTY_RESTORE_PROBE_UNKNOWN — unset leaves every real launch byte-identical; set, it deterministically forces the real 'both restore sources empty/failed' degradation. Fixture keeps its original user-shaped kill-app/relaunch flow.
- 2026-08-16 17:32 [session 788dbb9c]: Fixture fixed and validated: GHOZTTY_RESTORE_SKIP seam added to App.restoreSessionLayout (T657 pattern); script relaunches under it and asserts the seam engaged. Second break found while validating: T602's sorted roster invalidated the script's index-space walking (section 3 was Return-ing on the app's own live row, dismissing the chooser, and section 4's Wait-LogLine matched section 3's leftover focus line - contaminated PASSes). Fix: chooser now logs cursor landings ('chooser roster: cursor on session id=... (row N of M)') per keystroke - the T318 say-it-out-loud oracle - and the script walks by cursor-id (Step-Cursor/Walk-CursorToId), with section 4's focus oracle counted not awaited. ALL PASS (26) twice; negative control proven (Return to wrong window -> section 4 FAILs). Siblings confirmed safe: chooser-sessions.ps1 builds no orphan; chooser-restore-all.ps1 asserts the T194 recovery.
- 2026-08-16 17:54: status: in-progress -> done [commit 622cdf9e1]
