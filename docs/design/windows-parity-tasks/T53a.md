---
id: T53a
title: "Soak harness test/win32/soak.ps1 + first bounded on-box soak + findings filed"
deps: ["T40"]
status: "done"
commits: ["517967173"]
---

# T53a — Soak harness test/win32/soak.ps1 + first bounded on-box soak + findings filed

## Summary

Soak harness `test/win32/soak.ps1` + first bounded on-box soak + findings filed. FOUND+FIXED: WM_APP_WAKEUP message-queue flood broke ALL IPC under load (see details); regression guard `test/win32/ipc-under-load.ps1`
