---
id: T907
title: "The running agent adopts a newer on-disk build with sessions open: handoff choreography, rollback, and the handoff_now policy arm"
order: 4
deps: ["T906"]
status: "done"
commits: ["176f88c04"]
seat: "win"
priority: "P1"
milestone: "M1"
triage-reason: "Increment 4 of T705 and its end-to-end payoff: the long-lived box finally adopts agent fixes without losing sessions or asking (the T662 defect)."
tags: ["feature", "infra"]
---

# T907 - The running agent adopts a newer on-disk build with sessions open: handoff choreography, rollback, and the handoff_now policy arm

## Summary

Final Windows increment of the T705 split (design:
`docs/design/agent-nondestructive-handoff.md`). With holders (T904-T906) in
place, the actual upgrade:

1. Old agent spawns the newer on-disk binary; the new agent adopts holders,
   binds nothing public, and reports READY over a private pipe.
2. Only on READY does the old agent close its listener and exit; the new agent
   takes the single-instance guard and public pipe (existing
   `--force-replace` machinery in `single_instance.zig`).
3. Any failure before READY: the new process exits, the OLD agent keeps its
   listener and sessions. "Neither agent" is unreachable by construction.

Policy (`agent_upgrade.zig`): a third arm `handoff_now` — stale AND every live
session holder-backed AND capability advertised. Asks nobody, loses nothing,
never deferred. Mixed-generation agents (legacy direct-ConPTY sessions still
alive) DRAIN LAZILY: the handoff waits, and `+sessions --agent` names how many
legacy sessions are holding it back; the forced path remains the existing
explicit confirmation.

## In plain terms

The background session manager can finally update itself while you keep
working: a new copy starts, quietly takes over the caretakers holding your
sessions, and the old copy bows out — no dialog, no lost session. If anything
goes wrong mid-swap, the old copy simply stays in charge.

## Goals

- [x] READY-gated handoff with rollback that always leaves the original agent
      serving on failure.
- [x] `handoff_now` policy arm + capability gating; `+sessions --agent`
      reports handoff-readiness and legacy-session drain count.
- [x] Lazy drain for mixed-generation agents; confirmation stays the only
      forced path.
- [x] End-to-end acceptance (carried from T705's validation criteria).

## Validation criteria

(Carried verbatim from T705.)

- [x] A running agent adopts a newer build with live sessions open, no dialog,
      and no session lost - measured with an acceptance script that types into
      a pane before and after (`Test-PaneLive`).
      HOW: `test\win32\agent-handoff.ps1` sections A-C, ALL PASS (29
      assertions). Nothing in the script kills anything: a newer build is copied
      to the canonical path while the agent serves, and B1/B2 watch the
      successor appear and the predecessor exit on their own. C1 is the headline
      (the SAME shell pid under the new manager), C3 the pre-handoff marker, C5
      the round trip.
- [x] A handoff that fails leaves the ORIGINAL agent running and its sessions
      reachable; it never lands on "neither agent".
      HOW: section D. The candidate is a stand-in that answers `--version` and
      then exits at once, so READY never comes; D3/D4 assert the original
      manager is still the only one, and D5 types into its pane.
- [x] `+sessions --agent` reads `current` afterwards, from the same box, with
      the same pane still alive.
      HOW: the C5 diagnostic path reads `status: current` from the successor
      while the pane answers; A7 and E4 pin the new `handoff` /
      `legacy_sessions` fields (`ready` with an empty drain, `draining` with a
      legacy session live), and `test\win32\sessions-agent-build.ps1` (43
      checks) pins the human `next:` row and the JSON key set.
- [x] Skew both ways still degrades exactly as the agent contract requires.
      HOW: `capability.agent_handoff` is negotiated as the INTERSECTION, so a
      peer of either age leaves it false and the pre-T907 policy applies
      unchanged - asserted in `protocol.zig` ("negotiate: agent_handoff is the
      intersection, and skew keeps the old policy", both directions) and in
      `agent_upgrade.zig` (`decide` is pinned to the pre-T907 answers, and an
      incapable agent still takes `confirm_first`). `SessionInfo.holder_backed`
      is additive and absent decodes as legacy, which can only ever hold a
      handoff back - never permit one that would lose a session.
- [x] (added) The handoff arms are teeth-checked: `-NegativeControl` inverts
      exactly B1-B3 and C1-C4 and nothing else moves.

## Progress log

- 2026-08-17 02:33: claimed; work starting.
- 2026-08-17 02:39 [session e8b0bfa1]: Design settled. KEY MECHANISM CALL (mine per go.md 5b - no user-visible difference between the options): the handoff is AGENT-DRIVEN, not app-driven. The old agent runs a small supervisor thread that compares the on-disk binary beside it (its own exe path with a trailing .bak/.bak-* stripped - every delivery renames the running exe out of the way, so that IS the new build's path) against its own baked stamp, and hands off when stale AND every live session is holder-backed. Why this over an app->agent request: (a) it needs NO new wire opcode, only two additive fields (a capability string + SessionInfo.holder_backed); (b) the agent is the only side that KNOWS which sessions are holder-backed; (c) it works with no app running at all, which is exactly the morning-refresh case the T525 directive carves out. Choreography: old agent spawns the successor with the SAME argv + --handoff-successor=<private pipe> via spawnEscapingJob; the successor connects, sends READY, and WAITS - it acquires no lock and binds nothing public until GO. On READY the old agent snapshots rings + persists meta (so holder_snapshot_offset is exact for the successor's ATTACH ack), sends GO and process.exit(0)s - which is what releases the holders, exactly like the console-ctrl path. Any failure before GO: kill the successor, keep serving; nothing was given up, so 'neither agent' is unreachable.
- 2026-08-17 04:15 [session e8b0bfa1]: BUILT + VALIDATED. Shape that landed: (a) handoff.zig - a supervisor thread in the local pipe daemon that watches the binary beside it (selfExePath with a trailing .bak/.bak-* stripped, which IS the new build's path after any delivery), probes it with --version (cached on size+mtime so a steady box pays a stat per tick), and on stale-AND-no-legacy-session spawns it out of our job with the same argv plus --handoff-successor=<private nonce pipe> and --force-replace; (b) the successor binds that pipe FIRST, sends READY <pid> <stamp>, and takes no guard and binds nothing public until GO - so a failure before GO terminates it and the original agent is untouched; (c) on READY the old agent snapshots rings + persists meta (the successor's ATTACH ack offsets) and process.exit(0)s, which is what releases the holders - nothing is terminated; (d) capability.agent_handoff negotiated BOTH ways + additive SessionInfo.holder_backed, so the app's new agent_upgrade arm .handoff_now stands down and +sessions --agent answers ready/draining/unsupported with the drain count. TWO DEFECTS THE ACCEPTANCE CAUGHT, both mine: the force seam refused a successor reporting our own stamp - which is exactly the fixture shape (two copies of one binary) - so the whole arm was unreachable; and Test-PaneLive SENDS ONCE then polls, so its keystrokes were typed into a surface the app was about to replace during in-place recovery and could never reappear (the pane was live all along - a hand-typed token round-tripped). Bounded retries per candidate build (3) so a rollback cannot respawn forever. Green: floor lib/none/win32/agent ALL LANES PASS; agent-handoff.ps1 ALL PASS (29) with -NegativeControl reddening exactly B1-B3/C1-C4; sessions-agent-build 43; agent-sharing-uplink 18; release-artifacts ALL PASS (2 Docker skips); isolation-meta; P1/P2/P3 25/20/16. Filed T912 (holder default), T913 (morning refresh ships the agent), T914 (release-artifacts guard unclearable without Docker).
- 2026-08-17 04:17: status: in-progress -> done [commit 176f88c04]
