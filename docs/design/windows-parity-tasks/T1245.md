---
id: T1245
title: "git-commit-guard silently reuses a stale message file when -MessageFile does not exist"
order: null
deps: []
status: "done"
commits: ["f0a6eb2db"]
seat: "win"
priority: "P1"
tags: ["infra"]
---

# T1245 - git-commit-guard silently reuses a stale message file when -MessageFile does not exist

## Summary

`scripts\git-commit-guard.ps1 commit -MessageFile temp\msg2.txt` was run on
2026-09-01 with no such file written for that turn. It did not refuse: it
committed and PUSHED using a leftover `temp\msg2.txt` from an earlier turn, so
e4824918e - which records the sha that closed T1195 - is titled "record the
commit that closed T1170".

That is the exact failure mode T948 built this guard to prevent, from the
message side rather than the pathspec side: the guard reads the commit back and
verifies the PATHS, and nothing at all checks that the SUBJECT was written for
this commit. A stale file is worse than a missing one, because a missing file
would at least have produced an empty message somebody noticed.

The commit message is the dashboard's activity feed (go.md step 6), so a stale
subject is not cosmetic - it is a feed item describing work that is not in the
commit.

Fix: refuse when `-MessageFile` names a path that does not exist, and refuse an
empty one. Consider having the guard DELETE the message file after a successful
commit, so a stale one cannot exist to be picked up.


## Validation criteria

- [x] A `-MessageFile` that does not exist is refused, with the path named -
      `MESSAGE FILE MISSING: <path>`, exit 2, HEAD unmoved (W34/W35)
- [x] An empty or whitespace-only message file is refused - `MESSAGE FILE
      EMPTY`, exit 2, HEAD unmoved (W36/W37)
- [x] The message file is consumed (deleted) after a successful commit (W38/W39),
      and the 2026-09-01 shape end to end - a second turn reusing the same path
      without writing it - is now the refusal rather than a stale subject (W40)
- [x] Demonstrated red before green: with `scripts\git-commit-guard.ps1` reverted
      to HEAD the harness scored `4 FAILURE(S)` - W34, W36, W39, W40 - and the
      whole harness is ALL PASS with the fix in place

## Goals

- [x] a message file that was not written for this commit cannot be committed
- [x] a used message cannot survive to be reused by the next turn
- [x] the refusals are demonstrated red in the acceptance harness


## In plain terms

When the loop commits its work, the commit's description can silently be an old
one left over from a previous piece of work. The dashboard shows that
description as what happened, so the feed can end up describing the wrong
thing.

## Progress log

- 2026-09-01 21:56: claimed; work starting.
- 2026-09-01 22:03: root cause: the guard proved the commit PATHS but never that the message file belonged to this turn; a leftover temp\msg2.txt was reused. Fix in scripts\git-commit-guard.ps1: refuse a missing or empty -MessageFile (exit 2) and DELETE the file after a verified commit so staleness cannot exist. Arms W34-W40 in test\win32\go-loop-guard.ps1; green with the fix, red demo running against the reverted script.
- 2026-09-01 22:39: status: in-progress -> done
