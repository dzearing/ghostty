---
id: T584
title: "The tab strip and window title draw at 16px, a size the type ramp does not have"
order: null
deps: ["T313"]
status: "done"
commits: ["2bc45982f"]
seat: "win"
priority: "P2"
triage-reason: "P2: a size the ramp does not have. Same consistency debt as T583, same low user impact"
tags: ["polish"]
---

# T584 - The tab strip and window title draw at 16px, a size the type ramp does not have

## Summary

`Window.zig:686` sizes the tab strip and window title with a bare literal:

```zig
const font_height: i32 = -@as(i32, @intFromFloat(16.0 * self.scale));
```

16 is not on the type ramp. After T313 every dialog reads at 14, and the tab
titles — the text a user looks at more than any other chrome in the app — read
at 16. Unlike the dialogs before T313, this is not seven copies of one number;
it is one copy of a number that is not in the system at all.

## In plain terms

Ghoztty's tab titles are set slightly larger than every other piece of text in
its own interface. It is not obviously wrong on its own, which is why it has
survived — but it is a size nobody chose, and the app now has a rule about
sizes that this one number sits outside.

## Goals

- [x] The tab strip and the in-strip window title read at a size the type ramp
      actually has
- [x] The number lives in one named place, not twice in `Window.zig`
- [x] The design system says which role it is and why chrome is not an exemption
- [x] The strip's own geometry is asserted against that size at every scale

## Details

- This one needs a DECISION before a patch, and it is not the same decision as
  the dialogs. Two things make it different:
  1. **The face is user-configurable** (`window-title-font-family` feeds
     `title_font.faceName`), so this text is already partly the user's, not
     purely ours. Whether the SIZE should follow the ramp when the FACE does
     not is a real question.
  2. **A tab title is the app's primary chrome**, not dialog body text. Fluent
     ships tab labels at the body size, which argues for 14 — but check a
     current Win11 app (Terminal, Edge, File Explorer) and measure rather than
     asserting it from memory.
- If the answer is "the ramp", the change is small and the acceptance surface
  is `tab_strip_layout.zig`, which already asserts its geometry at
  1.0/1.25/1.5/2.0 — the tab height and the title's measured extent both move,
  so re-assert them rather than letting them drift.
- If the answer is "chrome has its own size", say so in
  `win32-design-system.md` §2.4 with the reason, and name the constant instead
  of leaving a literal in `Window.zig`. An off-ramp number that is WRITTEN DOWN
  as off-ramp is fine; an undocumented one is the defect.

## Validation criteria

- [x] The size is either on the ramp or a named, documented constant - no bare
      literal left in `Window.zig` (both `16.0 * self.scale` copies are gone;
      `createTabFont` and `titleFontEm` both read `title_font.em`, which returns
      `type_ramp.body(scale).height`)
- [x] `win32-design-system.md` A2.4 records which, and why (the ramp's BODY
      role, with the two arguments against it - user-configurable face, primary
      chrome - answered rather than skipped, and the weight exemption stated)
- [x] `tab_strip_layout.zig` asserts the resulting tab/title geometry at
      1.0/1.25/1.5/2.0 (two new tests: a ramp-body line box fits the tab band at
      every scale, and a content-sized tab is wider than an empty one by exactly
      the measured text)
- [x] The tab-strip acceptance scripts stay ALL PASS - `tab-color.ps1` ALL PASS
      (17). `tab-strip.ps1` has ONE red assertion and it is **not this task's**:
      the T584 bisect (em forced back to `16.0 * scale`, rebuilt, re-run) fails
      it identically, with the long and short tabs measuring equal widths either
      way. Filed as T1396. Two other scripts were red on arrival and bisected
      the same way: T1394 (`tab-strip-autohide.ps1`, the '...' probe geometry)
      and T1395 (`tab-tooltip.ps1`, the `~` home abbreviation).
- [x] Floor: both `zig build test` lanes, `test-agent`, `lib`, P1-P3 -
      `floor-lane.ps1 -Lane all` ALL LANES PASS (lib/none/win32/agent);
      ipc-p1 (25), ipc-p2 (20), ipc-p3 (16) all ALL PASS. Guards run clean:
      test-reach (14), printclient (8), viewer-close (44).

## Progress log

- 2026-09-06 01:49: claimed; work starting.
- 2026-09-06 02:07: decision (mine, not a user-facing one - the options differ only in which number the chrome reads, not in a mac->win translation the user could adjudicate): the tab strip and in-strip window title take the ramp's BODY role (14), read through a new title_font.em(scale). Rationale in win32-design-system.md 2.4: a tab label is the app's own chrome text, the same kind as a list-row title, and Win11's tabbed surfaces set labels at body; a user-configurable FACE says nothing about whose ramp the SIZE belongs to. Weight stays 400 - the strip's emphasis is color. Removed both bare '16.0 * scale' literals from Window.zig (createTabFont + titleFontEm). Strip height is unaffected (fixed 40 DIP band); what moves is the label size and every tab width measured from it.
- 2026-09-06 06:20 [session 10186e00-55fe-4648-87c0-ce2b44b06059]: turn ended mid-validation; resetting context. State on disk: title_font.em (ramp body) + Window.zig both literals removed + tab_strip_layout T584 tests + design-system 2.4 paragraph, ALL uncommitted. Floor lanes ALL PASS, win32 Debug build green, tab-strip.ps1 ALL PASS (70), tab-color.ps1 ALL PASS (17). OPEN: tab-strip-autohide.ps1 has 1 FAIL (the '...' probe answers HTMINBUTTON 8, not HTSYSMENU 3) and tab-tooltip.ps1 has 4 FAIL (paths render as C:\Users\David\... where the tip expects ~). Both look PRE-EXISTING and unrelated to a font size - the caption trio is right-anchored from caption_layout with no font input, and T496 made it full-height zero-gap slabs while the script still steps by (square+gap), which is exactly the cumulative drift that would land the leftmost probe on minimize - but that was NOT yet proven when the turn ended. A temporary 'if (true) return 16.0*scale' bisect line was added to title_font.em to prove it and has been REVERTED; the bisect (rebuild, re-run both scripts, compare) is what the next turn should do first. Also still owed: viewer-close.ps1 (my Window.zig edit makes that guard due) before validate will pass.
- 2026-09-06 06:28: stale in-progress claim picked up by a new turn; reassessing from this log and git status before resuming.
- 2026-09-06 06:28 [session 86c891e0]: resumed after reset; kept the on-disk work (reviewed the diff: title_font.em + both Window.zig literals removed + 2 unit tests + design-system 2.4). First job per the last entry: bisect whether tab-strip-autohide.ps1 (1 FAIL) and tab-tooltip.ps1 (4 FAIL) are pre-existing, by forcing em() back to the old 16.0*scale and re-running both.
- 2026-09-06 06:45 [session 86c891e0]: BISECT DONE, and both reds are PRE-EXISTING. Forced title_font.em back to the old off-ramp 16.0*scale, rebuilt win32 Debug (exit 0), re-ran both scripts: tab-strip-autohide.ps1 still fails the same single assertion (the '...' probe answers HTMINBUTTON 8, not HTSYSMENU 3 - and the script's own later assertion still opens the window menu from that button, so the button works and the probe coordinate is what drifted, exactly the T496 zero-gap-slab story), and tab-tooltip.ps1 still fails the same four (the tip prints the full C:\Users\David\... where it expects ~). Neither has anything to do with the tab strip's font size. Bisect line reverted; rebuilt clean (exit 0). Filed T1394 (the autohide probe geometry) and T1395 (the tooltip's home lookup goes through HOMEDRIVE+HOMEPATH and returns null when they are unset, where USERPROFILE is what the rest of the codebase uses - reproduce by hand before patching, because if it is real every user with those unset sees full paths).
- 2026-09-06 07:14: status: in-progress -> done [commit 2bc45982f]
