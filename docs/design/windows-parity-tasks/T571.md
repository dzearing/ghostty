---
id: T571
title: "The banner chevron\u0027s hovered FILL is a permanent SKIP in pane-banner.ps1 - give it the DIB-paint oracle T165 used for links"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
tags: ["test"]
---

# T571 - The banner chevron's hovered FILL is a permanent SKIP in pane-banner.ps1 - give it the DIB-paint oracle T165 used for links

## Summary

pane-banner.ps1 section 6f can prove the chevron's hover STATE (from the 'banner chevron hover=' debug line) but never its hovered FILL: on the background test desktop there is no real pointer, so Windows delivers WM_MOUSELEAVE a frame after every posted WM_MOUSEMOVE and the hovered frame is gone before PrintWindow reads it. It has printed 'SKIP T209 chevron fill' on every run since. T165 hit the same wall for the link underline and solved it in the zig lane instead: paint the same overlay twice into a CreateDIBSection memory DC with only the hover field different, and count pixels - no pointer, no desktop, no timing (see the test 'banner overlay: a link's underline is dotted at rest and solid on hover', which also carries a negative control). Do the same for the chevron's rest/hover fill and its rounded corner, and turn the acceptance SKIP into a pointer at that test.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
