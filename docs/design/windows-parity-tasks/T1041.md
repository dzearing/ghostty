---
id: T1041
title: "39 acceptance harnesses read DUE after the did-not-finish marker landed in every scored script"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "Follow-up bookkeeping to T1042: enumerate why 39 harnesses read DUE after the did-not-finish marker landed, so the next mechanical sweep does not mute the staleness gate again"
tags: ["test", "infra"]
---

# T1041 - 39 acceptance harnesses read DUE after the did-not-finish marker landed in every scored script

## Summary

T1039 inserted a one-line completion marker into all 58 scripts scored by lib\TestScore.ps1. Every one of those files is covered by a guard row, so scripts\guard-due.ps1 now reports 39 harnesses DUE - each of them correctly, since nobody has run them against the file as it now stands. The change to each is mechanical and its placement is proved statically by test\win32\body-complete-audit.ps1, which is why the T1039 commit was made under validate -NoGuardDue rather than by re-stamping anything. This task is the debt: work the DUE list off a few harnesses per turn (run it, read the last line, let a clean green run re-stamp), starting with the cheap non-GUI sweeps and the agent/holder families that share a fixture. Do not clear a row any other way - a stamp written by hand is the exact lie T783 exists to prevent.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-08-20 23:08 [session 16d4e950]: Filed by T1039's turn. That turn worked the list down from 39 to 32 before committing: the P1-P3 floor (ipc-p1/p2/p3), the suite-wide sweeps (verdict-exit, isolation-meta, launch-preflight, stderr-capture, divergence-inventory, body-complete) and three app-launching harnesses (cli-unknown-flag, window-name-env, hook-json) all ran green with the marker in place and re-stamped. What is left is the GUI/agent families - holder-*, agent-*, chooser-*, upgrade-*, crash-*, task-dashboard, test-desktop, resize-flicker, activity-*, and the rest.
