---
id: T331
title: "Resuming a REMOTE browsed session has no on-box coverage: FakeRelay can now answer one"
order: 358
deps: ["T320"]
status: "done"
priority: "P2"
triage-reason: "on-box coverage for the remote resume path that shipped in T320; test work"
commits: ["caae83b68"]
tags: ["test"]
---

# T331 - Resuming a REMOTE browsed session has no on-box coverage: FakeRelay can now answer one

## In plain terms

You can open the machine list, look at the sessions still running on another
computer, and pick one up here. That works - but nothing on this box had ever
actually tried it, so the day it stops working, nobody finds out until a user
does. This adds a test that does the whole thing for real: it stands up a second
"machine", walks the keyboard onto one of its sessions, presses Return, and then
checks the session really came back with its own output in it, that typing in it
reaches the far end, and that when the other machine goes offline mid-keystroke
you get told so instead of an empty window.

## Goals

- [x] a test that resumes a session on another machine, driven by the keyboard
- [x] proof the session came back live, not as a picture of itself
- [x] proof a resumed window does not inherit the machine's default folder
- [x] a machine that goes offline leaves the dialog open with an explanation

## Validation criteria

- [x] `test\win32\chooser-resume-remote.ps1` is ALL PASS on box (38 assertions,
      2026-08-20; the run also stamps its own guard row)
- [x] the resume is proven end to end rather than by the app's own claim: the
      relay's request log counts the dial on the far side, `attach: session=<id>`
      is the pane's own account, and `+sessions --json` (the agent, dialled
      directly over its pipe) reports the session ATTACHED where it was not
- [x] the ATTACH shape is asserted WITH a positive control: the resumed pane is
      not in the stored per-host directory, while a `+new-remote-window` on the
      same machine in the same run IS
- [x] the failure path is asserted with a positive control: a tripped relay
      leaves the chooser open with its hint and no window, and the same keys on
      the same row then resume it
- [x] the liveness arm has teeth: re-run under `GHOZTTY_TEST_LIVENESS_BREAK=1`
      reddened exactly that assertion (1 FAILURE, 37 still passing) and did not
      stamp
- [x] the LOCAL run is unchanged: `test\win32\chooser-resume.ps1` still ALL PASS
- [x] the floor is green (`scripts\floor-lane.ps1 -Lane all`)

## Summary

T320 shipped both halves of resume: `App.resumeLocalSession` and
`App.resumeRelaySession`. Only the LOCAL half is exercised on box —
`test/win32/chooser-resume.ps1` builds its fixture out of the local agent's own
orphaned sessions, because that is the machine the script can create sessions
on.

The remote half is therefore code that compiles, is reachable from the UI, and
has never once run on this box:

- `MachineChooser.resumeRow`'s `.remote` branch (the dial and its two failure
  messages),
- `App.resumeRelaySession` (`src/apprt/win32/App.zig`),
- `openDialedWindow`'s new ATTACH shape — `session_id` set, and the per-host
  cwd/shell defaults deliberately SKIPPED because an attach does not spawn.

That last one is the interesting risk: an ATTACH that also sent a working
directory would be describing a spawn that is not happening, and nothing on box
would notice.

## Why it is testable now

T319 built `test/win32/lib/FakeRelay.ps1` — a device directory plus a real RFC
6455 upgrade bridged to a `ghoztty-agent --listen`, with 401/502 injection and a
trip file. `activity-monitor-remote.ps1` and the roster's own remote path
already run through it. A remote resume needs nothing new from it: the bridged
agent is a real agent, so it has real sessions to resume.

## The work

Extend `chooser-resume.ps1` (or a sibling) with a remote run:

1. Start FakeRelay over a `ghoztty-agent --listen` with at least one live,
   unattached session.
2. Select the relay device row, walk the cursor into its roster, Return.
3. Assert `resume session: attaching remote session id=… device=…`, the chooser
   dismissing, and — the independent oracle — the bridged agent reporting that
   session `attached`.
4. Negative: with the relay tripped (or the bearer rejected), the resume must
   leave the chooser OPEN with its hint set, never a half-open window.

## Validation

The new run is ALL PASS, and the local run is unchanged.

## Progress log

- 2026-08-20 23:14: claimed; work starting.
- 2026-08-20 23:32 [session acba68f8]: new harness test\win32\chooser-resume-remote.ps1: PipeBridge+FakeRelay fixture (local agent as a relay device), resumes a browsed REMOTE session by keyboard; asserts the dial crosses the relay, the pane ATTACHes, the ATTACH skips per-host defaults (with a +new-remote-window positive control that DOES take them), pane liveness, agent-side attached oracle, and a tripped-relay failure that leaves the chooser open with its hint. ALL PASS (38); liveness teeth-check reddened exactly that arm.
- 2026-08-20 23:57: status: in-progress -> done [commit caae83b68]
