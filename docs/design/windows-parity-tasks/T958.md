---
id: T958
title: "Move the fork to Zig 0.16.0 - the single upstream commit that carries more than half the merge risk"
order: null
deps: ["T879", "T957"]
status: "blocked(D80 corrected 2026-08-22: an upstream pull happens only when the user calls for one, and lands in the mac build first. Un-block when the user asks for a pull)"
commits: []
seat: "win"
priority: "P2"
milestone: "M1"
triage-reason: "Over half the upstream merge cost sits in one toolchain commit; until it lands every other upstream stage inherits the same conflict surface."
tags: ["infra", "perf"]
---

# T958 - Move the fork to Zig 0.16.0 - the single upstream commit that carries more than half the merge risk

## Summary

Upstream commit e8525c0fd 'Update to Zig 0.16.0' (committed 2026-07-21) raised minimum_zig_version to 0.16.0; we are pinned at 0.15.2 in build.zig.zon. That one commit touches 357 files and 71 of the 131 merge-risk files - more than half the entire merge risk - and splits upstream's 764-commit range into 372 before and 392 after. Until it lands, no upstream commit after 2026-07-21 can be taken at all. Stage S4 of docs/design/windows-parity-upstream-pull-plan.md. THIS TASK MUST BE SPLIT WHEN PICKED UP: at minimum the toolchain bump itself, the src/apprt/win32 port to 0.16 APIs (2000+ files of ours upstream's bump never saw), ghoztty-agent, and the src/os/env.zig replacement tracked separately. Gated on D80.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-08-22 05:12: status: todo -> skipped(D80 reversed 2026-08-22: Ghoztty is a permanent hard fork, so there is no upstream merge whose risk this reduces) (by user directive 2026-08-22: never merge back to ghostty)
- 2026-08-22 05:24: status: skipped(D80 reversed 2026-08-22: Ghoztty is a permanent hard fork, so there is no upstream merge whose risk this reduces) -> blocked(D80 corrected 2026-08-22: an upstream pull happens only when the user calls for one, and lands in the mac build first. Un-block when the user asks for a pull) (by user clarification 2026-08-22: pulling FROM upstream is allowed but user-triggered, mac seat first)
