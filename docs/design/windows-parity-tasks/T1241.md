---
id: T1241
title: "The install, update and diagnostics acceptance scripts still open Ghoztty windows on the user\u0027s desktop (T1193 batch 4 of 4)"
order: null
deps: ["T1193"]
status: "in-progress"
commits: []
seat: "win"
priority: "P1"
tags: ["test", "fix"]
---

# T1241 - The install, update and diagnostics acceptance scripts still open Ghoztty windows on the user's desktop (T1193 batch 4 of 4)

## Summary

T1193 measured the gap and built the sweep; this batch is 9 of the install, update and diagnostics
scripts it left declared as pending. Each one starts the app from a process
sitting on the user's desktop - a bare `Start-Process $Exe`, or a cold
`& $Exe +new-window`, which is the one verb that auto-launches - so running it
throws a window across whatever the user is reading.

The migration is mechanical and T1193's `ipc-p1.ps1` / `ipc-p2.ps1` /
`ipc-p3.ps1` are the worked examples:

1. `. (Join-Path $PSScriptRoot 'lib\TestDesktop.ps1')` and `$td = New-TestDesktop`
   before the body; `Remove-TestDesktop` in the teardown.
2. Replace every `Start-Process $Exe` with `Start-OnTestDesktop`.
3. Replace every `& $Exe <verb>` with a local one-line `Ghoz` helper over
   `Invoke-OnTestDesktop`, and read `$r.ExitCode` where the script read
   `$LASTEXITCODE`. The helper captures stdout and stderr to a file, so a
   `cmd /c "... > file"` dance for the GUI-subsystem exe (T245) goes away too.
4. DELETE the script's `# @user-desktop-launch:` line from `lib\TestDesktop.ps1`.
   A migration that leaves it behind fails `desktop-launch-audit.ps1` as a
   stale declaration - that is what makes the list burn down.

Scripts in this batch:

- `test\win32\clipboard-retry.ps1`
- `test\win32\crash-diagnostics.ps1`
- `test\win32\install-prepare.ps1`
- `test\win32\path-selfheal.ps1`
- `test\win32\reset-context.ps1`
- `test\win32\soak.ps1`
- `test\win32\update-apply.ps1`
- `test\win32\update-check.ps1`
- `test\win32\wheel-scroll.ps1`


## In plain terms

Running the test suite used to throw terminal windows across whatever you were
reading. Nine of the tests still did that - the ones covering install, update
and crash diagnostics - because they started the app from a process sitting on
your desktop, so every window it opened landed there. After this they all run on
a private, invisible desktop instead. They check exactly what they checked
before; they just stop taking over the screen.

## Goals

- [x] all nine scripts start the app through the test-desktop harness
- [x] each one still asserts what it asserted before, and still passes on the box
- [x] the declaration list burns down by 9, so the sweep can prove it

## Validation criteria

- [x] every script in the list runs green, on the box, unchanged in what it asserts
      (each run individually: wheel-scroll, path-selfheal, crash-diagnostics,
      clipboard-retry, update-check, update-apply, install-prepare,
      reset-context all ALL PASS; soak.ps1 ALL PASS on its documented
      `-Minutes 5` smoke)
- [x] `test\win32\desktop-launch-audit.ps1` is ALL PASS with these entries gone from the declaration list
- [x] the pending-migration count in its NOTE line has dropped by 9 (10 -> 1;
      the one left is session-relaunch.ps1, held by T1265)
- [x] no ghoztty window appears on the user's desktop while the batch runs

## Progress log

- 2026-09-01 20:11: claimed; work starting.
- 2026-09-01 20:17: migrated all 9 scripts to the test-desktop harness (Start-OnTestDesktop / Invoke-OnTestDesktop) and deleted their declarations; audit NOTE now reads 6 declared, 1 pending (was 15/10). Two non-mechanical bits: wheel-scroll's private WheelDrv EnumWindows/PostMessage P/Invoke and update-apply's ApplierDialog EnumWindows both enumerate the CALLER's desktop, so both were replaced with harness calls. Now running each script.
