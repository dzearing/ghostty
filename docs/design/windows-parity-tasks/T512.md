---
id: T512
title: "cmd.exe panes never update their title or emit prompt marks: consider PROMPT-based $E integration (OSC 2/7/133)"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P2"
tags: ["polish"]
---

# T512 - cmd.exe panes never update their title or emit prompt marks: consider PROMPT-based $E integration (OSC 2/7/133)

## In plain terms

A pane running cmd never changes its tab title as you move around, the way
PowerShell panes do — it says "cmd" (or whatever it started as) forever, so a
strip of cmd tabs is a row of identical labels. cmd also never tells the
terminal "a prompt starts here", so prompt-jumping features don't work in it.

## Summary

cmd.exe has no profile/rcfile hook, so `shell_integration.zig` treats it as
non-integrable ("cmd.exe cannot support integration at all — it has no prompt
hook"). But cmd's `PROMPT` variable supports `$E` (ESC), which is enough to
emit OSC sequences at every prompt render — Windows Terminal uses exactly this
for its cwd tracking (`$e]9;9;%cd%$e\`). Ghoztty could inject a `PROMPT` env
var at spawn (append to the inherited value or the `$P$G` default):

- `$E]2;$P$E\` — title = current directory (OSC 2)
- `$E]7;kitty-shell-cwd://%COMPUTERNAME%$P$E\` — cwd (OSC 7; the win parser
  already normalizes `/D:\x` shapes, `stream_handler.zig reportPwd`)
- `$E]133;A$E\` / `$E]133;B$E\` — prompt marks (partial: no C/D possible)

Note the CWD half is already solved by T185's live process-cwd fallback
(`os/process_cwd.zig`) — the remaining user-visible value here is the TITLE
(and partial prompt marks). If this lands, the OSC 7 it emits would set
`pwd_reported` and the T185 fallback stands down automatically, so the two
mechanisms compose rather than conflict.

Hazards to design around: a user's own `PROMPT` customization must be
preserved (append, don't replace); `set PROMPT=` inside the session loses the
integration (acceptable, same as any shell unsetting its hooks); nested cmd
inherits the var (fine — still correct).

## Goals

- [x] cmd is a recognized shell (`Shell.cmd`) rather than a documented dead end
- [x] its integration rides an injected `PROMPT`, with argv left bare
- [x] the user's own `PROMPT` is wrapped, never replaced
- [x] `/c` and `/k` invocations are left completely alone
- [x] cmd's own `title` command keeps working alongside the new tracking
- [x] acceptance on the box, green

## Validation criteria

- [x] A cmd pane's tab title follows `cd` (shows the current directory) — B9
      of `test\win32\agent-shell-integration.ps1`. Nothing but the OSC 7 out
      of the injected PROMPT can move that title (the T185 PEB fallback feeds
      `+list` and directory inheritance, never the title), so this doubles as
      the proof the sequences reach the terminal and parse.
- [x] cmd's own `title` command still works and is not overwritten by the
      next prompt — B11/B12. This is the reason the prompt sends no OSC 2;
      see the note below.
- [x] A user-customized PROMPT still renders (integration appends, not
      replaces) — B8 reads `set PROMPT` back out of the pane and finds the
      wrapped `$P$G` intact; the `cmd wraps the user's prompt` unit test
      covers a customized one (`[mine]$P$G`).
- [x] `pwd_reported` flips true for cmd panes (OSC 7 arriving), fallback
      stands down — B7 (the sequence is emitted), B13 (`+list` reports the
      cd'd directory, which a mangled URL could not produce), and the
      `parse: kitty-shell-cwd with a native Windows path` test in
      `src/os/uri.zig`, which covers the link that could plausibly have
      failed: `$P` can only spell the path natively, so the URL carries
      backslashes and an unencoded drive colon.
- [x] Acceptance script or extension of an existing one, ALL PASS on the box —
      section B of `test\win32\agent-shell-integration.ps1`, ALL PASS.

## Why there is no OSC 2 (the title half, as built)

The task's own sketch had the prompt send `$E]2;$P$E\`. That was tried and
backed out, because it breaks `title`, which is how a cmd user names a window:
an OSC 2 on every prompt overwrites `title foo` a fraction of a second after
they type it. `test\win32\ipc-p2.ps1` caught it — three assertions there have
always used cmd's `title` as their oracle.

Dropping the OSC 2 alone did not work either, and measuring said why: cmd sets
its console title to its own image path at startup, so the pane arrives already
titled `C:\WINDOWS\system32\cmd.exe` and `stream_handler`'s `seen_title` is
true before the first prompt. That is the actual mechanism behind "it says cmd
forever", and OSC 7 could never override it.

So the title half lives in the terminal instead: the FIRST valid OSC 7 clears
`seen_title` (`seen_pwd` in `src/termio/stream_handler.zig`). A shell reporting
its directory for the first time means the shell is up, so any title before
that was startup noise; a title set after it still wins and stays won. cmd
therefore gets both — a tab that tracks the directory, and a `title` command
that sticks — and the change is a no-op for bash/zsh/pwsh, whose integrations
re-title on the same prompt.

## Progress log

- 2026-09-04 23:05: claimed; work starting.
- 2026-09-04 23:07: design settled: add .cmd to the Shell enum and inject a PROMPT that wraps the user's own prompt in OSC 133;A / OSC 2 / OSC 7 (kitty-shell-cwd://localhost/$P) / OSC 133;B, using cmd's $E escape. No script file to dot-source, argv is left untouched; /c and /k invocations are skipped, and the injection is idempotent so a nested cmd does not stack a second copy.
- 2026-09-04 23:37: landed: Shell.cmd + setupCmd in shell_integration.zig (env-only, PROMPT injection), Surface.zig arm for the agent path, a uri.zig regression test proving the kitty-shell-cwd URL survives a native backslash path, and section B of test/win32/agent-shell-integration.ps1 rewritten as the T512 acceptance (B5-B10). ALL PASS on the box; the tab title of a cmd pane now follows cd. Test trap found and fixed while writing it: 'echo %PROMPT%' is expanded by Run-Cli's own outer cmd.exe, so it passed with the harness's prompt for the wrong reason - 'set PROMPT' is the correct probe.
- 2026-09-04 23:59: course correction, and it is the substance of the task: the OSC 2 the task sketched breaks cmd's own 'title' command (ipc-p2 caught it - three of its assertions use 'title' as their oracle), and dropping OSC 2 alone does not work either. Measured why: cmd titles its console with its own image path at startup, so a pane arrives titled 'C:\WINDOWS\system32\cmd.exe' and seen_title is true before the first prompt - that IS the 'says cmd forever' defect. Title half moved into the terminal: the first valid OSC 7 clears seen_title (seen_pwd in stream_handler.zig), so a shell announcing its cwd un-suppresses pwd-titling while a title set AFTER still wins. cmd now gets both. No-op for bash/zsh/pwsh.
