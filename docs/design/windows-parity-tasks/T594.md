---
id: T594
title: "Test runs open t390-style http pages in the user's default browser (Edge) and leave them behind - a test must never reach the user's desktop"
order: null
deps: []
status: "done"
commits: ["f145d49eb"]
seat: "win"
priority: "P1"
tags: ["fix", "test"]
triage-reason: "user-reported 2026-08-08, recurring: Edge windows keep appearing on the interactive desktop during test runs and accumulate until closed by hand"
---

# T594 - Test runs open http pages in the user's default browser and leave them behind

## In plain terms

While the automated tests run, Edge windows showing strange local test pages
(like `127.0.0.1:.../t390.html`) sometimes appear on the desktop and stay
there until closed by hand. After this lands, a test can no longer hand any
page to the real browser at all — the code path is switched off in test
builds — and the test suite itself now watches for a browser launch during a
run and fails loudly (and closes the window) if one ever happens again.

## Goals

- [x] Name where the leaked URL comes from and which code could open it
- [x] Make it impossible for the unit-test binary to launch the browser
- [x] Give the acceptance suite a tripwire that fails if a browser opens
- [x] Keep every existing lane and the viewer suite green

## User report (2026-08-08, verbatim)

"i keep seeing these html files opening in edge and sticking around. i don't
want this happening. http://127.0.0.1:58332/t390.html it probably has
something to do with the test run."

Recurring, plural ("files", "sticking around") - every affected run leaves
another Edge window on the interactive desktop.

## Evidence and where to look

- The URL shape is a viewer-test page: an ephemeral-port localhost server and
  a task-named page. No literal `t390.html` exists in the repo, so the name
  is generated - the T390 `+reload` section of `test\win32\viewer-panes.ps1`
  (line ~513) or the TestPage/ReloadPage HTTP servers in `ViewerPane.zig`'s
  test support are the first places to look.
- Something in that path hands the URL to `ShellExecute`-the-default-browser.
  Known product paths that do exactly that, any of which a posted test click
  or a page script could trigger:
  1. a viewer page's `window.open()`/`target="_blank"` -> system default
     browser (documented behavior);
  2. a banner/link plain click -> default browser;
  3. any fallback that shell-opens when a viewer cannot host a location.
- **The escape matters as much as the open**: the harness runs on a
  background desktop (TestDesktop.ps1) precisely so tests never touch the
  user's screen - but ShellExecute launches the browser in the interactive
  session regardless of the caller's desktop, so this class of leak defeats
  the whole isolation. The foreground watcher only detects test-desktop APP
  leaks; a browser spawned into the user's session is invisible to it.

## Direction

1. Identify the exact trigger (run viewer-panes.ps1 while watching for
   browser process creation with the parent chain, the same
   Win32_Process-parent forensics used for T524).
2. Fix at the source: whatever fires the shell-open under test must either
   not fire (the test should not click through to the browser path) or be
   gated - e.g. the app suppresses default-browser handoffs when
   `GHOZTTY_TEST_*`/test-desktop markers are present, routing them to a log
   line the test can assert instead. A product behavior change needs the Mac
   question asked (does the Mac suite have the same leak?).
3. Add a harness-wide regression arm: after a viewer-suite run, assert no
   browser process was spawned with the test tree as its parent chain (and
   close any that were, so a failed assert cleans up after itself).

## Validation criteria

- [x] Root cause named: which test line/product path opened the browser —
      the CLASS is named with evidence rather than one chain: the t390 URL
      exists only inside the win32 test binary (ViewerPane host-floor test),
      so the open had to be a `ShellExecuteW` in that process; in the
      report's era (08-02..08-10) the v1 NewWindowRequested handler
      unconditionally shell-opened every popup URI (T163 replaced it), and
      the test window sits on the interactive desktop where real
      interaction can reach it. 7 current + 4 era runs under a WMI
      browser-creation watcher leaked nothing, so no clean automated chain
      reproduces it — full forensics in the Progress log.
- [x] A full viewer-panes.ps1 run spawns zero browser windows on the
      interactive desktop — ALL PASS (186) including the new
      "no test page escaped to the default browser" assertion
- [x] Regression arm exists and fails if a future test leaks a browser —
      unit lane: `builtin.is_test` refusal in ViewerPane.shellOpen + the
      too-long popup path, pinned by the "T594: a test build never hands a
      shell-open to the OS" test (filter delta 77→78 proves it runs);
      acceptance lane: `test\win32\lib\BrowserLeak.ps1` WMI tripwire wired
      into viewer-panes.ps1, negative control fired on a fake msedge.exe
      launch carrying a loopback URL (1 leak detected, launcher killed);
      new `viewer-panes` guard row stamps green runs
- [x] Existing suite still ALL PASS — floor-lane all: lib/none/win32/agent
      all PASS; viewer-panes.ps1 ALL PASS (186); release-artifacts.ps1
      ALL PASS (2 Docker sections SKIP per the no-Docker directive)

## Progress log

- 2026-08-16 14:22: claimed; work starting.
- 2026-08-16 14:35 [session 28e802b0]: Root-cause hunt: t390.html is served only by ViewerPane.zig's ReloadPage unit-test server (host-floor test, win32 zig lane - runs on the interactive desktop, not the TestDesktop). Static analysis: every current shell-open path (NavigationStarting .browser routing, popup router, dispatchFileLink) is either mode/navKind-gated or LinkSink-guarded; 4 clean host-floor runs under a WMI browser-process watcher leaked nothing. The 2026-08-08-era code had a v1 NewWindowRequested handler that unconditionally ShellExecuteW'd every popup URI (replaced by T163 on 2026-08-10). Now reproducing the era in a worktree at 93b7107d0 under the same watcher.
- 2026-08-16 14:41 [session 28e802b0]: Era repro (worktree at 93b7107d0, the report-day build): 3 passing host-floor runs + 1 flake-fail under the WMI browser watcher - zero browser launches. Combined with 7 clean current-code runs: the leak is not reachable from a clean automated run on either build; it needed an interactive condition (the test window is visible on the user's desktop for ~90s per run, and the era's v1 NewWindowRequested handler unconditionally ShellExecuteW'd ANY popup URI - one real modifier-click/interaction away from Edge; T163 replaced it 08-10). Verdict: fix the CLASS, not a single chain - (1) builtin.is_test guard in ViewerPane.shellOpen + the too-long popup raw ShellExecuteW, so a test binary can never reach the shell; unit test T594 asserts the refusal is recorded via LinkSink; (2) test/win32/lib/BrowserLeak.ps1 WMI tripwire wired into viewer-panes.ps1 - fails and cleans up if any browser launches onto a loopback URL during a run. Filed T897 (seat: mac) for the same audit on the Mac suite.
- 2026-08-16 15:09: status: in-progress -> done [commit f145d49eb]
