---
id: T486
title: "Local Restore All has no on-box rebuild oracle after T194: the launch now recovers before the button can"
order: null
deps: []
status: "done"
commits: ["84fab8ede"]
seat: "win"
priority: "P1"
milestone: "M1"
tags: ["test"]
---

# T486 - Local Restore All has no on-box rebuild oracle after T194: the launch now recovers before the button can

## In plain terms

There is a "Restore All" button that rebuilds all of a machine's terminal
windows. On this machine it used to be the only way to get windows back after a
crash, so a test drove it end to end. Now the app does that recovery by itself
the moment it starts, which is better for the user but means the button has
nothing left to do in that test — so the button's own rebuild is no longer
checked here. Either give it a scenario where it still matters, or decide it
does not need one.

## Goals

- [x] Decide whether the LOCAL Restore All still has a job worth testing now
      that launch-time recovery covers the crash case — YES: the
      already-running-viewer scenario is real (installed + portable release
      share one agent) and the local arm has two behaviours nothing else
      reaches (the shared-connection transport, the `markLayoutDirty`
      bind-back)
- [x] If yes: an on-box fixture where the button really rebuilds — the
      already-running viewer adopting sessions orphaned after it started
      (`test\win32\chooser-restore-all-adopt.ps1`; end-to-end run is gated on
      T851, which the fixture itself discovered — see below)
- [x] ~~If no~~ (decision went yes; recorded here so the next reader does not
      re-open it)

## Summary

Filed from T194 (2026-08-04). T194 made launch-time restore consult the agent's
layout blobs, so a relaunch after a crash rebuilds the orphaned windows on its
own. `test\win32\chooser-restore-all.ps1` section 3 used to prove the BUTTON did
that rebuild; its fixture (kill the app, drop the manifest, relaunch) now
recovers before the chooser is ever opened, so section 3 was rewritten to assert
the automatic recovery and section 4 (the double-attach guard: pressing it with
those panes on screen must rebuild NOTHING) is what the button has left under
test locally.

`restoreAllFrom` — the shared rebuild both paths run — is still driven end to
end by `chooser-restore-all-remote.ps1` across the relay, so the CODE is
covered. What is not covered is `restoreAllLocalSessions`' own path: the local
transport plus the `markLayoutDirty` bind-back that only the local arm does.

## Details

The realistic local scenario the button still owns is a viewer that was ALREADY
RUNNING when the sessions were orphaned — it cannot have recovered them at
launch because they did not exist yet. A fixture for that needs two app
instances on the same agent (same `LOCALAPPDATA`, different
`GHOZTTY_PIPE_SUFFIX`): start B, build multi-pane windows in A, kill A, then
press Restore All in B. Both instances share the session-layout manifest, so the
fixture has to be explicit about which one writes it last.

Weigh that against simply accepting the guard test as the local coverage and
leaning on the remote script for the rebuild — the answer may well be that the
local button is now a convenience with no unique behaviour to protect.

## Outcome (2026-08-15)

Decision: the local button DOES still have a job, and the fixture exists:
`test\win32\chooser-restore-all-adopt.ps1`. Two instances (different
`GHOZTTY_PIPE_SUFFIX`, one shared debug agent — the lineage knob is
`GHOZTTY_AGENT_INSTANCE`, not the pipe suffix), survivor B up first, topology
built in A, A killed, Restore All pressed in B. Oracles: B's `restore all:
rebuilt N window(s)` log line, window shapes over B's pipe, the agent's attach
flags read directly, pane liveness (T652), and the `markLayoutDirty` bind-back
(the manifest — deleted after the kill — must reappear recording the adopted
windows, which is the local arm's own behaviour).

Building it found **T851**: launch restore treats alive-attached-elsewhere as
attachable, so instance A's LAUNCH adopted B's window before the fixture could
build anything (B's pane froze — the exact experience a user with installed +
portable copies would hit). Until T851 lands the script's section-2 gate
detects the steal and SKIPs sections 2–5 loudly, naming T851; the verdict line
carries the count per the T219 skip-audit rule. Once T851 is fixed the same
script, unmodified, is its regression coverage. Section 1 (survivor launches
clean beside an empty agent) runs green today.

## Validation

Whichever way it goes: `chooser-restore-all.ps1` and
`chooser-restore-all-remote.ps1` both ALL PASS, and the task file records the
decision so the next reader does not re-open it.

## Validation criteria

- [x] Decision recorded in the task file (this file, Outcome section)
- [x] The fixture script exists and is skip-audit compliant —
      `chooser-restore-all-adopt.ps1` prints `ALL PASS (2 assertions, 1
      SKIPPED)` naming T851 while the blocker stands;
      `test\win32\skip-visibility.ps1` ALL PASS
- [x] `chooser-restore-all.ps1` ALL PASS (31 assertions, on-box 2026-08-15)
- [x] `chooser-restore-all-remote.ps1` ALL PASS (40 assertions, on-box
      2026-08-15)
- [x] Standing floor green (`floor-lane -Lane all`, then P1–P3 sequentially,
      on-box 2026-08-15)

## Related

- T194 (what moved the coverage), T335/T336 (the button, local and remote),
  T851 (the launch-restore session steal this fixture discovered; its fix
  un-skips the fixture), T121 (incumbent-keeps-name, which the fixture
  exercises in-process once un-skipped), T652 (attached-is-not-alive).

## Progress log

- 2026-08-15 04:30: claimed; work starting.
- 2026-08-15 04:36: Decision: YES, the local button keeps a job worth testing - the already-running viewer (real on this box: installed + portable release share one agent). Fixture is feasible: GHOZTTY_PIPE_SUFFIX isolates only the app IPC pipe (agent lineage is GHOZTTY_AGENT_INSTANCE, unset -> shared debug agent), so two instances A/B with suffixes -t486a/-t486b share one agent. New script test\win32\chooser-restore-all-adopt.ps1: launch B, build 2 multi-pane windows in A, kill A, press Restore All in B; oracles = 'rebuilt 2 window(s)' log, +list shapes in B, agent attach flags, and the markLayoutDirty bind-back (manifest deleted after the kill must reappear containing the adopted windows). Bonus coverage the relaunch fixture can never produce: A's auto 'window-1' collides with B's live 'window-1' (T121 incumbent-keeps-name).
- 2026-08-15 05:11: status: in-progress -> done [commit 84fab8ede]
