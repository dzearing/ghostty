---
id: T995
title: "skip-visibility.ps1 is red at HEAD: agent-handoff.ps1 has an uncounted skip site and an ALL PASS line that names no skip count"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P1"
tags: ["test", "fix"]
---

# T995 - skip-visibility.ps1 is red at HEAD: agent-handoff.ps1 has an uncounted skip site and an ALL PASS line that names no skip count

## Summary

The skip-visibility audit is red, so nothing enforces the rule it exists for. agent-handoff.ps1:385 skips section C without counting the skip, and its verdict line (L66) names no skip count - so a run that dropped a whole section prints the same green line as a full one. Found while closing T962 (asserted-nothing); agent-handoff.ps1 is not in that diff. The audit's own teeth are fine: B2-B4 pass, only B1 (the sweep) is red.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
