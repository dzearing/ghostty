---
id: T90d
title: "WebView2 host floor: web-mode viewer panes (Phase K)"
phase: "K"
deps: ["T90c"]
status: "todo"
commits: []
---

# T90d — WebView2 host floor: web-mode viewer panes (Phase K)

## Summary

WebView2 host floor: webview2.zig COM decls + loader-less probe, shared env/UDF, ViewerPane HWND/controller lifecycle, native error card, web-mode `--view` E2E, verb rejections

## Details

`webview2.zig` COM decls + loader-less runtime probe (design §1),
shared environment + UDF, `ViewerPane.zig` HWND/controller lifecycle
(bounds/visibility/DPI/focus, §4/§11), native error card (§2),
`--view=<url>` end-to-end for windows AND splits (web mode only),
`NewWindowRequested` → browser. Verb rejections land here with the
first live viewers (§9).

*Validation:* `viewer-panes.ps1` grown (web pane opens/resizes/
closes; +read/+send-keys/+set-state/+set-banner reject exit 1;
+close silent; runtime-absent path unit-faked) ALL PASS ×3; P1–P3 +
both lanes green.

## Re-scoped 2026-07-29 (T127)

Grown by two items of host plumbing that everything downstream needs, per
`docs/design/viewer-panes-windows.md` P1/P2:

- **The `window.webkit.messageHandlers` shim.** The shared viewer JS is
  WebKit-shaped: `viewer.js:115-127` and `selection.js:132-146` both post
  through `window.webkit.messageHandlers.viewerTOC.postMessage(obj)`. Define
  that object in terms of `window.chrome.webview.postMessage` and change NO
  shared JS -- forking it is rejected, it is the one part of the viewer that
  came free with T117 and must stay a single copy. Native side:
  `add_WebMessageReceived` + `get_WebMessageAsJson` sees the same JSON shape the
  Mac proxy does.
- **`AddScriptToExecuteOnDocumentCreated` with ONE blob** (shim +
  `selection.js`) so injection order is not a question. The toolbar itself is
  T162.

Also: do not consume `NewWindowRequested` in a way that discards the deferral --
T163 needs it.
