---
id: T769
title: "tab-strip-autohide.ps1 is RED: it models the pre-T496 caption run and aims the \u0027...\u0027 probe at the minimize slab"
order: null
deps: ["T264"]
status: "skipped(duplicate of T767)"
commits: []
seat: "win"
priority: "P1"
triage-reason: "a standing acceptance script has a genuinely failing assertion today; it measures a layout T496 replaced, which is the exact T256 shape - a red nobody caused and everybody learns to ignore"
tags: ["test"]
---

# T769 - tab-strip-autohide.ps1 is RED: it models the pre-T496 caption run and aims the '...' probe at the minimize slab

## Summary

Found while doing T264. tab-strip-autohide.ps1:161-165 derives the caption run privately as pre-T496 app squares (step = 28 DIP + 4 DIP): closeL = clientW - padSm - btn, then maxL/minL one step apart. Since T496 the trio is three native 46 DIP slabs flush to the client right edge with zero gaps. Measured at 125 percent: its '...' probe lands at clientW-148, which is inside the MINIMIZE slab (clientW-174..clientW-116), so the run reports 1 FAILURE(S) (14 passed) today with 'got 8' (HTMINBUTTON) instead of HTSYSMENU. The other three probes pass by luck - their points happen to fall in the right slabs. Fix: read CaptionOverflowLeft / CaptionMinLeft / CaptionMaxLeft / CaptionCloseLeft off Get-TestChromeMetrics, which T264 just proved correct against painted pixels.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-08-11 18:00: status: todo -> skipped(duplicate of T767) (by T264 turn: filed before reading the log tail; T263's turn had already filed the identical finding as T767. Evidence folded into T767.)
