---
id: T607
title: "A second ghoztty window on the test desktop makes the first refuse WS_EX_TOPMOST from any process"
order: null
deps: ["T277"]
status: "done"
commits: []
seat: "win"
priority: "P2"
tags: ["test", "infra"]
triage-reason: "Measured under T277: this is what keeps overlay-zorder.ps1 section E permanently skipped, and it is NOT the product bug T277 fixed - an external process cannot set the bit either, so no app-side change can reach it. P2 because the product path is proven working (float-on-top.ps1 ALL PASS); what is blocked is one acceptance assertion."
---

# T607 - A second ghoztty window on the test desktop makes the first refuse WS_EX_TOPMOST from any process

## In plain terms

There is one situation in the automated test environment where a Ghoztty window
simply cannot be pinned in front of other windows — and it is not the app's
doing: a completely separate program asking Windows to pin that same window is
refused too, while an unrelated app's window in the same place pins fine. Until
somebody explains it, one z-order test stays switched off.

## Summary

Found while fixing T277. On the **background test desktop**, once a SECOND
ghoztty window exists, the FIRST window will not take `WS_EX_TOPMOST`:

| condition (background test desktop, debug build) | result |
|---|---|
| one window, `toggle_window_float_on_top` from the keybind | pins, unpins, and re-pins (`float-on-top.ps1`, 12 assertions) |
| one window, banner up, pin + banner reposition | pin propagates to the overlay and survives |
| **second window created (IPC or keybind), then pin window A** | `SetWindowPos(HWND_TOPMOST)` x3 back to back, all `ok=1 lasterr=0`, ex-style stays `0x100` |
| **same state, external `SetWindowPos` from the harness on A** | also fails |
| same state, external `SetWindowPos` on `charmap.exe` | succeeds, every time |

So it is not the app's call site (three consecutive identical calls, no message
pump between them, no heal in the trace), not the harness, and not the desktop
in general — the ghoztty window specifically, and only once a second one exists.
A second window created and left un-repositioned reproduces it, so the harness's
`Set-TestWindowPos` is not the trigger either.

## What this task must do

1. Decide whether this reproduces on the INTERACTIVE desktop, which T277's step 1
   asked for and this box has never run (running acceptance windows on the user's
   own desktop was not something to do unattended). If it does not reproduce
   there, this is an environment-conditional harness gap (a T207 option-B case)
   and section E should say so permanently rather than pointing at a bug.
2. If it DOES reproduce interactively, find what about a second ghoztty window
   makes the first un-pinnable — start from what a second window changes in the
   process: another top-level in the same thread group, another message window,
   the window-placement/restore path, or a hidden owner relationship
   (`GetWindow(GW_OWNER)` was 0 in the single-window case; re-read it here).
3. Restore `overlay-zorder.ps1` section E to a real assertion, or replace its
   skip text with the permanent explanation.

## Validation criteria

- [x] The interactive-desktop question is answered with a measurement, not an
      assumption. It did not need the interactive desktop, which D35 puts
      off-limits: three probe runs on the BACKGROUND desktop answered it, and
      the answer is that the premise was wrong (see What was measured).
- [x] `overlay-zorder.ps1` section E either asserts or carries a skip that names
      the settled reason rather than a suspected product bug. It ASSERTS now -
      five assertions, all passing (run of 2026-09-07).
- [x] If it is a product defect, a regression arm covers the two-window case in
      `test/win32/float-on-top.ps1`. It is not a product defect; section E is
      itself the two-window arm, and float-on-top.ps1's NOTE now says so
      instead of claiming the first window cannot be pinned.

## What was measured (2026-09-07)

Three probes on the background test desktop, against the debug build:

| condition | result |
|---|---|
| one window, external harness pin on A | sticks on the first try |
| second window created, A NOT the thread's active window, external pin on A | never sticks in 8 tries |
| same, but A re-activated first, external pin | sticks |
| two windows, banner up, A activated, the PRODUCT's float keybind | **pins** |
| the same press issued straight after this script's B/C/D churn | does not pin |
| one more press, seconds later, same code path | pins |

So the filed premise - "a second ghoztty window makes the first refuse
WS_EX_TOPMOST from any process" - is false. A second window matters only
because creating it takes activation away from the first, and off the input
desktop there is no foreground window at all, which is the T277 condition:
`SetWindowPos(HWND_TOPMOST)` returns TRUE with `GetLastError()==0` and changes
nothing. `win32.setTopmost` already answers that by reading the ex-style back
and retrying, but its three attempts are one instant with no message pump
between them, so a request issued right after an activation change can lose all
three. A later press lands.

That makes this an environment-conditional harness gap in the D35 / T207
option-B sense, and one the harness can simply work around: section E now
presses the toggle until the band actually changes (`Set-Float`), which is safe
because the action reads the live ex-style rather than a stored flag, so a press
that did nothing is re-requested rather than undone.

Filed from this work: T1413 (sections F/dim and F/scrollbar are red - T1345
dropped the z-order heal on the live layout path) and T1414 (the product-side
hardening: yield between band-change attempts so a float asked for in a locked
session still takes).

### Decision D35 - Should acceptance scripts be allowed to run on the user's own desktop when a defect only reproduces there?

Resolved 2026-08-13T02:39:22Z : Keep the interactive desktop off-limits, and treat 'reproduces only on the background desktop' as a harness gap (Recommended)

## Progress log

- 2026-09-07 00:18: claimed; work starting.
- 2026-09-07 00:29: root cause: not a two-window product bug. Off the input desktop there is no foreground window, so a single SetWindowPos(HWND_TOPMOST) is silently refused (T277 condition); setTopmost's three retries run in one instant with no pump, so a request right after an activation change loses all three, and a later press lands. Section E now presses until the band changes and asserts again (5 assertions, green). Filed T1413 (F/dim + F/scrollbar red since T1345) and T1414 (product yield between band-change attempts).
- 2026-09-07 00:52: status: in-progress -> done
