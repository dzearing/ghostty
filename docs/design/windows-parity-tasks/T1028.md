---
id: T1028
title: "A win32 acceptance script can measure a stale zig-out exe and stamp its guard green"
order: null
deps: ["T316"]
status: "done"
commits: ["304bb8398"]
seat: "win"
priority: "P1"
triage-reason: "a green run over an exe that is not the source under test is a false result, and since T783 guard-due files it as evidence"
tags: ["test", "infra"]
---

# T1028 - A win32 acceptance script can measure a stale zig-out exe and stamp its guard green

## Summary

Acceptance scripts drive D:\git\ghoztty\zig-out\bin\ghoztty.exe but almost none of them BUILD it, and nothing checks that the exe is newer than the sources it is measuring. On 2026-08-20 T316's first relay-account run reported 3 FAILURE(S) against an exe built four hours earlier - the code under test was correct and the harness said otherwise. The inverse is the one that matters: a change a stale exe still passes gets a GREEN run, and since T783 a green run STAMPS the guard, so guard-due then records 'this harness has been run against the code as it stands' about an exe that was never built from it. lib\BuildMode.ps1 already refuses a wrong-MODE build before anything launches; the same seam should refuse (or rebuild on) a STALE one.

## In plain terms

The tests that drive the real Windows app run whatever copy of it happens to be
sitting on disk. If nobody rebuilt after the last code change, the tests are
grading yesterday's app — and they will say "all pass" about a change they never
saw. Nothing warns you. This makes the tests check that the app they are about
to launch was actually built from the code they are testing.

## Goals

- [x] The pre-flight every acceptance script already runs also asks "were these
      bytes built from this code?", with no per-script edit
- [x] A stale exe is refused before anything is launched, and says how to clear it
- [x] The refusal cannot be reached and then stamped: the run dies first
- [x] Two stated-intent hatches, both loud, and neither reachable by accident
- [x] Its own acceptance script with a negative control

## Details

- Every `test\win32\*.ps1` that launches the app defaults `-Exe` to
  `D:\git\ghoztty\zig-out\bin\ghoztty.exe`. Eleven of them run `zig build`
  first; the rest (including `relay-account.ps1`, `ipc-machine-chooser.ps1`
  and the P1-P3 floor) do not.
- The false RED is merely annoying and self-correcting — you rebuild and re-run.
  The false GREEN is the defect: a run that proves nothing exits 0, and since
  T783 exiting 0 **stamps the guard**, so `guard-due` afterwards answers
  "current" for a harness that never saw the change. That is the one question
  the stamp exists to answer, and it can be made to lie without anybody acting
  in bad faith.
- The seam already exists: `test\win32\lib\BuildMode.ps1` is dot-sourced by
  `TestDesktop.ps1`, `CleanSlate.ps1` and `Isolation.ps1`, and it already
  refuses a wrong-MODE build **before** anything is launched (T350). Staleness
  is the same shape of refusal against the same exe.
- Freshness has to be defined carefully. Newest mtime under `src\` vs the exe's
  mtime is the cheap version and would have caught this case, but it goes red on
  a docs-only edit under `src\` and on a `git pull` that touches a file the
  build does not use. A build-stamp comparison (the exe's baked commit vs
  `git rev-parse HEAD`, the way `upgrade-staleness` already checks a delivery)
  is precise but blind to uncommitted work, which is exactly the state a turn
  is in when it runs its acceptance script. Probably both: commit mismatch is a
  hard refusal, mtime drift is a refusal that a rebuild clears.
- Whether the seam REBUILDS or merely REFUSES is a real fork. Rebuilding makes
  every harness self-sufficient at the cost of a possible multi-minute surprise
  inside a test run; refusing keeps the harness fast and honest and puts the
  build in the caller's hands. An opt-in `-Rebuild` over a default refusal is
  the likely shape.

## Validation criteria

- [x] Launching an app-driving `test\win32` script against an exe older than
      the sources it measures refuses BEFORE anything is launched or typed,
      naming the exe, its age, and the rebuild command - the way
      `BuildMode.ps1` refuses a release build. Verified: sections C and E of
      `test\win32\build-fresh-guard.ps1`, and by hand against the real tree
      (touch `src\apprt\win32\chooser_layout.zig`, then
      `Assert-GhozttyIsolatedBuild -Exe zig-out\bin\ghoztty.exe` refuses,
      naming both files and "2.0 hours").
- [x] The refusal is not defeatable by accident: a run that hits it exits
      non-zero and does NOT stamp its guard. Verified: E5-E7 run a synthetic
      harness end to end in a child process - non-zero exit, no verdict
      printed, marker file never written.
- [x] A fresh exe is silent - no new line in the ordinary green path.
      Verified: C9, F2, and P1/P2/P3 plus `build-mode-guard.ps1` re-run
      unchanged (ALL PASS, no new output).
- [x] An acceptance script of its own with a negative control: stale exe ->
      refusal, fresh exe -> the run proceeds. Verified:
      `test\win32\build-fresh-guard.ps1` - 42 assertions ALL PASS;
      `-NegativeControl` exits 1 and leaves the stamp alone.
- [x] The rule is stated in `docs/claude/testing.md` beside the build-mode one.
      Verified: section G, plus a pointer added next to the `-Doptimize=Debug`
      rule in `docs/claude/build.md`.
- [x] Floor: both `zig build test` lanes, `test-agent`, the `lib` build, P1-P3.
      Verified: `floor-lane.ps1 -Lane all` -> lib/none/win32/agent all PASS;
      P1 25, P2 20, P3 16 assertions ALL PASS.

## Progress log

- 2026-08-20 16:09: claimed; work starting.
- 2026-08-20 16:30 [session f9c04559]: design settled: freshness is a second half of the SAME pre-flight (lib\BuildFresh.ps1, dot-sourced by lib\BuildMode.ps1, called from Assert-GhozttyIsolatedBuild) so all 49 callers gate on it with no per-script edit. Signal is newest-source-mtime vs exe mtime, NOT the baked commit: a commit check is blind to uncommitted work and fires on every turn (the loop builds, tests, THEN commits, so the exe's commit is HEAD's parent while its bytes are right). Sources = build.zig/.zon + src\ minus *.md and src\apprt\gtk\; ~1258 files in 17ms. Default is refuse; GHOZTTY_TEST_REBUILD_STALE=1 builds and writes a witness (the exit from the one false positive - an edit outside the module graph relinks nothing so the mtime never moves), GHOZTTY_TEST_ALLOW_STALE=1 is the loud hatch. New harness test\win32\build-fresh-guard.ps1 (ALL PASS, negative control red) + guard row build-fresh.
- 2026-08-20 16:45: status: in-progress -> done [commit 304bb8398]
