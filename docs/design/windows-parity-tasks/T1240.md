---
id: T1240
title: "The CLI and IPC acceptance scripts still open Ghoztty windows on the user\u0027s desktop (T1193 batch 3 of 4)"
order: null
deps: ["T1193"]
status: "done"
commits: []
seat: "win"
priority: "P1"
tags: ["test", "fix"]
---

# T1240 - The CLI and IPC acceptance scripts still open Ghoztty windows on the user's desktop (T1193 batch 3 of 4)

## Summary

T1193 measured the gap and built the sweep; this batch is 18 of the CLI and IPC
scripts it left declared as pending. Each one starts the app from a process
sitting on the user's desktop - a bare `Start-Process $Exe`, or a cold
`& $Exe +new-window`, which is the one verb that auto-launches - so running it
throws a window across whatever the user is reading.

The migration is mechanical and T1193's `ipc-p1.ps1` / `ipc-p2.ps1` /
`ipc-p3.ps1` are the worked examples:

1. `. (Join-Path $PSScriptRoot 'lib\TestDesktop.ps1')` and `$td = New-TestDesktop`
   before the body; `Remove-TestDesktop` in the teardown.
2. Replace every `Start-Process $Exe` with `Start-OnTestDesktop`.
3. Replace every `& $Exe <verb>` with a local one-line `Ghoz` helper over
   `Invoke-OnTestDesktop`, and read `$r.ExitCode` where the script read
   `$LASTEXITCODE`. The helper captures stdout and stderr to a file, so a
   `cmd /c "... > file"` dance for the GUI-subsystem exe (T245) goes away too.
4. DELETE the script's `# @user-desktop-launch:` line from `lib\TestDesktop.ps1`.
   A migration that leaves it behind fails `desktop-launch-audit.ps1` as a
   stale declaration - that is what makes the list burn down.

Scripts in this batch:

- `test\win32\auto-launch-cwd.ps1`
- `test\win32\cli-argv-fidelity.ps1`
- `test\win32\cli-launch-cwd.ps1`
- `test\win32\conformance.ps1`
- `test\win32\gui-launch-command.ps1`
- `test\win32\ipc-list-session-id.ps1`
- `test\win32\ipc-remote.ps1`
- `test\win32\ipc-send-keys-fidelity.ps1`
- `test\win32\ipc-under-load.ps1`
- `test\win32\ipc-when-idle.ps1`
- `test\win32\layout-blobs.ps1`
- `test\win32\pane-id.ps1`
- `test\win32\rearrange-session-drop.ps1`
- `test\win32\registration-sites.ps1`
- `test\win32\send-keys-bracketed.ps1`
- `test\win32\send-keys-soak.ps1`
- `test\win32\target-staleness.ps1`
- `test\win32\window-name-env.ps1`


## In plain terms

Running the test suite used to throw terminal windows across whatever you were
reading. Eighteen of the tests still did that: they started the app from a
process sitting on your desktop, so the window it opened landed there. After
this they all run on a private, invisible desktop instead - the tests check
exactly what they checked before, they just stop taking over the screen.

## Goals

- [x] every one of the 18 scripts starts the app through the test-desktop harness
- [x] each one still asserts what it asserted before, and still passes on the box
- [x] the declaration list burns down by 18, so the sweep can prove it

## Validation criteria

- [x] every script in the list runs green, on the box, unchanged in what it asserts
      (each run individually; the last one, `cli-launch-cwd.ps1`, ALL PASS after
      the two spawn fixes below)
- [x] `test\win32\desktop-launch-audit.ps1` is ALL PASS with these entries gone from the declaration list
- [x] the pending-migration count in its NOTE line has dropped by 18 (28 -> 10)
- [x] no ghoztty window appears on the user's desktop while the batch runs
      (measured directly during the `cli-launch-cwd` A-arm repro: the only
      ghoztty process reporting a window to the interactive session was the
      user's own installed release; the repo build's window was on the test
      desktop, visible through `+list` and nowhere on screen)

## Progress log

- 2026-09-01 19:11: claimed; work starting.
- 2026-09-01 19:51: migrated all 18 scripts: GUI launches -> Start-OnTestDesktop, CLI calls -> Invoke-OnTestDesktop (Ghoz helper), declarations deleted. Two real traps found on the way: CreateProcessW's lpApplicationName does not search PATH (cmd.exe must be $env:ComSpec), and T506Spawn's STARTUPINFO was CharSet.Ansi, so an lpDesktop string marshalled as mojibake and the spawn SUCCEEDED with no desktop.
- 2026-09-01 20:08: status: in-progress -> done
