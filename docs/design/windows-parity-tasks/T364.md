---
id: T364
title: "The T43 debug tint lightens the bar, so every chrome wash off it steps less far on dark themes"
order: 373
deps: ["T43"]
status: "done"
priority: "P2"
triage-reason: "debug-build-only wash degradation; harness disables the tint, so only a dev on a dark theme ever sees it"
commits: ["8ac3863d6"]
seat: "win"
---

# T364 - The T43 debug tint lightens the bar, so every chrome wash off it steps less far on dark themes

## Summary

Every chrome surface above the bar — an inactive tab, a hovered tab, an icon
button's hover fill — is `color_math.wash(bar, k)`: a fixed FRACTION of the
distance from the bar to white. So the step's absolute size depends on how dark
the bar is, and T43's debug tint makes a dark bar much lighter.

Measured on `--background=#000000`:

| | bar | inactive tab | step |
|---|---|---|---|
| release chrome | `(20,20,20)` | `(34,34,34)` | 14 / 14 / 14 |
| debug marker on | `(102,77,20)` | `(111,88,34)` | **9** / 11 / 14 |

A ~35% smaller step, on a lighter surface where it reads smaller still. It is a
debug-build-only degradation of exactly the separation T206 was filed to create
("inactive tabs should be visible somewhat").

## Details

Filed rather than fixed in T43 because the fix is a design decision, not an
adjustment. Three candidates, none free:

1. **Make the tint luminance-preserving** — adopt the marker's hue and
   saturation, keep the base's brightness. Every wash then steps exactly as far
   as it does in the release build, and `isLight` cannot flip. The catch is
   real: at constant luminance a near-black bar has no room to carry a hue —
   `(30,30,30)` shifted to saturated amber is `(30,25,13)`, a channel distance
   of 21 against a `debug_min_delta` of 48. So it needs a brightness FLOOR for
   dark bases, which reintroduces the problem in the range where it bites.
2. **Lower `debug_tint_amount`** (0.35 today). Cheap, and it only narrows the
   gap rather than closing it.
3. **Make the washes absolute-stepped rather than fractional** where the step
   is a separation signal. The most correct and by far the widest blast radius
   — `tab_shape.INACTIVE_LIFT` / `HOVER_LIFT` and `chrome_theme.hover_wash` are
   shared with the release chrome, so this changes what SHIPS to fix what a dev
   sees.

Note the scope honestly: with `GHOZTTY_DEBUG_MARKER=0` set by the acceptance
harness, this affects only a human running a debug build, and only on a dark
theme. That is why it is a follow-up and not part of T43.

*Validation:* pick one, and assert the property in `chrome_theme.zig`'s sweep —
for every background, the inactive/hover step off the MARKED bar is within some
stated fraction of the step off the unmarked one. A number in a test, not an
eyeball.

## In plain terms

A developer build of Ghoztty paints its window chrome amber so it can never be
mistaken for the real installed app. That amber wash makes the bar LIGHTER, and
everything the chrome draws on top of the bar — an inactive tab, a tab you are
hovering, the fill behind an icon button — is drawn as a fixed fraction of the
distance from the bar to white. A lighter bar has less of that distance left,
so every one of those surfaces separates less: on a black theme the inactive
tab lifted 9 levels off the bar where the shipped app lifts 14, and the tabs
looked flatter than they really are.

Nobody outside the project sees this — the marker only exists in dev builds —
but a dev looking at a dark theme was judging tab separation against a chrome
that had quietly lost a third of it.

## Goals

- [x] The marker keeps the chrome's separation: a wash off the marked bar steps
      at least 90% as far as it does off the unmarked one.
- [x] The band still reads as unmistakably not-the-release on every background
      (T43's guarantee is not traded away for the step).
- [x] What SHIPS is untouched — the fix lives entirely in the debug marker, not
      in the wash amounts the release chrome uses.
- [x] The property is a number in a test that sweeps every background, not an
      eyeball.

## Validation criteria

- [x] `chrome_theme.zig` sweeps every background and asserts the marked base
      keeps >= 90% of the unmarked base's wash headroom (`zig test
      src\apprt\win32\chrome_theme.zig`, and the `none` floor lane).
- [x] The `#000000` table from the Summary is asserted directly: the release
      chrome steps 14/14/14, the marked chrome now steps 11/13/14 where it
      stepped 9/11/14.
- [x] `color_math.zig` unit-tests the two new primitives, including that the
      restore never removes room and only misses its target on a clamped
      channel.
- [x] The existing T43 sweep still passes: every background comes back marked.
- [x] `test\win32\chrome-theme.ps1` green — its oracle derives the marked band
      the same way the app does, so it scores the real painted pixel.
- [x] Floor: `lib` + `none` + `win32` + `agent` lanes, P1-P3.

## Progress log

- 2026-09-02 03:00: claimed; work starting.
- 2026-09-02 03:22: design settled: keep the amber tint as the COLOR, then push the marked base back along the wash-headroom axis so the band keeps >=90% of the unmarked base's distance to the wash target (new color_math.washHeadroom/restoreWashHeadroom). T43 wins ties - a restored base that stops clearing debug_min_delta or crosses the light/dark line falls back to the plain tint. Option 3 (absolute-stepped washes) rejected: it would change what SHIPS to fix a dev-only surface. Measured on #000000: inactive-tab step 9/11/14 -> 11/13/14 against a release 14/14/14; bar headroom retention 0.803 -> 0.899.
- 2026-09-02 03:48: status: in-progress -> done
