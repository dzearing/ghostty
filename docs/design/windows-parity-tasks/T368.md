---
id: T368
title: "T56d: extend ipc-relay.ps1 with kill+restart-agent-under-a-live-remote-window reconnect validation"
order: 374
deps: ["T366", "T609"]
status: "todo"
priority: "P2"
triage-reason: "acceptance-script coverage for T366; test harness work, blocked until the driver exists"
commits: []
seat: "win"
---

# T368 - T56d: extend ipc-relay.ps1 with kill+restart-agent-under-a-live-remote-window reconnect validation

## Summary

Split out of T56 (WP-D1 remote reconnect on win32), 2026-08-02. T56's own
Validation clause, carried forward verbatim.

## Details

Extend `test/win32/ipc-relay.ps1`: kill + restart the agent under a LIVE remote
window, then assert the pane comes back (re-ATTACH) or reports a clear
disconnected state - no hang, no crash, no orphan connection threads.

Two traps this box has already paid for and this script must respect:

- **T248** - `+new-window --target=` is idempotent against a PERSISTED session,
  and killing `ghoztty.exe` does not remove one, so from run two onward a
  reused target name measures the previous run's pane. Kill the repo's agent
  too and launch with `--session-persistence=off`, or use CleanSlate.
- **T352** - fixed `--target` names across runs; make them unique.

Blocked on T366 (there is nothing to reconnect until the driver exists).

## What the driver gives you to assert against (T366, 2026-08-08)

The driver is in. Its behaviour, so the script knows what "came back" means:

- The ladder is **5 attempts at 1/2/4/8/15s**, so a full fast pass is ~30s. Give
  the script a budget past that, and do NOT read a 20s silence as a failure.
- Exhaustion is **not** terminal: the window is kept and a background re-dial
  runs every **45s**, so a script that restarts the agent late still sees a
  recovery — just not quickly.
- A restarted agent materializes its sessions as relaunchable tombstones, and
  the driver ATTACHes those, so the pane comes back with the
  `--- session restarted ---` / session-interrupted treatment rather than empty.
- **Oracles today are log lines** — `remote reconnect: '<name>' dropped;
  attempt N/5 in Nms`, `... is back on a fresh transport (N pane(s)
  re-attached)`, `... exhausted its fast ladder`, `... is terminally
  disconnected`. That is a weak contract (it moves when the wording does);
  **T609** adds a `connection` field to `+list --json` and this script should
  assert against that instead once it lands. Consider taking T609 first.
- **No orphan threads** is directly checkable: a replaced transport is retired
  (shut down off-thread) and freed at window close, so after `+close` the
  process's thread count must return to its pre-drop level.

## Note for whoever takes this (T609, 2026-09-02)

The strong oracle is in. `+list --json` now carries `connection` on a remote
window - `{"state":"connected"}` / `{"state":"reconnecting","attempt":N}` /
`{"state":"disconnected","self_healable":bool}`, absent entirely for a local
window - so assert against that, not the log lines listed above.
`test\win32\ipc-relay.ps1` sections 1b and 6 already read it; extend from
there rather than starting a new script.

While you are in that script: it has **no row in `scripts\guard-due.ps1`**, so
nothing today ties an edit to `RemoteReconnect.zig` / `remote_reconnect.zig`
to running it. Add one as part of this task - it is the harness that will own
the ladder.

## Progress log

- 2026-09-02 03:51: claimed; work starting.
- 2026-09-02 03:51 [session 0f027de4]: unclaimed and re-queued behind T609: this script's only oracles today are log lines, which T368's own Details calls a weak contract and tells the next turn to replace with the +list --json connection field. Writing it against log text now means rewriting it after T609 lands. Added T609 to deps and ordered T609 ahead; taking T609 this turn.
- 2026-09-02 03:51: status: in-progress -> todo (by re-queued behind T609 for a strong oracle)
- 2026-09-02 05:16: T1276 landed part of this: test\win32\remote-reconnect-relay.ps1 now walks connected -> reconnecting(N) -> connected on the RELAY path against the T609 json oracle, with the recovery arm proving the window comes back onto the SAME session and a fourth arm proving a rejected bearer still goes terminal in one attempt. What is left for T368 is the kill+restart-the-AGENT variant in ipc-relay.ps1 and the no-orphan-threads check after +close - and note T1278: measured on box, a hard-killed --listen agent loses its session records, so the restarted agent reaps the live holder and the ladder sees session_gone rather than the relaunchable tombstone this task's notes promise.
