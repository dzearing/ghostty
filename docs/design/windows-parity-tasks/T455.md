---
id: T455
title: "Banner text and list rows never wrap, and they run under the collapse chevron - long lines are simply cut off"
deps: []
status: "skipped(duplicate -> T377, which already root-caused the wrap half; the chevron half was folded into it)"
commits: []
seat: "win"
priority: "P0"
triage-reason: "duplicate of T377; merged rather than tracked twice"
---

# T455 - Banner text and list rows never wrap, and they run under the collapse chevron

Filed 2026-08-04 off a fresh user screenshot, then found to duplicate **T377**
("Banner paragraph, heading and list text do not wrap"), filed 2026-08-02 and
still `todo`.

Everything from this report was folded into T377 rather than tracked twice:

- the checklist-row clipping evidence from the new screenshot,
- the second, separable defect it exposed - `content_w` never subtracts the
  collapse chevron's box, so content paints under the chevron,
- the user's explicit "cap at 3 lines with an ellipsis" requirement.

**Work T377.** This file exists so the duplicate is visible rather than
silently deleted.
