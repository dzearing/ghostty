---
id: T614
title: "An Activity Monitor panel keeps sampling a retired transport after its window reconnects"
order: null
deps: ["T301"]
status: "done"
commits: []
seat: "win"
priority: "P2"
milestone: "M1"
tags: ["fix"]
triage-reason: "the panel reports a machine as unreachable while the window beside it has recovered and is working fine - a confident wrong answer, not a missing one"
---

# T614 - An Activity Monitor panel keeps sampling a retired transport after its window reconnects

## In plain terms

If the link to a remote machine drops and comes back, the terminal window
recovers — but an Activity Monitor panel watching that machine stays stuck on
"Couldn't connect" forever, even though the window right next to it is working.
Closing and reopening the panel is the only way out.

## Goals

- [x] A panel borrowing a window's connection follows that window onto the new
      connection when the reconnect ladder swaps transports.
- [x] It resumes sampling on its own, with no reopen.
- [x] A regression arm proves it (drop the agent, let it come back, assert the
      panel samples again).

## Summary

A panel borrows a **connection pointer**, not a window. When
`RemoteReconnect.applySwap` re-points the window's panes onto a freshly dialed
transport, the old one is RETIRED — shut down, kept alive until window teardown
so nothing dangles. The panel is still holding it, so every sample from that
moment fails and the card reads `failed`, permanently, while the window itself
is healthy.

Nothing is unsafe here (that is exactly what "retired, never destroyed" buys),
and T301's `ActivityMonitor.releaseBorrowed` covers the FREE at window teardown.
This is the other half: the swap should hand the panel the new connection the
same way it hands the panes theirs.

## Evidence

By inspection, not yet reproduced on the box:

- `RemoteReconnect.retire` calls `shutdownRetired(conn)` — the pointer the panel
  holds is deliberately left valid and permanently dead.
- `ActivityMonitor.RemoteConn` stores `conn` with `dialed == null` for a borrow
  and nothing re-resolves it; `beginMetrics`/`buildSnapshot` use it directly.
- Reachable two ways since T301: the palette entry (T296) and a carousel switch
  back to a machine a window is on.

## The shape of the fix

`applySwap` already knows the window and the new transport, so the natural
place is a sibling of `releaseBorrowed`:
`ActivityMonitor.rebindBorrowed(old_conn, new_conn)` — unsubscribe metrics on
the old, re-point `remote_conn`, `beginMetrics` on the new, `rebuildCards`. The
ordering constraints are the ones T301 already learned: do it after the pane
re-attach, and never while the state handler is mid-install.

Consider whether a borrow should store a re-resolvable KEY rather than a
pointer, so this class of drift cannot recur — but the sample worker needs a
stable pointer for the length of an RPC, so a key alone does not remove the
rebind, it only moves it.

## Validation criteria

- [x] With a panel borrowing a remote window's connection, kill and restart the
      agent: the window reconnects AND the panel resumes sampling (root pid ==
      the agent's new pid) without being reopened. — `activity-monitor-remote.ps1`
      section J: `J2 ... (root seen: 26780)`, the REPLACEMENT agent's pid, where
      the pid before the drop was 18728.
- [x] The panel's card leaves the `failed` state on its own. — the same arm: a
      panel still on the retired transport can only report failure, and J2 waits
      for a populated sample, never for a flag.
- [x] No leak or double-free across the swap (the retired transport is still
      freed exactly once, at window teardown). — G4 runs AFTER J on the rebound
      pointer: the closing window still logs `borrowed connection is going away`,
      the app survives, and the panel stays open. Lanes report 0 leaked test
      binaries.
- [x] Both `zig build test` lanes plus `zig build test-agent` green; P1-P3 ALL
      PASS; `test/win32/activity-monitor-remote.ps1` ALL PASS. — lanes
      lib/none/win32/agent ALL PASS; ipc-p1 (25), ipc-p2 (20), ipc-p3 (16) ALL
      PASS. The harness is **2 FAILURE(S) (94 passed)** and both are T1419, a
      naming regression from T610 that lands in sections A/B before anything
      here runs; every J assertion passes.

## Progress log

- 2026-09-07 03:32: claimed; work starting.
- 2026-09-07 03:35 [session 61deca14]: verified still real: applySwap retires the old transport (RemoteReconnect.zig retire/shutdownRetired) and never tells ActivityMonitor, so a borrowing panel keeps sampling it. Fix: activity_dial.rebindBorrowed(old,new) called from applySwap after install(window); unsubscribe old, bump source_gen, re-point remote_conn, beginMetrics, clear failed, kickSample. No shutdown/join - the parked worker is cut loose by retire's own thread. Builds green.
- 2026-09-07 04:13 [session 61deca14]: validated on box: floor lanes lib/none/win32/agent ALL PASS; ipc-p1/p2/p3 ALL PASS; the 15 guards the edit armed all green and stamped. New section J of activity-monitor-remote.ps1 passes 5/5 (root pid follows the replacement agent 18728 -> 26780, no reopen, no dial). Harness overall 2 FAILURE(S) (94 passed), both sections A/B and both T1419 (T610's display-name change), filed P1.
- 2026-09-07 04:14: status: in-progress -> done
