---
id: T471
title: "Cross-OS remote panes pick the scrollback guard from the LOCAL os, not the child\u0027s pty flavour"
order: null
deps: []
status: "done"
commits: ["4cd35096d"]
seat: "win"
priority: "P1"
milestone: "M1"
---

# T471 - Cross-OS remote panes pick the scrollback guard from the LOCAL os, not the child's pty flavour

## In plain terms

Making a terminal window taller used to silently delete some of the output you
had scrolled past. That is fixed for panes running on this machine (T431), but
the fix decides whether to switch itself on by looking at the operating system
the *window* is running on, not the one the *shell* is running on. So a Mac
window showing a shell that lives on a Windows box still loses history when it
is made taller, and a Windows window showing a shell on a Mac or Linux box
turns on a protection it does not need — harmless, but it means new blank rows
appear at the bottom instead of previously-scrolled output sliding into view.

Nobody hits this unless they use remote panes across two different operating
systems, which is why it was not fixed at the same time: closing it properly
means the two machines telling each other what kind of terminal they have,
which changes the protocol between them.

## Goals

- [x] Carry the child's pty flavour (ConPTY vs POSIX) across the agent HELLO
      handshake, additively, so an old peer degrades rather than breaks
- [x] Choose the scrollback guard from that, per pane, instead of from
      `builtin.os.tag`
- [x] Cover a cross-OS remote pane in an acceptance test, both directions
      (the Windows-app-with-POSIX-child direction end to end on this box; the
      Mac-app-with-ConPTY-child direction is the same code path and needs the
      other seat to watch it — T821)

## Validation criteria

- [x] The flavour survives the wire in every skew: reported, absent (older
      agent), and a spelling this build has never heard of — verified by
      `HELLO pty_flavor: additive + back-compat + unknown spelling` in
      `src/remote/protocol.zig` and the 4-case wire test in
      `src/remote/connection.zig` (which also asserts the client reports its
      own flavour, i.e. both directions of the field)
- [x] A missing/unknown flavour falls back to the local machine's — the exact
      pre-T471 behaviour, so an old agent is never a regression: `enabledFor:
      the CHILD's pty decides, not the host os` in `src/termio/history_guard.zig`
- [x] An agent-backed pane on this box still keeps its scrollback when grown
      (section A of `test\win32\history-guard-flavor.ps1`: 500 lines in,
      0 lost)
- [x] The decision is read off the WIRE, not off `builtin.os.tag`: the same box
      and the same ConPTY child, with the agent reporting `posix`, loses
      exactly the rows the taller viewport gained (section B: lost 18, lines
      459-476 — the T431 signature). A client still deciding from the local OS
      would come back clean here, so this is what makes the field load-bearing
- [x] No regression to the local (exec) path: `test\win32\scrollback-narrow.ps1`
      ALL PASS (11 assertions)
- [x] Floor: `floor-lane.ps1 -Lane all` (none + win32 + agent) PASS, P1/P2/P3
      ALL PASS, and the six harness audits (exit-code, skip, verdict-exit,
      asserted-nothing, foreground, persistence) still clean with the new
      script in the tree

## Summary

T431 fixed a real data-loss bug: growing a pane's row count imported scrollback
rows into the active area, where a ConPTY child's post-resize repaint erased
them — permanently, because the import is a move and not a copy. The guard is
`src/termio/history_guard.zig`, and it is switched on by

```zig
pub const enabled: bool = builtin.os.tag == .windows;
```

That constant answers "does the LOCAL machine run ConPTY children", which is the
right answer for every local pane and for the common remote case (a Windows app
driving a Windows agent). It is the wrong answer for a **cross-OS remote pane**:

- **macOS app + Windows agent** — the child IS a ConPTY and DOES repaint after
  every resize, but the guard is compiled out. Growing the window eats history,
  exactly as it did on Windows before T431. This is the half that still loses
  data.
- **Windows app + POSIX agent** — the child does NOT repaint, so the guard is
  unnecessary. It costs nothing but correctness of feel: a taller window shows
  blank rows instead of pulling previously-scrolled content down.

## The shape of the fix

The pane's terminal needs to know its child's pty flavour, and only the agent
knows that. Per the agent contract in `CLAUDE.md`, this is an **additive**
protocol change: a new capability/field on the HELLO handshake, absent from
older peers, with the reader falling back to today's `builtin.os.tag`
derivation rather than failing. Then `Termio` selects the guard per pane from
the negotiated value instead of from a compile-time constant.

`history_guard.arm` / `hold` already take no position on this — they are pure
terminal moves. Only the `enabled` gate and its call sites in `Termio` change.

## What landed

- `src/remote/protocol.zig` — `PtyFlavor` (`conpty`/`posix`, with `PtyFlavor.local`
  as the ONE derivation of what this build spawns) and `Hello.pty_flavor`, an
  additive optional FIELD. No capability gate: it opens no opcode and changes no
  existing value's meaning, so an older peer omitting it is already safe — the
  same class as `hostname`/`build_version`, now stated as such in the module's
  compatibility history.
- `src/remote/agent/server.zig` + `agent/main.zig` — the agent advertises what it
  really spawns, with `GHOZTTY_AGENT_PTY_FLAVOR` as a Debug/ReleaseSafe-only test
  seam (same shape and same gate as `protoVersionOverride`). It exists because the
  behaviour under test otherwise needs two machines and two operating systems.
- `src/remote/connection.zig` — captures `peer_pty_flavor` during the HELLO
  negotiation and exposes `peerPtyFlavor()`; the client reports its own flavour
  too, so the field means one thing in both directions.
- `src/termio/Remote.zig` — caches the flavour once at bring-up, after the pane is
  live (so the handshake has certainly resolved). Written and read on the IO
  thread, which is also the only thread that resizes, so it needs no
  synchronisation.
- `src/termio/backend.zig` + `Termio.zig` + `history_guard.zig` — `enabled`
  (a compile-time constant) became `enabledFor(?PtyFlavor)`, asked per pane
  through `backend.childPtyFlavor()`. Null (older agent, unknown spelling)
  falls back to `local_flavor`, which is bit-for-bit the pre-T471 answer.

macOS needs no Swift change: the guard lives in the shared Zig core, so the
half of this bug that was real data loss — a Mac window attached to a Windows
agent's ConPTY — is fixed by the same commit that fixes the Windows half.

## Related

- **T431** — the bug, the measurement, and the guard this refines.
- **T423** — the notice-specific pin the guard generalises.
- **T821** — watch the Mac-app-with-ConPTY-child direction from the Mac seat.

## Progress log

- 2026-08-12 22:48: claimed; work starting.
- 2026-08-12 23:17: design+build: pty flavour rides HELLO as an additive optional FIELD (protocol.PtyFlavor + Hello.pty_flavor), no capability gate needed since it opens no opcode. Agent advertises PtyFlavor.local; connection captures peer_pty_flavor; termio.Remote caches it at bring-up; history_guard.enabled -> enabledFor(?PtyFlavor) with null falling back to local_flavor (pre-T471 behaviour). Added GHOZTTY_AGENT_PTY_FLAVOR debug-only test seam so one box can reach the cross-OS case. Unit tests: protocol round-trip/back-compat/unknown-spelling, connection wire test (4 skew cases, both directions), history_guard.enabledFor. Floor: none PASS, win32 PASS, agent PASS.
- 2026-08-13 07:41: status: in-progress -> done [commit 4cd35096d]
