---
id: T1179
title: "Clean-machine proof: website -\u003e one installer -\u003e a working Ghoztty, verified end to end before anything else proceeds"
order: 5
deps: ["T1175", "T1176", "T1177", "T1178", "T1186", "T1188"]
status: "blocked(waiting on the user to install win-v1.35.0 from the website and report)"
commits: []
seat: "win"
priority: "P0"
triage-reason: "P0: user directive 2026-08-30. The user-verified gate: 'I want to go to the website and be able to install the prod version of ghoztty on a new machine and it should just work.' The merge-back phase does not start until this passes."
milestone: "M1"
unblock: "The user has run the 1.35.0 installer from https://dzearing.github.io/ghoztty/ on the cleaned box and reported what happened - see docs/design/windows-parity-install-handoff.md for the checks. Resume with: powershell -NoProfile -File scripts\go-loop-exec.ps1 resume"
tags: ["test"]
---

# T1179 - Clean-machine proof: website -> one installer -> a working Ghoztty, verified end to end before anything else proceeds

## Summary

The acceptance gate for the install epic. On a machine with no prior Ghoztty, go to the published site, download the single Windows installer, run it, and get a working terminal with sessions and the agent alive - no second download, no script, no silent failure. Then publish a newer build and take the in-app update. This task is the user-verified checkpoint; the cutover to `main` does not start until it passes.

## This task BLOCKS on the user. The loop does not install.

User directive 2026-08-30: "block on me closing this Ghoztty and running the
latest msi". The loop prepares and then STOPS; the human performs the install
and confirms. The loop may not run the MSI on the user's behalf, because the
whole point of the gate is to exercise the path a real user walks.

The handoff has a constraint that is easy to miss and fatal to get wrong:
**the go-loop and the controller chat both run inside Ghoztty panes.** Closing
Ghoztty to run the installer kills the loop, the controller, the task
dashboard and the agent. So the handoff cannot be "ask and wait" - there is no
process left to wait. It must be a durable, resumable checkpoint.

### The protocol

1. **Prepare.** Publish the release the user will install. Confirm the website
   offers exactly one Windows installer (T1175) and the link resolves to the
   new version.
2. **Inventory and clean.** Run the T1188 cleanup screen. The user removes the
   temporary and dev installs; the screen re-audits and prints a clean verdict.
   Record that verdict in this task's progress log - it is the evidence that
   "no prior Ghoztty" was true, not assumed.
3. **Write the checkpoint before stopping.** The resume instructions, the
   download URL, the expected version and the checks to perform must be on
   DISK (not only in a pane's scrollback), because every pane is about to die.
   The go-loop stop switch is held so the watchdog does not revive a loop into
   a machine mid-install.
4. **Hand off and stop.** Tell the user plainly: close every Ghoztty window,
   run the downloaded MSI, and confirm. Then stop.
5. **The user installs.** Ghoztty launches itself on completion (T1176).
6. **Resume from the checkpoint.** The user restarts the loop; it reads the
   checkpoint, verifies the install (version, agent alive, sessions, PATH,
   the `ghoztty` CLI, no error dialog), then publishes a newer build and takes
   the in-app update (T1178) as the second half of the proof.

## The handoff checkpoint

`docs/design/windows-parity-install-handoff.md` is the durable version of
steps 3-6, written for a human with no terminal open: the release under test,
the cleanup pass to run first, the install steps, the checks to make, and how
to restart the loop afterwards. Read it before doing anything; the loop reads
it back on resume.

## Validation criteria

- [ ] The cleanup screen's clean verdict is recorded here, with the four known
      install locations each shown gone or deliberately kept.
- [x] Exactly one Windows installer is offered on the published site. — the
      live page (fetched cache-busted 2026-08-30) carries three download
      cards: one macOS `.dmg`, one Windows `.msi`, one portable `.zip`, and no
      `agent-msi-link` / `agent-install-cmd` markers. Both Windows links
      answer 200. `test\win32\one-installer.ps1` ALL PASS,
      `test\win32\website-windows-download.ps1` ALL PASS (section F green
      against the freshly deployed gh-pages).
- [ ] A fresh install from that installer produces a working terminal: window
      opens, agent alive, sessions work, `ghoztty` resolves on PATH.
- [ ] Ghoztty launched itself when the installer finished.
- [ ] No silent failure and no error dialog on first run.
- [ ] A subsequent published build is taken via the in-app update, with no
      script and no manual file copying.
- [ ] The user has confirmed each of the above; the loop did not self-certify.

## Progress log

- 2026-08-30 17:12: claimed; work starting.
- 2026-08-30 17:13: Not startable yet: the gate's first step is 'the website offers exactly one Windows installer', and the LIVE page (https://dzearing.github.io/ghoztty/) still serves the retired agent MSI card (agent-msi-link -> relay /dl/ghoztty-agent.msi). T1175 landed the repo change only and explicitly deferred the deployment to T1186, which is P0 todo. Added T1186 to this task's deps and set it back to todo; taking T1186 this turn instead.
- 2026-08-30 17:13: status: in-progress -> todo (by blocked on newly-linked dep T1186 (live site still offers two installers))
- 2026-08-30 17:37: Correction to the previous note: after the dep was added, 'next' handed this turn T1172 rather than T1186 (both are P0 and unordered, so id order decides). T1186 remains the blocker for this gate's step 1 and is still the first P0 the queue owes it.
- 2026-08-30 18:43: Dep on T1186 added by the controller 2026-08-30: the gate is a walk of the LIVE site, so it cannot be reached while gh-pages and the relay still serve two Windows installers. T1175 removed them from the repo only.
- 2026-08-30 18:43: Correction to the previous note: the T1186 dep was already present - the loop added it, not the controller. The reasoning stands; the attribution was wrong.
- 2026-08-30 19:33: claimed; work starting.
- 2026-08-30 19:34: Resumed with all six deps done. Step 1 (prepare): the live site points at win-v1.34.0, published 2026-08-21, which PREDATES all four install-epic fixes (T1175 8906d4775, T1176 ffa75bc35, T1177 419901352, T1178 3aa5ef013, all 2026-08-30). So the gate needs a NEW published release before the user installs anything. Publishing via CI (push a win-v tag -> release-windows.yml) rather than scripts/publish-windows-release.ps1, which needs Docker and Docker is down. Version 1.35.0: check-release-parity treats a Windows release AHEAD of mac as IN STEP, and the second half of the proof (in-app update) needs a higher version still to publish onto.
- 2026-08-30 19:48: Step 1 (prepare) DONE. Published win-v1.35.0 through CI (run 33351129598, success): tag win-v1.35.0 at 890207079 -> Ghoztty-1.35.0-x64.msi (41.4 MB) + Ghoztty-portable-1.35.0-x64.zip (40.0 MB). That is the first published Windows build carrying the whole install epic - every earlier one predates T1175/T1176/T1177/T1178. The release run also retargeted gh-pages (1a54fe745); the live page now serves win-v1.35.0 and both links answer 200. The in-repo mirror was retargeted here with dist/website/update-windows-links.py so section F stays green - note that F compares against LOCAL origin/gh-pages, so it reads red until you 'git fetch origin gh-pages'.
- 2026-08-30 19:48: Step 2 (inventory) recorded read-only; the REMOVAL is the user's, per the directive. 'ghoztty-cleanup.ps1 inventory' finds 26 artifacts - 25 unaccounted, 1 protected. Installs (8): the release at %LOCALAPPDATA%\Programs\Ghoztty (in use by pid 40440, so it cannot go until the windows close), Desktop portable, \homeassistant\share portable, and five zig-out* dev prefixes. Plus the Remote Agent 1.12.1 ARP entry, four GhozttyAgent* Run entries, two settings keys, the user PATH entry, two Start Menu shortcuts, the GhozttyGoLoopWatchdog and GhozttyTaskDashboard tasks, and %LOCALAPPDATA%\ghoztty (1271 files, 750 MB). The ghost {A10466B5-...} is PROTECTED and no uninstall command was emitted for it. Step 3: checkpoint written to docs/design/windows-parity-install-handoff.md.
- 2026-08-30 20:11: Floor green before the handoff: all four lanes PASS (lib 52s, none 329s, win32 409s, agent 368s); ipc-p1/p2/p3 ALL PASS (25/20/16); one-installer.ps1 ALL PASS; website-windows-download.ps1 ALL PASS. Filed T1201 (section F blames the mirror when the local origin/gh-pages ref is stale) and T1202 (a release moves the deployed page and nothing moves the in-repo mirror, so that check is red by construction after every publish). Parking as blocked: steps 4-6 are the user's.
- 2026-08-30 20:11: status: in-progress -> blocked(waiting on the user to install win-v1.35.0 from the website and report) (by T1179's own protocol: the loop prepares, the human installs)
- 2026-08-30 20:13: Prepared and pushed as 6bc8acdcc. Handoff doc moved to docs/design/windows-parity-install-handoff.md (the tasks directory validator reads every .md in it as a task, so a non-task file there is BAD FRONTMATTER); a copy is on the Desktop. Next: the loop stops here.
