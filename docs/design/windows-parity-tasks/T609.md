---
id: T609
title: "Expose a remote window\u0027s connection state in +list --json so reconnect is observable without log scraping"
order: 373
deps: ["T366"]
status: "done"
commits: ["777af5f23"]
seat: "win"
priority: "P2"
triage-reason: "Observability for the reconnect ladder, useful to tests and scripts rather than to a person. Out of M1."
tags: ["feature"]
---

# T609 - Expose a remote window's connection state in +list --json so reconnect is observable without log scraping

## Summary

The remote reconnect ladder (T366) has real per-window state — connected /
reconnecting(attempt N) / disconnected(self-healable or terminal) — and today
the ONLY way to read it from outside the process is to grep the app log. Give it
a field in `+list --json`.

## In plain terms

A window connected to another machine now knows whether that connection is
healthy, retrying, or given up. Right now nothing outside the app can ask. This
puts the answer where every other window fact already lives, so a script — or a
person debugging — can see it without reading log files.

## Details

Filed out of T366, whose driver holds the state in `Window.reconnect.ladder`
(`RemoteReconnect.State`). Two things want it:

- **T368**, the reconnect acceptance script, currently has nothing to assert
  against but log lines. A log line is a weak oracle: it is a side effect of the
  implementation rather than a contract, and it moves whenever the message is
  reworded.
- **T367**'s pill paints exactly this state, so a JSON field and the pill would
  read one source instead of two.

Shape: a `connection` object on a remote window's `+list --json` entry —
`{"state": "connected" | "reconnecting" | "disconnected", "attempt": N,
"self_healable": bool}` — absent entirely for a local window, which is what
already distinguishes them. Additive, so no existing consumer breaks.

Do it on BOTH platforms per the standing symmetry rule: the Mac holds the same
ladder state in `BaseTerminalController` and its `+list` has the same gap. File
the Mac half as its own `seat: mac` task if it cannot land here.

## Goals

- [x] `+list --json` answers "how is this remote window's link doing" without
      anyone reading a log file
- [x] A local window is still visibly local — the field is absent, not a
      special value
- [x] The shape is additive, so every existing consumer keeps working
- [x] `+list` human output shows it too, for a person debugging

## Validation criteria

- [x] `+list --json` reports `connection` for a remote window and omits it for a
      local one - `test\win32\ipc-relay.ps1` section 1b, on a live relay
      window: `connection.state = connected`, neither conditional field present,
      and the LOCAL base window carries no `connection` key at all. ALL PASS
- [x] The field tracks a real drop - section 6 of the same script, after the
      agent is killed under the live window: it read
      `{"state":"disconnected","self_healable":false}`, i.e. it LEFT `connected`
      rather than being a constant. The assertion accepts either
      `reconnecting` or `disconnected` on purpose, because which of the two a
      caller catches at a given instant is timing
- [x] The three states and their conditional-field rules are pinned by unit
      tests: `listConnection:` in the none lane (the mapping, exhaustive over
      the union) and two `List: connection ...` tests in `src/apprt/ipc/list.zig`
      (the wire shape, including that an omitted field is absent and not empty)
- [ ] T368's acceptance script asserts against the field rather than log text -
      **T368's, not this task's.** The field and its first two assertions are
      in; T368 was re-queued behind this task and carries a note pointing at
      sections 1b and 6 to extend from
- [x] Both zig test lanes and `test-agent` green; P1-P3 ALL PASS -
      `FLOOR SUMMARY: lib#1=PASS none#1=PASS win32#1=PASS agent#1=PASS`
      (lib 47s, none 162s, win32 414s, agent 390s), then `P1 ACCEPTANCE: ALL
      PASS (25)`, `P2 ACCEPTANCE: ALL PASS (20)`, `P3 ACCEPTANCE: ALL PASS
      (16)`, run sequentially. Eight guard-due audits re-run green
      (isolation-meta, launch-preflight, verdict-exit, cleanslate,
      stderr-capture, test-reach, desktop-launch, printclient)

## Evidence

The wire shape, in `src/apprt/ipc/list.zig` (`List.Connection`):

```json
"connection": { "state": "connected" }
"connection": { "state": "reconnecting", "attempt": 2 }
"connection": { "state": "disconnected", "self_healable": true }
```

Two shape decisions, both taken from what the schema already does rather than
invented:

- **A local window has no `connection` key at all.** Absence is what already
  distinguishes local from remote everywhere else in this payload, so there is
  no `"state": "local"` for a caller to mis-handle - and a consumer that has
  never heard of the field is unaffected, which is the `chrome` / `banner` /
  `session_id` bargain.
- **A field a state does not carry is omitted, not emitted empty.** `attempt`
  only while `reconnecting`, `self_healable` only while `disconnected`. A `0`
  attempt on a connected window is a value a script can read and believe; an
  absent key is not.

Where it comes from: `WindowState.listConnection` in `remote_reconnect.zig`,
deliberately beside the union it maps and exhaustive over it - a ladder state
added later cannot quietly go unreported, which is the exact failure mode that
left the log as the only oracle. `IpcHandlers.handleList` emits it for any
window with `hasRemotePill()`, the SAME predicate the T367 status pill uses, so
the pill and the JSON cannot come to disagree about which windows have a link.

`src/cli/list.zig` prints `[connection: <state>]` on the window line (with the
attempt number, and `, needs attention` on a terminal verdict), so the human
formatter answers the same question without `--json`.

Filed on the way past:

- **T1275** (`seat: mac`) - the Mac half, per the platform-symmetry rule. The
  Mac holds the same ladder state on `BaseTerminalController` and its `+list`
  has the same gap; this seat cannot build Swift. The Zig side of the wire is
  already shared, so it is the `IPCMessage.swift` encoder alone.
- **T1276** (P1, M1) - what this field immediately exposed. Kill the agent
  under a RELAY window and the ladder reports `disconnected` with
  `self_healable: false` - the TERMINAL verdict - about three seconds later,
  rather than climbing. Nothing re-dials, so the window never comes back. That
  is T366's feature not working on the path a user's second machine actually
  uses, and it was invisible until there was something to read. Suspect: the
  `.unauthorized` short-circuit in `dialOnce`'s `.relay` arm reading "device
  offline" as "rejected bearer".

## Progress log

- 2026-09-02 03:51: claimed; work starting.
- 2026-09-02 04:12 [session 0f027de4]: shape landed: List.Connection in src/apprt/ipc/list.zig (state always; attempt only while reconnecting; self_healable only while disconnected; the whole object absent for a local window), mapped by WindowState.listConnection in remote_reconnect.zig so the switch is exhaustive and a future ladder state cannot go unreported, emitted by IpcHandlers.handleList for any window with hasRemotePill(). src/cli/list.zig prints [connection: ...] on the window line for the human formatter. Filed T1275 for the Mac half (seat: mac; this seat cannot build Swift).
- 2026-09-02 04:28: status: in-progress -> done [commit 777af5f23]
