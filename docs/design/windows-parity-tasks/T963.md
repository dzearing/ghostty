---
id: T963
title: "verdict-exit-audit.ps1 is red at HEAD: release-parity.ps1 exits 0 on its failure path and tab-tooltip.ps1 falls off the end"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P1"
triage-reason: "Two scripts exit 0 on their own failure path, so a red run reports success to the loop; a lying verdict is worse than no verdict."
tags: ["test", "fix"]
---

# T963 - verdict-exit-audit.ps1 is red at HEAD: release-parity.ps1 exits 0 on its failure path and tab-tooltip.ps1 falls off the end

## Summary

Two harnesses can PRINT failure and EXIT 0, which is the exact defect T221 closed: release-parity.ps1 L194 (exits-zero) and tab-tooltip.ps1 L346 (fallthrough). A suite driver or a guard reading the exit code sees green over a red run. Found by T883 running the meta-audits; neither file is in T883's diff.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
