---
id: T630
title: "win32 startup still goes deaf for one agent handshake timeout (~1.2s) inside dialExisting"
order: null
deps: ["T188"]
status: "done"
commits: ["7274bd282"]
seat: "win"
priority: "P2"
triage-reason: "A sub-two-second startup deafness after T188 removed the multi-second one. Real, but no longer user-visible. Out of M1."
tags: ["fix", "perf"]
---

# T630 - win32 startup still goes deaf for one agent handshake timeout (~1.2s) inside dialExisting

## Summary

T188 closed the 10.7 s startup blackout by pumping IPC from the points where
startup blocks. What is left is one of them: `LocalAgent.dialExisting` is a
single blocking call bounded by `probe_handshake_ns` (1200 ms), and against a
wedged agent it spends all of it. The pump brackets that call but cannot reach
inside it, which is exactly why `test\win32\ipc-startup-latency.ps1` measures
the suspended-agent first answer at ~1330 ms rather than the ~250 ms a healthy
restore gets.

Options, none of them investigated yet: thread a pump callback through
`tcp_dial.dialPipeTimeoutReport`'s wait (it is shared cross-platform code, so
this must not become a win32-shaped hook in `src/remote/`); do the dial on a
worker thread and post the result back (the `RestoreAllRelay` pattern, but
`LocalAgent`'s resolve mutates app-owned state and is not thread-safe today —
see D42's third option); or shorten the handshake budget for the STARTUP dial
specifically, on the grounds that an agent that has not answered in a few
hundred milliseconds is not going to.

## In plain terms

Commands now get answered while Ghoztty restores your windows, except for one
stretch of about a second when the background agent is wedged. Nobody has
reported noticing it; this is about closing the last of the gap.

## Validation criteria

- [x] `test\win32\ipc-startup-latency.ps1` arm C's first answer drops from
      ~1330 ms toward the healthy-restore figure, with the arm's ceiling
      tightened to match so a regression is caught — measured 72 ms then 56 ms
      over two runs (cold 164 ms, healthy restore 174 ms); C3's ceiling cut
      from 3000 ms to 800 ms, which the old single-wait shape (~1330 ms) fails
- [x] Whatever mechanism is chosen does not put a win32-shaped hook into
      shared `src/remote/` code — `tcp_dial.WaitTick` is `{ctx, func,
      interval_ns}` and names no platform; the pump lives in
      `LocalAgent.pumpWhileDialing`
- [x] Floor green; P1-P3 ALL PASS — `floor-lane.ps1 -Lane all` lib/none/win32/
      agent all PASS (the none lane needed a solo re-run: the first was
      overlapped by the dead turn's still-running agent lane), P1 (25), P2 (20),
      P3 (16), plus the thirteen guards this edit came due for

## Progress log

- 2026-09-07 23:51: claimed; work starting.
- 2026-09-08 00:06: root cause: the whole probe_handshake_ns (1200ms) is spent inside one Connection.waitHandshakeTimeout call in tcp_dial.dialConnected, which T188's brackets cannot reach. Fix: tcp_dial.WaitTick - a shapeless {ctx, func, interval_ns} the dial runs while it waits, slicing the SAME budget (a timed-out wait tears nothing down, so slicing is free). win32 fills it with gui_pump.pump via LocalAgent.pumpWhileDialing, null off the GUI thread so dialProbe is untouched. No win32 shape in src/remote/. Unit tests: silent peer still times out but ticks >=5 times; MiniAgent peer still negotiates with a tick installed.
- 2026-09-08 00:12: RESUME STATE at context reset: fix is written and the win32 debug build is GREEN, but nothing is committed and validation is incomplete. Uncommitted: src/remote/tcp_dial.zig (adds WaitTick + waitHandshakeTicking, threads an optional tick through dialConnected and dialPipeTimeoutReport, 2 new unit tests) and src/apprt/win32/LocalAgent.zig (pumpWhileDialing/pumpTick, passed from dialExisting). STILL OWED: floor-lane.ps1 -Lane all was running at reset and its verdict was never read - re-run it; then test\win32\ipc-startup-latency.ps1 to measure arm C's first answer and TIGHTEN the C3 ceiling (currently <3000ms) to match the new figure; then P1-P3; then tick the Validation criteria, set-status done, log entry, commit+push.
- 2026-09-08 00:14: stale in-progress claim picked up by a new turn; reassessing from this log and git status before resuming.
- 2026-09-08 00:37 [session 2df01dc7]: resumed; moved WaitTick below DialReport (its doc comment had been hijacked). floor green: lib/none/win32/agent all PASS (none needed a solo re-run - the first was overlapped by the dead turn's still-running agent lane). Proved the new none-lane test really runs and can fail by inverting calls>=5 to >=100000 (red) and back.
- 2026-09-08 00:57: status: in-progress -> done [commit 7274bd282]
