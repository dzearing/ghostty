---
id: T625
title: "the Windows agent-restart dialog has no What\u0027s new accessory"
order: null
deps: ["T624"]
status: "done"
commits: []
seat: "win"
priority: "P2"
triage-reason: "The accessory on the agent-restart dialog is secondary to T624 shipping the notes at all. Out of M1 until its parent lands."
tags: ["feature"]
---

# T625 - the Windows agent-restart dialog has no What's new accessory

## Summary

Split out of T125 on 2026-08-08. Mac's `LocalAgentManager.makeUpgradeAlert`
hangs a `WhatsNewNotesView` off the mandatory agent-restart confirmation, so the
user can see what the restart actually buys them before agreeing to lose their
sessions. The win32 dialog (`App.promptAndRefreshLocalAgent` ->
`ConfirmDialog.show`) has title + body + two buttons and no notes area at all.

## In plain terms

When Ghoztty on Windows asks permission to restart the background process that
holds your terminal sessions -- which closes them -- it tells you the cost and
nothing about the benefit. On a Mac the same dialog lists what the update
actually changed, so the answer is an informed one. Windows users are asked to
give up their sessions on trust.

## Goals

- [x] `ConfirmDialog` grows an optional notes region (its own scroll area, so
      the notes never resize the dialog), or a sibling dialog reusing the same
      renderer.
- [x] The agent-restart confirmation passes the AGENT-scoped notes,
      partitioned against the last-seen version, and shows nothing extra when
      there are none.
- [x] The renderer is the one T624 built -- shared, not a second copy, so the
      window and the accessory cannot drift.

## Details

Depends on T624 for the store, the bundled notes and the tracking. The notes
here are AGENT-scoped only (`release-notes/agent/`), per `release-notes/README.md`:
the dialog asks the user to reset live sessions, so its notes must be the
reasons a user would restart the agent, never viewer/banner/UI items.

Existing win32 code: `src/apprt/win32/App.zig` `promptAndRefreshLocalAgent`,
`src/apprt/win32/ConfirmDialog.zig`, copy in `src/apprt/win32/agent_upgrade.zig`.

## Validation criteria

- [x] Layout of the notes region asserted at 1.0/1.25/1.5/2.0 in a pure module
      (`whats_new_layout.accessoryHeight` at all four scales, plus six new
      `ConfirmDialog.layoutFor` tests: no-notes is byte-identical, the band
      seats between message and buttons, the height delta is gap+band, it
      stacks above checks/note/field, and the 460dip prose floor holds).
- [x] `test/win32/agent-upgrade.ps1` gains an arm proving the dialog shows the
      notes when bundled agent notes exist, and is unchanged when they do not
      (arm J: J5a-J5e, model text `releases=1 notes=4 height=902`; arm N is
      the control at the newest anchor - dialog raised, no accessory).
- [x] Both zig test lanes, `test-agent`, and P1-P3 stay green (floor-lane -Lane
      all: lib/none/win32/agent ALL PASS; P1 25, P2 20, P3 16 assertions).

## Progress log

- 2026-09-07 19:34: claimed; work starting.
- 2026-09-07 20:24: IN FLIGHT, uncommitted in the tree - resume from here. Design: extract T624's renderer instead of copying it. (1) NEW src/apprt/win32/whats_new_notes.zig - Renderer (fonts, measure/drawText/drawWrapped/tokenize, render/renderVersion/renderItem/renderLabelledRule, link table + linkAt, shared fillRect/cr), plus a fresh_only knob for the accessory; has 3 unit tests and is registered in src/apprt/win32.zig. (2) whats_new_layout.zig gained Density{spacious,compact} + metricsFor(scale,density) (bare metrics() = spacious) and accessoryHeight(scale)=196dip, with 4 new tests at 1.0/1.25/1.5/2.0. (3) WhatsNewWindow.zig now DELEGATES to that Renderer (all the moved code deleted from it) - win32 lane compiles green after the refactor. (4) NEW src/apprt/win32/WhatsNewNotesView.zig - child control, class GhozttyWhatsNewNotes, own scroll/wheel/thumb/link hit-test, WM_PRINTCLIENT arm for the T940 audit; publishes 'releases=N notes=N height=PX' as its WINDOW TEXT so the harness can assert the model via WM_GETTEXT. (5) ConfirmDialog.zig: Options.notes (?Notes{split}), Layout.notes rect, layoutFor gained a notes_h param after has_icon (all 42 single-line test call sites already updated with a 0), 460dip width floor when notes are present, creates/destroys the accessory, and runModal routes WM_MOUSEWHEEL by POINTER (a focused BUTTON never forwards it). Compiles clean. LEFT TO DO: (a) App.zig handleAgentProtocolSkew - parse release_notes_bundle.agent, partition against WhatsNewWindow.previousSeen()/currentVersion(), pass opts.notes only when split.fresh.len>0 (store+split are stack-owned across the modal, freed after show returns); (b) ConfirmDialog layout unit tests for the notes band at 1.0/1.25/1.5/2.0 (band between message and buttons, spans the text column, notes_h=0 is byte-identical to the old layout); (c) an arm in test/win32/agent-upgrade.ps1 on the skew dialog (arm J) asserting Get-TestChildWindow -Class GhozttyWhatsNewNotes exists with the right model text when agent notes are bundled, and is absent when they are not; (d) full floor + guards.
- 2026-09-07 20:28: stale in-progress claim picked up by a new turn; reassessing from this log and git status before resuming.
- 2026-09-07 21:03: resumed: App.zig now builds a WhatsNewWindow.Model over release_notes_bundle.agent, partitions on the same anchor the window uses, and passes opts.notes only when split.fresh is non-empty (model stack-owned across the modal). WhatsNewWindow.TabModel is now pub Model so there is one splitter, not two. Added 6 ConfirmDialog layoutFor tests for the notes band (no-notes byte-identical, seating between message and buttons, height delta, stacking above checks/note/input, the 460dip prose floor, and all four scales). Harness: arm J gains J5a-J5e (accessory present, model text releases/notes non-zero, real band size, buttons still live) with the debug anchor seeded one release below the newest bundled agent notes; NEW arm N is the control (anchor at the newest release, dialog still raised, no accessory). guard-due: whats-new covers whats_new_notes.zig, agent-upgrade covers WhatsNewNotesView.zig + ConfirmDialog.zig. win32 lane green; floor + GUI harnesses running.
- 2026-09-07 21:46: status: in-progress -> done
- 2026-09-07 21:48: landed in dd5f545f4 (pushed). Validation: floor-lane -Lane all green; P1/P2/P3 green; agent-upgrade ALL PASS (129) incl. new J5a-J5e (accessory model 'releases=1 notes=4 height=902') and control arm N; whats-new (46), remote-disconnect (49), startup-failure (42) re-stamped, plus msg-timer-ids/printclient/isolation-meta/launch-preflight/verdict-exit/cleanslate/stderr-capture/body-complete/test-reach/desktop-launch/command-resolve audits. Only advisory guards remain due (release-artifacts-packaging, rdp-session, install-walkthrough - all pre-existing). No new gaps found: the skew confirmation is the only mandatory agent-restart dialog left since T1056, so there is no second site owed the accessory.
