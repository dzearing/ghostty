---
id: T1276
title: "A relay window whose agent dies goes terminally disconnected in seconds instead of climbing the reconnect ladder"
order: null
deps: ["T609"]
status: "done"
commits: ["13f19d6e2"]
seat: "win"
priority: "P1"
milestone: "M1"
triage-reason: "The reconnect ladder T366 shipped does not appear to run at all on the relay path, which is the path a user's second machine actually uses. User-visible: a window that should come back does not."
tags: ["fix"]
---

# T1276 - A relay window whose agent dies goes terminally disconnected in seconds instead of climbing the reconnect ladder

## Summary

Observed while validating T609, which is what made it visible at all. Kill the
agent under a live RELAY window and, about three seconds later,
`+list --json` reports:

```json
"connection": { "state": "disconnected", "self_healable": false }
```

`self_healable: false` is the TERMINAL verdict — the fast ladder is not running,
no background re-dial is armed, and nothing will bring the window back. The
expected reading three seconds after a drop is
`{"state":"reconnecting","attempt":1}`, and the expected resting state after the
ladder is spent is `disconnected` with `self_healable: true`.

Evidence: `test\win32\ipc-relay.ps1` section 6, which prints the connection
object it read. Its assertion deliberately accepts either `reconnecting` or
`disconnected` — it exists to prove the FIELD tracks the link — so this harness
is green today and will stay green when this task is fixed.

## In plain terms

If the machine at the other end of a Ghoztty window restarts, the window is
supposed to quietly re-dial and pick your session back up. For a window opened
through the relay — which is how you reach a machine that is not on your
network, so the common case — it does not: within a couple of seconds it gives
up for good, and nothing short of closing the window and opening a new one
brings it back. This is the feature T366 built, not working on the path most
users are on.

## Goals

- [x] Find out what actually stops the ladder on the relay path, from the app's
      own log rather than from reading the code
- [x] A relay window re-dials after a drop instead of giving up, whichever way
      it was opened
- [x] It comes back on its own when the machine does, with the same session
- [x] A rejected bearer still stops the ladder dead, in one attempt
- [x] An on-box script that would have caught this, and a tightened section 6

## Details

The suspected cause, to be confirmed before fixing:
`RemoteReconnect.dialOnce`'s `.relay` arm turns `error.WebSocketUnauthorized`
into `dial = .unauthorized`, which is terminal on purpose — "a rejected bearer
is not an unreachable machine: retrying cannot sign anyone in, so the ladder
must stop instead of spending five attempts on a 401." That reasoning is right
for a genuinely rejected bearer. The question is whether a relay answers a dial
for a device whose agent is GONE with the same 401/403 it uses for a bad token.
If it does, an offline machine is being read as a bad credential and the two
must be told apart — an unreachable device is exactly what the ladder exists
for.

Second thing to check while in there: whether the ladder runs at all on the
relay path, or whether it stops even earlier. Three seconds is fast even for a
first dial plus a 401.

Do not weaken the `unauthorized` rule to fix this. A real 401 must still stop
the ladder; what needs to change is the classification of "device offline".

Confirm from the app log during a section-6 run (`remote reconnect: relay dial
device=… failed err=…` names the error the dial actually got).

## Validation criteria

- [x] The app log names the error a relay dial gets when the far agent is dead,
      and it is distinguished from a rejected bearer — measured on box:
      `relay dial device=… failed err=error.WebSocketUpgradeFailed` (the relay
      answers a dead device 409, the fake relay 502), against
      `err=error.WebSocketUnauthorized` for a rejected bearer. The two were
      never confused; the ladder died before either could be reached
- [x] Three seconds after the agent is killed under a relay window,
      `+list --json` reports `{"state":"reconnecting","attempt":N}` — section 6
      of `ipc-relay.ps1` against the REAL Go relay now prints
      `{"state":"reconnecting","attempt":2}`
- [x] After the fast ladder is spent it reports `disconnected` with
      `self_healable: true`, and the 45s background re-dial is armed —
      measured over a 35s walk: `reconnecting` 2 → 3 → 5 then
      `{"state":"disconnected","self_healable":true}` with
      `exhausted its fast ladder; re-dialing in the background` in the app log
- [x] Restarting the agent brings the window back (the T368 walk) — carried as
      the RECOVERY arm of `test\win32\remote-reconnect-relay.ps1`, with the
      path (not the agent) taken away, because an agent that is killed and
      restarted owns no sessions and the automatic ladder goes terminal on that
      BY DESIGN (T611). The window returns to `connected` on its own and holds
      the same session id
- [x] A genuinely rejected bearer STILL goes terminal in one attempt — arm D of
      `remote-reconnect-relay.ps1`: `disconnected`/`self_healable:false`,
      exactly one dial after the drop, and that dial logged as
      `WebSocketUnauthorized`
- [x] `test\win32\ipc-relay.ps1` ALL PASS, with section 6 tightened from
      "left connected" to the specific expected state (still recoverable:
      `reconnecting`, or `disconnected` that is self-healable)
- [x] Floor lanes green (lib + none + win32 + agent), plus P1–P3 and the
      audit harnesses the new script put in scope

## Progress log

- 2026-09-02 04:30: claimed; work starting.
- 2026-09-02 04:44: root cause is NOT the 401 classification: measured on box, the relay answers a dead device with 409/502 (WebSocketUpgradeFailed), which is already a retry. The ladder never got that far - startAttempt resolves the relay bearer from the account/env store ONLY, and a window opened by '+new-remote-window --token=...' records no token, so attempt 1 hit 'needs a relay credential (signed out)' and went terminal with no dial reaching the relay (app log + relay log both confirm). Fix: Window.RemoteMachine.relay now carries the bearer the window was dialed with, and RemoteReconnect prefers the renewable store token with the recorded one as fallback (pure rule: remote_reconnect.chooseRelayToken, unit-tested + negative-controlled). Measured after: reconnecting 2 -> 3 -> 5 then disconnected/self_healable=true with the background re-dial armed, five dials in the relay log.
- 2026-09-02 05:26: status: in-progress -> done
