---
id: T1281
title: "Audit pixel assertions for oracles that cannot separate the outcomes they exist to separate"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "P2: the same defect class landed twice in two days (T363, T1115). An assertion that cannot fail for the right reason is worse than no assertion, because it is counted as coverage."
tags: ["test"]
---

# T1281 - Audit pixel assertions for oracles that cannot separate the outcomes they exist to separate

## In plain terms

Some of our automated colour checks are asking a question that cannot tell the
two answers apart - like judging whether a photo came out by measuring its
average brightness. They pass, so they are counted as checks, but they would
pass just as happily if the thing they are checking were broken. Two of these
were found in two days; this is the sweep for the rest.

## Summary

Two instances, both closed, both the same shape:

- **T363** (2026-09-02): chrome colour checks were scored against a literal
  rather than against the chrome, so they could not fail for the reason they
  existed.
- **T1115** (2026-09-02): `test-desktop-spike.ps1` asserted "PrintWindow returns
  real content" by scoring **mean luminance over the whole window** and
  requiring it to be bright. The terminal surface is dark by design and fills
  most of the window, so "light chrome over a flat, uncomposed client area"
  scores about the same as "the whole window captured" - the exact two outcomes
  the assertion existed to distinguish. It read green for a month while
  `lib\TestDesktop.ps1` had already measured and recorded the opposite answer,
  then flipped red with no behaviour change when the chrome shrank relative to
  the surface.

The generalisable defect: **an aggregate over a region that mixes the subject
with something else**. A whole-window mean, a whole-window distinct-colour
count, or any threshold satisfied by the chrome alone cannot speak about the
surface, and vice versa. The remedy that worked in T1115 was to score **by
region** and to keep a second region as the in-script positive control, so that
"the subject is flat" is a measurement rather than a broken capture.

## Where

- `test/win32/lib/TestDesktop.ps1` - `Get-TestWindowPixels` and the capture
  helpers; its uniform-interior throw is already one guard of this kind
- `test/win32/lib/ComposerSurface.ps1`, `lib/HoverCapture.ps1`,
  `lib/PaneCapture.ps1` - the other capture routes
- the ~21 scripts under `test/win32/` that reach for `CopyFromScreen`, plus the
  pixel probes named in [[T1100]]

## Goals

- [ ] Enumerate every pixel assertion and record, for each, WHICH REGION it
      scores and whether that region contains anything other than its subject.
- [ ] For each one that aggregates over a mixed region, either re-score it by
      region or record why the aggregate is sound for that particular claim.
- [ ] Where a probe is re-scored, add the second region as its positive control,
      the way T1115's chrome band controls its surface reading.
- [ ] Consider whether this belongs in an existing static audit (the
      `verdict-exit` / `body-complete` family) so a new probe cannot land with a
      mixed-region oracle unnoticed.

## Validation criteria

- [ ] every pixel assertion in `test/win32/` has its scored region recorded, and
      the mixed-region ones are listed with a disposition each
- [ ] each re-scored probe carries a positive control in the same script
- [ ] the audit that would have caught T1115 exists and demonstrably fails on a
      constructed mixed-region oracle, or the task records why an audit cannot
      express it

## Progress log

- 2026-09-02: filed from [[T1115]]'s turn, which is the second instance of the
  class in two days ([[T363]] being the first).
