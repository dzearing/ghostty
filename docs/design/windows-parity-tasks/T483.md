---
id: T483
title: "reset-context acceptance: B7 intermittently fails - the continuation is not delivered to a pane whose /clear failed"
order: null
deps: []
status: "done"
commits: ["c88fce629"]
seat: "win"
priority: "P1"
milestone: "M1"
tags: ["test", "fix"]
---

# T483 - reset-context acceptance: B7 intermittently fails - the continuation is not delivered to a pane whose /clear failed

## In plain terms

The test that guards the session-restart machinery fails about one run in three,
in a part of it that has nothing to do with what is being tested. A test that
cries wolf is the same problem as an alarm that cries wolf: after the second
false failure nobody reads the result, and the run stops protecting anything.
Either the test is wrong about what it should expect, or a keystroke really does
go missing sometimes — and which of those it is matters, because the second one
would mean the loop's own continuation can be dropped.

## Goals

- [x] know which of the two it is, from the diagnostics the run now prints
      (neither survives on today's build - the loss died with the pre-T604
      delivery path; see Resolution)
- [x] `test\win32\reset-context.ps1` passes repeatedly (>= 5 consecutive runs)
      (10 consecutive ALL PASS, 2026-08-15)

## Resolution (2026-08-15)

The defect the flake pointed at no longer exists on this branch, and the
timeline says why: the failure was observed 2026-08-05, and the send-keys
delivery path it rode on was REPLACED on 2026-08-09/10 (T604's shared
implementation, T661's resolved-keys marker, and the T428 bracketed-paste
framing that gives the trailing Enter an unambiguous meaning). T664 then
chased the sibling symptom (a lost first character, same section) with wire
capture, receipt oracles, delay sweeps and a 150-send hammer: **315
deliveries of the exact failing shape, zero losses**, every seam in the
write path ruled out by reading. What T664 could not attribute
retroactively — whether the one 2026-08-05 total loss was an input-side or
a display-side failure — cannot be answered from a screen read, which is
the only oracle section B had at the time.

So the answer to goal 1 is: **neither hypothesis survives on today's
build**; the historical loss is attributed to the pre-T604 delivery path
(unframed burst, LF-rewritten Enter) that no longer ships. To make any
recurrence self-attributing instead of another mystery, section B now
carries the same out-of-band receipt oracle E/F/G have: proxy-normal
appends every submitted line to a file outside the pane, B2b proves the
oracle is live in every run, B7c asserts the sentence arrived byte for
byte, and the B7 failure diagnostics print the receipt — a sentence in the
receipt but missing from the screen is a display/read-side loss, a
sentence in neither never reached the shell.

The harness also joined the T783 guard table (`reset-context` row), so an
edit to it that nobody re-runs now fails `validate` instead of being
trusted from memory.

## Summary

`test\win32\reset-context.ps1` section B drives the pre-fix helper (composer
wipe deleted) against `proxy-normal`, reproducing the filed 2026-07-28 symptom:
`nn/clear` arrives as ordinary text, the clear fails, and the helper shouts.
B7 then asserts the part that must hold anyway — **the continuation is sent
regardless** (liveness beats cleanliness).

Observed 2026-08-05 while extending the script for T182: B7 failed on 1 of 3
otherwise-identical runs. In the failing run the helper's OWN read-back also
reported the continuation missing (the B pane's banner carried the
`never echoed the continuation and did not repaint for 10s` text rather than the
usual `'/clear' is still on screen` one), so the pane oracle and the helper
agreed: `RC-TEXT[continue-marker-B]` never appeared. Runs 2 and 4 passed with
the identical script and no code change between them. Sections A and C-F were
stable across all runs.

## What is known

- It is **not** the T182/T261 change: sections E/F (added in the same commit)
  passed every run, and B is untouched by it.
- The B pane is the only one where `+set-banner` fires **before** the
  continuation send — a banner insets the terminal and reflows it, which is a
  difference worth suspecting before anything else.
- The other candidate is the T428 framing edge: if the trailing CR is consumed
  as part of the bracketed paste, the text sits unsubmitted in the composer,
  which produces exactly this pair of symptoms.

## The fix

First reproduce with the evidence the script now prints. B7 dumps the helper's
`typed/send-keys/continuation` log lines and the pane tail at the moment of
failure (added with T182 — the shared `/tmp/reset-context-last.log` is
overwritten by sections C-F, so the evidence had to be captured in-place).
That tells the two hypotheses apart: text sitting in the composer means the
submit was swallowed; nothing on screen at all means the write never landed.

## Validation criteria

- [x] Five-plus consecutive `test\win32\reset-context.ps1` runs, all
      `ALL PASS`, on the final script (runs 5-10 of 2026-08-15's ten: six
      consecutive on the hardened script, ALL PASS (54) each).
- [x] The new receipt oracle is proven live in every run (B2b) and byte-exact
      on the pass path (B7c) - both PASS in all six hardened-script runs.
- [x] Teeth: `GHOZTTY_TEST_T664_BREAK=1` turns the five whole-arrival arms
      (B7b, B7c, E3b, F1b, G3b) red and nothing else - measured
      `5 FAILURE(S) (49 passed)`, and the red run left the stamp alone.
- [x] The new guard row holds: `test\win32\guard-due.ps1` ALL PASS (36), and
      the green runs stamped `test\win32\reset-context.stamp.json`
      (`STAMP UNCHANGED` on re-run proves content-keyed stability).
- [x] Standing floor green: `scripts\floor-lane.ps1 -Lane all` (ALL LANES
      PASS: lib/none/win32/agent) and P1 (25) / P2 (20) / P3 (16) ALL PASS,
      run sequentially after the lanes (T401).

## Progress log

- 2026-08-15 03:19: claimed; work starting.
- 2026-08-15 03:31 [session c1a392e4-32bc-4204-88e7-83d8dca0c37a]: Root-cause reassessment: T664 (2026-08-10) already hammered the exact section-B shape - 315 deliveries, receipt oracles, delay sweeps, wire capture - ZERO losses, and the send-keys delivery overhaul (T604/T661/T428 framing) landed 08-09/08-10, AFTER the 08-05 flake observation. Hypothesis: the defect died with the old delivery path. Evidence run in progress: 8x full reset-context.ps1 (3/3 ALL PASS so far). Plan: if stable, harden section B with the E/F/G-style receipt oracle so any recurrence self-attributes input-loss vs display-loss, then close.
- 2026-08-15 04:05 [session c1a392e4-32bc-4204-88e7-83d8dca0c37a]: Evidence complete: 10 consecutive ALL PASS (4 on old script @52, 6 on hardened @54); teeth run turns exactly B7b/B7c/E3b/F1b/G3b red (5 FAILURE(S), 49 passed); guard-due acceptance ALL PASS (36); reset-context guard row self-stamped. Waiting on floor lanes + P1-P3, then closing.
- 2026-08-15 04:13: status: in-progress -> done [commit c88fce629]
