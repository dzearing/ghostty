---
id: T1390
title: "Closing a remote window kills the session - there is no Disconnect"
order: null
deps: []
status: "done"
commits: []
seat: "win"
priority: "P1"
triage-reason: "P1: main intake 830ba93cd. A user-visible remote-session capability Mac now has and Windows does not - closing a remote window destroys work that should have been resumable"
milestone: "M1"
tags: ["feature"]
---

# T1390 - Closing a remote window kills the session - there is no Disconnect

## Summary

Main intake of `830ba93cd` (merge `669b39b68`, 2026-09-04): the Mac close
confirmation for a window whose terminals run on a **remote machine** now offers
a third button - **Disconnect** (rightmost, and the default), then Close, then
Cancel. Disconnect skips the CLOSE-on-free marking, so the remote process keeps
running and the session stays resumable from the chooser. Windows has no such
button: closing a remote window always terminates the remote child.

## In plain terms

Right now, closing a window that is running on another machine kills whatever is
running there. There is no way to step away from a long build on a remote box and
come back to it later - shutting the window is the same as ending the work. Mac
just grew a Disconnect button for exactly this; Windows has not.

## Details

Points the Mac change makes that the Windows port has to carry, not just the
button:

- **The answer is recorded as a PIN, not a one-shot intent.** A single close
  marks CLOSE-on-free from more than one path, in an order that varies by how
  the window was closed, and a later marking would silently kill the session the
  user asked to keep. The pin refuses later CLOSE markings and is released on
  re-adoption into a live tree (an undone close, a pane moved between windows);
  a rebuild swap deliberately does NOT release it.
- **A remote window is confirmed even when IDLE** - ending a process on another
  machine is not the recoverable thing an idle local shell is. Widen the gate at
  each INTERACTIVE caller only.
- **Not inside the programmatic close path.** IPC `+close`, viewer panes and any
  scripted close must never raise a modal, or every later `ghoztty +...` command
  wedges behind it.
- **Excluded on purpose:** local session-persistence windows (whose "closing a
  pane ends its session" contract the CLI documents), viewer and exited panes,
  and users who set `confirm-close-surface = false`.
- A tab group can mix machines, so a group-scoped close merges the affected
  surfaces into one prompt.

Read `830ba93cd` in full before starting; the Windows seat owns the win32
confirmation dialog and its own surface-free path.

## Goals

- [x] Closing a remote window offers Disconnect / Close / Cancel, Disconnect default
- [x] Disconnect leaves the remote session running and resumable from the chooser
- [x] The keep-alive answer survives every close path (pin, not one-shot)
- [x] Scripted / IPC closes stay modal-free

## Validation criteria

- [x] An acceptance script in `test\win32\` closes a remote window, takes
      Disconnect, and finds the session still listed and resumable -
      `test\win32\remote-disconnect.ps1` section A, oracle = the OS process table
      (the shell the loopback agent spawned is still alive 3s after the window
      is gone). `-NegativeControl` inverts that claim and scores red.
- [x] The same script proves Close still ends it - section B
- [x] `ghoztty +close` against a remote window raises no dialog and returns -
      section D, which also asserts it still ENDS the session
- [x] `confirm-close-surface = false` still closes without a prompt - section E
- [x] A LOCAL window and a LOCAL pane are offered no Disconnect - section F
- [x] Floor: both `zig build test` lanes, `test-agent`, `lib`, P1-P3

## How it was built

- `src/apprt/win32/session_disconnect.zig` - the policy, stated over plain
  facts (`PaneFacts`, `machineIsDisconnectable`, `isDisconnectable`,
  `informativeText`) plus `DetachPin`, so both are checkable in the win32 test
  lane without a desktop. 18 unit tests, including the pin's ordering
  invariant.
- `ConfirmDialog` grew an OPTIONAL third button: `Options.alt_label` /
  `default_alt`, `Result.alt`, laid out `[alt] [ok] [cancel]` right-aligned.
  Mac puts its default rightmost; this dialog has always put the dismissive
  answer last on Windows, and that convention was kept - the platforms disagree
  about the ROW, not about the answer. A two-button caller's layout is
  byte-identical (asserted in the lane).
- The offer is made at the two INTERACTIVE close paths -
  `Window.confirmCloseIfNeeded` (title-bar X / Alt+F4 / `close_window` /
  `close_all_windows`) and `Surface.close` (Ctrl+Shift+W) - and at neither of
  the programmatic ones, so `+close` and the viewer paths never raise a modal.
- The answer is a PIN on the surface: `Surface.setSessionCloseIntent` REFUSES a
  later CLOSE marking while pinned. Three separate paths mark
  (`Window.close`, `closeTabByIndex`, `closeSplitPane`) in an order that varies
  with the close, so a one-shot write would be overwritten by whichever ran
  last. `+rearrange` releases the pin for a pane it keeps - that is
  re-adoption, and a later close is then an ordinary close.

## Decisions taken here

Both were mechanism, not experience, so they were made rather than filed:

- **Button order** follows this dialog's existing Windows convention
  (`[Disconnect] [Close] [Cancel]`, Cancel last) rather than Mac's
  default-rightmost row.
- **The machine name** is the identity that was dialed (hostname, or the relay
  device id) rather than a prettier nickname: what the confirmation has to
  answer is "which box keeps running this", and a name that does not match what
  the user typed answers a different question.

## Progress log

- 2026-09-06 08:57: claimed; work starting.
- 2026-09-06 09:24: policy module session_disconnect.zig (facts + DetachPin, 18 unit tests); ConfirmDialog grew an optional THIRD button (Result.alt / alt_label / default_alt, [alt][ok][cancel] right-aligned, Cancel last per this dialog's own Windows convention); Window.confirmCloseIfNeeded and Surface.close offer Disconnect on a cross-machine window and pin the panes; +rearrange re-adoption clears the pin. Debug app + win32 test lane green.
- 2026-09-06 10:16: validated: floor-lane -Lane all ALL LANES PASS (lib/none/win32/agent); remote-disconnect.ps1 ALL PASS (47) + -NegativeControl red; close-confirm-idle.ps1 section C rewritten to the reversed contract and green (its two remaining reds are pre-existing, filed as T1398); P1 25 / P2 20 / P3 16 ALL PASS; ten audits + viewer-close/install-maintenance/startup-failure guards green and re-stamped; new remote-disconnect guard row registered and stamped.
- 2026-09-06 10:16: status: in-progress -> done
