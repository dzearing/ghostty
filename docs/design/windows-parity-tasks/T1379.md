---
id: T1379
title: "The health line says the loop is down without saying what the pane was doing"
order: null
deps: ["T1370"]
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "P2: the watchdog now reads and logs the pane state; the health line still decides on turn_age alone, so a DEGRADED verdict still sends a human to read the pane. Report-only, so behind anything that changes behaviour."
tags: ["infra", "test"]
---

# T1379 - The health line says the loop is down without saying what the pane was doing

## Summary

P2: the actor (the watchdog) now reads working/idle/unknown off the pane and logs it (T1370), the observer (go-loop-health.ps1) still decides on turn_age alone - so a DEGRADED line still sends a human to read the pane by hand, which is the step T1290 was trying to remove. Not P1: the recovery path is correct, this is only the report.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
