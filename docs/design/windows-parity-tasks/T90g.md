---
id: T90g
title: "Viewer chrome & command integration (Phase K)"
phase: "K"
deps: ["T90f"]
status: "todo"
commits: []
---

# T90g — Viewer chrome & command integration (Phase K)

## Summary

Viewer chrome & commands: T92 titles, hero exclusion, accelerator forwarding, dim walk, split-from-viewer cwd, palette Open File/URL in Pane

## Details

Titles → T92 pane level (§15), DocumentTitleChanged, hero exclusion
(§13), accelerator forwarding for app chords (§11), dim-overlay walk,
split-from-viewer cwd inheritance, palette "Viewer: Open File/URL in
Pane…" (§17).

*Validation:* `viewer-panes.ps1` grown (tab title, ctrl+shift+p from
focused viewer, hero no-op tab, palette entries) ALL PASS ×3;
hero-mode.ps1 regression green.

## Re-scoped 2026-07-29 (T127)

NARROWED, per `docs/design/viewer-panes-windows.md`. Still here: titles/T92
level, `DocumentTitleChanged`, hero exclusion, accelerator forwarding for app
chords, dim walk, split-from-viewer cwd, palette "Viewer: Open File/URL in
Pane...". Moved OUT to their own tasks so this row does not hide them:

- nav chrome + address bar + history/home -> **T159**
- page zoom + pane-scoped chords -> **T161**

Added: the palette entry "Viewer: Open Browser Pane" (`--view=about:blank` with
the caret already in the address field, P11). Mac seeds its `originDirectory`
from the pane it split from (`ViewerCommands.swift:35-38`) -- keep that; it is
what makes provenance work for T164 later.

Not here, deferred with a row each: popups as ghoztty windows (**T163**),
worktree feedback capture and the nav bar's feedback button (**T164**).
