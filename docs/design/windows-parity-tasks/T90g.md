---
id: T90g
title: "Viewer chrome & command integration (Phase K)"
deps: ["T90f"]
status: "skipped(split -\u003e T394 accelerator forwarding, T395 split-from-viewer cwd, T396 palette entries, T397 hero tile)"
commits: []
---

# T90g — Viewer chrome & command integration (Phase K)

## Summary

Viewer chrome & commands: T92 titles, hero exclusion, accelerator forwarding, dim walk, split-from-viewer cwd, palette Open File/URL in Pane

## Details

Titles → T92 pane level (§15), DocumentTitleChanged, hero exclusion
(§13), accelerator forwarding for app chords (§11), dim-overlay walk,
split-from-viewer cwd inheritance, palette "Viewer: Open File/URL in
Pane…" (§17).

*Validation:* `viewer-panes.ps1` grown (tab title, ctrl+shift+p from
focused viewer, hero no-op tab, palette entries) ALL PASS ×3;
hero-mode.ps1 regression green.

## Re-scoped 2026-07-29 (T127)

NARROWED, per `docs/design/viewer-panes-windows.md`. Still here: titles/T92
level, `DocumentTitleChanged`, hero exclusion, accelerator forwarding for app
chords, dim walk, split-from-viewer cwd, palette "Viewer: Open File/URL in
Pane...". Moved OUT to their own tasks so this row does not hide them:

- nav chrome + address bar + history/home -> **T159**
- page zoom + pane-scoped chords -> **T161**

Added: the palette entry "Viewer: Open Browser Pane" (`--view=about:blank` with
the caret already in the address field, P11). Mac seeds its `originDirectory`
from the pane it split from (`ViewerCommands.swift:35-38`) -- keep that; it is
what makes provenance work for T164 later.

Not here, deferred with a row each: popups as ghoztty windows (**T163**),
worktree feedback capture and the nav bar's feedback button (**T164**).

## Two legs already have rows of their own (2026-08-02)

This row is an umbrella over six legs, and two of them were filed separately
while it waited. Both are OUT of here, not duplicated in it:

- **titles / `DocumentTitleChanged` → T383, done 2026-08-02.** Panes name
  themselves (file basename, or the host until the document says otherwise) and
  that name walks the T92 pane→tab→titlebar chain. `onTabTitleChanged` became
  `onPaneTitleChanged(*PaneView, …)` on the way.
- **dim-overlay walk → T380**, still todo, because a layered popup over
  WebView2's Chromium children needs measuring rather than copying.

## SPLIT 2026-08-02 — the four remaining legs each got a row

Picked up as `next`, and the four legs left in it are not one context: one is
a WebView2 COM event-sink build-out, one is a decision that has gone stale
against the Mac, and two are independent features. Split per go.md's
"a single task that would push past ~250k is too big":

- **T394** — accelerator forwarding for app chords
  (`add_AcceleratorKeyPressed`). A focused viewer swallows every app keybind
  today; `ViewerPane.zig` has no key path at all.
- **T395** — split-from-viewer cwd inheritance (T90a §15, Mac
  `splitConfigFromViewer`).
- **T396** — the three palette entries (Open File in Pane…, Open URL in
  Pane…, Open Browser Pane).
- **T397** — the hero-mode tile. **T90a §13's "viewers are excluded" is now
  stale**: `HeroModeView.swift:11` says every pane participates and
  `HeroCarouselView.swift:398` snapshots viewers with `takeSnapshot`.
  Meanwhile win32 counts a viewer as a tile slot and paints nothing into it —
  a hole, which is neither answer. Decision first, then code.

Original text of what was left, kept for the record:

> What was LEFT here: **hero exclusion** (the walks already narrow with
`entry.view.surface() orelse continue` — what is missing is the coverage, and a
decision about what a viewer's carousel tile paints), **accelerator forwarding
for app chords** (`add_AcceleratorKeyPressed`, so ctrl+shift+p reaches the app
from a focused viewer), **split-from-viewer cwd inheritance**, and the **palette
entries**. The palette's "Viewer: Open Browser Pane" can open
`--view=about:blank` today, but its "caret already in the address field" half
needs T159's address bar and lands with it.
