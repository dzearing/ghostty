---
id: T888
title: "mac: validate the T546 sharing uplink on --listen-unix (POSIX arm is comptime-skipped on the win seat, so the graft there is unanalyzed until a mac build)"
order: null
deps: ["T546"]
seat: "mac"
status: "todo"
commits: []
triage-reason: "P1: the win seat cannot analyze the POSIX listen arm (comptime-gated), so the mac build is the first compiler and first runtime this graft meets"
priority: "P1"
tags: ["test"]
---

# T888 - mac: validate the T546 sharing uplink on --listen-unix (POSIX arm is comptime-skipped on the win seat, so the graft there is unanalyzed until a mac build)

## Summary

TODO: describe the defect or gap, the evidence, the fix, and the validation.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
