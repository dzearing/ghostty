---
id: T28
title: "Minor action no-ops cleanup (Phase I)"
phase: "I"
deps: []
status: "skipped(split -\u003e T445 readonly badge, T446 key-state indicator, T447 pwd in chrome, T448 notification click-to-focus)"
commits: ["see details", "b4d797174"]
---

# T28 — Minor action no-ops cleanup (Phase I)

## Summary

Minor action no-ops cleanup. **Split 2026-08-04 → T445, T446, T447, T448.**

## Details

Bundle of small `return true`-but-do-nothing gaps, each cheap: `readonly`
(visual indicator, e.g. tab-title glyph), `key_sequence`/`key_table`
(pending-sequence indicator, e.g. status bubble reuse), `pwd` (append to
window title or tooltip), `color_change` (handle fg/cursor, not just bg),
`close_all_windows` (close each window with confirm, distinct from quit),
notification click→focus round-trip (verify it works; fix if not).

*Validation:* per-item spot checks on box; note each here.

## Disposition (2026-08-04)

**Done under T28 itself:**

- `close_all_windows` now closes each window honoring confirm (was: quit the
  whole app - a real bug).
- `color_change` now retints the scrollbar on foreground/cursor (was:
  background only).
- **`pwd` - the action IS handled**, contrary to this file's earlier "not yet
  done" note: `App.zig:4024-4033` caches it via `Surface.setPwd`
  (`Surface.zig:1572`), landed by T111b so `+list` need not take the
  renderer mutex. What is still missing is only the *chrome surfacing* half,
  which is T447.

**Split out, because the remainder is not a bundle of cheap gaps.** Reading
the three survivors against the Mac source shows each is a real per-pane
overlay port with its own geometry module, design-system compliance at four
DPI scales, and acceptance script - together well past one context:

- **T445** - read-only badge (Mac: `SurfaceView.swift:1249-1300`, a corner
  badge with a click-to-disable popover; win32 has no visual at all).
- **T446** - key-table / pending-key-sequence indicator (Mac:
  `KeyStateIndicator`, `SurfaceView.swift:958-1090`, a draggable pill over a
  *stack* of key tables plus pending keycaps).
- **T447** - surface the cached pwd in the chrome (Mac's counterpart is the
  AppKit titlebar proxy icon, `BaseTerminalController.swift:1712-1721`,
  which has no Windows control - needs a translated affordance).
- **T448** - verify the notification click-to-focus round-trip. The handler
  exists (`App.zig:5723-5743`); the evidence does not.
