---
id: T1223
title: "The daily digest has no enforcement, so a missed one is invisible until someone asks for it"
order: null
deps: []
status: "done"
commits: []
seat: "win"
priority: "P1"
triage-reason: "P1: on 2026-09-01 the loop skipped go.md step 0.5 entirely - it finished T1193 at 04:48 and went straight into T1194. Nobody noticed until the user asked for the digest at 05:18, and the controller wrote it. A step that only runs when someone notices is not a step, and the digest is the user's daily read of what the loop did."
milestone: "M1"
tags: ["infra"]
---

# T1223 - The daily digest has no enforcement

## In plain terms

The loop is supposed to write a short summary of yesterday's work every morning
at 5am. On 2026-09-01 it just didn't, and nothing anywhere noticed. The user
asked for the digest at 05:18 and that is the only reason anyone found out.

## Summary

go.md step 0.5 says to write the day's digest into
`docs/design/windows-parity-digests/<date>.md`. There is no guard, no harness
row, and no health-line field that reports whether today's digest exists - so
skipping it is silent. `guard-due.ps1` has nothing for it (its "digest" matches
are SHA digests, unrelated).

This is the same shape as the failures the tracker keeps re-learning: T1219 was
"the loop pushes every turn and never looks at CI", T1191 was "a module's unit
tests can sit un-run forever because nothing checks the import list". A step
whose omission produces no signal will eventually be omitted.

Note the existing history here: digests exist for 08-21, 08-22, 08-23, then
jump to 08-30. That gap is a **week** of missed digests that nobody caught at
the time, and the standing rule is never to backfill them - which makes
detecting the miss *on the day* the only chance to have one at all.

## What to build

- Surface it where the loop and the supervisor already look: a
  `digest=` field on the `go-loop-health.ps1` line (present / due / missing),
  so a missed digest is visible in the same glance as a dead lock.
- Make the check honest about time: due only after 05:00 local, and only on a
  day the loop was actually alive - a loop that was legitimately stopped
  overnight (the T1179 handoff, for instance) owes nothing.
- Do NOT auto-generate one to satisfy the check. The digest is commentary and
  its value is that a reader wrote it; a generated placeholder would make the
  signal green while destroying the thing it measures.

## Validation criteria

- [x] `go-loop-health.ps1` reports the digest state for today, and a missing
      one after 05:00 shows without `-Postmortem`. Verified: `digest=missing`
      on the plain line, plus a note naming go.md step 0.5 and the file path,
      and the verdict drops to DEGRADED (arms Y1/Y2 of
      `test\win32\go-loop-guard.ps1`).
- [x] A day the loop spent stopped does not report a missing digest. Verified:
      arm Y5 (history with no event in today's window reads `not-due`) and
      arm Y6 (before 05:00 reads `not-due` even with the loop turning).
- [x] A test proves the missing case actually goes red (a teeth-check), not
      just that the green path is green. Verified: section Y leads with the
      missing case (Y1), and Y4 is its positive control.
- [x] go.md step 0.5 points at the check, so the step and its enforcement are
      not two separate ideas. Verified: the step now names the `digest=` field
      and its three states.
- [x] Backfill stays forbidden: the check reports a miss, and never writes one.
      Verified: arm Y3 asserts the file is still absent after a `missing` run.

## Progress log

- 2026-09-01 14:54: claimed; work starting.
- 2026-09-01 15:11: digest= field added to go-loop-health.ps1 (present/missing/not-due; missing reads DEGRADED, never writes a file); -DigestAsOf makes both sides of the 05:00 window testable; section Y (6 arms, missing-case first) in test/win32/go-loop-guard.ps1; go.md step 0.5 now points at the check. go-loop-guard ALL PASS.
- 2026-09-01 15:24: status: in-progress -> done
