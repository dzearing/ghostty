---
id: T1113
title: "context-menu-real-input.ps1 cannot take the foreground and never runs a single assertion"
order: null
deps: ["T1100"]
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "The script asserts nothing at all, so the behaviour it covers is entirely unmeasured rather than merely red."
tags: ["test"]
---

# T1113 - context-menu-real-input.ps1 cannot take the foreground and never runs a single assertion

## In plain terms

One of the checks could not get the app's window to the front, so it gave up before testing anything. It counts as a failure, but the more important part is that nobody is checking that behaviour at all right now.

## Summary

`test\win32\context-menu-real-input.ps1` failed at setup in the T1094 sweep, in 5.8 seconds:

```
SETUP FAIL (plain): could not take the foreground (a fullscreen app or an input lock owns it)
```

No assertion ran. The script's whole subject - the context menu under REAL input rather than synthesised messages - is currently unmeasured, and has been for as long as this has been failing.

The message names its own two suspects. On this box both are live possibilities: the suite now runs scripts on a background test desktop where the notion of 'the foreground' is different, and there is prior history of a system service holding a foreground lock that no amount of correct code can beat.

Attribute before fixing - see [[T1100]].

## Reproduced alone

Re-run on its own with `scripts\suite-run.ps1 confirm` (T1137), against a Debug
`zig-out` at HEAD. Logs: `temp\suite-runs\fwd1\<script>.alone.log`.

- `context-menu-real-input.ps1` - **reproduced alone: yes** (2026-08-23, `SETUP FAIL (plain): could not take the foreground (a fullscreen app or an input lock owns it)`). Red on its own too, so the failure is not an artefact of the sweep it was found in.

## Where

- `test/win32/context-menu-real-input.ps1` - the setup foreground grab
- `test/win32/lib/TestDesktop.ps1`

## Goals

- [ ] Determine which of the two causes it is, by checking the foreground owner's window class at the moment of failure and by running the same setup on the interactive desktop.
- [ ] If it needs the interactive desktop, declare that and have the suite report SKIP with the reason, so 'unmeasured' is visible instead of looking like a product failure.
- [ ] If a service is holding the lock, record the remedy where the next person will find it.
- [ ] Either way the script must end in a state where a reader can tell whether the context menu works.

## Validation criteria

- [ ] the cause is identified and written down here
- [ ] the script either runs its assertions or reports SKIP naming the missing capability
- [ ] the context-menu-under-real-input behaviour has a green run recorded somewhere

## Progress log

- 2026-08-22: filed by T1094's full acceptance sweep (242 scripts, forward order, Debug build at 9a764deea). Evidence quoted above is from that run's log in `temp\suite-runs\fwd1\`.
- 2026-08-23 12:30: reproduced alone: YES (2026-08-23, T1137's confirm pass against a Debug zig-out at HEAD): context-menu-real-input.ps1 (SETUP FAIL: could not take the foreground). Red on its own as well as in the sweep, so the failure is not an artefact of what ran before it - the triage this task carries stands. Log: temp\suite-runs\fwd1\<script>.alone.log.
