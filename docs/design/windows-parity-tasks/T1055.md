---
id: T1055
title: "A restored pane can hand a dead TUI\u0027s mouse-tracking agreement to a plain shell, so moving the mouse types garbage at your prompt"
order: 2
deps: []
status: "done"
commits: ["7124457a7"]
seat: "win"
priority: "P1"
milestone: "M1"
tags: ["fix"]
---

# T1055 - A restored pane can hand a dead TUI's mouse-tracking agreement to a plain shell, so moving the mouse types garbage at your prompt

## Summary

Main intake of b723409c4 (upstream fix on origin/main, shared core). The persisted screen snapshot is serialized with every DEC mode that differs from default - mouse tracking and its SGR encoding included - but that snapshot is a picture of the PREVIOUS app run's grid. Between runs an agent restart can relaunch a session as a plain login shell, so repainting the snapshot hands a dead TUI's 'keep sending me motion reports' agreement to a shell, which reads every pointer move as typed input (35;106;15M... at the prompt, and 'command not found' when one carries a newline). Main's fix: the snapshot stops recording the INPUT-REPORTING modes (modes.isInputReporting: mouse tracking + encoding, focus events, bracketed paste, DECCKM/DECNKM, colour-scheme and in-band size reports) because those are an agreement with a process, not part of the frame; snapshots already on disk are neutralised with input_mode_reset right after they paint, gated on the grid_snapshot capability. Files: src/Surface.zig, src/remote/connection.zig, src/terminal/formatter.zig, src/terminal/modes.zig, src/termio/Remote.zig - all shared core, so this branch ships the defect. PARTIAL COVERAGE HERE: T824 already emits replay_mode_reset on the .rerun/.prompt relaunch paths (src/termio/Remote.zig:477), so the relaunch case is disarmed; the gap is the ALIVE RE-ATTACH path and the snapshot itself carrying the modes. Work: port the upstream change onto this branch, reconcile it with T824's replay_mode_reset so the two mechanisms do not double up, and add a win32 acceptance arm that restores a snapshot taken while a TUI had mouse tracking on and proves a pointer move produces no bytes at the prompt. Raise to P0 if the alive re-attach path reproduces on the box.

## In plain terms

Close a full-screen program (an editor, a chat agent, anything that follows your
mouse), quit Ghoztty, and open it again. Your panes come back looking exactly
right — but the terminal has also quietly promised the *old* program that it will
keep reporting every mouse move, and the shell sitting there now has no idea what
those reports are. It types them at your prompt: `35;106;15M35;103;14M…`, and
`command not found` whenever one of them carries a newline. After this lands, a
restored pane comes back as a picture of your screen and nothing more; only a
program that is still running keeps its mouse.

## Goals

- [x] Saved screen snapshots stop recording the input-reporting modes (mouse
      tracking and its encoding, focus events, bracketed paste, arrow/keypad
      encoding, colour-scheme and in-band size reports)
- [x] Snapshots already on disk are neutralised right after they repaint, on the
      live re-attach path that T824/T922 did not cover
- [x] A live program that really does want the mouse still gets it back, from the
      agent's own repaint of the current screen
- [x] The two reset lists cannot drift apart — `replay_mode_reset` is built from
      `input_mode_reset`, and gains the `?2031l`/`?2048l` it was missing
- [x] The libghostty C formatter options mirror the new field (the `lib` lane's
      comptime guard)

## Validation criteria

- [x] `zig build test -Dapp-runtime=none` green — includes the new
      `isInputReporting` and "keeps the picture, drops the reports" formatter
      tests (`scripts\floor-lane.ps1 -Lane all`)
- [x] `zig build test -Dapp-runtime=win32` green — includes the two new
      `T1055 input_mode_reset` tests in `src/termio/Remote.zig`
- [x] `zig build test-agent` green
- [x] `lib` lane green (`zig build -Dapp-runtime=none`) — this is what caught the
      missing C-API mirror, so it is the evidence the Mac seat still builds
- [x] `test\win32\session-snapshot-reattach.ps1` ALL PASS, including the new
      section M: a pane arms `?1003h/?1006h/?1004h/?2004h` the way a TUI does and
      the snapshot the app records carries none of them (M3), while the picture
      mode armed in the same breath survives (M4, the positive control that mode
      serialization did not simply go dark)
- [x] P1-P3 acceptance floor still ALL PASS

## Progress log

- 2026-08-21 08:20: claimed; work starting.
- 2026-08-21 08:48: Ported b723409c4 + its follow-up e36fa0eb6 onto this branch: modes.isInputReporting, formatter Extra.input_modes (plus the C-API mirror in src/terminal/c/formatter.zig + include/ghostty/vt/formatter.h, which the lib lane caught), Surface.sessionSnapshot clears input_modes, and Remote.zig splits input_mode_reset out of replay_mode_reset (gaining ?2031l/?2048l) and emits it after the ALIVE re-attach snapshot paint gated on peerRepaintsOnAttach() (this branch's name for supportsGridSnapshot, so no duplicate accessor was added). T824/T922 already cover the relaunch and restore-policy paths; no double-up, replay_mode_reset is now literally input_mode_reset ++ the display bits. Acceptance arm M added to test/win32/session-snapshot-reattach.ps1.
- 2026-08-21 09:30: status: in-progress -> done [commit 7124457a7]
- 2026-08-21 15:30: Correction from T1067 (2026-08-21): section M's arming child ('cmd /c type' of a byte file) never actually armed anything. conhost forwards a mouse-tracking DECSET out of a ConPTY only while the child has ENABLE_VIRTUAL_TERMINAL_INPUT set, and a cooked cmd child does not - the ?1004h/?2004h that looked like survivors were conhost's own ConPTY preamble (?9001h ?1004h ?2004h). The section's comment blaming ConPTY for eating ?1003h/?1006h has been corrected, the arming child replaced with a real TUI shape, and M3's negative control now scores the whole family (was conhost's preamble pair). The fix this task shipped is unaffected and still correct.
