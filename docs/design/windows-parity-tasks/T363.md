---
id: T363
title: "tab-strip.ps1 hardcodes the chrome bar's R range (10..40): a private absolute restatement of a derived color"
order: 372
deps: ["T43"]
status: "done"
priority: "P2"
triage-reason: "test-script hygiene: a hardcoded color range restating a value the app derives"
commits: ["542cb9cfe"]
seat: "win"
---

# T363 - tab-strip.ps1 hardcodes the chrome bar's R range (10..40): a private absolute restatement of a derived color

## Summary

`tab-strip.ps1:340` asserts

```powershell
Assert ($px.R -ge 10 -and $px.R -le 40) "strip: dead space right of the tab is bar background, not tab or accent (R=$($px.R))"
```

The claim is *"this pixel is bar background, not a tab and not an accent"* —
a claim about which SURFACE the pixel belongs to. What it actually checks is
that the bar's red channel is a dark grey, which is only true because the
script's `--background` happens to be black and `chrome_theme.bar_wash` happens
to be 0.08. It is the color-domain twin of the geometry duplication T257 spent
a task deleting: a datum the app DERIVES, restated privately as a literal.

## Details

**How it surfaced (T43).** With the debug chrome marker live the bar is amber,
so `R = 102` and the assertion went red on a build that was painting exactly
what it was told to. T43 shipped with the marker disabled for the acceptance
harness (`GHOZTTY_DEBUG_MARKER=0` in `test/win32/lib/TestDesktop.ps1`), so the
literal survives — but it will fail again for any change that legitimately
moves the bar: a different `--background` in this script, a `bar_wash` tune, a
`window-theme` case, or anyone who runs the suite with the marker on.

The failure mode is worse than a false red. The range 10..40 also *passes* for
colors that are not the bar at all — any dark tab fill lands in it — so the
assertion is weak in the direction it claims to be strong.

**The fix.** Score the claim the way `Get-TestTabExtents` already scores the
same question: sample the bar somewhere that can belong to no tab and no
button, and assert this pixel EQUALS that one. That is exact, needs no
constant, and cannot be satisfied by a coincidentally-dark tab.
`chrome-theme.ps1`'s `Get-Wash` shows the derived alternative if an absolute
value is ever really wanted.

Audit the rest of the GUI scripts for the same shape while here — a literal RGB
or channel range standing in for a color the app derives. Absolute constants
are legitimate where the app itself spells one (`tab-color.ps1`'s
`(255,69,58)`, an accent the user picked); they are not where the app computes
one.

*Validation:* `tab-strip.ps1` ALL PASS, and ALL PASS again with
`GHOZTTY_DEBUG_MARKER=1` (which is what proves the assertion stopped depending
on the bar being dark grey).

## In plain terms

Nothing a user sees changes here. This is about a test that could cry wolf: it
checked that a patch of the tab bar was "bar coloured" by looking for a
particular shade of dark grey, written into the test by hand. The app does not
have that shade written down anywhere - it works it out from the terminal's
background colour - so the moment anything moved that colour (a different
background, a lighter theme, the amber tint the debug build paints), the test
went red against a window that was drawn perfectly. It was also weak the other
way: any dark tab passed it too. Now every one of these checks compares the
pixel it is asking about with a pixel of the surface it claims to be, so it is
right for any colour scheme.

## Goals

- [x] the strip's dead-space check compares against the bar, not a channel range
- [x] the "+" and close-glyph scans derive their "lit" floor from the bar too
- [x] audit the other GUI scripts and fix the same shape where it appears
- [x] a repeatable run proves the colour claims no longer depend on a dark bar

## Validation criteria

- [x] `test\win32\tab-strip.ps1` ALL PASS (70 assertions) - run 2026-09-02
- [x] `test\win32\tab-strip.ps1 -DebugMarker` ALL PASS (62) - the amber-bar run,
      which is what proves no colour claim depends on the bar being dark grey
- [x] `test\win32\close-glyph-white.ps1` ALL PASS (5) - the derived resting-chrome
      check reads `50,50,50 vs band 50,50,50`, where the old literal `R < 80`
      passed by luck
- [x] `test\win32\caption-bar.ps1`: all four rewritten checks PASS (`20,20,20 vs
      derived 20,20,20`). The run still ends on ONE pre-existing failure, a
      uniform maximized capture, which reproduces byte-for-byte on the HEAD copy
      of the script and is filed separately
- [x] floor lanes green

*Note on the criterion this task was filed with.* It asked for ALL PASS with
`GHOZTTY_DEBUG_MARKER=1` outright. Four assertions cannot meet that and should
not: they are claims about how far a chrome WASH steps away from the bar it is a
fraction of, and the marker tints the bar, so the app genuinely paints less
contrast (`Window.zig`'s `debugMarkerEnabled` says exactly this, and it is why
T43 turned the marker off for the suite). Those four are now SKIPped under
`-DebugMarker` with that reason printed, so the marker run answers the question
T363 actually asks - is every MEASUREMENT derived? - and is green precisely when
the answer is yes.

## Progress log

- 2026-09-02 02:24: claimed; work starting.
- 2026-09-02 02:47: root cause: the assertion restated a DERIVED chrome color (bar_wash over --background) as a literal channel range. Fixed section 2 to compare the sampled pixel against the bar's own reference pixel (m.RunRight-2, the same one Get-TestTabExtents uses), and derived the three 'lit' glyph-scan floors from that bar pixel too (bar sum + 90, which is the old literal 150 over a (20,20,20) bar). Added -DebugMarker so the amber-bar proof is a repeatable run. Audit: caption-bar.ps1 (4 literals -> Get-Wash/chrome-relative) and close-glyph-white.ps1 (1) carried the same shape and are fixed; remote-pill.ps1's hue tests and tab-color.ps1's accent are legitimate absolutes. Filed T1273 for a pre-existing uniform-capture failure at the end of caption-bar.ps1.
- 2026-09-02 02:57: validated: tab-strip ALL PASS (70); tab-strip -DebugMarker ALL PASS (62, 4 wash-contrast claims SKIPped with reason); close-glyph-white ALL PASS (5); caption-bar's four rewritten checks PASS. Floor: lib/none/win32/agent ALL LANES PASS; P1 (25), P2 (20), P3 (16) ALL PASS. Six suite-wide script audits re-ran green and re-stamped.
- 2026-09-02 02:57: status: in-progress -> done [commit 542cb9cfe]
