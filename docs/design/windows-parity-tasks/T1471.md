---
id: T1471
title: "The report-body arms in viewer-feedback-images.ps1 still check for phrases, not the whole published body"
order: null
deps: ["T672"]
status: "todo"
commits: []
seat: "win"
priority: "P3"
triage-reason: "Same weak-needle shape T672 fixed one level up, on the artifact the feature exists to produce; ranked behind the composer arms because the body is a rendering of text those arms now pin exactly."
tags: ["test"]
---

# T1471 - The report-body arms in viewer-feedback-images.ps1 still check for phrases, not the whole published body

## Summary

Same weak-needle shape T672 fixed one level up, now on the artifact the feature exists to produce: report.json's body is asserted with three -match/-notmatch needles, and 'Image #1' is nowhere in the body is satisfied by a fragment such as 'Imag'. The whole body is knowable at that point in the script (two rendered links plus the typed words), so compare it whole, the way the composer arms now do.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
