---
id: T988
title: "The chooser's focused row fails two acceptance probes that still describe the pre-T828 accent pill"
order: null
deps: []
status: "done"
commits: ["1889e3c8a"]
seat: "win"
priority: "P1"
triage-reason: "two chooser acceptance scripts are red on the same feature - ipc-machine-chooser and chooser-selection both mis-read the focused selection"
tags: ["test", "fix"]
---

# T988 - The chooser's focused row fails two acceptance probes that still describe the pre-T828 accent pill

## Summary

Found while converting the chooser probes to the synchronous capture (T942).
Both failures reproduce identically with the default capture and with `-Sync`,
so they are neither new nor a capture artifact — they were red before that work
started and stayed red after it.

Two red assertions, both on the FOCUSED selected row:

1. `test\win32\ipc-machine-chooser.ps1:480` —
   `focused list: the selected row is accent-tinted (b-r = 0 at the pill)`.
   It samples the pill body at `$lr.Left + 14` and demands `b - r >= 25`, which
   is the pre-T828 treatment: an accent-TINTED pill. T828 made the pill a
   neutral wash at two weights and moved the accent onto the 4x16 indicator
   capsule at the leading edge, so a neutral reading here is the shipped
   behavior and the assertion is the thing that is stale. Whatever replaces it
   has to keep the oracle honest — the point of the pair at 479/480 is that a
   region signature alone cannot tell a repaint from a stale capture, so the
   colour half needs a real successor (the indicator's accent), not a deletion.

2. `test\win32\chooser-selection.ps1` section C —
   `focused - the fill IS wash(row bg, 0.16)` reads `#eeeeef` where it wants
   `#51545a`, and then `the selected row is a full row tall (5 px)`. `#eeeeef`
   is a near-white neutral, which is what `chooser_rows.focusRing` derives from
   a dark pill: section C samples the pill's TOP EDGE line
   (`$lr.Top + $fillInsetY`), and that is exactly where §2.2's focus rim lands
   once the list holds keyboard focus. So the probe's chosen sample point
   collides with a treatment that ships on purpose.

   The open question inside this one: section C selects the row with a MOUSE
   click, and `rowPaint` only draws the rim when `focus_visible` is set —
   Windows' own convention is to hide focus visuals while the user is driving
   with the pointer. Either the app is leaving `focus_visible` on after a click
   (a real defect a user would see as a rim appearing on a mouse click), or the
   earlier `ctrl+shift+n` chord legitimately cleared `UISF_HIDEFOCUS` for the
   window and the probe simply has to sample below the rim. Decide from
   evidence before moving either side.

## In plain terms

Two of the tests that check the machine list are still describing the old look
of the selected row — the bright coloured pill that was deliberately replaced
with Windows 11's quiet grey selection. They fail against the app doing the
right thing. One of them may also have caught something real: a focus outline
that shows up when the row is clicked with the mouse, which Windows normally
only shows for keyboard users.

## Goals

- [x] `ipc-machine-chooser.ps1`'s colour oracle asserts the T828 treatment (the
      indicator carries the accent, the pill does not) rather than the old pill
- [x] The mouse-click focus-rim question is answered from evidence, and the
      answer lands either in the app or in the probe's sample point
- [x] Both scripts are ALL PASS over three consecutive runs

## Validation criteria

- [x] `powershell -NoProfile -File test\win32\ipc-machine-chooser.ps1` is ALL
      PASS, three consecutive runs — 77 assertions each, exit 0 (was 73 passed
      + 1 FAIL)
- [x] `powershell -NoProfile -File test\win32\chooser-selection.ps1` is ALL
      PASS, three consecutive runs — 35 assertions each, exit 0 (was 29, before
      section E existed)
- [x] The finding on `focus_visible` after a mouse click is written into this
      file, whichever way it went — see **The focus-rim finding** below

## What was measured (2026-08-19)

### 1. The machine-chooser colour oracle was pre-T828, and its successor is not a deletion

The assertion demanded `b - r >= 25` in the pill BODY at `Left + 14`. T828 moved
the accent off the fill entirely, so the shipped build reads 0 there and always
will. The replacement measures the treatment that actually ships, split at the
body sample so no rounding assumption about the capsule's rect is needed:

| what | unfocused | focused |
|---|---|---|
| pill body tint (`b - r` at `Left+14`) | 0 | 0 — the fill has no accent |
| pill body max tint (`Left+14` → DIP 60) | 0 | 0 — and none anywhere else |
| mark band max tint (DIP 4 → `Left+14`) | 0 — neutral mark | **183** — the accent |
| fill weight (`r`) | 60 | **73** — wash 0.10 → 0.16 |

The fill-weight step is the piece that keeps the T233 pair honest: the region
signature says the row repainted, and `r 60 → 73` says it repainted INTO the
emphasized state, which a stale capture cannot fake.

### 2. The focus-rim finding: the app is right, the probe was state-blind

**Not a defect.** `rowPaint` honours `ODS_NOFOCUSRECT` exactly as it should.
Measured both ways on the same build, by pinning the bit with
`WM_CHANGEUISTATE` and reading it back with `WM_QUERYUISTATE`:

- `UISF_HIDEFOCUS` **set** (pointer-driven, UI state `0x3`) + a mouse click on
  the row → the 194-point perimeter probe finds **no outline at all**. So the
  app is not leaving `focus_visible` on after a click.
- `UISF_HIDEFOCUS` **cleared** (keyboard-driven, UI state `0x2`) → the rim
  appears at `y = 2` as `#eeeeef`, neutral (chroma 1), 6.55:1 against the pill.

`#eeeeef` is precisely what the T942 run measured as a failure, which identifies
that run as the keyboard-driven state. Windows does not re-hide focus visuals
when the pointer is used again after a keyboard chord, so a real `ctrl+shift+n`
clears the bit for the rest of the dialog's life while a POSTED chord does not —
that alone flips the state between runs. On top of it, whether the rim reaches
the probe's sample line is scale-dependent: at 125% the rim spans `y = 2..4` and
the sample at `fill_inset_y = 1` is clean, but at 100% `focus_path_inset` is 2
with a 2px stroke, so it covers `y = 1..3` and lands on the sample.

The remedy is therefore in the probe, and it is to name the state rather than to
move the sample: sections C/D pin `UISF_HIDEFOCUS` **set** (D's claim is "no
outline", and the rim is an outline drawn on purpose), and a new **section E**
covers the other state — the rim draws, in neutral ink, not as a second accent
mark. Nothing pixel-tested the rim before; the unit tests assert the colour
model only.

## Evidence as found (2026-08-19, T942's turn)

```
chooser-selection: 2 FAILURE(S) (19 assertions passed)
     focused row bg #30333b, pill #eeeeef, want #51545a
  FAIL focused - the fill IS wash(row bg, 0.16), not an accent tint
  FAIL focused - the selected row is a full row tall (5 px)

MACHINE-CHOOSER ACCEPTANCE: 1 FAILURE(S) (73 passed)
  FAIL focused list: the selected row is accent-tinted (b-r = 0 at the pill)
```

Identical output on both capture modes, measured back to back.

## Progress log

- 2026-08-19 08:09 [session cf304820]: chooser-selection.ps1 is GREEN on this box today: three consecutive runs, ALL PASS (29 assertions) each, against the T942 turn's three runs of 2 FAILED / 19 passed. The assertion COUNT differs too (29 vs 21), so yesterday's runs did not merely fail two arms - they stopped short of sections this run reaches, which points at a setup/box condition rather than at the two focused-selected-row assertions being wrong. Its guard row re-stamped on the green run, so nothing is gated on this any more. Do NOT close on this evidence: the premise (the probes describe the pre-T828 accent pill) has not been re-read against the code, and an intermittent red is exactly the standing-red shape today's digest called out. Re-read the two assertions against ChooserSelection's current painting before deciding.
- 2026-08-19 19:23: claimed; work starting.
- 2026-08-19 19:35 [session 1ea0132f]: root cause found and both halves fixed. (1) ipc-machine-chooser's colour oracle was pre-T828: it demanded an accent tint in the pill BODY (b-r>=25 at Left+14) while T828 moved the accent onto the 4x16 indicator capsule. Replaced with the T828 treatment measured in three parts - pill body stays neutral (b-r=0), the fill steps up to the emphasized wash (r 60 -> 73, i.e. wash 0.10 -> 0.16), and the accent appears in the mark band only (max b-r=183 there, 0 across the body). ALL PASS 77 assertions. (2) chooser-selection section C/D: NOT an app defect. rowPaint honours ODS_NOFOCUSRECT correctly - with UISF_HIDEFOCUS SET and a mouse click, the 194-point perimeter probe finds no outline at all. Clearing the bit makes the rim appear at y=2 as #eeeeef - exactly the colour T942 measured as a failure. So the red run was the keyboard-driven state (a real chord clears the bit; a posted one does not) landing the rim on the sample line, which happens at 100% DPI where fill_inset_y and focus_path_inset collide. Fix: C/D now PIN the bit they claim about, and a new section E covers the other state - the rim draws, in neutral ink (chroma 1), 6.55:1 against the pill. 35 assertions.
- 2026-08-19 20:03: status: in-progress -> done [commit 1889e3c8a]
