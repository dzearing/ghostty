---
id: T1031
title: "Pane/window resize and banner expand-collapse flicker: panes clear and repaint every frame instead of resizing smoothly"
order: null
deps: []
status: "done"
commits: ["5e338b2c5"]
seat: "win"
priority: "P1"
triage-reason: "User-reported on 2026-08-20: dragging a split divider, resizing the window, or expanding/collapsing the banner flickers badly. Resize is one of the most-touched interactions in the app and this is the first thing anyone notices about how the terminal feels."
tags: ["perf", "polish"]
---

# T1031 - Pane/window resize and banner expand-collapse flicker: panes clear and repaint every frame instead of resizing smoothly

## Summary

Reported by the user, 2026-08-20: "when i resize a pane, the panes flicker
fairly badly... It should be butter smooth but it seems like we're clearing and
repainting on every frame." Three interactions are named and all three run the
same layout path: dragging a split divider, resizing the whole window, and
expanding/collapsing the sticky banner.

## In plain terms

Grabbing the line between two panes should feel like sliding a physical
divider - the text on both sides just moves. Right now the panes blink: for a
moment each side goes flat background, then the text snaps back. Every frame of
the drag does that, so a slow drag looks like it is strobing. Same when you drag
the window edge, and same when the banner above a pane opens or closes. Nothing
is broken - the end state is correct - but it reads as cheap, and it is the
interaction people touch most.

## Investigation starting points (all `? INFERRED` from reading, none confirmed on the box)

Not a diagnosis - a list of the suspects the fixing turn should measure before
changing anything.

1. **Per-leaf `MoveWindow(.., bRepaint = TRUE)`, one child at a time.**
   `Window.layoutNode` (`src/apprt/win32/Window.zig:2968`, and the hero path at
   `:2772`/`:2818`) walks the split tree and moves each pane HWND individually.
   There is no `BeginDeferWindowPos`/`DeferWindowPos` batch, so during a single
   layout pass sibling panes are transiently at mismatched geometry, and each
   move schedules its own erase+paint.
2. **`WM_ERASEBKGND` fills the whole client with the background brush every
   time** (`src/apprt/win32/App.zig:8078`), with the comment that "the OpenGL
   renderer will overwrite the entire client area on the next frame". The next
   frame is the problem: that is one displayed frame of flat background per
   erase, which is exactly the flicker being described.
3. **`WM_PAINT` is asynchronous w.r.t. the renderer**
   (`src/apprt/win32/App.zig:8098`): it `ValidateRect`s and wakes the renderer
   thread. During a live resize the GL frame lands some time after the erase, so
   the composited result is background-then-content rather than one atomic
   frame. `surface.in_live_resize` is already tracked at
   `WM_ENTERSIZEMOVE`/`WM_EXITSIZEMOVE` (`App.zig:7995`) but check what, if
   anything, currently changes because of it.
4. **Divider bands are painted unbuffered via `GetDC` right after the layout
   pass** (`Window.zig:2841`). Deliberate and documented (T252 audited it and it
   stays for the moved-band case), but it is a second, separately-timed paint on
   top of the children.
5. **Banner expand/collapse goes through the same path** - it changes
   `view.bannerLayoutInset` and relayouts, so if 1-3 are the cause it explains
   the banner case for free, and fixing them should fix all three.
6. **Check the Mac seat for parity.** If AppKit is smooth here, the target is
   defined by it; if it flickers too, this is a both-seats task and needs the
   Mac half filed or landed.

## Directions worth measuring

- Batch the layout pass: `BeginDeferWindowPos`/`DeferWindowPos`/`EndDeferWindowPos`
  over every leaf, or `MoveWindow(.., FALSE)` + one invalidate at the end, so all
  panes land in a single frame.
- Stop erasing what will be fully repainted: erase only genuinely-exposed
  regions during resize, or skip the fill entirely when a fresh GL frame is
  guaranteed within the same paint cycle.
- Present synchronously while `in_live_resize` is set - render and swap inside
  the size/paint handling rather than waking the renderer thread and returning.
- Consider holding the last good frame (blit/stretch) instead of clearing to
  background while the new grid size is being rasterized.
- Whatever lands, it must not regress T252's moved-band repaint or T155's
  tiling, and must not reintroduce stale pixels at the divider.

## What actually caused it (measured, not inferred)

Four mechanisms on one layout path, and they do NOT all drive all three
interactions - which the investigation notes assumed and the measurement
corrected:

1. **No clip styles.** The container was created without `WS_CLIPCHILDREN` and
   the pane children without `WS_CLIPSIBLINGS`, so parent-side drawing and the
   transient mismatched geometry of an unbatched layout pass could land on
   pixels a pane owns. Drives all three.
2. **The unconditional erase.** `WM_ERASEBKGND` filled the whole client with
   the background brush every time, "because the OpenGL renderer will overwrite
   the entire client area on the next frame". The *next* frame is the bug: the
   erase is synchronous and the swap is not. Measured to fire on **window
   resize**, and NOT during a divider drag - so this one is the window-edge
   case specifically.
3. **One `MoveWindow` per leaf**, so one layout pass was one frame per pane.
4. **`signalFrameDrawn` had no caller at all.** It documented itself as "called
   by the renderer thread after SwapBuffers" and nothing called it, so the
   `in_live_resize` wait in `Surface.handleResize` sat on an event nobody ever
   set and expired after 16ms every time. The window-frame drag was never
   synchronous either - it just slept. And `in_live_resize` is only set at
   `WM_ENTERSIZEMOVE`, so the divider drag and the banner toggle never even
   reached that path.

Evidence for 4, off the debug build's own log: before the wiring, 9 of 9 erase
decisions read `presented=false`; after it, the pre-first-frame ones fill and
every later one declines.

## Goals

- [x] Divider drag, window resize, and banner expand/collapse are all free of
      visible background flashes at the default config.
- [x] The three interactions share one fix rather than three special cases.
- [x] No stale-pixel or divider-band regression (T155, T252).

## Validation criteria

- [x] Root cause named with evidence, not just "it looks better now" - the
      erase handler logs its decision (`surface erase surface=.. presented=..
      fill=..`) and the before/after counts are above.
- [x] An on-box acceptance script in `test/win32/` that catches the regression
      mechanically - `test\win32esize-flicker.ps1`, 16 assertions, guarded by
      a `resize-flicker` row in `scripts\guard-due.ps1`. **Teeth proven twice
      over**: with the erase gate and both clip styles reverted and rebuilt it
      reports 4 FAILURES, and `-NegativeControl` fails on the restored build.
      Its FIRST oracle was thrown away for failing exactly this check - see the
      progress log.
- [x] Floor lanes (`floor-lane.ps1 -Lane all`) and P1-P3 green.
- [x] Mac seat: verified unaffected by inspection of the diff. Every change is
      in `src/apprt/win32/` except one comptime-`win32`-guarded line in
      `src/renderer/generic.zig` (beside the existing `heroSnapshot` hook of the
      same shape) and a test-only import in `src/apprt.zig`. The defect is a
      Win32 painting-model artifact - erase-then-paint, unclipped children -
      with no AppKit counterpart, so no Mac task is owed.

## Progress log

- 2026-08-20: filed from a user report in the controller chat.
- 2026-08-20 18:19: claimed; work starting.
- 2026-08-20 18:25 [session 92f56fd0]: Root cause read out of the code (4 mechanisms, all on the same layout path): (1) container window is created WITHOUT WS_CLIPCHILDREN and pane children WITHOUT WS_CLIPSIBLINGS (Window.zig:858-864 style, Surface.zig:529 WS_CHILD only), so parent-side drawing and the transient mismatched geometry of an unbatched layout pass are free to land on child pixels; (2) the surface's WM_ERASEBKGND (App.zig:8078) unconditionally FillRects the WHOLE client with the flat background brush, and WM_PAINT only ValidateRect+wakes the renderer thread, so the flat fill is DISPLAYED and the GL frame lands a compositor frame or more later - one flat frame per erase; (3) layoutNode MoveWindows each leaf one at a time with bRepaint=TRUE (Window.zig:2969), no DeferWindowPos batch; (4) the 'block until a new frame is presented' path in Surface.handleResize is gated on in_live_resize, which is ONLY set at WM_ENTERSIZEMOVE - so a divider drag and a banner expand/collapse never take it. That explains all three reported interactions with one story. Fix going in: clip styles, gate the erase fill on has-presented-a-frame (surface window only - the search/palette popups share the class and DO need the fill), batch the layout with BeginDeferWindowPos, and route divider-drag + banner relayout through the same synchronous-present path window resize already uses.
- 2026-08-20 19:05 [session 92f56fd0]: Two findings the teeth check forced, both now folded in. (a) The first acceptance oracle - send WM_ERASEBKGND into a DC the SCRIPT owns and see whether the sentinel survives - went GREEN with the fix fully reverted. An HDC is a per-process handle, so the app's FillRect was writing through a value that means nothing on its side; the probe measured nothing. Removed from lib\TestDesktop.ps1 (with a note so nobody rebuilds it) and replaced with a debug-build stderr oracle: the handler logs 'surface erase surface=.. presented=.. fill=..', same shape as pane-banner.ps1's 'banner collapse from='. Every erase assertion is now paired with a vacuity check that the oracle is alive. (b) Measuring THAT showed presented=false on every line: Surface.signalFrameDrawn had NO CALLER despite documenting itself as 'called by the renderer thread after SwapBuffers', so the in_live_resize wait in handleResize was sitting on an event nobody ever set and expiring after 16ms every time - the window-frame drag was never synchronous either. Wired it into the drawFrameEnd defer in src/renderer/generic.zig, comptime-guarded to win32 exactly like heroSnapshot beside it. Evidence on the box: before the wiring, 9/9 erase lines read presented=false fill=true; after, the pre-first-frame ones fill and every later one declines. Teeth: with the erase gate and both clip styles reverted and rebuilt, resize-flicker.ps1 reports 4 FAILURES; restored and rebuilt, ALL PASS (16 assertions). Also precise now about which mechanism drives which interaction - the erase fires on window resize, NOT during a divider drag, so the drag case is carried by the clip styles, the DeferWindowPos batch and the sync present.
- 2026-08-20 19:29 [session 92f56fd0]: Validation complete on the box. floor-lane.ps1 -Lane all: lib/none/win32/agent ALL LANES PASS. resize-flicker.ps1 ALL PASS (16 assertions) on the final build; 4 FAILURES with the erase gate and both clip styles reverted and rebuilt (teeth), and -NegativeControl fails on the restored build. Five guards run green and re-stamped after the harness edits: printclient-audit (8), isolation-meta (7), verdict-exit, stderr-capture (25), test-desktop (32). P1 25 / P2 20 / P3 16 assertions ALL PASS. Filed T1033 (acceptance scripts that launch the app without Assert-GhozttyIsolatedBuild - banner-resize-repaint.ps1 is one, and nothing enumerates them), T1034 (sweep for other hooks documenting a caller they never got, the signalFrameDrawn class), T1035 (the synchronous-present path still has no oracle with teeth).
- 2026-08-20 19:30: status: in-progress -> done
