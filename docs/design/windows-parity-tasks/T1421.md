---
id: T1421
title: "Signing out of the relay account does not revoke this Windows machine's enrollment"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P0"
milestone: "M1"
triage-reason: "Security. Main intake 2026-09-07 (0c3a19764, f3b1e5fb5). Sign-out on Mac now revokes the machine's device enrollment; the Windows client almost certainly still drops only the user session, which means a machine the user believes they have signed out of stays listed, stays online, and keeps streaming a bridged session to every other client on the account."
tags: ["security", "fix"]
---

# T1421 - Signing out of the relay account does not revoke this Windows machine's enrollment

## In plain terms

When you sign out of your account in Ghoztty on a machine, you expect that
machine to stop being yours - gone from the machine list on your other
computers, no longer reachable, no longer streaming whatever was on screen. On
Mac that is now true. On Windows, signing out very likely only forgets *you*:
the machine itself stays enrolled, stays listed, stays online, and a session
already bridged to another computer keeps sending bytes. Sign-out looks like
"this machine is no longer mine" and isn't.

## Goals

- [x] Confirm what the Windows client actually does on sign-out today — verified: `relay_signin.signOut` posted `/oauth/signout` (the user SESSION) and deleted the local store, never reading relay.env and never touching the enrollment
- [x] Signing out revokes this machine's device enrollment, so it disappears from the machine list on every other client — `POST /v1/agent/deenroll` with the machine's own credential
- [x] Any session already bridged from this machine stops carrying bytes on sign-out — de-enroll deletes the device row, which severs control and in-flight bridged data
- [x] The device credential no longer authenticates against the relay after sign-out — `enroll.deEnroll` clears relay.env once the relay confirms
- [x] A revocation that was REQUIRED and did not happen aborts the sign-out and leaves the account signed in, rather than reporting success
- [x] A machine enrolled to another account is left completely alone
- [x] An unparseable `RELAY_BASE` falls back to the account's relay instead of destroying the credential (`f3b1e5fb5`)
- [x] A `ws://` / `wss://` `RELAY_BASE` is normalized rather than failing every request forever (`f3b1e5fb5`)
- [x] "Sign Out Anyway" does not re-run the whole revocation and make the user wait out the same timeouts twice (`f3b1e5fb5`)
- [x] The recovery half is filed rather than half-built — T1424, T1425, T1426

## Summary

Main intake, 2026-09-07. `origin/main` commits `0c3a19764` ("signing out of the
account revokes this machine") and `f3b1e5fb5` ("close the edge cases in
sign-out machine revocation") fixed this on the Mac seat. The relay's semantics
are deliberate - an account may own headless hosts with no app signed in - so
the revocation belongs in the client, and the Windows client is a separate
client.

Mac's fix touched `macos/Sources/Features/Remote/{RelayAccount,MachineEnrollment,RelayDirectoryClient}.swift`
and `MachineChooserView.swift`, with `relay/signout_revoke_test.go` as the
relay-side reproduction. The Windows counterparts live under `src/remote/`
(the relay account / enrollment store) and the win32 machine chooser.

Start by reproducing: enroll this box, bridge a session to a second client,
sign out here, and check whether the machine is still listed and still
streaming. `relay/signout_revoke_test.go` (`TestSignoutAloneLeavesMachineReachable`)
is the shape of the assertion.

## Validation criteria

- [ ] A reproduction that shows the pre-fix behavior (machine still listed / bridge still carrying bytes after sign-out) and goes red on the fix
- [ ] After sign-out, the machine is absent from `+sessions` / the machine chooser on another client on the same account
- [ ] After sign-out, the stored device credential is gone from the DPAPI store and does not authenticate
- [ ] `scripts\floor-lane.ps1 -Lane all` green; the relay-account acceptance harness re-stamped

## Progress log

- 2026-09-07 05:10: claimed; work starting.
- 2026-09-07 05:11 [session 81003ca2]: Verified the defect on Windows: src/remote/relay_signin.zig signOut() does best-effort POST /oauth/signout (user SESSION only) then deletes the local account store. It never reads relay.env and never touches the device enrollment, so the machine stays listed, online and bridgeable - exactly the pre-fix Mac behavior. The primitives already exist here: enroll.whoami() and enroll.deEnroll() (src/remote/agent/enroll.zig:409,451), relay.env load/parse (loadRelayEnv/relayEnvPath), and relay_creds.baseMatches for the scheme-tolerant base compare. Plan: a pure decision module (has-credential x whoami-answer x signing-out email -> none/revoke/foreign/unknown), signOut returning an outcome instead of a bool, no local store delete when a required revocation did not happen (staying signed in is the point - reporting signed out while the machine is reachable IS the bug), and a chooser confirm offering Sign Out Anyway. Splitting the Mac change's recovery half into follow-ups: pending-revocation retry, suspend-and-restore on signing back in, and the chooser's still-connected warning.
- 2026-09-07 05:38 [session 81003ca2]: Landed: new pure module src/remote/relay_revoke.zig (decide/revocationBase/normalizeBase/isUsableBase/sameRelay, 14 unit tests, registered in main_ghostty.zig so it runs in both the none and win32 lanes); relay_signin.signOut now takes a SignOutMode and returns a SignOutResult carrying a MachineOutcome, reads relay.env, asks /v1/agent/whoami whose the credential is, de-enrolls on a match, and REFUSES to delete the local account store when a required revocation did not happen; RelayAccountRow carries the outcome to the GUI with a pure signOutMessage() per outcome; MachineChooser offers a Sign Out Anyway confirmation on .not_revoked. Split the recovery half of main's change into T1424 (pending revocation retried at launch / network-back), T1425 (suspend and restore the enrollment on signing back in) and T1426 (the chooser's still-connected mark). Floor: lib PASS, none PASS, win32 PASS; agent lane running. Guards made due by these edits: startup-failure, printclient-audit, chooser-controls, relay-account, chooser-selection, test-reach.
- 2026-09-07 06:13 [session 81003ca2]: Validated on the box. floor-lane.ps1 -Lane all: ALL LANES PASS (lib, none, win32 224s, agent 395s). A too-long comptime utf8ToUtf16LeStringLiteral in the new confirm dialog blew the eval branch quota in the APP build while the test lane compiled fine - caught by relay-account.ps1 refusing a stale exe, fixed with a @setEvalBranchQuota. Acceptance re-run and re-stamped for these edits: relay-account ALL PASS, chooser-controls ALL PASS (54), chooser-selection ALL PASS (35), startup-failure ALL PASS (42), printclient-audit (8), test-reach-audit (14), docs-routing (20). Standing floor: ipc-p1 25, ipc-p2 20, ipc-p3 16. guard-due is clear apart from the three pre-existing advisories. Not validated here: the live end-to-end against a real relay and a second enrolled client - this box has one machine, so that half stays unticked and is named in the task.
