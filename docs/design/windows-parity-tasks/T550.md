---
id: T550
title: "Retire the standalone agent MSI publishing pipeline; keep /dl/install.ps1 answering"
order: null
deps: ["T547", "T549"]
status: "todo"
commits: []
seat: "win"
priority: "P2"
tags: ["infra", "docs"]
---

# T550 - Retire the standalone agent MSI publishing pipeline; keep /dl/install.ps1 answering

## Summary

Cleanup half of T417: delete relay/deploy/msi/ (ghoztty-agent.wxs + its build-msi.sh), relay/deploy/install.ps1, relay/deploy/publish-agent.sh, scripts/deploy-windows-agent.sh, and the agent self-update + tray-account code paths that only exist for the standalone install; update the docs that point at them (relay/README.md, remote-relay-roadmap.md, remote-machines-CONTINUATION-3.md, .claude/commands/release.md). The hosted https://<relay>/dl/install.ps1 URL must keep working: replace the hosted copy with a stub that prints where to get Ghoztty (the app install now carries the agent). See docs/design/one-installer-agent-consolidation.md.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
