---
id: T228
title: "Two split-divider oracles cannot be expressed off the input desktop (cursor shape, cross-pane stale-line scan)"
order: 323
deps: ["T214"]
status: "done"
priority: "P2"
tags: ["test"]
triage-reason: "records two test oracles a background desktop cannot express; the file states neither is a product defect"
commits: ["70786906e"]
---

# T228 - Two split-divider oracles cannot be expressed off the input desktop (cursor shape, cross-pane stale-line scan)

## In plain terms

When you point at the line between two split panes, the pointer turns into a
double arrow that tells you the line can be dragged. Nothing checked that the
arrow points the right way — the automated tests run on an invisible desktop
where there is no pointer to look at, so a build that showed a left-right arrow
on a line you drag up and down would have shipped with every test green. That
rule is now written down in one place and checked every build, and the tests
that watch the divider itself also look at more of it than they used to.

## Goals

- [x] The pointer-shape rule is checked by something, rather than by nobody.
- [x] The divider band is checked along its whole length, not at one point.
- [x] Say plainly which part of the old check cannot come back, and what it is
      actually waiting on.

## Summary

Migrating `split-divider.ps1` onto the test desktop (T218 batch 3) cost two
assertions that no rewrite on a background desktop can recover. Both were
replaced with narrower probes that pass today; this task exists so the
narrowing is visible rather than quietly absorbed, and so it is reconsidered
when T214 lands a real capture/input path.

Neither is a product defect. Both are limits of what a background desktop can
observe, in the same family as T214 (terminal-surface capture) and T224
(overlay z-order).

### 1. The SIZENS cursor SHAPE (T94)

`split-divider` used to assert the system cursor is `IDC_SIZENS` on the divider
line and 4 DIP either side of it, and is NOT at the pane center.

Measured on box 2026-07-31, and it **corrects a claim the harness header used
to make**: `SetCursorPos` returns FALSE on a background desktop (it requires
the calling thread's desktop to be the input desktop) and `GetCursorPos`
returns `-1,-1` there. `Window.zig`'s `WM_SETCURSOR` handler is
coordinate-driven and *must* be - `WM_SETCURSOR` carries no coordinates, only a
hit-test code and the triggering message - so with no readable cursor position
it falls straight through to `DefWindowProcW`. Measured: `SendMessageW(top,
WM_SETCURSOR, ...)` returns 0 at every point on the grab band, including
directly on the line.

Replaced by four `WM_NCHITTEST` probes sent to the PANE at the same four
points: HTTRANSPARENT (-1) across the band, HTCLIENT (1) at the pane center.
That is the fall-through the cursor feedback rides on, read at the source, and
it pins the band's extent more precisely than a glyph does. The band's AXIS
(the vertical/horizontal choice that picks SIZENS over SIZEWE) stays covered by
the drags and by the T155 section running both axes. **The literal glyph is
what is unasserted** - a regression that hit-tests the band correctly but loads
the wrong cursor would not be caught.

Note the same code path is dead in production whenever the input desktop is not
`Default` (locked workstation, UAC/secure desktop, disconnected RDP) - the T216
/ T223 class - but there it is cosmetic-only and nobody is looking at a cursor,
so it is not filed as a bug.

### 2. The cross-pane stale-line scan (T155)

`split-divider` used to scan a full scanline ACROSS both panes and count runs
of divider-colored pixels, on the premise that a stale line left under a pane
has been overpainted by that pane and so cannot be counted. **That premise is a
composited-screen fact, and there is no composite off the input desktop.**

Measured 2026-07-31: a `PrintWindow` capture of the top-level window retains
EVERY intermediate line a drag ever painted - three drags produced 13 runs
against a healthy product - because the GL child does not overpaint the
parent's backing store in that render. Forcing a full-window repaint (a window
move, and separately three resizes) does not clear them. Scored the old way the
test fails for pixels no user can see.

The scan is now confined to the parent-visible strip between the two pane
rects, asserting (a) the gap IS the band (`gap == bandPx`, geometry only - the
pre-fix ~5 DIP gap fails this outright) and (b) every pixel of that gap is
divider-colored in ONE run (a hairline in a wider gap reads as a run narrower
than the gap; a surviving stale line inside the gap reads as a second run).
**What is unasserted is a stale line that lands UNDER a pane** - invisible on
screen, so it was always a proxy, but a proxy with margin that is now gone.

## ANSWER (2026-08-11)

Both halves were re-measured against what T214 actually decided, and the answer
is different for each.

### 1. The cursor glyph — RECOVERED, off the box

The Direction's second option, taken. The layout → cursor decision is now a
named pure function rather than a conditional inside a 7000-line window
procedure:

- `split_geometry.dividerCursor(layout)` → `DividerCursor{ size_we, size_ns }`,
  plus `HERO_DIVIDER_CURSOR` for the carousel divider (always a column
  boundary). `DividerCursor.idc()` carries the `IDC_*` NUMBER, because this
  module takes no OS imports — that is what lets its tests run in every lane,
  the Mac seat's `-Dapp-runtime=none` included.
- `Window.zig`'s `WM_SETCURSOR` calls it for both the split and the hero
  divider, converting the tree's nested `Split.Layout` with an exhaustive
  switch (so a new variant there is a compile error here).
- The two halves are stapled together by a **win32-lane** test in `Window.zig`
  asserting `size_we.idc() == w32.IDC_SIZEWE` and `size_ns.idc() ==
  w32.IDC_SIZENS`. Without it the pure module could drift from the constants
  the handler actually loads and every lane would stay green.

What is still untested is `LoadCursorW` + `SetCursor` themselves — exactly the
residue the Direction predicted, and nothing an acceptance script on this box
could reach anyway.

### 2. The cross-pane stale-line scan — the fold-into-T214 route is CLOSED

T214 finished (2026-08-01) by deciding routes 1–4 and **deliberately not
building route 0** (an app-side GL readback over IPC), which is filed as T275.
So "if T214 lands a capture path that reflects real composition" never happened
and is not going to; the pixel-perfect version of this scan now waits on
**T275**, and this task's dep is re-pointed accordingly.

Re-measuring the margin also narrowed what is worth recovering. The 13 runs the
migration measured are **not a defect the product could fix**: the parent never
erases (`WM_ERASEBKGND` returns 1) and only ever repaints the band region, so a
healthy build genuinely leaves old lines in its backing store under the panes.
Scoring them needs a composite the parent is not part of. What CAN be said off
the input desktop was said, as two new assertions per axis, both of which the
old wide scan never made either:

- **The band is solid at three points along its LENGTH**, not only at its
  midpoint (`Measure-GapSolid`). One `PrintWindow` capture, three parallel
  strips, so this adds no new empty-capture SKIP site. A band painted only
  where the last drag repainted it reads as solid at the middle.
- **The panes tile the split across the other axis** (`Test-PanesTile`), so the
  gap is the ONLY parent-visible strip there is. With `gap == bandPx` that is
  `split_geometry`'s tiling invariant measured on the live layout — the pure
  tests assert `axis()`, this asserts the layout code that uses it, and it is
  the reason an under-pane stale line is unreachable on screen at all.

## Direction (as filed — see ANSWER above for what was done)

Do not "fix" either by weakening the current probes further. Options, in the
order they are worth trying:

- **Fold into T214.** If T214 lands a capture path that reflects real
  composition (child over parent), the cross-pane scan comes back for free -
  restore it from git history and re-measure the 13-run number, which should
  drop to 1.
- **Cursor shape has no test-desktop answer.** The only honest recoveries are
  an interactive-desktop lane (`-Interactive`, run by hand, never scored) or a
  unit test over the decision function rather than the glyph -
  `hitTestDivider`'s returned `layout` is pure logic and could be covered in
  the none-runtime lane, which would leave only `LoadCursorW`+`SetCursor`
  untested.

## Validation

- Whatever replaces either probe must FAIL against a product regressed in the
  way the original caught (a wider gap with a hairline; a wrong cursor id).
- `split-divider.ps1` ALL PASS x3 and its negative control still FAILS.

## Validation criteria

- [x] The cursor mapping is covered by a test that FAILS on a swap — teeth-checked
      by swapping `dividerCursor`'s arms: `zig test src\apprt\win32\split_geometry.zig`
      went `67 passed; 1 failed` naming the test, and back to `All 68 tests passed`
      when restored.
- [x] The pure numbers cannot drift from the OS constants — teeth-checked by
      pointing the `Window.zig` test at the wrong constant: `zig build test
      -Dapp-runtime=win32` went red (`4466/4536 passed, 1 failed`), proving the
      test is compiled into that lane rather than merely written.
- [x] The two new script assertions FAIL against the shape they exist to catch —
      inverted both predicates and re-ran: exactly `6 FAILED / 51 passed`, the six
      being the new ones, nothing else moved.
- [x] `split-divider.ps1` ALL PASS x3 — `ALL PASS (57 assertions)` three times
      (up from 51), and `-NegativeControl` still `1 FAILED / 56 passed`.
- [x] Standing floor green — `scripts\floor-lane.ps1 -Lane all` (none, win32,
      agent) and P1–P3.

## Progress log

- 2026-08-11 01:42: claimed; work starting.
- 2026-08-11 02:17: cursor glyph recovered OFF the box: split_geometry.dividerCursor/DividerCursor (pure, none-lane tests) + a win32-lane check that its IDC numbers equal w32.IDC_SIZEWE/SIZENS; Window.zig WM_SETCURSOR now calls it. Stale-line half: 'fold into T214' is CLOSED (T214 decided against route 0); recovered the expressible margin instead - band solid at 3 points along its LENGTH, and panes tile across the other axis so the gap is the only parent-visible strip. Both teeth-checked (6 FAIL when inverted). split-divider.ps1 ALL PASS (57) x3, negative control still fails.
- 2026-08-11 02:33: status: in-progress -> done [commit 70786906e]
