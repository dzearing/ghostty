---
id: T1109
title: "The '--- session restarted ---' divider never prints on a deferred relaunch"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "User-facing but cosmetic: the restart happens, the user is just not told where it happened."
tags: ["fix"]
---

# T1109 - The '--- session restarted ---' divider never prints on a deferred relaunch

## In plain terms

When a session restarts, a line is printed across the pane so you can see where the old output stopped and the new run began. On one restart path that line never appears, so the new output runs straight on from the old with nothing marking the seam.

## Summary

`test\win32\session-relaunch.ps1` scored `1 FAILURE(S)` in the T1094 sweep, out of a long script:

```
FAIL B7 the deferred relaunch printed the '--- session restarted ---' divider
```

Only the DEFERRED relaunch path is affected - the rest of the script, including the immediate path, passes. The likely shapes are the divider being written before the ConPTY is ready to show it (and so overwritten by the first repaint), or the deferred path simply not calling the same write the immediate path does.

Small, but it is the only thing that tells a user their scrollback has a seam in it.

## Reproduced alone

Re-run on its own with `scripts\suite-run.ps1 confirm` (T1137), against a Debug
`zig-out` at HEAD. Logs: `temp\suite-runs\fwd1\<script>.alone.log`.

- `session-relaunch.ps1` - **reproduced alone: yes** (2026-08-23, `1 FAILURE(S)`). Red on its own too, so the failure is not an artefact of the sweep it was found in.

## Where

- the session relaunch path - deferred branch
- `test/win32/session-relaunch.ps1` section B

## Goals

- [ ] Compare the immediate and deferred relaunch branches and find where the divider write is missing or lost.
- [ ] If it is a timing race against the first repaint, write it after the pane is ready rather than earlier.

## Validation criteria

- [ ] a deferred relaunch shows the divider in the pane, verified by eye
- [ ] `test\win32\session-relaunch.ps1` is ALL PASS

## Progress log

- 2026-08-22: filed by T1094's full acceptance sweep (242 scripts, forward order, Debug build at 9a764deea). Evidence quoted above is from that run's log in `temp\suite-runs\fwd1\`.
- 2026-08-23 12:30: reproduced alone: YES (2026-08-23, T1137's confirm pass against a Debug zig-out at HEAD): session-relaunch.ps1 (1 FAILURE(S)). Red on its own as well as in the sweep, so the failure is not an artefact of what ran before it - the triage this task carries stands. Log: temp\suite-runs\fwd1\<script>.alone.log.
