---
id: T1257
title: "asserted-nothing is red at HEAD: soak.ps1 scores a zero-assertion abort green and the uncounted-final ceiling is 3 over"
order: null
deps: []
status: "done"
commits: ["02a57369f"]
seat: "win"
priority: "P1"
triage-reason: "P1: an audit that is red at HEAD stops being read, and this one is the guard against a green run that proved nothing. Same shape as T962, which fixed the previous instance."
milestone: "M1"
tags: ["test"]
---

# T1257 - asserted-nothing is red at HEAD: soak.ps1 scores a zero-assertion abort green and the uncounted-final ceiling is 3 over

## In plain terms

One of the checks that keeps the test suite honest is itself failing, and has
been for a while. Nothing is broken in the terminal - but a red check is a
check people learn to scroll past, and this particular one exists to catch a
test run that announced success without testing anything.

## Evidence (measured 2026-09-01, at `9aa532901`, before this turn's changes)

```
FAIL C1 no acceptance script can score green having asserted nothing
     soak.ps1:179 early-green - a pass verdict ends the run green before the
     final verdict: 'ALL PASS'
FAIL C3 the uncounted-final count did not grow past 37
     (40 script(s) still print an UNCOUNTED final verdict - T775 converts them)
```

Confirmed pre-existing by stashing this turn's work and re-running against a
clean HEAD: identical verdict, `2 FAILURE(S) (26 assertions passed)`.

## What to do

- `soak.ps1:179` is the `early-green` kind: a branch that prints a pass verdict
  and ends the run before the final verdict. This is the same defect [[T962]]
  fixed for `job-escape-startup`; follow it.
- The uncounted-final ceiling is a ratchet that [[T775]] is meant to walk down,
  and it has walked UP by three instead. Either convert the three scripts that
  grew one, or - if they are legitimate - move the ceiling deliberately, in a
  commit that says which three and why. Do not simply raise the number.

## What was done

- **`soak.ps1` is on the shared scorer.** Both of its verdicts - the
  `-SelfTest` one at the flagged site and the real end-of-run one - now go
  through `Write-TestVerdict` via a small `Write-SoakVerdict` wrapper that also
  tees the verdict line into the run's report file. The selftest branch used to
  print a bare `ALL PASS` and `exit 0` in the middle of the file, which is the
  `early-green` shape; there is no hand-rolled pass verdict left in the script,
  so a run that graded nothing now ends `ASSERTED NOTHING` and exits 2.
- **The ratchet drift was five scripts, not three.** Measured against the sweep
  at `7fc22be42` (where T962 left it at 37): `daily-publish`, `install-launch`,
  `install-ownership`, `one-installer` and `viewer-find` were each filed after
  T962 hand-rolling their own `"ALL PASS"`, and `morning-refresh` was retired -
  41 = 37 + 5 - 1. All five are converted onto `Write-TestVerdict` (each grew a
  `$script:passes` counter, since four of them only ever counted failures), and
  the ceiling comes DOWN to 36 rather than up to 41.
- Each conversion places `Complete-TestBody` before the guard-due stamp (T1039:
  the stamp is a child process that refuses to write while the body reads
  `pending`), which is the ordering `release-parity.ps1` established.

## Validation criteria

- [x] `test\win32\asserted-nothing.ps1` reports `ALL PASS` at HEAD - was
      `2 FAILURE(S) (26 assertions passed)`, now `ALL PASS (28 assertions)`.
- [x] It still has teeth: `-TeethCheck` is `ALL PASS (28 assertions)`, i.e. C1
      goes red on a synthesized violator.
- [x] The hard-kind sweep is EMPTY (`Get-AssertedNothingSweep` over
      `test\win32`: `hard: 0`, was 1 x early-green).
- [x] The new uncounted-final scripts are each named above and each converted;
      the count fell 41 -> 36 and the ceiling with it (37 -> 36).
- [x] `soak.ps1 -SelfTest` is `T53 SOAK SELFTEST: ALL PASS (7 assertions)`,
      exit 0; the zero-assertion arm of the same scorer is section A of
      `asserted-nothing.ps1` (`exits 2, not 0`).
- [x] All five converted harnesses green with their stamps re-written:
      daily-publish ALL PASS (57), install-launch ALL PASS (25),
      install-ownership ALL PASS (42), one-installer ALL PASS (21),
      viewer-find ALL PASS (30) on a real background test desktop.
- [x] Their negative controls still score red / their teeth still catch:
      daily-publish `-NegativeControl` = `57 FAILURE(S)` exit 1;
      install-launch / install-ownership / one-installer `-TeethCheck` all
      ALL PASS (every mutation caught).
- [x] The meta-audits that read the whole suite stay green over the edits:
      body-complete ALL PASS (40), isolation-meta (13), launch-preflight (19),
      verdict-exit, cleanslate (21), stderr-capture (25), desktop-launch (29),
      suite-run (88).
- [x] Standing floor: `floor-lane.ps1 -Lane all`, P1-P3.

## Progress log
- 2026-09-01 23:13: claimed; work starting.
- 2026-09-01 23:45: root cause on both counts was the rule failing on itself: soak.ps1's -SelfTest branch hand-rolled a bare ALL PASS + exit 0 mid-file (early-green), and the T775 ratchet had drifted 37 -> 41 because five harnesses filed since T962 (daily-publish, install-launch, install-ownership, one-installer, viewer-find) each hand-rolled their own verdict while morning-refresh was retired. All six scripts converted onto Write-TestVerdict, Complete-TestBody placed before each guard-due stamp, ceiling lowered to 36. asserted-nothing.ps1 ALL PASS (28) and still red under -TeethCheck.
- 2026-09-01 23:46: status: in-progress -> done [commit 02a57369f]
