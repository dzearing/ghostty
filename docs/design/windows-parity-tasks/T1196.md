---
id: T1196
title: "Mac seat: check the in-app update path still works after T1178\u0027s shared-config change to auto-update"
order: null
deps: []
status: "skipped(no Mac half needed: T1178 changed no shared code - it only READS the existing cross-platform auto-update config from the win32 apprt. The Mac already had in-app update via Sparkle; T1178 closes the gap rather than opening one.)"
commits: []
seat: "win"
priority: "P1"
tags: ["test"]
---

# T1196 - Mac seat: check the in-app update path still works after T1178's shared-config change to auto-update

## Summary

TODO: describe the defect or gap, the evidence, the fix, and the validation.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-08-30 16:36: status: todo -> skipped(no Mac half needed: T1178 changed no shared code - it only READS the existing cross-platform auto-update config from the win32 apprt. The Mac already had in-app update via Sparkle; T1178 closes the gap rather than opening one.) (by filed then re-read: the premise was wrong)
