---
id: T1102
title: "Image chips never appear in the feedback composer, taking four viewer-feedback scripts red"
order: null
deps: []
status: "done"
commits: ["f3f9c3160"]
seat: "win"
priority: "P1"
triage-reason: "User-facing: attaching a screenshot to a feedback report appears to do nothing, and four acceptance scripts agree."
tags: ["fix"]
---

# T1102 - Image chips never appear in the feedback composer, taking four viewer-feedback scripts red

## In plain terms

When you send feedback from a viewer pane you can attach screenshots, and each one should show up in the message box as a little '[Image #1]' tag. Right now no tag ever appears, the pictures do not reach the saved report, and deleting them misbehaves. Four separate automated checks all fail on it, which means attaching a picture to feedback is effectively broken.

## Summary

Four scripts in the T1094 full sweep fail on the same mechanism - the composer's image chip. Filed as one task because the failing assertion is literally the same string in three of them.

`viewer-feedback-capture.ps1` - `6 FAILURE(S) (57 passed)`:

```
FAIL  the composer shows an '[Image #1]' chip (holds '')
FAIL  the folder holds images/image-1.png
FAIL  report.json carries the one captured image (got 0)
FAIL  ...at the size that was dragged (x, want 160x120)
FAIL  the body links the screenshot as a markdown image reference
FAIL  the keyboard capture attaches a chip (composer holds '')
```

`viewer-feedback-images.ps1` - `6 FAILURE(S) (19 passed)`:

```
FAIL  the composer shows an '[Image #1]' chip (holds '')
FAIL  ...and only IT is live -- the deleted one left the report (2)
FAIL  ...and two images are now live (3)
FAIL  the folder holds images/image-2.png
```

`viewer-feedback-carousel.ps1` - `6 FAILURE(S) (32 passed)`:

```
FAIL  the composer holds both chips (holds '')
FAIL  the strip dropped to one tile with the chip (got '2')
FAIL  the survivor is image #2 -- numbers are stable, never renumbered to #1
FAIL  ...and the band is exactly as tall as it was before any picture (175 vs 95)
```

`viewer-feedback-utf16.ps1` - `4 FAILURE(S) (25 passed)`:

```
FAIL  the chip lands EXACTLY at the caret, with no doubled space
      (got 'eee <emoji> YZ', want 'eee [Image #1] <emoji> YZ')
FAIL  one Backspace removes the WHOLE chip after non-ASCII text
```

The composer text is EMPTY where a chip is expected in every case, and the picture never reaches `images/` or `report.json`. That points at insertion, not at rendering or at the UTF-16 caret arithmetic the fourth script is actually about - utf16's caret assertions are almost certainly collateral, and should go green on their own once a chip is inserted at all.

Note the healthy assertion counts (57, 32, 25, 19 passing): the feedback flow as a whole works. It is the image attachment that does not.

## Where

- the viewer feedback composer - chip insertion into the message field
- the capture path that writes `images/image-N.png` and the `report.json` image list
- `test/win32/viewer-feedback-capture.ps1`, `-images.ps1`, `-carousel.ps1`, `-utf16.ps1`

## Goals

- [x] Reproduce by hand first: open a viewer, drag-select a capture, and watch whether a chip appears. This is a user-visible flow and the manual result decides whether it is product or harness. **Reproduced on a fresh Debug build; the answer is HARNESS.**
- [x] Trace chip insertion from the capture completing to the composer text being set. The chip lands: the pane logs `image=#1 bytes=430 live=1`, and `live` is counted from the chips in `pane.feedbackText()`.
- [x] Fix, then re-run all four scripts - expect utf16's caret assertions to come back on their own. They did, with no change to the caret code.
- [x] If any of the four is red for its own separate reason afterwards, split that one out into its own task rather than keeping this one open. None was: all four are ALL PASS.

## Validation criteria

- [x] attaching a capture by drag AND by keyboard both put an `[Image #1]` chip in the composer -- both arms of `viewer-feedback-capture.ps1` (the mouse drag in section B, the keyboard gesture in section H) assert the chip and both pass. On the WEB surface, which is what a user actually gets, the same splice is covered in-process by the `host floor` test in `ViewerPane.zig`: it pastes a real PNG into a real WebView2 and asserts the buffer is exactly the words plus the chip.
- [x] `test\win32\viewer-feedback-capture.ps1` is ALL PASS -- **ALL PASS (64)**, was `6 FAILURE(S) (57 passed)`
- [x] `test\win32\viewer-feedback-images.ps1` is ALL PASS -- **ALL PASS (40)**, was `6 FAILURE(S) (19 passed)`
- [x] `test\win32\viewer-feedback-carousel.ps1` is ALL PASS -- **ALL PASS (39)**, was `6 FAILURE(S) (32 passed)`
- [x] `test\win32\viewer-feedback-utf16.ps1` is ALL PASS -- **ALL PASS (30)**, was `4 FAILURE(S) (25 passed)`
- [x] each of the four PROVES the surface it got (`richedit(forced)`), so the next time the default moves the failure is one readable line rather than six misleading ones

## Progress log

- 2026-08-22: filed by T1094's full acceptance sweep (242 scripts, forward order, Debug build at 9a764deea). Evidence quoted above is from that run's log in `temp\suite-runs\fwd1\`.
- 2026-08-22 09:44: claimed; work starting.
- 2026-08-22 09:50 [session ba55183b]: Root cause found and it is the HARNESS, not the product. The four scripts drive the composer's RichEdit through window messages, but T934/T936 made the WebView2 page the default surface: the pane stderr from a fresh Debug run says 'composer surface=web', and the same log says 'image=#1 bytes=430 live=1' - so the chip DOES land in the pane buffer. The scripts then read Get-TestControlText on a hidden, empty RichEdit (hence 'holds ''''), and Send-TestControlText into that same dead control fires EN_CHANGE -> readBack, which replaces the pane buffer with just the typed line and takes the picture out of the report (hence 0 images in report.json). Fix: pin these four to GHOZTTY_COMPOSER_SURFACE=richedit the way test\win32\viewer-feedback.ps1 already does, and add a surface assertion to each so the next divergence fails in one readable line instead of six.
- 2026-08-22 10:16 [session ba55183b]: Filed T1136 on the way past: pinning these four to the fallback means the whole screenshot story is now proven on a surface no user gets. viewer-composer.ps1 has no image assertion at all, and ViewerPane.zig's host floor covers a chip via PASTE, not via the region selector. Also swept for the same class of harness drift: every script that touches GhozttyViewerFeedback now pins a surface (grep Set-ComposerSurface / GHOZTTY_COMPOSER_SURFACE), so there is no second script sitting in the position these four were in.
- 2026-08-22 10:16: status: in-progress -> done
