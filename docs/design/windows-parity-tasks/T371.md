---
id: T371
title: "PaneView ref/unref ownership has no test: the terminal arm needs a live Surface, so a leak or double-free only shows up as a crash"
order: 376
deps: ["T90c"]
status: "in-progress"
priority: "P2"
triage-reason: "adds unit coverage for PaneView refcounting; no reported defect, purely test debt"
commits: []
seat: "win"
---

# T371 - PaneView ref/unref ownership has no test: the terminal arm needs a live Surface, so a leak or double-free only shows up as a crash

## Summary

`PaneView` (T90c) is now the split tree's view type, so ITS reference count is
what keeps every pane alive. Nothing tests that counting. The `.terminal` arm
needs a real `*Surface` (which needs an App, a Window and a child HWND), so a
unit test cannot construct one, and the only signal for a leak or a
double-free is a crash or a slow bleed under a long session - exactly the
class of defect the T53 bar exists to keep out.

## Details

What is untested today:

- `createTerminal` takes exactly ONE surface reference, and `unref` at zero
  releases exactly that one.
- `destroyUnowned` (the "never made it into a tree" path taken by `addTab`,
  `replaceTabRootSurface` and `newSplitAt` when `SplitTree.init` fails) frees
  the pane AND the leaf under it, and cannot underflow the count.
- The viewer arm frees its `ViewerPane` outright.
- The back-pointer contract: `Surface.pane_view` stays valid across
  `Surface.deinit` (the IPC-registry forget reads it) and dangles only on
  memory that is being destroyed anyway.

Two approaches, either acceptable:

1. **Make the counting generic.** Move ref/unref/eql onto a tiny
   `RefCounted(V)` mixin that `PaneView` composes, and test the mixin in the
   none-runtime lane with a fake leaf that records ref/unref calls. Cheapest,
   and it tests the arithmetic - which is where the underflow lives.
2. **A test-only constructor.** A `PaneView.createForTest` that wraps a stub
   leaf, exercised in the win32 lane. Tests more of the real type but needs a
   third union arm or a comptime seam, which the design deliberately does not
   have.

Prefer (1) unless it distorts `PaneView`.

Related: the viewer branches T90c added to `IpcHandlers.buildNode`
(`"type": "viewer"` + `url`) and to `App.captureLayout` (`kind` /
`viewer_location`) are likewise unreachable until T90d constructs a viewer -
their coverage belongs to T90d/T90h, not here.

*Validation:* new unit test(s) green in the named lane; `zig build test
-Dapp-runtime=none` and `-Dapp-runtime=win32` both green.

## In plain terms

Every pane in a window - a terminal or a viewer - is kept alive by a small
counter. When the last thing holding a pane lets go, the counter hits zero and
the pane's contents are torn down. Nothing tested that counter, and the way it
goes wrong is invisible: a pane that is let go one time too many wraps its
counter around to a huge number instead of reaching zero, so the teardown never
runs and the memory behind it is simply never given back. A user would not see
an error, only a terminal that grows heavier the longer it is open.

The counting has been pulled out into a small piece of code with no Windows
dependencies, so it can be tested directly, and the too-many-releases case is
now caught at the moment it happens instead of silently wrapping.

## Goals

- [x] The ref/unref arithmetic lives in one tested place rather than being
      open-coded in two.
- [x] Releasing a pane nobody holds is an assertion, not a wrap to
      `maxInt(u32)`.
- [x] The "never made it into a tree" teardown path frees exactly once and
      cannot underflow.
- [x] `Surface` uses the same counter, so the two leaves cannot drift.
- [x] The tests run in EVERY app-runtime lane, not just win32.

## Validation criteria

- [x] New unit tests green - 6/6 in `src/apprt/win32/pane_refcount.zig`
      (`zig test src/apprt/win32/pane_refcount.zig`).
- [x] They actually run in the lanes, proven by negative control: an inverted
      assertion turned `zig build test -Dapp-runtime=none
      -Dtest-filter=pane_refcount` red (1 failed), and reverting it turned it
      green again. Same filter green on `-Dapp-runtime=win32`.
- [x] Standing floor green: `scripts/floor-lane.ps1 -Lane all` -> lib PASS,
      none PASS, win32 PASS, agent PASS.
- [x] Acceptance floor green on the Debug build: `ipc-p1.ps1` (25),
      `ipc-p2.ps1` (20), `ipc-p3.ps1` (16), all ALL PASS.
- [x] Guards re-stamped for the touched `src/` files: `test-reach-audit.ps1`
      ALL PASS (14), `printclient-audit.ps1` ALL PASS (8).

## Progress log

- 2026-09-02 14:29: claimed; work starting.
- 2026-09-03 06:09: extracted the split-tree leaf refcount arithmetic into src/apprt/win32/pane_refcount.zig (RefCount: retain/release/isUnowned/adoptUnowned); PaneView and Surface both compose it, so the two counters cannot drift. release() now asserts on an already-zero count instead of wrapping to maxInt(u32), and destroyUnowned goes through adoptUnowned. 6 unit tests, registered in apprt.zig so they run in EVERY lane; proven in-lane by negative control. Filed T1294 for the remaining slice (the Surface.deinit / pane_view back-pointer ordering contract, which still has no coverage).
