---
id: T220
title: "Session-layout geometry can override a NEWER window-placement memory"
order: 131
deps: []
status: "in-progress"
priority: "P1"
milestone: "M1"
tags: [fix]
triage-reason: "a restored window forgets the user's chosen size after a crash; two stores both write geometry and race"
commits: []
---

# T220 - Session-layout geometry can override a NEWER window-placement memory

## In plain terms

If Ghoztty crashed (or was force-killed) shortly after you resized a window,
the window sometimes came back at its OLD size on the next launch - the app
had already remembered your new size in one place, but the restore feature
read a second, slightly stale record instead. Now both records are saved at
the same instant you finish resizing, so a crash can no longer catch them
disagreeing: a restored window always comes back the size you left it.

## Goals

- [x] State once which store wins: the manifest is authoritative for a
      RESTORED window's frame, the placement memory for a NEW window's size.
- [x] Close the race so the manifest can never be staler than the placement
      memory (both written at the same user gesture, manifest first).
- [x] Turn the intermittent repro into a deterministic acceptance test that
      runs with session persistence ON.

## Validation criteria

- [x] `test/win32/session-layout-geometry-race.ps1` (new): resize, kill with
      no settle delay, assert the on-disk manifest already has the new frame,
      relaunch and assert the restored window size - x3 iterations. ALL PASS
      (23 assertions), 2026-08-07.
- [x] Negative control: with the fix reverted to the debounced write, the
      same script fails 6/6 on exactly the two race assertions (manifest
      stale, restore at 800x600) - the test discriminates. 2026-08-07.
- [x] `window-size-memory.ps1` still ALL PASS (30 assertions, 2026-08-07;
      keeps its `--session-persistence=false` scope - placement memory in
      isolation).
- [x] Standing floor 2026-08-07: `floor-lane.ps1 -Lane all` =
      none PASS / win32 PASS (331s) / agent PASS (312s); P1, P2, P3 all
      `ALL PASS`, run sequentially after the lanes (T401).
- [x] Precedence rule documented in `docs/design/session-persistence.md`
      (5.3) and `src/apprt/win32/window_memory.zig`.
- Note: the originally-suggested "unit test in the none lane over the
  precedence rule" was reassessed - the fix is write ORDERING at a win32
  call site (`Window.savePlacement`), with no new pure logic to unit-test;
  the deterministic acceptance script + negative control is the coverage.

## Summary

Two independent stores decide how big a restored window is, and it is not
clear which one is supposed to win:

- `%LOCALAPPDATA%\ghoztty\window_placement[-debug]` - the T85 placement
  memory: last USER-chosen size + maximized flag, written on WM_EXITSIZEMOVE
  and on maximize/restore transitions.
- `%LOCALAPPDATA%\ghoztty\session-layout[-debug].json` - the session-
  persistence manifest, which also carries per-window geometry and is
  replayed on re-attach.

Found while migrating `window-size-memory` for T217 (2026-07-31). With
session persistence ON, a relaunch after the app was killed opened at
`800,600` even though the placement memory said `950 700 0` - and did so
intermittently: 6 assertions failed on one run and the same script passed the
next three unchanged. `--session-persistence=false` makes it deterministic,
which is what the migrated script now does, so **the acceptance script is not
blocked on this**; the product question is.

## Details

The reproduced sequence, all on a throwaway `LOCALAPPDATA`:

1. Launch, drag-resize 800x600 -> 950x700. Placement memory becomes
   `950 700 0`; the layout manifest is written at some point too.
2. Force-kill the app (`Stop-Process -Force`).
3. Relaunch. Expected 950x700 (the placement memory). Sometimes 800x600.

The suspicion is that the manifest write races the kill, so step 3 replays a
manifest snapshotted BEFORE the resize, and the replayed geometry beats the
placement memory that was already updated. A force-kill is the test's doing,
but a crash is the same shape, and a crash is exactly when a user is most
likely to notice their window forgot its size.

Note `Kill-RepoInstances`-style cleanup does not remove the variable: it kills
`ghoztty.exe`, not `ghoztty-agent.exe`, so the agent (and its sessions)
survive into the next launch.

## What to decide, then enforce

1. **Which store wins**, stated once in the code and in
   `docs/design/session-persistence.md`. The likely answer is that the
   placement memory is authoritative for window SIZE and the manifest is
   authoritative for LAYOUT (splits, ratios, pane contents) - they are
   answering different questions and should not both write geometry.
2. Whether the manifest should carry geometry at all, or read it back from
   the placement memory on restore.
3. Whether the manifest write needs to be ordered against the placement write
   so a crash cannot leave the pair inconsistent.

## Validation

- A unit test in the none lane over the precedence rule.
- An on-box script: resize, force-kill, relaunch WITH session persistence on,
  and assert the remembered size - the case `window-size-memory` currently
  has to disable. Run it x3; the bug is intermittent, so a single green run
  is not evidence.
- `window-size-memory.ps1` keeps its `--session-persistence=false` (it is
  testing the placement memory in isolation, which is still the right scope);
  the new coverage is additive.

## Progress log

- 2026-08-07 01:29: claimed; work starting.
- 2026-08-07 01:32 [session 7b0fba2a-85c9-4056-962e-670f9711c3f3]: Root cause confirmed: savePlacement (WM_EXITSIZEMOVE + maximize/restore transitions) writes window_placement synchronously but only arms the 250ms debounced manifest write (markLayoutDirty). A kill/crash inside the debounce leaves session-layout.json + agent blobs with the pre-resize frame; restore replays it over the newer placement memory. Fix: savePlacement calls syncSessionLayout() directly (manifest first, placement second) so the two stores cannot diverge by the debounce.
- 2026-08-07 01:40 [session 7b0fba2a-85c9-4056-962e-670f9711c3f3]: Fix in: savePlacement now syncs the manifest FIRST (immediate syncSessionLayout, not the 250ms markLayoutDirty debounce), then writes the placement file. Precedence documented in session-persistence.md 5.3 + window_memory.zig header. New acceptance test/win32/session-layout-geometry-race.ps1: ALL PASS x3 iterations (23 asserts); negative control (fix reverted) fails deterministically 6/6 on the two race assertions - the intermittent bug is now a deterministic repro. Floor lanes next.
- 2026-08-07 01:58 [session 7b0fba2a-85c9-4056-962e-670f9711c3f3]: Validation complete: floor lanes all PASS (none/win32 331s/agent 312s), P1-P3 ALL PASS sequential, window-size-memory.ps1 ALL PASS (30). Filed T554 (Mac half, seat: mac) and decision D23 (manifest keeps per-window geometry). Closing out.
