---
id: T184
title: "banner text lines and list items clip instead of wrapping to the pane width"
order: 101
deps: ["T123"]
status: "skipped(duplicate -\u003e T377)"
priority: "P1"
triage-reason: "banner text, headings and list items clip at the card edge instead of wrapping; Mac wraps all three"
commits: ["f4dccc957"]
tags: ["fix"]
---

# T184 - banner text lines and list items clip instead of wrapping to the pane width

## In plain terms

Long text in the little status card above a pane used to get chopped off at
the card's right edge instead of flowing onto a second line — plain sentences,
headings and bullet points alike; only tables knew how to wrap. That was fixed
under a different task number (T377, landed 2026-08-04): everything in the
banner now wraps by the same rule tables use. This task was filed before that
fix landed and turned out to be an earlier report of the same defect (as was
T418, closed the same way), so there was nothing left to build.

## Goals

- [x] Paragraphs and headings wrap to the pane width — landed via T377
      (commit cd509de1b, 2026-08-04)
- [x] List items wrap with continuation lines aligned to the shared marker
      gutter, not back under the marker — landed via T377
- [x] A long unbroken token breaks mid-string; a capped run tail-truncates
      with an ellipsis — landed via T377 (3-line cap per block)
- [x] The banner's 10-line display budget still holds — the cap is applied at
      parse time (`banner_markdown.max_lines = 10`), and per the Mac rule a
      wrapped block counts ONCE toward it (each block wraps to at most 3
      display lines)

## Validation criteria

- [x] Wrap unit tests pass at HEAD — `zig build test -Dapp-runtime=win32
      -Dtest-filter=wrap` exit 0 (2026-08-06)
- [x] Chevron-reservation geometry tests pass at HEAD —
      `-Dtest-filter=contentWidth` exit 0 (2026-08-06)
- [x] 10-line display-cap unit tests pass at HEAD — `zig build test
      -Dapp-runtime=none -Dtest-filter=max_lines` exit 0 (2026-08-06); tests
      in `banner_markdown.zig` assert the cap and that displayLines == 10
- [x] Acceptance covered by `test\win32\pane-banner.ps1` section 6h
      (paragraph +25px, 3-line cap, heading +70px, bullet row +25px, checkbox
      row +25px, chevron ink probe), recorded ALL PASS (77) in T377's
      close-out with a `T377_NEUTERED` negative control (exactly 5 failures)
- [x] P1–P3 recorded ALL PASS in T377's close-out (no code change made here)

## Summary

T123 taught the banner's TABLES to size themselves to the pane. Everything else
in the banner still renders as one unwrappable display line: a `.text` block, a
heading, and a list item each lay out with `drawInlineLine`, which walks the
segments left to right at a single `y` and returns a width nobody bounds. Too
long for the pane and it simply runs off the card and gets cut by the clip
region. Mac wraps all three (`Text(...).lineLimit(n).truncationMode(.tail)` in a
width-constrained VStack), so the same banner reads differently on the two
platforms - and it is the same user complaint T123 came from ("the text ...
doesn't use available space"), one block type over.

## Details

Where it lives, all in `src/apprt/win32/BannerOverlay.zig`:

- `renderContent` gives each `.text` / `.heading` block a height of exactly one
  line (`line_h`, or the heading's `hl`) with no reference to `content_w`.
- `renderList` does the same per item (`y += line_h`).
- `drawInlineLine` has no width parameter at all - it is the single-line
  primitive. The clip region set in `paint` is the only thing stopping the
  overflow from painting past the card, which is why this reads as "the text is
  cut off" rather than "the banner is broken".

The machinery to fix it already exists and is already proven by T123: tokenize
the run (`tokenizeCell`), hard-break over-long tokens (`breakWideTokens`), wrap
(`markdown.wrapTokens`), and cap with a tail ellipsis
(`banner_layout.fitWithEllipsis`). The work is lifting that path out of
`renderTable` into a shared "lay out an inline run in W pixels" helper and
calling it from the text, heading, and list arms - measure and paint must go
through the SAME helper or the reserved band and the paint will disagree (the
T101 lesson).

Two decisions to make from the Mac source rather than by guess: the display-line
budget per block type (Mac's `parseBlocks(maxLines:)` hands each `.text` block a
`lineLimit` out of the banner's 10-line budget, so a wrapped line must consume
from that budget - a wrapping banner must NOT be able to exceed 10 display
lines), and whether a list item's continuation lines indent to the marker gutter
(they should - the gutter exists to make content left-align).

**Validation:** extend `test/win32/pane-banner.ps1` in the shape section 6g
already uses - band height as the oracle, self-relative, no pixel constants. A
long single-line banner must grow the band when the pane narrows and shrink it
back when it widens; a long unbroken token must break mid-string; a list item
must wrap with its continuation aligned to the item content, not the gutter; and
a banner of many long lines must still stop at 10 display lines. Prove each with
a negative control the way `T123_NEUTERED` does.

## Progress log

- 2026-08-06 03:17: claimed; work starting.
- 2026-08-06 03:21 [session 23c7105c-9e38-4adb-b0b6-c89be84b5245]: Verified duplicate of T377 (done 2026-08-04, commit cd509de1b), same as T418: .text/.heading/list rows all render via layoutInline+drawWrapped (BannerOverlay.zig renderContent/renderList), list continuation lines start at content_x right of the gutter, breakWideTokens splits unbroken tokens, 3-line cap + ellipsis via MAX_CELL_LINES/fitWithEllipsis. One nuance vs this task's original wording: the 10-line budget counts a WRAPPED BLOCK ONCE (the Mac T377 rule, documented in CLAUDE.md), not every wrapped display line - so the cap is parse-time max_lines=10 with each block bounded at 3 wrapped lines. Re-ran filtered tests at HEAD: win32 'wrap', win32 'contentWidth', none 'max_lines' - all exit 0. Closing skipped(duplicate -> T377); no code change.
