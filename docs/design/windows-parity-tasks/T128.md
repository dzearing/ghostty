---
id: T128
title: "INVESTIGATE: does `+rearrange` leak the dropped pane's session?"
order: 304
deps: ["T117"]
status: "done"
priority: "P2"
triage-reason: "Explicitly an unverified investigation into whether +rearrange leaks a dropped pane's session. Measure before ranking higher."
commits: ["ba736dd36"]
tags: ["fix", "test"]
---

# T128 — INVESTIGATE: does `+rearrange` leak the dropped pane's session?

## Summary

INVESTIGATE: does `+rearrange` dropping a pane LEAK its agent session? — main's `e65cfa4d5` fixed the mirror-image bug on Mac (a tree SWAP marked every departed leaf CLOSE-on-free, so in-place recovery killed the very sessions it recovered) and hardened it into an invariant: a session still referenced by the new tree is never marked CLOSE-on-free. win32 does NOT infer intent from tree departure — `setSessionCloseIntent(true)` is called only at explicit user-close sites (`Window.zig:979/1083/3515`) — so win32 is structurally immune to main's bug. But the `+rearrange` handler swaps trees and destroys panes absent from the new layout via refcount, with no close-intent set, which suggests the OPPOSITE defect: a dropped pane DETACHes and its agent session lingers forever instead of ending. Unverified — confirm with `+sessions` before/after a `+rearrange` that drops a pane; if it leaks, set the intent on exactly the dropped leaves (and only those, so recovery-style swaps stay safe)

## In plain terms

Rearranging a window's panes from the command line can drop a pane out of the
new layout — the pane disappears, exactly as if you had closed it. Until now
the program running inside it did NOT go away: it kept running invisibly in the
background forever, holding memory and CPU, with no window left anywhere that
could reach it. Now a pane the new layout leaves out is shut down properly, the
same as closing it — while a pane that is only moved around keeps running
untouched.

## Goals

- [x] Measure whether the leak is real (it is: both sessions stayed alive)
- [x] A pane dropped by `+rearrange` ends its session, like `+close` does
- [x] A pane the new layout keeps is never ended by the rearrange
- [x] An acceptance script covers both directions, with a liveness arm

## Details

Main's `e65cfa4d5` fixed a serious Mac bug and hardened it into an invariant
worth restating here: *a session still referenced by the new tree is never
marked CLOSE-on-free.* The Mac inferred "user closed this pane" from "this leaf
left the tree", which is wrong for a tree SWAP — in-place session recovery
replaces a controller's whole tree with fresh surfaces re-attaching the SAME
sessions, so every old leaf "left", and marking them CLOSE-on-free meant
recovery terminated the very sessions it had just recovered.

**win32 is structurally immune to that particular bug**, because it does not
infer intent from tree departure at all: `setSessionCloseIntent(true)` is called
only at explicit user-close sites (`Window.zig:979`, `:1083`, `:3515`).

But that asymmetry suggests the **opposite** defect. The `+rearrange` handler
swaps trees and destroys panes absent from the new layout by refcount, with no
close intent set anywhere — which would make a dropped pane DETACH, leaving its
agent session alive and unreferenced forever rather than ending.

**Unverified — this is an investigation, not a filed bug.** Confirm with
`+sessions` before and after a `+rearrange` that drops a pane. If it does leak,
set the intent on exactly the dropped leaves and only those, so that any
future tree-replacing path (recovery-style swaps) stays safe — i.e. adopt main's
invariant in the win32 shape rather than copying its mechanism.

## Findings (2026-08-09)

**The leak is real, and measured.** `test\win32\rearrange-session-drop.ps1`
section A stands up two agent-backed panes, `+rearrange`s to a layout naming
only one, and reads `+sessions --json` back: pre-fix, BOTH sessions were still
alive, the dropped one now referenced by nothing. Its child kept running,
pinned against the agent's idle reaper (local panes are pinned so they survive
the viewer quitting), with no pane anywhere that could reach it — a permanent
leak of a process per dropped pane, not a delayed cleanup.

Section B measured the other direction pre-fix too: a rearrange that KEEPS both
panes ended nothing, which is what makes it safe to mark only the departing
leaves.

**The fix** is `agent_recovery.closesDepartingLeaf(in_new_tree, session_id,
surviving_ids)` — pure, unit-tested — plus one loop in `handleRearrange` that
marks close intent on exactly the old-tree leaves the new layout omits. It runs
through `sessionSpared`, so main's `e65cfa4d5` invariant holds by construction:
a session the new tree still references is never ended, which keeps every
future tree-replacing caller (recovery-style swaps) safe. The loop is
infallible and runs after the response allocation, so no error path can leave a
still-in-tree pane wearing a close intent it never earned.

## Validation criteria

- [x] `test\win32\rearrange-session-drop.ps1` ALL PASS (A: dropped pane's
      session ends and the kept one survives; B: a keep-everything reshuffle
      ends nothing)
- [x] Its liveness arms bite: re-run under `GHOZTTY_TEST_LIVENESS_BREAK=1`
      turns A7/B6 red and nothing else
- [x] The pre-fix run of the same script is red on A4/A5/A6 — the measurement
      the task asked for
- [x] `closesDepartingLeaf` unit test green in the win32 lane
- [x] Floor: `scripts\floor-lane.ps1 -Lane all` PASS, P1-P3 ALL PASS

## Progress log

- 2026-08-09 22:52: claimed; work starting.
- 2026-08-09 22:57: CONFIRMED the leak: new acceptance script test\win32\rearrange-session-drop.ps1 shows a +rearrange that drops a pane leaves BOTH agent sessions alive (A4/A5/A6 red pre-fix); the dropped pane DETACHes because handleRearrange never sets close intent. Section B (a reshuffle keeping both panes) is green pre-fix, so the fix must mark only the dropped leaves.
- 2026-08-09 23:07: FIX: agent_recovery.closesDepartingLeaf (pure, unit-tested) + handleRearrange marks close intent on exactly the leaves the new layout omits, gated on the sessionSpared id invariant. Post-fix rearrange-session-drop.ps1 ALL PASS; teeth-checked with GHOZTTY_TEST_LIVENESS_BREAK=1 (only A7/B6 go red).
- 2026-08-09 23:26: status: in-progress -> done [commit ba736dd36]
