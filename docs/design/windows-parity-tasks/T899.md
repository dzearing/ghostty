---
id: T899
title: "chooser-resume.ps1\u0027s fixture can no longer mint orphans: crash recovery re-attaches them on relaunch"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
tags: ["test"]
---

# T899 - chooser-resume.ps1's fixture can no longer mint orphans: crash recovery re-attaches them on relaunch

## Summary

TODO: describe the defect or gap, the evidence, the fix, and the validation.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-08-16 17:07: Found during T602 validation. The fixture kills the app and deletes %LOCALAPPDATA%\ghoztty\session-layout-debug.json, expecting the still-live agent sessions to render as orphans; the relaunched app instead logs 'session-restore: recovered 1 crash-orphaned window(s) from the agent (0 in the local manifest)' and re-attaches both sessions, so 'a live row with no viewer' never exists and the script SETUP FAILs at its first section, before the chooser opens. Pre-existing break (the failing reads are agent-side, upstream of any T602 code): the crash-recovery arm that reads the agent-held layout copy landed after this fixture was written. Fix: make the fixture defeat the agent-carried copy (clear it from the agent state dir alongside Remove-LayoutManifest, or add a test-only launch flag that skips crash recovery), then re-green sections 1-3. Until then T320's keyboard-resume walk is covered by chooser-session-sort.ps1 section 5 (enter+resume over a sorted roster) and the none-lane cursor unit tests.
