---
id: T298
title: "Activity Monitor: per-card live metrics for INACTIVE machines (Mac's MachineMetricsProbe)"
order: 166
deps: ["T296"]
status: "done"
priority: "P1"
milestone: "M1"
triage-reason: "Mac paints live CPU/mem on inactive machine cards; Windows has no probe at all, a real parity gap"
tags: ["feature"]
commits: ["26869cb76"]
---

# T298 - Activity Monitor: per-card live metrics for INACTIVE machines (Mac's MachineMetricsProbe)

## In plain terms

The Activity Monitor has a row of cards along the top, one per machine you can
watch. Only the card you are currently looking at shows anything useful — the
others just say "online" or "offline". On the Mac, every card shows how long
that machine has been up and how busy it is right now, so you can tell at a
glance which box is pinned before you switch to it.

When this lands, every card carries those numbers on Windows too. A machine
that cannot be reached says "unreachable" instead of a stale number, and the
readouts stop entirely while the window is minimised so nothing is paid for a
panel nobody is looking at.

## Goals

- [x] Every inactive machine card shows live uptime and CPU/memory
- [x] The Local card shows the same while another machine is on screen
- [x] A machine that refuses the connection says so, and is retried on a
      widening delay rather than hammered
- [x] A card whose machine goes quiet stops showing its last reading
- [x] Minimising the panel drops the connections; restoring it re-opens them
- [x] Closing the panel gives every connection back, and takes none that
      belonged to a terminal window

## Summary

**The deliberately deferred half of T296**, named here rather than left as a
silent scope cut. T296 shipped the carousel — Local first, every registered
machine after it, one click to switch, arrows that never dial. What it did NOT
ship is what Mac paints on the cards it is not currently showing: a live status
dot, uptime, and a `CPU n% · Mem n%` readout for EVERY registered machine at
once.

Mac gets that from `MachineMetricsProbe`
(`RemoteActivityMonitorView.swift:153-155`, wired at :227-262): it **dials every
registered machine** and subscribes each one's metrics stream for the panel's
whole lifetime. Windows has no equivalent anywhere — verified by reading
`src/apprt/win32/`: no probe exists, and the chooser's rows carry no metrics
either.

## What T296 does instead, and why it is not a lie

An inactive card reports the relay directory's own `online` flag and says
exactly that — `online` / `offline`, with a neutral dot for offline. It does not
print a CPU or memory number it does not have. `activity_cards.State` has a
fourth state, `.idle`, for precisely this: "we are not connected to this
machine". The ACTIVE card is live (it is the panel's own source).

So the gap is missing INFORMATION, not wrong information — which is the only
form of gap worth shipping.

## Why it is its own task

It is a connection-budget design, not a paint pass. N registered machines means
N relay dials (TCP + TLS + WebSocket upgrade + HELLO) plus N metrics
subscriptions, held open for as long as the panel is open, on a **non-modal**
window the user may leave up for hours. Every one of them is a thread, a
teardown path, and a failure mode. Questions this task has to answer, none of
which T296 needed:

- What is the cap, and what happens past it (Mac probes all of them)?
- Do probes stop when the panel is minimized? (T290 asks the same question about
  the process enumerator — the answers should agree.)
- Does a probe dial share anything with the panel's own dial, or is the active
  machine simply excluded from the probe (Mac excludes it by preferring `host`)?
- What does a probe failure do to the card — `unreachable` forever, or a retry
  with backoff?
- Teardown ordering against `close`/`switchTo`, which already has the
  unsubscribe-then-join-then-free rule for ONE connection and would now need it
  for N.

## Details

Land it behind the existing model: `activity_cards.Summary` already carries
`state`/`uptime_s`/`cpu_pct`/`mem_used`/`mem_total`, and
`ActivityMonitor.cardSummary` already picks live-for-active vs directory-flag
for the rest. The probe fills in the non-active branch; nothing in the painter
or the layout has to change.

## Validation criteria

- [x] Both `zig build test` lanes plus `zig build test-agent` green; P1-P3 ALL
      PASS. — `scripts\floor-lane.ps1 -Lane all`: none PASS 293s, win32 PASS
      341s, agent PASS 322s. `ipc-p1/p2/p3.ps1` each ALL PASS.
- [x] `activity-monitor-remote.ps1` H3: an inactive card shows a live
      uptime/CPU readout for a machine the panel is NOT showing — asserted on
      the carousel log line's new `states=` field, which reports each card's own
      summary (the struct the painter formats, not a restatement of the claim).
      Observed `127.0.0.1:47913 → live, up=277701, cpu=6%, mem=22.3/68.4 GB`.
- [x] H1: the Local card carries a live reading too while another machine is the
      source (`local → live, cpu=6%, mem total 68.4 GB`).
- [x] H1b: the ACTIVE machine gets NO probe — exactly one socket to the agent
      while the panel is showing it, which is the window's.
- [x] H6/H7: the probe holds its OWN socket beside the window's (n=2), and
      closing the panel gives it back (n=1) — counted with
      `Get-NetTCPConnection` from outside the app, not from its own log — and
      the remote pane still round-trips afterwards.
- [x] H4: one dial per panel, not one per 1.5 s tick.
- [x] A machine that refuses the dial paints `unreachable` and is not re-dialed
      inside its backoff — **`test/win32/activity-monitor-probe-fail.ps1`**
      (new) B and C: dialed once, still once 20 s later, card `failed`. Its
      `-NegativeControl` run fails as required.
- [x] The probe policy (which machines, the backoff curve, the staleness rule,
      the `host:port` parse) is unit-tested in the `none` lane —
      `activity_probe.zig`, 16 tests, registered in `apprt.zig`.

### Why the refused-dial arm needed its own script

The remote fixture cannot produce an unreachable card. A `127.0.0.1:PORT`
machine has a card only because a WINDOW is connected to it, so every card
there is reachable by construction — and a second loopback agent to be a dead
one cannot even start: `--listen` takes the single-instance guard
(`single_instance.zig`) and exits 183. Tried it, watched
`+new-remote-window` come back `error.ConnectionRefused`, and moved the arm to
a script that fakes the relay DIRECTORY instead, which can list a machine that
was never there.

## Progress log

- 2026-08-08 12:46: claimed; work starting.
- 2026-08-08 12:51: design settled: new pure activity_probe.zig (which machines to probe, backoff, staleness) + probes[] in ActivityMonitor. Each probe OWNS its dial (relay via relay_dial, direct-tcp via tcp_dial) - never shares a window's connection, because Connection has ONE metrics_handler slot and two subscribers would clobber each other. Active source excluded (its own connection feeds that card). Probes stop entirely while the panel is unobservable (T290 gate answer) and re-dial on resume. Dial failure = card 'unreachable' + bounded backoff (30s..5min), never a tight loop. Staleness (no metric for 5 intervals) retires the link and re-enters backoff. Carousel log line gains per-card state so the acceptance script can assert.
- 2026-08-08 13:19: built: activity_probe.zig (pure: want/retryDelayMs/isStale/parseHostPort/dialDue, 16 unit tests, registered in apprt.zig for the none lane) + ActivityMonitor probes[8] with own dialed connections. Fixed two of my own bugs before they shipped: (1) compacting the probe array would have moved a live metrics-callback context - slots are now freed IN PLACE, never moved; (2) signed-out/alloc-failure paths re-read the DPAPI account store every 1.5s tick - they now take the same backoff (deferProbe). Carousel log line gained probes= and states= so acceptance can read a card's readout. Acceptance section H added to test/win32/activity-monitor-remote.ps1 (second agent killed on purpose gives a card whose dial is refused).
- 2026-08-08 14:04: validated: floor all green (none/win32/agent PASS), P1-P3 ALL PASS, activity-monitor.ps1 ALL PASS (114), activity-monitor-remote.ps1 ALL PASS (87, new section H), activity-monitor-probe-fail.ps1 ALL PASS (17) + its -NegativeControl run fails as required. Two test findings on the way: the second-agent design for the refused-dial arm is impossible (--listen is single-instance, exit 183) so that arm moved to a new fake-relay-directory script; and my first no-tight-loop assertion counted dials across the whole run rather than the section, which sections E/F legitimately contribute to.
- 2026-08-08 14:05: status: in-progress -> done [commit 26869cb76]

### Decision D39 - When the Activity Monitor is minimised, should its per-machine probes drop their connections or just pause the readings?

Resolved 2026-08-13T14:41:44Z : Drop the connections while hidden (Recommended)
