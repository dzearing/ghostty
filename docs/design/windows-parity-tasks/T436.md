---
id: T436
title: "An assertion inside a hand-locked store.mutex turns a test flake into a Deadlock panic (11 sites in server.zig)"
order: 389
deps: []
status: "done"
priority: "P2"
triage-reason: "failing agent assertions report as a deadlock panic; misleading, but only inside test output"
commits: ["fdbaa739c"]
seat: "win"
---

# T436 - An assertion inside a hand-locked store.mutex turns a test flake into a Deadlock panic (11 sites in server.zig)

## In plain terms

When one of the agent's automated checks fails, it is supposed to say what it
expected and what it got. Instead, eleven of them will report something else
entirely — a crash blaming a completely different part of the program — because
of how they hold an internal lock while checking. Whoever hits it spends their
time chasing the wrong thing.

## Goals

- [x] A failing assertion in these tests reports the assertion, not a deadlock
- [x] The spin-until-gone loop below reports what it was still waiting for

## Summary

Eleven test sites in `src/remote/agent/server.zig` are shaped like this:

```zig
h.server.store.mutex.lock();
try testing.expect(h.server.store.table.getByChannel(o.channel) == null);
h.server.store.mutex.unlock();   // <- never reached when the expect fails
```

When the assertion fails, `try` returns out of the function **still holding the
store lock**. The test's own `defer h.deinit()` then runs
`Server.shutdown()` → `detachAll()` → `store.mutex.lock()` on that same thread,
and zig's Debug `Mutex` reports:

```
thread 44780 panic: Deadlock detected
  ...\std\Thread\Mutex.zig:34:19  in lock
  src\remote\agent\server.zig:421 in detachAll
  src\remote\agent\server.zig:383 in shutdown
  src\remote\agent\server.zig:2181 in deinit
  src\remote\agent\server.zig:3041 in test."child exit emits EXIT after final
                                        DATA; reattach -> dead+exit_code; CLOSE frees"
```

So a plain "the session was still in the table" reads as a mutex deadlock in
the shutdown path — a part of the code that is working correctly. Measured live
on 2026-08-03 during T430's validation.

## Sites

**Re-surveyed 2026-08-05 after T346's rewrite** (`099bd6b5c` converted three of
the original eleven to scoped `defer` blocks as a side effect of replacing the
spin waits): 8 sites were listed at `src/remote/agent/server.zig` lines 3924,
4076, 4190, 4295, 4418, 4489, 4545, 4609. The original list (3088, 3139, 3226,
3378, 3491, 3600, 3722, 3793, 3849, 3913, 3970) predates that rewrite - line
numbers have shifted; do not chase either list.

**Fixed 2026-09-04: 9 sites**, found by scanning for a `try` sitting between a
hand-written `lock()` and its `unlock()` rather than by line number. The same
scan over all of `src/**.zig` now reports none left, in test code or product
code.

## Fix

Compute under the lock, assert after it — or scope the section and `defer` the
unlock. Do **not** blanket-convert to `defer` at function scope without reading
each site: some of these are followed by code that takes the same lock again,
and holding it to end-of-scope would produce the very deadlock being removed.

## The flake underneath it

The run that exposed this failed because the session was still in the table
after the test's 10,000-spin wait for `CLOSE` to land — twice green before and
after on the same source, so it is a timing flake and not a regression. The
spin loop exits silently on exhaustion and leaves the assertion to report it;
it should say that it timed out, and how long it waited.

Fixed with the rest of this task: `waitUntil` now takes a plain-words label and
prints `waitUntil TIMEOUT after N ms waiting for: <what>` when the deadline
expires, so an exhausted wait names the condition it was watching instead of
handing a bare `false` to the assertion after it.

## Validation

- [x] A/B on `test "P1: session survives connection drop; reattach replays the ring
  gap (catch-up)"`, with the same deliberately-flipped expectation both times:
  the old shape (assert while holding the lock) reported
  `thread 46036 panic: Deadlock detected` with `detachAll` on the stack and exit
  code 3; the fixed shape reported the assertion itself
  (`... P1: session survives connection drop ... failed: ... in expect`).
- [x] The timeout diagnostic prints - `waitUntil TIMEOUT after 8 ms waiting for:
  <what>` - from the new `waitUntil: a predicate that never holds gives up at
  the deadline` test.
- [x] `powershell -NoProfile -File scripts\floor-lane.ps1 -Lane agent -Repeat 5`.

## Progress log

- 2026-09-04 06:59: claimed; work starting.
- 2026-09-04 07:37: 9 assert-under-lock sites (not 8) fixed in server.zig: read fields under the lock, assert after it. A/B proved the defect and the fix on the P1 catch-up test: old shape -> 'thread panic: Deadlock detected' in detachAll; new shape -> the assertion itself (TestUnexpectedResult at the expect). waitUntil now takes a plain-words label and PRINTS 'waitUntil TIMEOUT after N ms waiting for: <what>' on the deadline (new waitUntilFor makes that path testable without spending the 60s bound); all 10 call sites labelled. Found while validating: src/remote/test_util.zig's own tests ran in NO lane - the re-export references its decls, not its container - so it is now referenced from the agent test root's import list.
- 2026-09-04 08:13: status: in-progress -> done
