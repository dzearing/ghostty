---
id: T1431
title: "A full C: drive makes every build fail with a bare \u0027error: Unexpected\u0027"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P1"
tags: ["infra", "fix"]
---

# T1431 - A full C: drive makes every build fail with a bare 'error: Unexpected'

## Summary

Zig's C/C++ compile steps scratch in %TEMP%, which lives on C:. On 2026-09-07 C: had 0.1 GB free and every 'zig build' died with a bare 'error: Unexpected' on glslang, oniguruma, translate-c and helpgen - no mention of disk, no mention of C:, and it reads exactly like broken code. Setting TMP/TEMP to a directory on the repo drive made the same build succeed with no other change. T1054 already solved this shape for the repo drive (scripts\build-cache.ps1 reports free space at claim time and sweeps) but it only ever looks at the CACHE drives, so the drive that %TEMP% is on is unmeasured. Two halves: point the build shells' TMP/TEMP at the repo drive so a full C: cannot stop a build, and make the claim report C: free space so the box state is visible before a turn burns its context on a fake compile error.

## In plain terms

Builds on this box kept dying with a one-line error that named no file and no
reason, so it read as "the code is broken" - and the real cause was that the C:
drive was full. Nothing measured C:, because everything the build watches lives
on D:. After this, a build scratches on D: like everything else, so a full C:
cannot stop it at all; and if C: does get tight the claim at the top of a turn
says so, in a sentence that names the drive rather than the compiler.

## Goals

- A `zig build` never fails because %TEMP%'s drive is out of room.
- When it is out of room anyway, whoever is looking sees "C: has 0.1 GB free",
  not `error: Unexpected`.
- One rule for where a build scratches, in the same file as the rule for where
  its cache lives, so the two cannot drift apart.

## Validation criteria

- [x] Every build path scratches on the repo's drive: the floor lane's build
      shell, its `-Command` shell, the acceptance rebuild (`BuildFresh`), the
      release build, the delivery's staging build, and the databreak runner's
      `.cmd`. Verified by `test\win32\build-cache.ps1` F16-F22b (source) plus
      F23-F25, which drive the REAL wrapper and read the child shell's own
      answer (`TMPIS-D:\zig-build-temp`).
- [x] The rule lives in exactly one place, beside the global-cache rule it
      mirrors, and honours an explicit path and `GHOZTTY_BUILD_TEMP` - F1-F3.
- [x] Moving TMP/TEMP is scoped: `Push-BuildTempEnv` sets both and creates the
      directory, `Pop-BuildTempEnv` restores exactly what was there and is a
      no-op on `$null` so a `finally` can call it unconditionally - F4-F9.
- [x] The drive %TEMP% is on is measured and named. A low one reads as low and
      the summary names the drive, and the wording distinguishes "this does not
      stop a build" from "a build WILL fail" - F10-F14, wired into the sweeper
      the claim runs (F15).
- [x] `floor-lane.ps1`'s disk pre-flight covers the build scratch directory too,
      so an override onto a third drive is still gated - F18.
- [x] Green: `build-cache.ps1` ALL PASS (76), red under `-NegativeControl`.
      Floor lanes lib/none/win32/agent ALL PASS. ipc-p1/p2/p3 ALL PASS. Every
      guard the edits made due re-run and re-stamped.

## Progress log

- 2026-09-07 11:06: claimed; work starting.
- 2026-09-07 11:08 [session e806d253]: design: two halves as filed. (a) a shared Resolve-BuildTempDir/Set-BuildTempEnv in scripts\lib\BuildCache.ps1 puts TMP/TEMP on the repo drive (<drive>\zig-build-temp, GHOZTTY_BUILD_TEMP overrides) and every build-driving script asks it rather than each inventing a path - same shape as Resolve-ZigGlobalCacheDir; floor-lane sets it on the CHILD cmd only, so lane logs stay where the acceptance harness looks for them. (b) build-cache check/sweep reports the drive %TEMP% actually lives on, so the claim names a full C: before a lane can read as red code, and floor-lane's disk preflight covers the build temp dir too.
- 2026-09-07 11:31 [session e806d253]: built: Resolve-BuildTempDir/Push-BuildTempEnv/Pop-BuildTempEnv + Get-SystemTempState in scripts\lib\BuildCache.ps1; six build paths wired (floor-lane build shell and -Command shell, BuildFresh, publish-windows-release, launch-upgrade staging, crash-databreak .cmd); build-cache check/sweep prints the %TEMP% drive; floor-lane preflight covers it. Acceptance section F (F1-F25) added to test\win32\build-cache.ps1, ALL PASS 76, red under -NegativeControl; guard row build-cache widened 3 -> 9 files. Side fix: body-complete-audit was RED at HEAD because its 'scored by TestScore.ps1' test matched a COMMENT - chooser-restore-all-remote.ps1 was reported twice for a scorer it deliberately does not use; comment lines are now skipped, arms C5b/C5c prove both directions.
- 2026-09-07 11:42 [session e806d253]: validated: build-cache.ps1 ALL PASS (76), red under -NegativeControl; floor-lane -Lane all ALL LANES PASS (lib 5s / none 156s / win32 225s / agent 413s, zero leaked binaries on every lane); ipc-p1/p2/p3 ALL PASS (25/20/16); crash-databreak, upgrade-staleness (153), install-ownership (42), daily-publish (160), release-artifacts (1 Docker skip, pre-existing), build-fresh and the seven meta-audits ALL PASS and re-stamped. guard-due check is clean but for the three advisory rows that were already due at claim time. Withdrew T1434 (skipped(invalid)) - I read the clock as 12:50 when it was 11:30 and filed a watchdog defect over a 4-minute cache-lock wait that was correct behaviour; killed a healthy win32 lane doing it. Lesson recorded in the log: run acceptance scripts and floor lanes serially.
