---
id: T1095
title: "Rip out the Stage 0 upstream-merge plumbing now that D80 is reversed to a permanent hard fork"
order: null
deps: []
status: "skipped(filed on a misreading. D80 corrected 2026-08-22: upstream pulls are still allowed, so Stage 0 plumbing stays as the thing that makes a user-called pull cheap)"
commits: []
seat: "win"
priority: "P1"
tags: ["infra", "docs"]
---

# T1095 - Rip out the Stage 0 upstream-merge plumbing now that D80 is reversed to a permanent hard fork

## Summary

D80 was reversed on 2026-08-22 by explicit user directive: Ghoztty never merges back to Ghostty, and the ghostty-org remote (which had a push URL) has already been removed by hand. Stage 0's remaining scaffolding is now dead weight that a future turn could mistake for live plan. Remove or retarget: scripts/upstream-remote.ps1 and its acceptance test test/win32/upstream-remote.ps1 plus stamp; scripts/fork-identity.ps1 and test/win32/fork-identity.ps1 plus stamp (decide first whether the branding-overlay verifier still earns its keep as a standalone identity check with no upstream tree to overlay); the .gitattributes merge-driver block header, which cites T957 and Stage 0 - KEEP the two union drivers on windows-parity-log.md and windows-parity-feature-ideas.md because both seats append to them on the same branch, and re-justify them on that ground alone; the .gitignore union driver, whose only justification was upstream conflicts; docs/design/windows-parity-merge-back-plan.md and windows-parity-divergence.md, which should be archived with a header saying the plan was cancelled rather than deleted, so the history stays readable. Acceptance: git remote -v shows origin only; no script, test, or doc describes a live upstream merge path; the floor lanes stay green.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-08-22 05:24: status: todo -> skipped(filed on a misreading. D80 corrected 2026-08-22: upstream pulls are still allowed, so Stage 0 plumbing stays as the thing that makes a user-called pull cheap) (by user clarification 2026-08-22)
