---
id: T476
title: "zig 0.15.2 self-hosted x86_64 backend cannot compile ghostty-test: zig.exe access-violates at a fixed offset, deterministically"
order: null
deps: []
status: "done"
commits: ["3cfd083bc"]
seat: "win"
priority: "P2"
---

# T476 - zig 0.15.2 self-hosted x86_64 backend cannot compile ghostty-test: zig.exe access-violates at a fixed offset, deterministically

## Summary

Measured under **T473** (2026-08-04). Building the test binary with the
self-hosted backend instead of LLVM crashes the compiler:

```
zig build -Dapp-runtime=win32 -Demit-test-exe -Dtest-llvm=false
  -> compile test ghostty-test Debug native-native-msvc failure
  -> error: the following command exited with error code 5
```

`error code 5` is `std.process.Child` truncating an NTSTATUS to a byte
(T444): the real code is `0xC0000005`, confirmed against the Windows
`Application Error` log.

**Four attempts, four crashes, all at the same address:**

| attempt | elapsed | faulting module + offset |
|---|---|---|
| 1 | 12s | `zig.exe+0x0000000000fcb364` |
| 2 | 9s | `zig.exe+0x0000000000fcb364` |
| 3 | 9s | `zig.exe+0x0000000000fcb364` |
| 4 (`-Dtest-filter=terminal.`) | 9s | `zig.exe+0x0000000000fcb364` |

The identical offset every time is what separates this from **T451** (`zig.exe`
taking *sporadic* access violations on this box): this one is deterministic and
reproduces in under 10 seconds. Narrowing with `-Dtest-filter` does not help
and cannot — a filter changes which tests *run*, not what gets *analysed*, so
the compile is byte-identical.

Upstream Ghostty already carries `// Crash on x86_64 without this` next to the
`.use_llvm = true` on this artifact, so the backend has been known-bad for this
tree for a while; what is new is that the failure is now *the compiler dying*,
with an address to report.

## In plain terms

There are two ways to translate this project's code into a program, and the
newer one crashes the translator itself, the same way every time, within ten
seconds. Nothing ships that way today, so nothing is broken for users — but it
removes a tool the team wanted for chasing a hard bug, and it is very likely a
bug in the compiler worth reporting.

## Goals

- [x] Reduce to a case small enough to report (start from the failing module,
      bisect imports — the crash is fast, so bisection is cheap) —
      `test/zig-repro/t476-selfhosted-backend`, fifteen lines and one
      dependency, no ghoztty source in it
- [ ] File it upstream against zig 0.15.2 with the reduction and the offset —
      moved to T1341, because posting it is a public act under the user's
      GitHub identity and is theirs to authorise
- [ ] Recheck on the next zig the repo moves to, and drop `-Dtest-llvm` if it
      starts working — automated: `test\win32\zig-repro-t476.ps1` fails the
      day the reduction compiles clean, and says what to do about it

## Details

- `-Dtest-llvm` is the knob T473 added to `build.zig`; `-Dtest-llvm=false`
  selects the self-hosted backend for the three test binaries only.
- Reproduce in one command from a clean shell:

  ```powershell
  $env:ZIG_GLOBAL_CACHE_DIR = 'D:\zig-global-cache'
  zig build -Dapp-runtime=win32 -Demit-test-exe -Dtest-llvm=false `
      --prefix zig-out-t473\win32-debug-selfhosted
  ```

- Read the real exception back with
  `Get-ProcessCrashEvent -Since (Get-Date).AddMinutes(-5) -NameLike 'zig.exe'`
  from `scripts/lib/CrashDiag.ps1`.
- **P2 on purpose**: nothing we ship uses this backend, so this blocks no
  delivery. It cost T473 one of its two arms, which is the reason to write it
  down rather than the reason to drop everything.

## What the reduction turned out to be (2026-09-04)

Bisected downwards from `src/main.zig`, each step re-measured on the box:

`main_ghostty.zig`'s test block → `input.zig` → `input/command.zig` →
`input/Binding.zig` → its tests (truncating just before the first one compiles
clean) → `test "set: parseAndPut typical binding"` → `Set.put` →
`Trigger.hash()` → `foldedCodepoint` → a single
`uucode.get(.case_folding_full, cp).with(&buffer, cp)`.

`Command.zig`, `font/main.zig`, `apprt.zig`, `renderer.zig` and `termio.zig`
crash on their own too — they all reach the same table.

The standalone project in `test/zig-repro/t476-selfhosted-backend` reproduces
it with **no ghoztty source at all**: `std`, `uucode` 0.2.0 (the tarball the
repo pins) and a verbatim copy of `src/build/uucode_config.zig`. Two flavours,
both `zig.exe` dying with no diagnostic of any kind:

| command | outcome |
|---|---|
| `zig build` | `0x80000003` STATUS_BREAKPOINT at `zig.exe+0x4016f2` |
| `zig build -Dtarget=native-native-msvc -Dcpu=baseline` | `0xC0000005` STATUS_ACCESS_VIOLATION at `zig.exe+0x1d9fd1` |
| ghoztty's own `ghostty-test` | `0xC0000005` at `zig.exe+0xfcb364` |

The offsets differ, so this is a family of failures in the same backend rather
than provably one instruction. What is constant is the trigger: a module that
reaches uucode's generated tables, and **the table's shape matters** — a
`uucode_config.zig` with only the `runtime` table compiles clean, and so does
one with a second table holding a single plain field. It takes ghoztty's real
`buildtime` table with its four extensions, which is why the copy is verbatim
rather than trimmed and why `test\win32\zig-repro-t476.ps1` fails when the two
drift apart.

## Validation criteria

- [x] The reduction reproduces the AV on a tree that is not ghoztty —
      `D:\t476repro` built from `test/zig-repro/t476-selfhosted-backend`,
      `0xC0000005` read back from the Windows Application Error log via
      `Get-ProcessCrashEvent`.
- [x] The reduction is committed and cannot silently stop describing this
      build — `test\win32\zig-repro-t476.ps1`, ALL PASS (12 assertions),
      registered in `scripts\guard-due.ps1` so an edit to
      `src\build\uucode_config.zig` puts it back on the due list.
- [x] The floor is green over the change — `scripts\floor-lane.ps1 -Lane all`,
      plus the seven `test\win32` meta-audits a new harness file puts on the
      due list (isolation-meta, launch-preflight, verdict-exit, cleanslate,
      stderr-capture, body-complete, desktop-launch), all ALL PASS.
- [ ] An upstream issue link is recorded here — **T1341**, which needs the
      user's go-ahead to post.

## Progress log

- 2026-09-04 15:56: claimed; work starting.
- 2026-09-04 16:43: re-verified on zig 0.15.2: the AV still reproduces (zig.exe+0xfcb364). Bisected the ghostty-test root down to src/input/Binding.zig -> Trigger.hash -> foldedCodepoint -> uucode.get(.case_folding_full, cp); built a standalone non-ghoztty project (uucode + ghostty's uucode_config) that crashes the same compiler two ways: default target = silent STATUS_BREAKPOINT, -Dtarget=native-native-msvc -Dcpu=baseline = STATUS_ACCESS_VIOLATION.
- 2026-09-04 17:10: floor green: all four lanes PASS, P1/P2/P3 ALL PASS, test\win32\zig-repro-t476.ps1 ALL PASS (12), and the seven meta-audits a new test\win32 script makes due all re-ran green. Upstream filing split out as T1341 (public post, user's call).
- 2026-09-04 17:11: status: in-progress -> done [commit 3cfd083bc]
