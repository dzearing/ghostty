---
id: T434
title: "test-agent builds and runs the same ~600 agent tests TWICE, concurrently, in two binaries"
order: 387
deps: []
status: "done"
priority: "P2"
triage-reason: "agent lane runs every test twice; halves lane time and may ease T437, but payoff is indirect"
commits: ["9ccf70384"]
seat: "win"
---

# T434 - test-agent builds and runs the same ~600 agent tests TWICE, concurrently, in two binaries

## In plain terms

One of the four automated check suites runs every one of its checks twice, at
the same time, in two separate copies of itself. That roughly doubles how long
it takes, and — because the two copies race each other for the same ports,
pipes and temp files on the machine — it is the leading suspect for why that
suite occasionally freezes instead of finishing.

## Goals

- [x] `zig build test-agent` runs each agent test once, not twice - one
      artifact now, 5443 passed / 78 skipped, byte-identical to what the exe
      binary reported before
- [x] The lane's wall time drops accordingly - 6m10s -> 3m35s, and 5 repeats
      landed within 210-215s of each other
- [x] The freeze recorded in T430 is either gone or shown to have a different
      cause - 5 x PASS with `leaked test binaries: 0` every run; the two
      same-test processes that raced for the ports, pipes and temp files no
      longer exist to race

## Summary

`zig build test-agent` wires **two** test artifacts (`build.zig`, the
`agent_step` block):

- `ghoztty-agent-test`, rooted at `src/agent_main.zig` → `remote/agent/main.zig`
- `ghoztty-agent-core-test`, rooted at `src/agent_core_test.zig` →
  `remote/agent_test.zig`

`remote/agent_test.zig` aggregates server, session, grid_snapshot, session_meta,
ring_snapshot, metrics, keepalive, self_update, socket_stream, socket_rw and
pipe_stream. **`remote/agent/main.zig` imports every one of those** (verified:
`main.zig:117-132`), and a zig test binary includes the tests of every file in
its import graph. So both binaries contain the same test set, and
`test_agent_step` depends on both run artifacts — which the build runner
executes **concurrently**.

Two consequences:

1. **Roughly double the lane time**, for zero extra coverage.
2. **Two processes running the same real-pty / loopback-socket / named-pipe
   tests at the same instant.** T430 recorded exactly this pair frozen together
   with identical `UserModeTime` across two samples 45s apart, and noted
   concurrency as a suspected aggravator without identifying its source. This
   is the source.

## Where to look

`build.zig`, the `agent_step` block (~lines 98-166). The comment on
`agent_core_test` says its ~140 tests "ran in NO build step", which was true
when it was written of the *standalone* `zig test -Mroot=…` invocation — but the
exe test binary already covered them via the import graph.

Do not just delete one: check first whether either binary reaches a test the
other does not (the exe root pulls tray/enroll/single_instance; the core root
pulls `grid_snapshot`'s src/terminal path). The answer is probably "keep the exe
root, drop the aggregator", but it must be established rather than assumed —
count the tests each binary reports today and diff the names.

## The other half: `ghoztty-agent-test.exe` links the WebView2 viewer

**Split out as T1331 on 2026-09-04**, because the fix is a shared-core change to
what EVERY test lane compiles rather than an agent-lane edit. The import path
this section asked for is now known and recorded there:

```
src/os/main.zig -> src/os/open.zig:5 -> src/apprt.zig
    test { ... 112 x  _ = @import("apprt/win32/<file>.zig") }
```

`apprt.zig`'s test block is the T59a aggregator that runs the pure win32 unit
tests in the `none` lane too; `os/main.zig` re-exports `open.zig`, so essentially
every file under `src/` reaches it. That is why the agent's test binary carries
the whole win32 app suite - 5443 tests against the agent's own 13 files - and can
start a WebView2 host. `open.zig` needs one type from there
(`apprt.action.OpenUrl.Kind`) and `apprt/action.zig` imports `apprt.zig` itself,
so there is no one-line edge to cut.

## Validation criteria

- [x] Test count before/after, per binary, so nothing silently stops running -
      before: exe 5443 passed / 78 skipped + core 5335 / 72 (10778 / 10928
      total); after: 5443 / 78. The per-file diff of the test names embedded in
      both binaries found no file in the core binary absent from the exe one.
- [x] `powershell -NoProfile -File scripts\floor-lane.ps1 -Lane all` green -
      lib PASS 47s, none PASS 277s, win32 PASS 372s, agent PASS 215s.
- [x] `powershell -NoProfile -File scripts\floor-lane.ps1 -Lane agent -Repeat 5`:
      5 x PASS at 210 / 210 / 210 / 215 / 210s, 0 leaked test binaries.
- [x] `test\win32\test-binary-soak.ps1` ALL PASS (77 assertions) - the
      two-binary attribution path it covers now runs on a fixture name declared
      through the new `-ExtraTestExeNames`, since no real lane builds two
      binaries any more.
- [x] The nine other harnesses the crash-tooling edit made due are green:
      crash-first-chance (49), crash-stacks, crash-databreak, isolation-meta
      (13), launch-preflight-audit (19), verdict-exit-audit, cleanslate-audit
      (21), stderr-capture-audit (25), desktop-launch-audit (29).
- [ ] (moved to T1331) `ghoztty-agent-test.exe` no longer contains `ViewerPane`
      / `EBWebView`.

## Progress log

- 2026-09-04 05:51: claimed; work starting.
- 2026-09-04 06:22: root cause established, not assumed: ghoztty-agent-core-test is a strict SUBSET of ghoztty-agent-test. Measured 2026-09-04 - exe 5443 passed/78 skipped vs core 5335/72, and a per-file diff of the test names embedded in both 103MB binaries found ZERO files present in the core binary and absent from the exe one (the exe adds 12: main, pty_child, pty_holder_child, pty_host_proto, pty_host_spec, single_instance, tray_account, adopt, handoff, holder_adopt, link_control, relay_creds). Dropping the agent_core_test artifact + its two dead test roots. Second half (the win32/WebView2 suite inside the agent binary) split out as T1331 with the import path finally identified: os/main.zig -> os/open.zig -> apprt.zig, whose test block aggregates 112 win32 files.
- 2026-09-04 06:52: status: in-progress -> done
- 2026-09-04 06:57: post-commit floor completion: the win32 debug app was rebuilt over the build.zig edit (BuildFresh refused a stale exe, correctly) and P1/P2/P3 acceptance ran ALL PASS - 25 / 20 / 16 assertions. guard-due check is clean apart from the three standing advisories.
