---
id: T929
title: "The release-artifacts guard can never go green on this box, so every turn pays the hatch"
order: null
deps: []
status: "skipped(duplicate of T898)"
commits: []
seat: "win"
priority: "P2"
tags: ["infra", "test"]
---

# T929 - The release-artifacts guard can never go green on this box, so every turn pays the hatch

## Summary

guard-due reports release-artifacts DUE and it cannot be cleared here: sections B1/B2 need msitools in Docker, and starting Docker on this box is ruled out (its WSL2 backend has buried the machine before). A run therefore ends ALL PASS (2 skipped) and does not stamp, so validate fails and each turn either reaches for -NoGuardDue or is blocked on something it did not break. Decide the shape: split the Docker-only sections into their own guard row so the rest can stamp, or teach the stamp to record a skipped-because-unavailable section, or move those two claims somewhere that runs without Docker.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-08-17 12:27: status: todo -> skipped(duplicate of T898) (by filed this turn before finding T898, which is the same problem with better options)
