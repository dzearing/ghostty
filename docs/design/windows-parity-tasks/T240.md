---
id: T240
title: "Right-click shows no menu under mouse reporting: Mac shows it unconditionally, win32 gates on consumed (T102 closed this as Mac-identical; it is not)"
phase: "K"
deps: []
status: "done"
commits: ["2c982a879"]
---

# T240 - Right-click shows no menu under mouse reporting

## Summary

User report, 2026-07-31, verbatim: *"there is no right click context menu like
in the mac version. WTF. another parity item missing."*

The menu is not missing - `context_menu.zig` builds all 17 items in Mac order
and T102 shipped it on 2026-07-20. **What is missing is the menu ever
appearing in the user's actual workflow**, and the reason is a one-line
divergence that T102 explicitly examined and got wrong.

**T102's disposition was:** *"The user's 'right-click pastes' was Claude Code
receiving the reported right-click (mouse reporting consumes the press -
Mac-identical)."*

**That claim is false.** Mac does NOT consume the right-click press for menu
purposes, and this is checkable in the Mac source in under a minute.

## Evidence (VERIFIED - read both implementations)

**Mac** (`macos/Sources/Ghostty/Surface View/SurfaceView_AppKit.swift:1646`,
`menu(for:)`):

```swift
switch event.type {
case .rightMouseDown:
    // Good
    break                      // <-- falls through and BUILDS THE MENU

case .leftMouseDown:
    if !event.modifierFlags.contains(.control) { return nil }
    ...
    if surfaceModel.mouseCaptured { return nil }   // <-- capture guard is HERE ONLY
```

The `mouseCaptured` guard lives **inside the `.leftMouseDown` case**, guarding
ctrl+left-click. The `.rightMouseDown` case has no guard of any kind: it breaks
out of the switch and returns a fully built `NSMenu` **unconditionally**.

And AppKit consults `menu(for:)` *before* delivering `rightMouseDown` - a
non-nil return means the view's mouse handler never runs. The Mac source says
so itself, in the leftMouseDown branch: *"If we return a non-nil menu then
mouse events will never be processed by the core, so we need to manually send a
right mouse down event."*

So on Mac: **right-click always shows the context menu**, mouse reporting or
not, and `rightMouseDown`'s `consumed` check (line 1024) is dead code for the
menu path.

**Win32** (`src/apprt/win32/Surface.zig:2822`):

```zig
if (!consumed and button == .right and action == .press) {
    self.showContextMenu(lparam);
}
```

Gated on `!consumed`. Under mouse reporting the core consumes the press, so the
menu never opens.

**Why this is 100% of the user's experience, not an edge case:** every pane
they use runs Claude Code, which enables mouse reporting. vim, tmux, htop,
lazygit and every other TUI do the same. So on Windows the context menu is
reachable only from a bare shell prompt - which is close to never - while on
Mac it is always one right-click away. The feature reads as absent because in
practice it is.

## The fix

Match Mac: **right-click opens the context menu unconditionally.** Move the
call out from behind `!consumed`, and follow Mac's compensating behavior - when
the menu is shown, the core still gets a synthesized right press/release pair
so its click state does not desync (`showContextMenu` already synthesizes the
release for the `TrackPopupMenuEx` capture problem; extend that reasoning).

Decisions to make explicitly, not by default:

- **`right-click-action = paste`** (the WT-style opt-in T102 added) must keep
  working, and under it right-click should still go to the app when reporting
  is on. Only the `menu` disposition changes.
- **Shift+right-click** already bypasses reporting and must keep opening the
  menu (it becomes redundant, not wrong).
- Confirm whether an app that has requested mouse reporting should be able to
  get right-click at all on Windows. Mac says no. Follow Mac - this is a
  parity task, and divergence here is what produced the report.

## The process failure, recorded

T102 marked itself `done` with `context-menu.ps1 ALL PASS (19) x3`. The script
passed because it drives the menu with **`PostMessage`** ("PostMessage-driven,
wedge-immune") - it posts the message that opens the menu, so it can never
observe that a real right-click under mouse reporting does not produce one.
A green suite and a broken feature, from a test that skips the input path the
user uses.

Two lessons for the tracker's bar:

1. **A parity claim about the Mac's behavior must cite the Mac source line**,
   not a plausible inference about what the shared core does. "Mac-identical"
   was asserted about a code path nobody read; reading it takes a minute and
   refutes it.
2. **An acceptance script that synthesizes the trigger cannot validate the
   trigger.** `context-menu.ps1` must gain a real-input case: turn mouse
   reporting ON in the pane, right-click for real, assert the menu appears.

## Validation

- With mouse reporting ON in the pane (run Claude Code, or emit
  `\033[?1000h`), a real right-click opens the menu. This is the case that
  fails today and is the whole task.
- With reporting OFF, unchanged behavior.
- `right-click-action = paste` still pastes.
- Shift+right-click still opens the menu.
- The core's click state is not desynced after the menu closes: right-click,
  dismiss, then drag-select - the selection must behave normally (this is the
  regression `showContextMenu`'s synthesized release exists to prevent).
- `test/win32/context-menu.ps1` gains the real-input case above and stays
  ALL PASS x3, with a negative control that fails when the gate is restored.

## Outcome (2026-07-31)

Fixed as filed: **a right-press that mouse reporting would swallow now opens
the context menu**, so the menu is reachable in the panes the user actually
runs. Verified with a PHYSICAL right-click, not a posted message.

### The symptom is exactly what T240 described - measured, not inferred

A probe launched a pane, waited for Claude Code, and right-clicked:

```
--- pane state before click ---
  ctx: 0k/1000k (0%) | v2.1.220 (Opus 5 (1M context))
RESULT: NO MENU   (right-click consumed => mouse reporting active for 'claude')
```

**Measurement trap worth keeping:** the same probe run against a *freshly
launched* Claude Code reported the OPPOSITE ("mouse reporting NOT active"),
because the pane was still sitting on the "do you trust this folder" dialog,
which does not enable reporting. Reporting turns on at the real prompt. A
fixture that has not reached the state under test is not a measurement of it.

### The Mac premise, corrected

T240 argued Mac shows the menu unconditionally because AppKit consults
`menu(for:)` BEFORE delivering `rightMouseDown`. **That ordering is not
supported**, and the Mac file argues against it in its own comment: the
"AppKit calls menu BEFORE calling any mouse events" note sits inside the
`.leftMouseDown` (ctrl+click) case, which is also the only branch that
compensates by manually sending a press. AppKit's documented right-click
path is the other way round - `NSView`'s default `rightMouseDown:` is what
calls `menuForEvent:` and pops the menu - so Ghostty's override, which
returns early when the core consumed the press, would suppress the menu under
reporting on Mac too. The `.rightMouseDown` case in `menu(for:)` exists
because `super.rightMouseDown` calls back into it.

This was NOT confirmed on a Mac (no Mac on this seat), so it is recorded as
the better-supported reading, not a verified fact. It does not change the fix:
the user asked for a reachable right-click menu and that is what shipped. What
it does change is the *justification* - this is a deliberate, documented
divergence in the user's favor if Mac really does gate on `consumed`, and the
Mac seat should check `menu(for:)`/`rightMouseDown` ordering directly when it
next touches that file (filed as T246).

Recorded because T240's own first lesson applies to T240: **a parity claim
about Mac behavior must cite the Mac source line** - and citing the line is
not the same as modeling the framework that calls it.

### What changed

- `src/Surface.zig`: `rightPressWouldReport(mods)` (public) + the pure
  `rightPressWouldReportLogic(action, reporting, shift, shift_capture)` behind
  it, with unit tests. Only the `context-menu` disposition diverts; `paste`,
  `copy`, `copy-or-paste` and `ignore` keep handing the click to the app.
- `src/apprt/win32/Surface.zig`: `handleMouseButton` asks that question
  BEFORE the core sees the press and, when the answer is yes, opens the menu
  via a new `showContextMenuUnreported`. Deciding afterwards is too late - by
  then the press has been reported to the app and the selection the menu would
  act on has been cleared. Nothing is synthesized back to the core on that
  path: it never saw a press, so there is no stuck click state, and a lone
  synthesized release WOULD have been reported to the app as an orphan.
  Capture cleanup is shared by both paths (`releaseMouseForMenu`).

### Evidence

- `test/win32/context-menu-real-input.ps1` (new): **ALL PASS (11)**, physical
  SendInput on the interactive desktop. `-NegativeControl` (expect the menu
  suppressed under reporting) → **1 FAILURE(S)**, so the assertion
  discriminates.
- `test/win32/context-menu.ps1`: section D inverted to the new expectation
  and given a real oracle - the fixture must print `MOUSEMODE-ON` before the
  section runs, and a new section D2 (reporting + `right-click-action=paste`)
  proves reporting is still able to swallow a click. Without D2, D's "a menu
  appeared" would pass on a fixture whose console mode never landed.
- `zig build test` both lanes + `zig build test-agent`: green.

### Two traps this task walked into, both worth carrying forward

1. **A synthetic-input probe needs a positive control, always.** The first
   real-input probe reported "NO MENU on a real right-click" in a *plain*
   pane - a confident, wrong reproduction. The cause was the probe: its
   `INPUT` struct carried 16 bytes of extra padding, so `SendInput` accepted
   0 events and sent nothing. x64 `INPUT` is 40 bytes (4 type + 4 pad + 32
   union): `MOUSEINPUT` is already 32 and needs no tail padding, `KEYBDINPUT`
   is 24 and needs 8. The control now typing a sentinel into the pane is why
   the second run could be trusted.
2. **`Start-Process -ArgumentList` does not quote its elements** (the same
   trap as T200's `launch-upgrade.ps1`). `--command=powershell -NoProfile ...`
   was re-tokenized and the reporting fixture came up as a plain shell, which
   *passed* the reporting-off assertions for the wrong reason. Quoting is done
   once, in the script's `Start-Gui`.
