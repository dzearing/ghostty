---
id: T872
title: "win32 agent integrations (8/8): no end-to-end acceptance coverage - sandboxed install/uninstall/rollback/refusal script for the whole agent-integration flow"
order: 8
deps: ["T870", "T871"]
status: "in-progress"
commits: []
seat: "win"
priority: "P0"
tags: ["test"]
---

# T872 - win32 agent integrations (8/8): no end-to-end acceptance coverage - sandboxed install/uninstall/rollback/refusal script for the whole agent-integration flow

## In plain terms

The final slice: a repeatable on-box test that proves the whole
agent-integration feature works end to end — install, update, remove,
graceful refusal — without ever touching the real user's agent
configuration. This is what keeps the feature honest after every future
change.

## Summary

Eighth slice of the T598 split: `test\win32\agent-integrations.ps1`
following the harness rules in `docs/claude/testing.md` (BuildMode refusal,
TestDesktop where GUI is driven, single ALL PASS / N FAILURE(S) verdict,
positive controls).

Sandboxing model: every path the subsystem touches must be overridable to a
temp sandbox (home dir override env var, or the components' existing test
seams surfaced through a hidden CLI/env hook) — the suite must NEVER read or
write the real `%USERPROFILE%\.claude`. Fake `claude.exe`/`copilot.exe`
stubs on a prepended PATH drive the probe and the migration's
`plugin uninstall` call (stub logs its argv for assertion).

Scenario floor (each is one lettered section):

- A: fresh install for Claude — skills, scripts, hooks all land marked;
  settings.json merge preserves pre-seeded user hooks.
- B: idempotence — second install byte-identical, reports up-to-date.
- C: staleness — doctored marked file → update refreshes it.
- D: refusals — unmarked same-named skill; unparseable settings.json;
  reparse-point at a destination. Nothing altered, typed errors surfaced.
- E: rollback — induced failure on the last component leaves no partial
  install, and a pre-existing component survives the rollback.
- F: shared banner refcount — both agents installed; removing one keeps the
  scripts, removing both clears them.
- G: uninstall exactness — user-added files in skills/ survive.
- H: migration — fake plugin registration + state files: uninstall invoked,
  state carried, stale script handled per T870's ownership rules.
- Register the harness in `scripts\guard-due.ps1`'s coverage table mapping
  the new `src/apprt/win32` integration modules to this script — that row
  is the T783 contract and is NOT optional.

## Goals

- [x] A debug-only `agent-integration` IPC seam (capture-pane pattern) so
      the scenarios the GUI cannot reach are drivable through the real app
- [x] `test\win32\agent-integrations.ps1` green on the box, sections A–H
- [x] Guard-due row `agent-integrations` over the component/service modules
- [x] The migration ownership arm claude-integration.ps1 lacks (user-owned
      script survives) covered end-to-end

## Validation criteria

- [x] The script runs green on this box against the Debug build, prints one
      ALL PASS verdict, and refuses a release build (BuildMode guard).
      *Run 3: `ALL PASS (65 assertions, 1 SKIPPED)` exit 0; against
      `%LOCALAPPDATA%\Programs\Ghoztty\ghoztty.exe` it exits 1 immediately
      launching nothing (Assert-GhozttyIsolatedBuild).*
- [x] Every section above present with at least one positive control
      (deliberately-broken expectation that must FAIL) proving the oracle
      sees what it claims to.
      *In-line controls per section (marker oracle vs unmarked file, hash
      oracle vs doctored bytes, recovery installs after each refusal, E's
      poison proven by F's clean success); `-NegativeControl` inverts the
      merge-preserves-user-hooks and refcount-clears-banner claims and that
      run fails exactly those two (2 FAILURE(S), 63 passed) without
      re-stamping.*
- [x] Zero reads/writes outside the sandbox during a run (verify with a
      canary: pre-hash the real `.claude` dir, compare after).
      *Manifest-hash canary over `~\.claude` settings/skills/plugins
      manifest/scripts, `~\.copilot`, `~\.config\ghoztty\hooks` before vs
      after: `PASS the real user integration surfaces are bit-for-bit
      undisturbed (canary)`.*
- [x] `scripts\guard-due.ps1 check` reports the new harness row and goes
      due when an integration module changes (edit-a-file smoke check).
      *Probe comment in `managed_marker.zig` → `GUARD DUE agent-integrations:
      … changed src/apprt/win32/managed_marker.zig` exit 1; revert →
      `GUARD CURRENT agent-integrations (15 files)` exit 0.*

## Scenario-floor notes (what each section measured)

- A–G run over the IPC seam against one live app instance
  (`GHOZTTY_CLAUDE_SETUP=0` keeps the prompts out; those flows are
  claude-integration.ps1's). H relaunches with `force` and drives the real
  migration dialog (title verified before Enter).
- D3 has two arms mirroring the managed_file unit tests: a directory
  junction is unreadable as a file so the marker guard refuses it
  `failed:WriteFailed` (never overwritten blind, target untouched); the
  `ReparsePointRefused` arm needs a real file symlink and SKIPs counted on
  a box without symlink privilege (this one), with the refusal itself
  pinned by managed_file/HookComponent unit tests.
- D3's recovery reports `upgraded`, not `installed`: with the
  activity-state sibling still present a partially-missing banner component
  deliberately reads `outdated` (BannerScriptInstaller.state) — the harness
  documents that semantic rather than fighting it.
- H's new coverage vs claude-integration.ps1 case 5: the plugin-era
  `~\.claude\scripts\ghoztty-banner.sh` holds USER bytes matching no
  registered install, and survives the accepted migration byte-identical
  while the uninstall still runs, state still carries, and success stays
  silent.

## Progress log

- 2026-08-15 21:02: claimed; work starting.
- 2026-08-15 21:15: Design settled: debug-only agent-integration IPC action (capture-pane pattern; GUI offers one action per state so idempotence/refusals/rollback/refcount are unreachable from outside) + test\win32\agent-integrations.ps1 sections A-H; H covers the user-owned-script migration arm claude-integration.ps1 lacks. New src\apprt\win32\ipc_agent_integration.zig wired into IpcHandlers; guard-due row agent-integrations added over the component/service modules. Build green; harness run next.
- 2026-08-15 21:52: Validated on box: agent-integrations.ps1 ALL PASS (65 assertions, 1 SKIPPED: ReparsePointRefused arm needs symlink privilege; unit tests pin it); -NegativeControl fails exactly its 2 inverted claims, no stamp; release exe refused by BuildMode; canary clean; guard row due-on-edit proven both ways. Floor: lib 5s / none 155s / win32 208s / agent 204s ALL LANES PASS; P1-P3 25/20/16 ALL PASS. Also fixed pre-existing T219 'unreported' skip verdict in agent-shell-integration.ps1 (T513 edit) and re-ran it green; skip-visibility sweep back to ALL PASS.
