---
id: T1309
title: "focus-defer.ps1 has no guard-due row, so a deferred-focus change never marks the T48 regression harness stale"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "closes a harness-staleness gap for the T48 deadlock regression script; no user impact"
tags: ["test", "infra"]
---

# T1309 - focus-defer.ps1 has no guard-due row, so a deferred-focus change never marks the T48 regression harness stale

## Summary

Filed by T408 (2026-09-03). scripts\guard-due.ps1 has no row for test\win32\focus-defer.ps1, so an edit to the deferred-SetFocus path leaves the T48 deadlock regression harness unstamped and nothing says it is due. The obstacle is scope: WM_APP_SETFOCUS is referenced by a dozen win32 files, so a naive Covers list would be huge and permanently red. Work out the true core surface (the App.zig message-loop dispatch plus the surface WndProc mouse handler plus the harness itself), add the row, and stamp it from a green run.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
