---
id: T308
title: "The dialog panels (ActivityMonitor, MachineChooser, HeroCarousel rests) are still hardcoded dark"
order: 139
deps: ["T305"]
status: "in-progress"
priority: "P1"
milestone: "M1"
triage-reason: "chooser and Activity Monitor are hardcoded dark, so they open dark on a light system theme"
commits: []
---

# T308 - The dialog panels (ActivityMonitor, MachineChooser, HeroCarousel rests) are still hardcoded dark

## Summary

T305 fixed the two surfaces T274 named — the caption band and the tab strip —
and took the invented accents out of every surface. What it deliberately did
NOT do is re-theme the PANELS, which are still written as dark-only: they open
dark on a light system theme, which is the panel-scale version of the same
defect.

`chrome_theme.zig` already answers this; nothing consumes it here yet.

## What is hardcoded at HEAD (VERIFIED by reading, 2026-08-01)

- `ActivityMonitor.zig:169-225` — about thirty `COLOR_*` constants
  (`COLOR_BG = RGB(32,32,32)`, `COLOR_TEXT = RGB(230,230,230)`,
  `COLOR_CARD_BG`, `COLOR_HEADER_BG`, the chart/gauge/dot palettes …). The
  accent is derived now; everything around it is not.
- `MachineChooser.zig:103` — `DIALOG_BG = RGB(32,32,32)`, and `ROW_BG` washed
  off it. `chooser_rows`' washes follow luminance since T305, so the module is
  ready; the two base constants are the whole remaining problem.
- `HeroCarousel.zig` — the two REST colors, `RGB(96,96,96)` (divider) and
  `RGB(110,110,110)` (tile border). Mid-greys, so they degrade rather than
  vanish on a light band, but they are still not derived from anything. (The
  divider's WIDTH and hover are T250's, separately.)

## Fix

Resolve each panel's base from `chrome_theme.chromeBase(window-theme,
background, systemUsesLightTheme())` and derive the rest of its ramp from the
palette, the way `Window.chromePalette()` now does. The semantic colors that
are NOT a wash of the surface — the status dots, the CPU/MEM series, the amber
warning — stay literal, but must be re-checked against their floors on a light
panel rather than assumed.

Watch the scale: `ActivityMonitor.zig` is a 3k-line file and T299 already wants
it split. Splitting the palette out is probably the first move, not a
thirty-site edit in place.

## Validation

- Extend `test/win32/chrome-theme.ps1`: open each panel under a light chrome
  base and assert the panel background is light and its primary text clears
  4.5:1 against it, computed from captured pixels.
- `activity-monitor.ps1`, `ipc-machine-chooser.ps1`, `hero-mode.ps1` stay
  ALL PASS. Both test lanes, `test-agent`, P1-P3.

## In plain terms

Open the machine picker or the Activity Monitor while Windows is set to the
light theme and you get a black dialog with pale grey text on it, sitting on
top of a light window. Every color in those two panels was written down once,
by hand, for a dark background - so they had no light version to show. After
this they take their colors from the same place the window frame does, so a
light theme gets a light panel with dark text, and a dark theme looks exactly
as it does today.

## Goals

- [x] one place that derives a panel's colors from the theme, with the
      contrast floors checked by test rather than by eye
- [x] the machine chooser follows the theme
- [x] the Activity Monitor follows the theme
- [x] the hero carousel's resting divider and tile borders follow the band
- [x] a theme flip while a panel is open repaints it
- [x] the dark panel does not visibly move - this is a light-theme fix, not a
      redesign

## Validation criteria

- [x] `test/win32/chrome-theme.ps1` section D: both panels opened on a light
      and on a dark `--background`, the panel fill measured EQUAL to
      `chromeBase` and the DERIVED text ramp found in the capture by exact RGB.
      ALL PASS (62 assertions) - 8 new: `panel surface IS chromeBase #f3f3f3`
      / `#1e1e1e` and `panel paints its derived primary text #181818` /
      `#e9e9e9`, for the Activity Monitor and the chooser each.
- [x] `panel_theme.zig` sweep, none lane: every floor holds across the whole
      surface x accent space, the dark panel lands within a few levels of the
      literals it replaces, and each surface stays distinguishable from the one
      it sits on.
- [x] `activity-monitor.ps1` ALL PASS (92), `ipc-machine-chooser.ps1` ALL PASS
      (72), `hero-mode.ps1` ALL PASS (63). Both scripts had PASTED the retired
      literals; their oracles now derive from `lib/ColorMath.ps1`.
- [x] `scriptsloor-lane.ps1 -Lane all` = ALL LANES PASS (none 312s, win32
      345s, agent 320s); P1/P2/P3 ALL PASS.

## Progress log

- 2026-08-07 07:12: claimed; work starting.
- 2026-08-07 07:48: panel_theme.zig added (pure, swept): panels derive bg/field/well/card/divider/text/label/secondary/select/boundary + semantic hues from chromeBase + accent, with 4.5:1 text and 3:1 mark floors. Wired ActivityMonitor (~35 sites), MachineChooser, HeroCarousel; brush_cache.CachedBrush replaces the frozen class brushes; both panels now repaint on WM_SETTINGCHANGE/WM_DWMCOLORIZATIONCOLORCHANGED. chrome-theme.ps1 gains section D.
