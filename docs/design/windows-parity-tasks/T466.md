---
id: T466
title: "A dead-but-relaunchable session is listed in the chooser roster with no way to revive it"
order: null
deps: []
status: "done"
commits: []
seat: "win"
priority: "P2"
triage-reason: "the only remaining kind of unrestorable row in the chooser; the dead non-relaunchable ones are already reaped and filtered"
---

# T466 - A dead-but-relaunchable session is listed in the chooser roster with no way to revive it

## In plain terms

The machine chooser lists sessions you can take over. It also lists sessions
whose process is already gone — kept because the command they were running
is worth reading — but selecting one does nothing: Resume only works on a
live session. So the list contains rows that look like offers and are not.
Either those rows should offer to start the recorded command again, or they
should not be in the list.

## Goals

- [x] A dead-but-relaunchable row either (a) offers a Relaunch action that
      restarts its recorded command, or (b) is not listed as a resumable row
      — (a), via `rowAction`: Return on such a row opens a window over the same
      ATTACH a live row uses, and `session-relaunch` revives it
- [x] Whichever way it goes, a row in the roster that cannot be acted on does
      not look like one that can — the invariant is now **listed ⇔ actionable**
      (`isConnectable` decides listing, `rowAction` is non-`.none` for exactly
      that set, asserted over the whole domain in the `win32` lane), and the
      tombstone wears a `can relaunch` badge beside its exit label
- [x] `docs/design/windows-parity-tasks.md` and CLAUDE.md's "Browsing and
      resuming sessions from the chooser" section describe the behavior that
      actually ships — that section now lives in `docs/claude/sessions.md`
      (the CLAUDE.md split), and carries a **Relaunch one** bullet

## Evidence

`src/apprt/win32/chooser_sessions.zig:53-65` — `isConnectable` keeps a
session when `alive or relaunchable`, and the comment is explicit that a
dead, non-relaunchable tombstone is "an unreconnectable dead end — filtered
out as a client-side backstop" to the agent's own reaper. That half is
already right, on both sides: the agent reaps dead+unbound tombstones
(`src/remote/agent/server.zig:470-493` and `:1104-1119`).

What is left is the relaunchable tombstone. `isResumable`
(`chooser_sessions.zig:321-328`) is deliberately NOT `isConnectable` — it
collapses to `alive` — and CLAUDE.md states the rule outright: *"Dead-but-
relaunchable rows are listed — their recorded command is worth seeing — but
cannot be resumed; reviving one is a RELAUNCH, a different verb."* The verb
exists in the agent protocol. Nothing in the chooser calls it.

## The judgement this needs — settled: Relaunch (D91)

Listing a row for its information value is a real argument, and so is the
user's ask that started this ("don't show me a bunch of sessions that can't
be restored"). Offering Relaunch satisfies both and is more work; hiding the
rows satisfies the ask and loses the recorded command. Filed as **D91** and
built on the recommended option meanwhile, since effort is never part of that
balance and option (a) gives up nothing option (b) gives up.

What decided it beyond the two arguments: the revival machinery already exists
and already runs on this path. ATTACHing a tombstone is what launch-time
restore does with a tombstone leaf, and `termio.Remote` applies
`session-relaunch` on finding the target dead. So (b) would have deleted rows
the app can already honour, and the "more work" (a) costs was a predicate and a
badge. **The user's revised ask is satisfied either way** — no row in the list
is a dead end any more.

The Mac seat owes the same change: **T1339** (blocked behind T322, whose missing
`relaunchable` in the C API may be hiding these rows there entirely).

## Validation criteria

- [x] Unit tests over `rowAction`/`isConnectable`/`hasLiveChild` for the chosen
      behavior, including the tombstone case and the listed ⇔ actionable
      invariant over the whole domain — `chooser_sessions.zig` tests, green in
      the **win32** lane (these live in a win32-only file, so the `none` lane the
      card asked for never compiled them)
- [x] The badge run is asserted at its widest (exited + open + can relaunch),
      so the third slot the buffer grew for is exercised — same tests
- [x] `test/win32/` acceptance: produce a relaunchable tombstone (kill the agent
      with a live session, restart it), open the chooser, and assert the row
      behaves as decided — `test\win32\chooser-resume.ps1` section 3, which was
      the harness ASSERTING the old refusal and is now inverted: the badge count
      is cross-checked against `+sessions --json`, Return must log the verb
      `relaunching` and an attach, the chooser must dismiss, and the agent must
      show the tombstone retired with one more live session than before
- [x] Positive control that the dialog distinguishes the two kinds of row, not
      "any Return opens something" — section 4 reopens the chooser and acts on a
      live row
- [x] Floor: `floor-lane.ps1 -Lane all` and P1–P3

## Progress log

- 2026-09-04 14:23: claimed; work starting.
- 2026-09-04 14:39: verified still real: resumeTarget collapsed to alive, so a relaunchable tombstone was listed and Return answered 'it cant be resumed'. Chose option (a) Relaunch (decision filed): the ATTACH path already revives a tombstone via termio.Remote's session-relaunch policy, which is exactly what launch-time restore does with a tombstone leaf, so hiding the rows would throw away both the recorded command and machinery that already works. Landed rowAction()/RowAction, resumeTarget now resolves a tombstone to the same transport arm, hasLiveChild (was isResumable) keeps Restore All's live-only floor, a 'can relaunch' badge, and a logRelaunchable oracle. none+win32 lanes green.
- 2026-09-04 15:01: status: in-progress -> done
