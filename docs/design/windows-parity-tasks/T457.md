---
id: T457
title: "Rebase onto origin/main and fold in the Mac new-window dialog work (per-process CPU, dead-session cleanup) as Windows tasks"
order: 1
deps: []
status: "done"
commits: ["c8d09ca63"]
seat: "win"
priority: "P0"
triage-reason: "the branch is 20 commits behind; every parity judgement made until this lands is made against a stale Mac"
---

# T457 - Rebase onto origin/main and fold in the Mac new-window dialog work as Windows tasks

## In plain terms

A lot of Mac work merged over the last day - the machine chooser now measures
what each remote session is doing, and it stops listing sessions that could
never be restored. None of it is on this branch yet, so the Windows side is
being judged against a Mac that no longer exists. Pull it in, look at what
landed, and turn each piece into its own Windows task.

## Goals

- [x] `users/dzearing/windows-amd64` carries all of `origin/main`
      (**merged**, not rebased — see D7 and "How this landed" below)
- [x] The floor lanes are green after the merge (`scripts\floor-lane.ps1
      -Lane all`)
- [x] Every Mac feature in the inventory below has either a Windows task filed
      against it or a written reason it needs none
- [x] `docs/design/windows-parity-tasks.md` and CLAUDE.md reflect anything the
      merge changed about the shared contract

## How this landed

**Merged, not rebased** (decision D7). The branch is 521 commits ahead of the
merge base and `go.md` says both seats work this same branch; a rebase rewrites
every one of those shas under the Mac seat. The branch's own precedent is a
merge — `c91b42777 Merge origin/main (1e1cdbbd2) into
users/dzearing/windows-amd64`. `git log HEAD..origin/main` is empty either way,
which is what the validation actually asks for.

Five files conflicted. Two of them were the two seats inventing the same thing
at once, and both were resolved as a **union**, not a pick:

- **`--view=` resolution.** Main extracted the shared helper as
  `cli/view_arg.zig` (teaching it the `git-diff:`/`git-status:` revspec
  schemes); this branch extracted it as `cli/view_args.zig` (teaching it that
  `std.fs.path.isAbsolute`, not `value[0] == '/'`, is what makes
  `C:\src\README.md` absolute). Keeping either alone re-breaks the other's bug.
  `view_arg.zig` now carries both rules and both test sets; `view_args.zig` is
  deleted and both call sites point at the survivor. Main's filename won so a
  later merge does not re-fight this.
- **The per-session CPU pump** (`remote/agent/server.zig`). Main's generation
  counter retires a pump whose `stop` flag a re-subscribe flipped back before
  it woke; this branch's `pumps_closed` latch refuses to spawn one after
  shutdown has latched. Both kept — they guard different ends of the same
  lifetime.

The other three (`CLAUDE.md`, and the two call sites in `cli/new_window.zig` /
`cli/split.zig`) were adjacent-edit conflicts with no semantic disagreement.

## Do the rebase at a turn boundary

**The working tree is shared with a live loop.** Rebasing rewrites every local
sha, so it must happen while nothing else holds the tree - at the start of a
turn, before any other work, with `git status` clean. Confirm the loop lock is
yours (`scripts\go-loop-exec.ps1 claim`) first.

As of filing (2026-08-04), `HEAD..origin/main` is **20 commits**.

## Inventory to explore (from `git log HEAD..origin/main`)

### 1. One warm connection per machine + per-session CPU (60ccd4f45)

The one the user named. Touches both halves:

- Mac: `MachineConnectionPool.swift` (**new, 534 lines**),
  `SessionCPUProbe.swift`, `SessionBrowserProbe.swift` (+200),
  `RemoteActivityMonitorView.swift`, `MachineChooserView.swift`.
- **Shared:** `src/remote/agent/server.zig` (+235) and
  `src/remote/protocol.zig` (+133) - new messages and a **`cpu_pct`
  capability gate**.

The shared zig arrives with the rebase; the win32 chooser UI does not. Expect
at least: a connection pool behind `MachineChooser.zig`/`SessionRoster.zig`, a
CPU column in the roster, and the probe that feeds it.

**This is also an agent-protocol change**, so it lands on the compatibility
boundary CLAUDE.md describes: a running agent is routinely a different build
from the app. Check that the new messages are gated on the negotiated HELLO
capability and that an old agent degrades rather than breaks.

### 2. Sessions that cannot be restored are no longer listed

The user's second half: "process cleanup so that we don't show you a bunch of
sessions in the dialog that can't be restored." Look in
`SessionBrowserProbe.swift` and `src/remote/agent/server.zig` from the same
merge for how a dead or unrestorable session is now filtered or reaped, and
mirror it in the win32 roster (`chooser_sessions.zig`, `SessionRoster.zig`).

### 3. Git diff viewer panes with a native file tree (69eb4aeb0)

A whole new viewer mode: `ViewerDiffPanel`, `ViewerDiffSpec`, `ViewerDiffTree`,
`ViewerGit`, `ViewerSidePanel` (all new), plus a large `ViewerView` rework and a
`ViewerTOC` refactor. Windows has none of it. Almost certainly its own task, or
several.

### 4. `needs_input` renders as "(question)" (d0c056ee5)

Small and self-contained: `BaseTerminalController`, `TerminalWindow`,
`Ghostty.Action`. The win32 title/activity-state path needs the same label.

### 5. Auto-named windows resolve without a prior `+list` (de932e4d7)

`IPCServer.swift`. This is CLI surface, which CLAUDE.md says must be identical
on both platforms - so win32's `IpcRegistry` needs the matching resolution.

### 6. Already shared, arrives with the rebase

`fix(remote): elide null optionals from the HELLO encoding` (04c854450,
`src/remote/protocol.zig`) and the v1.29.0 release notes. Verify rather than
re-implement.

## Inventory closeout

Every item above is either a filed task or a written reason it needs none.

| # | Item | Outcome |
|---|---|---|
| 1 | Warm connection per machine | **T461** — the win32 roster re-dials a remote machine on every fetch (`SessionRoster.zig:340-369`); only the local agent has a warm connection. This is also the likely cause of T328. |
| 1b | Per-session CPU | **T462** — `session_cpu` exists only in `protocol.zig` / `agent/server.zig` / `connection.zig` / `embedded.zig`. Nothing under `src/apprt/win32/` references it, so the producer arrived unused. |
| 2 | Sessions that cannot be restored are no longer listed | **Mostly already done, one gap → T466.** The premise in this task file was wrong: no filtering landed in the 20 commits (only `60ccd4f45` touched `macos/…/Remote/`, and it contains none). Windows already does both halves — the agent reaps dead+unbound non-relaunchable tombstones (`agent/server.zig:470-493`, `:1104-1119`) and `chooser_sessions.isConnectable` (`:53-65`) filters them client-side as a backstop. What is left is the dead-but-**relaunchable** row, which is listed and cannot be resumed: T466. |
| 3 | Git diff viewer panes | **T463** (render the diff) + **T464** (the file-tree side panel). The shared halves — `cli/view_arg.zig`'s scheme parsing and `src/viewer/diff.js`/`diff.css` — arrived with the merge, so the CLI now accepts a value the win32 viewer cannot draw. Noted in CLAUDE.md rather than left silent. |
| 4 | `needs_input` renders as "(question)" | **T465** — `Window.zig:3231` still interpolates the raw token. |
| 5 | Auto-named windows resolve without a prior `+list` | **No task needed.** The Mac bug was that a window only entered the registry via `+new-window --target=` or a `+list` walk. win32 registers **every** non-quick-terminal window's auto-name at construction (`Window.zig:886-897`, `app.ipcNextWindowName()`), so `--target=window-N` resolves from the moment the window exists. The registry is populated eagerly here, not lazily. |
| 6 | HELLO null-optional elision | **Verified, not re-implemented.** Both branches had already made `encode` pass `.emit_null_optional_fields = false`; the conflict was two wordings of the same doc comment. |

## Validation

- `git log HEAD..origin/main` is empty.
- `powershell -NoProfile -File scripts\floor-lane.ps1 -Lane all` is green.
- `test\win32\ipc-p1.ps1`, `-p2`, `-p3` all print `ALL PASS`.
- Each inventory item above is closed out with a task id or a written reason.
