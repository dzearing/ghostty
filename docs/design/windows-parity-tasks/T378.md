---
id: T378
title: "Pane banners do not repaint while a split divider is dragged (overlay class has no CS_HREDRAW and updatePosition never invalidates)"
order: 112
deps: []
status: "skipped(duplicate of T456 - fixed by 113af0a28)"
priority: "P1"
triage-reason: "user-reported: banners paint stale content while a split divider is dragged; visible but transient"
commits: []
seat: "win"
---

# T378 - Pane banners do not repaint while a split divider is dragged (overlay class has no CS_HREDRAW and updatePosition never invalidates)

## Summary

User report 2026-08-02: dragging a vertical split divider left/right does not repaint the pane banners during the move. They are correct again once the drag ends and something else forces a paint.

VERIFIED, by reading the path rather than inferring it from the symptom. The reposition DOES run on every motion tick: updateDividerDrag -> layoutSplits -> `defer updateDimOverlays()` -> updatePaneBanners -> BannerOverlay.updatePosition. What updatePosition does is SetWindowPos (new x/y/w/h) + healOverlayZOrder, and nothing else - there is no InvalidateRect in it (Window.zig ~2594, ~2165, ~2269; BannerOverlay.zig:279-313).

The overlay's window class registers `.style = 0` (BannerOverlay.zig:1312) - no CS_HREDRAW/CS_VREDRAW. So on a width change Windows invalidates only the newly exposed sliver when the window GROWS and nothing at all when it SHRINKS. The card backdrop, the wrapped table columns and every right-anchored piece stay rendered for the PREVIOUS width until an unrelated event invalidates the whole thing.

Why only the banner shows it, which is the part worth keeping: the dim overlay rides the exact same updateDimOverlays path with the same class style, but it paints a flat fill, and a flat fill repainted only in the newly-exposed strip is indistinguishable from a correct one. Banner content is width-DEPENDENT, so it is the one overlay where a stale paint is visible. Do not "fix" this by changing what dim does.

It also compounds with T123: insetHeight re-measures the content at the new pane width on the same tick and the reserved band changes height, so mid-drag the band the layout reserved and the pixels on screen actively disagree.

Fix: make a size change repaint. Prefer an explicit InvalidateRect in updatePosition when w/h actually changed over CS_HREDRAW on the class, for the T233 reason (the house rule is InvalidateRect, never a GetDC paint, and an explicit call is the one a test can assert). Per-tick full repaints are affordable because the glass-card backdrop is already cached and regenerated only when the band changes (BannerOverlay.zig:1109).

Validation: an acceptance script that sets a banner with width-dependent content (a table), drags the divider with the mouse, and reads the card off a PrintWindow capture MID-drag - the T233 precedent, where the color was proved mid-drag because dragging_split does not depend on a real cursor. A drag that ends before the capture proves nothing.

## In plain terms

Dragging the divider between two side-by-side panes used to leave the status
banner above a pane painted for its old size until the drag ended. This was
filed on 2026-08-02; two days later the same defect was independently
root-caused and fixed under T456 (which the user had reported twice). Nothing
was left to build here — this task is a duplicate, closed after re-proving the
fix on the current tree.

## Resolution: duplicate of T456

T456 (P0, done 2026-08-04, commit 113af0a28) fixed exactly the defect this
task describes, and went further than this task's sketch:

- The banner class registers `CS_HREDRAW | CS_VREDRAW` (BannerOverlay.zig:1494),
  so a size change invalidates the whole card — this task predicted the
  `.style = 0` root cause correctly.
- `updatePosition` detects a genuine resize, passes `SWP_NOCOPYBITS` (no
  stale-card blit for a frame), and calls `UpdateWindow` so the repaint lands
  in the same layout pass as the pane move — not one pumped WM_PAINT later.
- `insetHeight` invalidates on any pane-width change (the T123 half).

This task preferred an explicit `InvalidateRect` over `CS_HREDRAW` "because an
explicit call is the one a test can assert" — T456 resolved that concern the
other way and made the class style assertable anyway: its unit test asserts
`GetUpdateRect` covers the full client after a widen, with a confirmed
negative control (reverting `.style` to 0 fails it).

## Validation criteria

- [x] The fix is present in the current tree — read BannerOverlay.zig:1494
      (class style) and updatePosition (SWP_NOCOPYBITS + UpdateWindow).
- [x] Unit tests pass on the box: `floor-lane.ps1 -Lane win32 -Filter banner`
      → ALL LANES PASS (2026-08-06).
- [x] Acceptance passes on the box: `test\win32\banner-resize-repaint.ps1`
      → ALL PASS (10 assertions) (2026-08-06).

## Progress log

- 2026-08-06 08:10: claimed; work starting.
- 2026-08-06 08:15: reassessed — T456 already fixed this; re-ran its
  validation (win32 lane banner filter PASS, banner-resize-repaint.ps1 ALL
  PASS 10 assertions) and closed as skipped(duplicate of T456).
- 2026-08-06 08:11 [session 35e8af65-ddef-4c88-ac1c-5c4f75099fc0]: Reassessed: T456 (done, 113af0a28, 2026-08-04) already fixed this exact defect - CS_HREDRAW|CS_VREDRAW on the banner class (BannerOverlay.zig:1494), SWP_NOCOPYBITS + same-pass UpdateWindow in updatePosition, width-change InvalidateRect in insetHeight. T378 was filed 2026-08-02 pre-fix. Re-running T456's validation on the current tree before closing as duplicate.
