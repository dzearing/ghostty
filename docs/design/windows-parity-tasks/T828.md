---
id: T828
title: "Machine chooser selected row is a loud purple pill instead of the quiet WinUI selection treatment"
order: null
deps: []
status: "done"
commits: ["a8ee5d4f3"]
seat: "win"
priority: "P1"
milestone: "M1"
triage-reason: "user-reported with a screenshot; the chooser is the first thing a remote workflow touches and it currently reads as unfinished next to a native Windows 11 dialog"
tags: ["polish"]
---

# T828 - Machine chooser selected row is a loud purple pill instead of the quiet WinUI selection treatment

## Summary

User report (2026-08-14, with screenshot): the selected "Local" row in the
machine chooser draws as a bright purple pill with a thick purple outline. A
native Windows 11 dialog (Settings was the comparison screenshot) marks the
selected list item with a QUIET treatment: a subtle neutral fill and a thin
accent bar on the left edge — the accent is a small mark, not the whole row's
border.

Why it looks the way it does (traced, not guessed):

1. The accent is the user's system accent, `#680081` on this box
   (`system_colors.accent()` reads `HKCU\...\DWM\AccentColor`). That dark
   purple fails the 3:1 chrome floor against the dark chooser surface, so
   `chrome_theme.accentOn`'s contrast search brightens it into the vivid
   violet in the screenshot. Working as designed, but it means dark accents
   always paint LOUDER than the color the user picked.
2. `chooser_rows.rowPaint` gives a selected+focused row the maximum-weight
   treatment: accent fill at 0.25 (`selectionFill`), accent border at 0.7
   (`selectionBorder`), PLUS the §2.2 focus ring — a 2 DIP accent ring inset
   1 DIP inside the pill (T312). Border + ring in the same accent reads as
   one very thick outline.
3. The pill shape itself follows the Mac chooser's rounded accent selection
   (`MachineChooserView.row(for:)`), per the file header of
   `chooser_rows.zig`. Parity of INTENT is right, but the Mac original is an
   accent-tinted fill with no heavy border — the win32 rendering is louder
   than both the Mac source and the WinUI convention.

The fix direction (to be designed against `docs/design/win32-design-system.md`
§2.2/§3.2, decision doc if the focus-ring rule needs amending): keep the
selected-vs-unfocused-vs-hover ordering and the 3:1 floors, but drop the
selected row's weight to the platform's quiet convention — neutral/low-alpha
fill with a restrained accent mark (left edge bar, or a hairline border), and
don't stack the focus ring on top of an accent border in the same hue. The
focused-caret ring must stay distinguishable per T312; the defect is the
doubled full-perimeter accent, not the existence of focus indication.

Files: `src/apprt/win32/chooser_rows.zig` (`selectionFill`, `selectionBorder`,
`rowPaint`), consumed by `MachineChooser.zig`; same treatment is shared by
`chooser_sessions.zig` rows and `ViewerTOCPanel.zig` — audit all consumers so
the chooser doesn't diverge from the other selection surfaces.

## In plain terms

When you pick a machine to open a terminal on, the row you picked lights up in
a bright purple block with a heavy purple outline around it — it shouts, and it
looks nothing like the lists in Windows' own Settings app. After this, the
picked row is a quiet grey highlight with a small coloured bar down its left
edge, which is how Windows 11 marks the selected item in a list. The outline
that says "the keyboard is here" is now a plain light line instead of a second
coloured ring, and it follows Windows' own rule about when focus outlines are
shown at all — so a row picked with the mouse, in a window the mouse opened,
shows the quiet highlight and nothing else.

## Goals

- [x] The selected row is a neutral highlight with a small accent bar, not an
      accent-tinted block with an accent outline
- [x] The focus rim is neutral ink and honours Windows' focus-visual rule
      (`ODS_NOFOCUSRECT`), instead of a second accent ring
- [x] The session list's keyboard cursor gets the same treatment, so the dialog
      has one selection language
- [x] Every mark still measurably clears its contrast floor, in unit tests
- [x] Verified on the box with a screenshot of the real chooser

## Validation criteria

- [x] Selected+focused row in the machine chooser no longer draws a
      full-perimeter high-alpha accent border stacked with the inner accent
      focus ring; side-by-side screenshot against Windows 11 Settings reads as
      the same family of treatment.
      **How:** `test\win32\chooser-selection.ps1` section D probes 194 points
      along the pill's top, right and bottom edges in both focus states and
      requires every one of them to BE the fill — an outline of any colour
      fails it. Screenshot of the real chooser taken on the box: the Local row
      is a neutral pill with a 4x16 accent bar at its leading edge.
- [x] The three list states stay ordered by weight (hover < unfocused
      selection < focused selection) — existing `chooser_rows.zig` unit tests
      still pass, updated to the new weights.
      **How:** `zig build test -Dapp-runtime=none` / `-Dapp-runtime=win32`
      (both green through `scripts\floor-lane.ps1 -Lane all`), including the
      rewritten "three list states are mutually distinguishable" test and the
      new "three selection weights are one ramp" test. Measured on the box too:
      the unfocused pill reads `#45474f` and the focused one `#51545a` over a
      `#30333b` row.
- [x] All boundary/ink colors still clear the 3:1 floor
      (`chrome_theme.ui_contrast_target`) — asserted in unit tests, not
      eyeballed.
      **How:** the `rowPaint` background x accent sweep asserts the indicator
      and the rim against the surface each one actually sits on; the acceptance
      script re-measures it in pixels (indicator 3.01:1 focused, 3.68:1
      unfocused, against the pill).
- [x] Other selection surfaces sharing `rowPaint` (chooser sessions list,
      viewer TOC) audited and updated in the same change, or explicitly
      recorded here as intentionally different.
      **How:** the session roster's keyboard cursor moved onto the same
      vocabulary (`chooser_sessions.cursorFill` / `cursorIndicator` /
      `cursorIndicatorRect`, painted by `SessionRoster.paintRow`) and is
      visible in the screenshot with its own accent bar; `chooser-resume.ps1`
      and `chooser-sessions.ps1` re-run green over it. `ViewerTOCPanel.zig`
      turned out NOT to share `rowPaint` — it has its own solid-accent active
      row — so it is filed as **T930** rather than changed here.
- [x] `zig build -Dapp-runtime=win32 -Doptimize=Debug` green and the chooser
      visually verified on-box via screenshot.
      **How:** debug build green; chooser opened on the test desktop and
      captured in both focus states.

## Progress log

- 2026-08-17 12:29: claimed; work starting.
- 2026-08-17 12:54: root cause + design settled: the accent was BOTH the pill fill (Mac's 0.25 tint) and the focus rim, and chrome_theme.accentOn brightens a dark system accent to clear 3:1 before either is composited - so a #680081 accent came out vivid violet twice over. Fix: Windows 11's list treatment - neutral wash fill at two weights (0.10 unfocused / 0.16 focused), the accent spent on a single 4x16 capsule indicator bar at the pill's leading edge, and the focus rim redrawn in neutral high-contrast ink and gated on ODS_NOFOCUSRECT so a mouse click no longer paints a rim. Same vocabulary applied to the session roster's keyboard cursor (chooser_sessions.cursorFill/cursorIndicator). ViewerTOCPanel's solid-accent active row audited and filed as T930. Design system 2.2 amended for list rows.
- 2026-08-17 13:18: built + validated: floor lanes all PASS (lib/none/win32/agent); new acceptance test\win32\chooser-selection.ps1 ALL PASS (29 assertions) and stamped; chooser-resume, chooser-sessions, isolation-meta, ipc-p1/p2/p3 all green. Measured on the box: row bg #30333b, unfocused pill #45474f = wash(bg,0.10) with a NEUTRAL #a2a3a7 bar, focused pill #51545a = wash(bg,0.16) with the user's accent #dd7df4 at 3.01:1, and 0 of 194 perimeter probes differ from the fill in either state. release-artifacts stays GUARD DUE (its two Docker sections skip on this box) - already tracked as T898/T910/T914, so validate runs with -NoGuardDue.
- 2026-08-17 13:20: status: in-progress -> done [commit a8ee5d4f3]
