---
id: T1114
title: "caption-bar.ps1 captures a single flat colour instead of a picture of the window"
order: null
deps: ["T1100"]
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "Its 23 geometry and hit-test assertions pass; only the pixel capture is blind, and the harness already explains why."
tags: ["test"]
---

# T1114 - caption-bar.ps1 captures a single flat colour instead of a picture of the window

## In plain terms

The check on the title bar takes a picture of the window and looks at it. The picture comes back as one flat colour - not a picture at all - so the colour-based part of the check cannot run. Everything else it measures about the title bar passes.

## Summary

`test\win32\caption-bar.ps1` scored `1 FAILURE(S)` in the T1094 sweep with a diagnostic that does most of the triage itself:

```
Get-TestWindowPixels: the capture is UNIFORM - 3858x2118, one color over its whole
interior, still uniform after 11 attempt(s) across 2000ms. That is not a picture of
the window; it is what PrintWindow returns when there is nothing GDI-painted to copy
... The other cause is a window that had not painted yet, which is what the retries
above rule out.
```

The helper already rules out the not-yet-painted case with 11 retries over 2 seconds, and names the other known cause (a DirectComposition window that never paints into its DC). All 23 of the script's other assertions pass, including every hit test and both the press-lights-the-fill and release-cancels behaviours - so the caption bar itself is in good shape.

The 3858x2118 dimensions are full-screen sized, which is consistent with capturing a desktop that has nothing composited onto it. See [[T1100]].

## Where

- `test/win32/lib/` - `Get-TestWindowPixels`
- `test/win32/caption-bar.ps1`

## Goals

- [ ] Attribute against the test-desktop capture question rather than treating this as a caption-bar defect.
- [ ] If capture cannot work there, have the script SKIP the pixel assertions with a reason and keep running the 23 that do work - a script that is 96% useful should report as such rather than as one flat FAIL.
- [ ] If capture can be made to work, do that and re-run.

## Validation criteria

- [ ] the capture returns real content, or the pixel assertions report SKIP with a named reason
- [ ] the 23 non-pixel assertions keep passing
- [ ] `test\win32\caption-bar.ps1` ends green

## Progress log

- 2026-08-22: filed by T1094's full acceptance sweep (242 scripts, forward order, Debug build at 9a764deea). Evidence quoted above is from that run's log in `temp\suite-runs\fwd1\`.
