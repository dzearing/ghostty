---
id: T396
title: "Viewer panes: command palette entries - Open File in Pane, Open URL in Pane, Open Browser Pane"
order: 118
deps: ["T90f"]
status: "in-progress"
priority: "P1"
milestone: "M1"
triage-reason: "no interactive way to open a viewer pane at all on Windows; Mac has three palette entries"
commits: []
seat: "win"
---

# T396 - Viewer panes: command palette entries - Open File in Pane, Open URL in Pane, Open Browser Pane

## In plain terms

On the Mac you can open a side-by-side preview pane — a rendered document, a
website, or a blank mini-browser — straight from the command palette. On
Windows the only way to get one of those panes was to type a CLI command in
another terminal. This adds the same three palette entries to Windows: pick
"Open File in Pane" and a file picker appears, pick "Open URL in Pane" and a
small prompt asks for a web address (typing just "example" works — it is
completed to a real address the way a browser's address bar would), or pick
"Open Browser Pane" and an empty browser opens beside your terminal with the
cursor already in its address bar.

## Goals

- [x] The win32 command palette lists the three viewer entries with the same
      names as the Mac palette.
- [x] "Viewer: Open File in Pane…" opens the standard Windows file picker and
      shows the chosen file in a viewer split, without freezing the app's IPC.
- [x] "Viewer: Open URL in Pane…" opens a one-field prompt; a bare host is
      completed omnibox-style (https://, .com, localhost→http).
- [x] "Viewer: Open Browser Pane" opens an about:blank viewer split with the
      caret in its address field.
- [x] The new pane's origin directory is seeded from the pane it split from,
      like the Mac's `surfaceView.pwd`.

## Validation criteria

- [x] None-lane unit tests: the three registry entries exist with Mac-verbatim
      names (`commands.zig` "viewer entries carry the Mac palette names");
      bare-host→https completion covered (`viewer_nav.completeAddress`,
      already none-lane); RenameDialog level strings distinct — verified by
      the none lane passing (2026-08-06, floor-lane).
- [x] `zig build test -Dapp-runtime=none`, `-Dapp-runtime=win32`, and
      `zig build test-agent` all green — `floor-lane.ps1 -Lane all`
      2026-08-06: `none#1=PASS win32#1=PASS agent#1=PASS`.
- [x] `test/win32/viewer-panes.ps1` grown with T396 cases A–C: palette
      dispatch of all three entries by outcome — Browser Pane produces a
      `+list --json` viewer leaf with `about:blank`; URL prompt carries the
      Mac caption and completes `localhost:1` to `http://localhost:1`; File
      entry opens the #32770 dialog with IPC still answering (`+list --json`
      parses while it is up), Escape cancels cleanly, no pane created. All
      17 T396 assertions PASS, 2026-08-06 (suite: 151 PASS, 1 pre-existing
      FAIL = T540, unrelated and already filed/confirmed at HEAD).
- [x] P1–P3 acceptance scripts stay ALL PASS (2026-08-06, run sequentially
      after the lanes per T401).

## Summary

Split out of T90g (2026-08-02). Mac's palette carries three viewer entries
(`TerminalCommandPalette.swift:195-219` → `ViewerCommands.swift`); win32's
`commands.zig` registry has none, so there is no interactive way to open a
viewer pane at all — only the CLI.

## Details

The three, verbatim from Mac:

- **"Viewer: Open File in Pane…"** — `NSOpenPanel` sheet → viewer split
  right. Win32: `IFileOpenDialog` (or `GetOpenFileNameW`), non-blocking
  enough that the IPC pump keeps running.
- **"Viewer: Open URL in Pane…"** — a one-field prompt; a bare host is
  completed to `https://`. Win32 already has a one-field dialog pattern
  (`RenameDialog.zig`).
- **"Viewer: Open Browser Pane"** — `--view=about:blank` (P11). Its "caret
  already in the address field" half needs T159's address bar and lands
  with it; the entry itself does not wait for that.

Registry mechanics: each needs a new `commands.Id`, a `Kind` (they are
apprt-local like `.remote`/`.activity` — there is no binding action), and a
decision in `menu_bar.omitted` or a real menu row, or the static
exhaustiveness test fails. Mac puts them in the palette only.

The split itself goes through `Window.newViewerSplitAt` (already exists,
T374). Mac seeds the blank pane's `originDirectory` from the pane it split
from (`ViewerCommands.swift:35-38`) — keep that pin for T164; the field
itself is P12/T90h.

*Validation:* `test/win32/viewer-panes.ps1` grown — the palette lists all
three entries; "Open Browser Pane" produces a `+list --json` leaf with
`"type": "viewer"` and an `about:blank` url. Pure half: the bare-host →
`https://` completion is unit-tested in the none lane.

## Progress log

- 2026-08-06 17:26: claimed; work starting.
- 2026-08-06 17:31 [session 77e01331-312b-41ac-80a5-74b76f46b095]: Design settled: 3 new Ids+Kinds in commands.zig (registry after activity_monitor, names verbatim from Mac); menu_bar.omitted entries (palette-only on Mac too); dispatch in Surface.performCommand -> openViewerSplitBeside helper (newViewerSplitAt .right 0.5, origin seeded from livePwd/pwd like Mac's surfaceView.pwd); Open File = GetOpenFileNameW (comdlg32 already linked, T67 pattern; modal loop still dispatches WM_APP_IPC window messages so IPC stays live); Open URL = RenameDialog grows a .viewer_url level reusing all dialog plumbing, commit runs viewer_nav.completeAddress (already none-lane tested, satisfies the pure-half validation); Open Browser Pane = about:blank + focusAddressBar. Acceptance: grow viewer-panes.ps1 sec 13 via the command-registry.ps1 palette harness.
- 2026-08-06 17:39 [session 77e01331-312b-41ac-80a5-74b76f46b095]: Built: commands.zig +3 Ids/Kinds (registry after activity_monitor, Mac-verbatim names incl. a name-pinning unit test); menu_bar.omitted +3; win32.zig +OPENFILENAMEW/GetOpenFileNameW (OFN_NOCHANGEDIR to protect process cwd); Surface.performCommand dispatch + viewerOpenFilePrompt + pub openViewerSplitBeside (origin from livePwd->pwd); RenameDialog grew .viewer_url level committing through viewer_nav.completeAddress; ViewerPane WM_APP_VIEWER_FOCUS_ADDRESS (posted behind the T48 deferred pane focus so the caret is not stolen). viewer-panes.ps1 grew section 11c (cases A-C). Build green; floor lanes running.
