---
id: T1089
title: "release.yml\u0027s appcast push retry is still dead code nobody can run"
order: null
deps: ["T353"]
status: "todo"
commits: []
seat: "win"
priority: "P2"
tags: ["infra", "test"]
---

# T1089 - release.yml's appcast push retry is still dead code nobody can run

## Summary

T353 extracted the WINDOWS half of the gh-pages write into dist/website/publish-windows-links.sh and section G now runs its rejected-push retry for real. The macOS half in .github/workflows/release.yml still has the same loop inline in YAML, still only reachable during a release, and it is the OTHER side of the same collision - if its retry is broken, an appcast push loses to the website push and installed macs stop seeing updates. Give it the same treatment: extract the clone/commit/push-with-retry, and cover it from the same harness.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
