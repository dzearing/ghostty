---
id: T626
title: "a protocol skew where the APP is the older side tells the user nothing"
order: null
deps: ["T125"]
status: "done"
commits: []
seat: "win"
priority: "P2"
milestone: "M1"
triage-reason: "The agent contract requires a skew to degrade loudly; an app older than its agent currently degrades silently. M1: the user is the one left guessing."
tags: ["fix"]
---

# T626 - a protocol skew where the APP is the older side tells the user nothing

## Summary

Found while building T125. `agent_upgrade.evaluateSkew` returns `.none` /
`skew_app_older` when the running agent speaks a NEWER protocol than the app —
correctly, because restarting it would be a downgrade AND would end sessions a
newer app could still attach to. But the only trace of that decision is a log
line. The user gets new windows with no session persistence and no explanation,
which is the same invisible degradation T125 fixed for the other direction.

## In plain terms

If you roll Ghoztty back to an older version, the background process that holds
your terminal sessions stays on the newer one, and the two can no longer talk.
Ghoztty is right not to kill it — that would destroy your sessions to install
something older — but it says nothing at all. Windows just quietly stop keeping
their sessions, and there is no way to find out why short of reading a log file.
It should say: this copy of Ghoztty is older than its background process, update
Ghoztty.

## Goals

- [x] The `skew_app_older` decision produces something the user can see, once
      per run, not a modal that blocks work (there is nothing to consent to).
- [x] It names the actionable step — update Ghoztty — rather than describing the
      protocol.
- [x] Nothing is restarted or killed on this path, ever (assert it stays that
      way).

## Details

The decision already exists and is unit-tested (`agent_upgrade.evaluateSkew` →
`Reason.skew_app_older`); this is purely the notification. Candidate surfaces,
in rough order of fit: the existing update balloon path
(`App.showUpdateNotification` / `tray_notify.zig`), an `ok_only` `ConfirmDialog`
(what `showAgentRefreshFailed` uses), or a pane banner. A modal is probably
wrong — the user cannot fix it from inside the dialog.

Symmetric work for the Mac seat rides with T627.

## Validation criteria

- [x] An arm in `test/win32/agent-upgrade.ps1` extending K: with
      `GHOZTTY_AGENT_PROTO_VERSION` set high, the user-visible notice appears
      AND the agent pid is unchanged. — K7/K8/K9 (the notice is raised and the
      shell's answer recorded, version pair in the LOG not the balloon) and K14
      (same agent pid after the notice AND its click); K15 adds "no dialog was
      raised to deliver news". `agent-upgrade.ps1` ALL PASS (138).
- [x] Shown at most once per app run (a repeated check must not re-nag). — K12,
      with K11 as its control: the app's own re-check message (WM_APP+12) is
      posted, the skew line count grows, the notice count stays at 1.
- [x] Both zig test lanes, `test-agent`, and P1-P3 stay green. —
      `floor-lane.ps1 -Lane all` ALL LANES PASS (lib/none/win32/agent); P1 (25),
      P2 (20), P3 (16) ALL PASS. New unit tests: the app-older sweep (never acts
      at any distance, deferred or not), the balloon-fit/word check, and the
      distinct-uid check in `tray_notify.zig`.
- [x] The click has somewhere to go. — K13: a synthetic balloon click on uid 5
      routes to a manual update check.

## Progress log

- 2026-09-07 21:50: claimed; work starting.
- 2026-09-07 22:10: design settled: the .none/skew_app_older arm now raises a tray balloon (new uid 5, tray_notify.app_outdated_uid) once per app run; a click routes to startUpdateCheck(.manual), never near the agent. Text lives in agent_upgrade.app_older_notice_{title,body} so it is unit-testable; showTrayBalloon now returns the shell's answer so the log records shown=. Acceptance: arm K gains K7-K15 (notice raised, shell answer recorded, version pair in the log not the balloon, once-per-run with a re-check control, click routing, agent pid unchanged, no dialog). win32 build green; floor lanes running.
- 2026-09-07 22:41: status: in-progress -> done
