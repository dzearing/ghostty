---
id: T1398
title: "A busy LOCAL pane closes with no confirmation - Ctrl+Shift+W does not prompt"
order: null
deps: []
status: "done"
commits: ["97ee39aa1"]
seat: "win"
priority: "P1"
triage-reason: "P1: user-facing data loss - the confirmation that stands between Ctrl+Shift+W and a running build is not appearing on a local pane, found by close-confirm-idle.ps1 while validating T1390"
milestone: "M1"
tags: ["fix"]
---

# T1398 - A busy LOCAL pane closes with no confirmation - Ctrl+Shift+W does not prompt

## Summary

`test\win32\close-confirm-idle.ps1` scores red on the two sections whose subject
is the POSITIVE half of T41: with `ping -n 100 127.0.0.1` demonstrably running
under the pane's shell (asserted from `Win32_Process` in the line immediately
above), Ctrl+Shift+W produces **no confirmation dialog**.

```
  PASS A/exec ping is running under the shell (1 descendants)
  FAIL A/exec busy shell: ctrl+w opens the confirm dialog
  PASS A/exec ctrl+c leaves the shell with no descendants (found 0)
  PASS A/exec idle shell: ctrl+w opens NO confirm dialog
```

Both backends fail the same way (`A/exec` and `B/agent`), and in both the pane
survives the chord - so the close did not merely proceed unconfirmed, the whole
close appears not to have run. The IDLE cases in the same sections pass, which
means the chord IS reaching the app: the difference is entirely the busy branch.

## In plain terms

Hitting the close-pane shortcut while a build, a test run or an install is still
going is supposed to stop and ask. Right now it does not ask - and in the runs
seen so far it does not close either, so the shortcut simply appears dead
whenever anything is running. Either way the guard that stands between a stray
keystroke and a killed build is not doing its job.

## Details

Found on 2026-09-06 while validating T1390 (the remote Disconnect offer), on
commit `4556380e9` plus that change. **It is not T1390's doing**: the busy branch
in `Surface.close` is byte-identical before and after that change, which only
adds a preceding `if (offering)` arm that a LOCAL window never enters
(`Window.remote_machine` is null, and Zig's `and` short-circuits before the pane
facts are read). The same failure reproduces from `remote-disconnect.ps1`'s own
local section, where the pane DID close silently - so the two symptoms (no
dialog + no close, vs no dialog + closed) may not be one bug.

Where to look:

- `Surface.close` (`src/apprt/win32/Surface.zig`) - the `process_active and
  !self.shellIsIdleNow()` gate. `process_active` is the core's `cursorIsAtPrompt`
  verdict; if shell integration now DOES emit OSC 133 for the configured shell,
  a busy shell could be reported as "at the prompt" and skip the confirmation
  entirely. That would explain both symptoms and is the first thing to measure.
- `Surface.shellIsIdleNow` / `ProcessTree.snapshot` - the other half of the gate.
  A snapshot that fails answers "not idle", so this direction cannot suppress a
  dialog on its own.
- Whether `close_surface` reached `Surface.close` at all in the A/exec case,
  where the pane survived.

Start by instrumenting the two inputs to that `if` rather than by changing it:
the two symptoms have to be reconciled before a fix means anything.

## Goals

- [x] Root-caused: `process_active` answered wrong — ConPTY emits OSC 133 prompt marks for cmd.exe (`133;B` at the prompt, no `133;C` on command start), so `cursorIsAtPrompt()` stayed true all through `ping` and `needsConfirmQuit()` said nothing was active
- [x] A busy local pane confirms again on Ctrl+Shift+W, both backends (`Surface.closeNeedsConfirm`)
- [x] The two symptoms are explained: there was only ever ONE — the pane CLOSED silently. The harness's later checks (ctrl+c leaves no descendants, idle ctrl+w opens no dialog, the window closed) all pass trivially once the window is already gone, which is what made it read as a survival.
- [x] `close-confirm-idle.ps1` is ALL PASS (58 checks)

## Validation criteria

- [x] `powershell -NoProfile -File test\win32\close-confirm-idle.ps1` is ALL PASS (58 checks)
- [x] Its `-NegativeControl` run still scores red (6 FAILURE(S))
- [x] `test\win32\remote-disconnect.ps1` section F restored to the busy-shell
      positive control it was written as - ALL PASS (49 assertions)
- [x] Floor: `floor-lane.ps1 -Lane all` lib/none/win32/agent ALL LANES PASS;
      ipc-p1 (25), ipc-p2 (20), ipc-p3 (16) ALL PASS

## Progress log

- 2026-09-06: filed from the T1390 turn; reproduced twice, once from each harness.
- 2026-09-06 13:55: claimed; work starting.
- 2026-09-06 14:08: root cause: ConPTY emits OSC 133 prompt marks for cmd.exe, so cursorIsAtPrompt() reads TRUE while a command runs (cursor semantic_content stays .input - no 133;C on command start). needsConfirmQuit() therefore answered 'nothing active' and Surface.close skipped the dialog AND closed the pane. Fix: new Surface.closeNeedsConfirm(map, process_active) puts the process table in front of the core verdict for the confirm-close-surface=true case, keeping the core's answer only for =false and child_exited; Window.confirmCloseIfNeeded uses the same helper. close-confirm-idle.ps1 now ALL PASS (58).
- 2026-09-06 14:56: validated: close-confirm-idle ALL PASS (58) + NegativeControl red; remote-disconnect ALL PASS (49) with section F back on the busy-shell control; viewer-close ALL PASS (44); gate-negatives ALL PASS (58); floor lib/none/win32/agent ALL LANES PASS; ipc-p1/p2/p3 ALL PASS. Added a guard row (close-confirm) so an edit of either close path obliges this harness. Filed T1403 for the two remaining consumers of the ConPTY marks.
- 2026-09-06 14:57: status: in-progress -> done [commit 97ee39aa1]
