---
id: T429
title: "The restart notice never names what was running: the agent records no command label for a plain shell pane"
order: 123
deps: []
status: "done"
priority: "P1"
milestone: "M1"
triage-reason: "restart notice can never name what was running; the agent records no command label at all"
commits: ["ce2333a8d"]
seat: "win"
---

# T429 - The restart notice never names what was running: the agent records no command label for a plain shell pane

## In plain terms

When the computer reboots (or the background terminal service restarts), each
terminal window comes back with a note saying the old session was closed — and
the note is supposed to name the program that was running so you can copy the
command and start it again. Today that line is always missing, because nothing
ever wrote down what was running. After this change, the background service
keeps track of the program running in each terminal (say, an AI coding session),
and the note after a restart names it. A terminal that was just sitting at an
empty prompt keeps saying nothing, because nothing was running in it.

## Goals

- [x] The agent records the live foreground command of each session (what the
      user is actually running inside the shell), refreshed periodically.
- [x] That record survives an agent restart (persisted in `sessions.json`) and
      rides the dead ATTACHED reply to the app.
- [x] The restart notice names the recorded command in both carriers (scrollback
      text and sticky banner).
- [x] A pane idling at a bare prompt still omits the command line entirely.
- [x] Old app ↔ new agent and new app ↔ old agent skews degrade to today's
      behavior (no line), never to an empty or wrong line.

## Validation criteria

- [x] Agent unit tests: the foreground-command refresh records a sampled
      command, clears it when the shell returns to the prompt, and keeps the
      last value on a failed query — `SessionStore.refreshForegroundCommands`
      tests in `session.zig`, green in `zig build test-agent` (floor-lane,
      2026-08-06).
- [x] Agent unit tests: `fg_cmd` round-trips through `sessions.json`
      (persist → materialize: `SessionTable.materialize` T429 test +
      `session_meta` round-trip test) and rides the dead ATTACHED reply
      (extended "RELAUNCH: ATTACH to a materialized session" test, which also
      pins the relaunch CLEARING the stale sample).
- [x] Protocol test: an ATTACHED payload without `foreground_cmd` parses to
      null (old-agent skew ⇒ no line) — `protocol.zig` pid+tty round-trip
      test extended; also proves the field is elided when null.
- [x] Windows E2E: a plain shell pane with a user-typed long-running command
      survives an agent kill/restart and the notice names that command in
      BOTH carriers — new arm F (F1–F13) of
      `test\win32\session-relaunch-notify.ps1`, ALL PASS (91). Also verified
      live outside the harness: a hermetic debug instance recorded
      `"fg_cmd":"ping  -n 500 127.0.0.1"` into `sessions.json` within one
      10s tick. (First scripted run failed F5/F10/F11 with leftover zig-out
      agents from earlier suites contending for the shared
      `ghoztty-agent-debug` pipe; after a path-filtered kill the rerun was
      fully green — the shared-pipe hazard is environmental, not a product
      defect.)
- [x] Floor: `zig build test -Dapp-runtime=none`, `-Dapp-runtime=win32`,
      `zig build test-agent` (rerun on final source) all PASS via
      `floor-lane.ps1`; P1–P3 ALL PASS (2026-08-06).

## Summary

**Split out of T425 (2026-08-03), which carried two symptoms of the user's one
report:** *"I expected 'Previous prompt:' to be preserved and the last known CWD
to be restored in the shell."* T425 said to split the moment the two stopped
sharing a cause. They did — measured on box, they are different mechanisms:

- **cwd** WAS recorded and was merely never refreshed after spawn → **T425**
  (fixed there).
- **the command label** was never recorded at all, for any session, ever →
  this task.

So the "previous command:" line is absent from the restart notice not because
the notice is broken but because there is nothing to print.

## Evidence (2026-08-03, from the box, ✓ VERIFIED by reading both)

The live release agent's `%LOCALAPPDATA%\ghoztty\local-agent\sessions.json`, all
four persistent sessions — **not one carries an `argv` key**, while every one
carries a `cwd`:

```json
{"version":1,"sessions":[
 {"id":"da392fc6…","cwd":"D:\\git\\ghoztty\\","pinned":true,"created_ms":1785781070609},
 {"id":"78e2a8ff…","cwd":"C:\\Users\\David","pinned":true,"created_ms":1785773925039},
 {"id":"1c514abd…","cwd":"D:\\git\\ghoztty\\","pinned":true,"created_ms":1785773924937},
 {"id":"d30c78af…","cwd":"D:\\git\\ghoztty","pinned":true,"created_ms":1785673742135}]}
```

`ghoztty +sessions` agrees — every row ends at `cwd=`, with no command:

```
da392fc64a9dbdf431a96e1ce02ceee2  alive  attached  idle  pid=2704   pinned  cwd=D:\git\ghoztty\
78e2a8fff9588922734bf0341afc23bc  alive  attached  idle  pid=36468  pinned  cwd=C:\Users\David
```

## Root cause

`server.zig:750-754` records the label from the REQUEST, and only from two of
its fields:

```zig
if (open.command) |cmd| {
    s.setArgv(cmd);
} else if (open.shell) |sh| {
    s.setArgv(sh);
}
```

A plain local GUI pane on Windows sends **neither**. `Surface.zig:1078` passes
`.shell = rb.shell`, which is null when the user has not configured one, and
`.command = remote_command`, null for an interactive pane. The agent then
resolves the shell **itself** — `resolveShellPath` (`pty_child.zig:992`):
`OPEN.shell` → `%COMSPEC%` → `C:\Windows\System32\cmd.exe` — and that resolved
value is never handed back to the session record. `Spawner.Result`
(`server.zig:159`) reports `child`, `pid` and `tty`; it does not report what was
actually spawned.

`OPEN.argv` is not a way in either: it is POSIX-only by design
(`pty_child.zig:1026`, "a Windows agent never receives it"), and on this box it
was null anyway — the log line is `local shell integration injected shell=zsh`,
which is `Surface.zig:659`, the **env-only** branch, not the argv-rewrite branch
at `:686`.

## The design question this task must answer FIRST

Recording the resolved shell is the small fix, and it may be the wrong one.
It would make the notice read:

```
    previous command: C:\WINDOWS\system32\cmd.exe
```

That is *true* and close to useless — arguably worse than the current silence,
because it asserts a command was running when the pane was simply a shell. What
the user was actually looking at was `claude`, running INSIDE that shell.

So decide between:

1. **Record the resolved shell** (cheap: add a field to `Spawner.Result`, extend
   the `open.command → open.shell → resolved` chain in `handleOpen`; no wire
   change, agent-internal only). Honest, low value, possible noise.
2. **Record the live FOREGROUND command** — what the user means by "previous
   prompt". The agent already samples the foreground pid every second
   (`session.zig:1050 sampleForegroundPids`, `s.fg_pid`) and already walks the
   process table for `PROC_LIST` (`proc.zig`), so the command line is reachable.
   This is the version with real value and the version that costs more: it needs
   a **new session field** (do NOT overload `argv` — `handleRelaunch`
   `server.zig:1381` re-executes `argv`, so overwriting it with a sampled
   foreground command would make `session-relaunch = auto` run `claude` instead
   of the shell), a new optional field on the DEAD `ATTACHED` reply, and a
   `sessions.json` field.
3. **Both**: the foreground command when known, the resolved shell as the
   fallback, with the notice wording distinguishing them (settle the wording in
   **T424**, which is already rewording this surface — do not reword it twice).

Option 2/3 crosses the app↔agent wire, so it is governed by CLAUDE.md's
**agent contract** rule: evolve additively, gate on the negotiated HELLO
capability, and degrade to today's behavior (no line at all) against a peer that
does not advertise it — an old agent talking to a new app is the normal case
right after an upgrade, which is *exactly* when this notice fires.

## Resolution (2026-08-06)

**Option 2, pure — the notice names the live FOREGROUND command, and never the
resolved shell** (decision receipt: D21). The shell path is technically true
and useless, and printing it for a pane that sat at an idle prompt asserts a
command was running when none was — the task's own analysis. `argv` keeps its
exact relaunch semantics.

What landed:

- `src/os/process_cwd.zig`: `cmdlineFromHandle`/`cmdlineFromPid` — the PEB
  walk refactored into shared steps, command line read alongside the cwd.
- `src/remote/agent/foreground.zig` (new): pure `pickForeground` (most recent
  direct child of the shell, console infra excluded) + the Windows Toolhelp
  walk + Linux `/proc/<pid>/cmdline`. Tri-state contract: `.cmd` record /
  `.none` clear (idle prompt) / null keep (failed query, T425's rule).
- `session.zig`: `Session.fg_cmd` (separate from `argv` on purpose —
  `handleRelaunch` re-executes `argv`), optional
  `Child.queryForegroundCommand` vtable hook, and
  `refreshForegroundCommands` on the same 10s tick as the T425 cwd refresh,
  persisting on change.
- `session_meta.zig`: additive `fg_cmd` record field (round-trips, elided
  when null).
- `protocol.zig`: additive `Attached.foreground_cmd` on the dead reply —
  the `argv`/`tty` precedent, no capability gate; both skew directions
  degrade to today's behavior.
- `server.zig`: dead ATTACHED carries it; a successful RELAUNCH clears the
  stale sample (it described the old child's world).
- `pty_child.zig`: the vtable arm — Windows walks the ConPTY shell's
  children; POSIX uses `tcgetpgrp` (shell in front ⇒ `.none`), Linux reads
  procfs, macOS returns null (deferred to **T544**, seat: mac).
- `connection.zig` + `Remote.zig`: `AttachOutcome.fg_cmd`, and the notice
  prefers `fg_cmd orelse argv`.
- E2E: arm F of `test\win32\session-relaunch-notify.ps1` — a TYPED command
  is sampled into `sessions.json` and named by the restored pane's notice in
  both carriers, and is not re-executed.

Follow-ups filed: **T544** (macOS sampler arm), **T545** (surface `fg_cmd` in
`+sessions`/LIST_SESSIONS rosters).

## Not a bug in the notice module

`session_notice.format` (`src/termio/session_notice.zig:50`) already does the
right thing with what it is given, and its unit tests already cover both arms —
`test "format: no recorded command omits the command line entirely"` pins the
current, correct behavior for a null label. The gap is entirely upstream.

## Validation

- Agent unit tests (`zig build test-agent`): a session opened with no
  `command`/`shell` still ends up with a non-null label in `sessions.json`.
- `test\win32\session-relaunch-notify.ps1` extended: a pane put through an agent
  restart comes back with the notice **naming** something, asserted in both
  carriers (the in-stream text and the sticky banner — see T423/T422 for why
  there are two).
- If option 2/3 is chosen: a skew test proving an app that asks a label-less
  older agent still gets a clean notice with no command line, rather than an
  empty `previous command:` (`sanitizedCommand` already treats whitespace-only
  as absent — keep that property).

## Related

- **T425** — the cwd half of the same user report (recorded, but never
  refreshed). Fixed by a periodic refresh from the live child.
- **T424** — the wording of this same notice; owns the "previous command" vs.
  "previous prompt" label question.
- **T423** — where the notice is rendered (inline scrollback vs. banner).

## Progress log

- 2026-08-06 20:29: claimed; work starting.
- 2026-08-06 20:36 [session session_01VFBSWtYbFH16MrDNetoTmt]: Design settled: option 2 (record the live FOREGROUND command in a new session field fg_cmd; no resolved-shell fallback - the shell path is noise per the task's own analysis, and argv keeps its relaunch semantics untouched). Mechanism: new optional Child vtable queryForegroundCommand (expensive-ok, called OUTSIDE the store lock, T425 refreshCwds shape); Windows = Toolhelp direct-child walk of the shell pid + PEB CommandLine read (same technique as process_cwd.zig); POSIX = tcgetpgrp + /proc cmdline on Linux, macOS arm deferred to a seat:mac task. Persist fg_cmd in sessions.json (additive), send on dead ATTACHED as foreground_cmd (additive/optional, argv/tty precedent, no capability gate), app prefers fg_cmd orelse argv for the notice. Filing a decision receipt.
- 2026-08-06 20:51 [session session_01VFBSWtYbFH16MrDNetoTmt]: Built: process_cwd.zig gains cmdlineFromHandle/FromPid (shared PEB walk); new agent/foreground.zig (pure pickForeground + Windows Toolhelp direct-child walk + Linux /proc cmdline); session.zig fg_cmd field + refreshForegroundCommands on the 10s tick (record/clear/keep tri-state) + sessions.json persistence; Attached.foreground_cmd additive wire field; connection.zig AttachOutcome.fg_cmd; Remote.zig notice prefers fg_cmd orelse argv; relaunch clears the stale sample. Unit tests in all touched modules; E2E arm F added to session-relaunch-notify.ps1 (typed command named by the notice). Filed T544 (seat:mac arm) + T545 (+sessions surfacing) + decision D21. Floor lanes running.

### Decision D21 - Should the restart notice name the foreground command, the resolved shell, or both?

Resolved 2026-08-07T14:14:30Z : Name the live foreground command only (Recommended)
