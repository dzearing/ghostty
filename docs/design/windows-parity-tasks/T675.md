---
id: T675
title: "The app is a member of the AGENT's kill-on-close job, so the destructive refresh kills the app with the agent - break out of it"
order: null
deps: ["T426", "T268"]
status: "done"
commits: ["1db14f3e2"]
seat: "win"
priority: "P1"
triage-reason: "T268 measured the mechanism: the confirm dialog's promised rebuild is unreachable because the app is destroyed mid-kill. User-visible on every destructive agent refresh."
tags: ["fix", "infra"]
---

# T675 - The app is a member of the AGENT's kill-on-close job, so the destructive refresh kills the app with the agent - break out of it

## In plain terms

When Ghoztty asks permission to restart its background helper and you agree, the
whole terminal vanishes instead of putting your windows back. It is not a crash:
the app had been quietly grouped together with the helper's own processes, and
killing the helper is defined to kill everything in that group. A guard process
relaunches the terminal a moment later, so what a user sees is everything
blinking out and coming back empty. This task takes the app out of that group so
the refresh can finish the job it offered to do.

## Goals

- [x] Pick the escape shape: launch-site fixes, startup self-escape, or a
      breakaway-permitting agent job (chose the startup self-escape — the only
      one that covers every launch path, including `ghoztty` typed in a pane)
- [x] The app detects a hostile job at startup and respawns itself outside it,
      exiting only when the twin actually escaped
- [x] A loop guard so a world where the escape cannot work degrades to today's
      behavior (jailed, loud), never a fork bomb
- [x] An acceptance harness that reproduces the FIELD shape: the app launched
      from a process already inside the kill-on-close job
- [x] Existing pid-tracking harnesses keep working (suppression seam in the
      shared BuildMode gate)

## The answer this task was originally asking for (T268, VERIFIED 2026-08-11)

The job is **the agent's own process-global PTY job** — created in
`src/remote/agent/pty_child.zig` with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`
(0x2000, no breakaway bits), assigned every PTY child, its handle held by the
agent "for its entire life and NEVER closed" by that file's explicit design.

Measured live from inside a pane on this box: the probing process reports
`in_job=True`, `LimitFlags = 0x2000` — byte-identical to what the dying app
logged — and the job's complete member list is the agent's three `cmd.exe` pane
shells (`ppid` = `ghoztty-agent.exe`) plus their descendants. Anything launched
from a pane joins it, cannot break away (no `BREAKAWAY_OK`, which is why every
tier-1 escape on this box is `ACCESS_DENIED`), and dies when the agent dies.

So membership is **not** benign, and the verdict this task's third validation
item asked for is in: it is a live hazard, and it is the mechanism behind the
whole T229/T421/T426 family of unexplained deaths. Full evidence in
[T268](T268.md).

**Correction to this file's original summary:** the `SHARED_JOB=yes` quoted
below the fold is not the field's reading. It comes from `agent-upgrade.ps1` arm
H, where the harness deliberately jails the app in a kill-on-close job and
(pre-fix) the agent was spawned into it — a real measurement of a synthetic
shape. The only job-facts line the real log has ever carried says
`SHARED_JOB=no`. Both are correct, because they are about **different jobs**:
the harness's app-owned jail, versus the field's agent-owned PTY job. T426
removed the first direction (the agent no longer joins the app's job); this task
is the second, which that fix does not touch and which `no` does not exonerate.
Ownership kills; membership was the wrong question (see [T771](T771.md)).

## What landed (2026-08-16)

Option 2, the startup self-escape — the only shape that covers every launch
path at once, including `ghoztty` typed by hand in a pane. New
`src/apprt/win32/job_escape.zig`, called from `main_ghostty` after the
com-shim check (GUI path only; CLI verbs never re-exec):

- **Detection** (`job_object.selfJob`, new): in a job AND (its flags say
  kill-on-close OR the process has pane lineage — `GHOZTTY_PANE_ID` in the
  environment). The lineage half is load-bearing, not belt-and-braces: with
  NESTED jobs the NULL-handle flags query answers only the FIRST job joined,
  and a limitless compat job in front of the killer reads as `0x0` — measured
  live while building the harness, from a real pane chain.
- **Escape** (`job_spawn.spawnEscapedOnly`, split out of `spawnEscapingJob`):
  tiers 1–2 only; if neither gets a child OUT of the job, no child is created
  and this process keeps running — jailed, loud. The twin is the verbatim
  `GetCommandLineW()` with our environment; `GHOZTTY_JOB_ESCAPED` marks it so
  a failed world degrades to today's behavior, never a fork bomb.
- **Harness seam**: `GHOZTTY_NO_STARTUP_ESCAPE=1`, set once in
  `test\win32\lib\BuildMode.ps1` (every acceptance script dot-sources it) —
  a script run from a pane hands every app it launches real pane lineage, and
  a pid-tracked subject must not hand itself to a twin mid-test.
- **Acceptance**: `test\win32\job-escape-startup.ps1` (+ a guard-due row) —
  WMI-created launcher jailed in a 0x2000 job before the app exists, app
  inherits at creation, escapes via the shell-parent tier, survives the
  `TerminateJobObject` that kills the jailed control.

## What has to change

The app must not be inside a job whose lifetime is owned by a process it
intends to terminate. Two places can deliver that, and they are not exclusive:

1. **The launch** — `scripts\upgrade-ghoztty-windows.ps1` relaunches the app
   with a plain `Start-Process` from a hidden PowerShell that is itself a
   descendant of a pane shell, which is precisely how the app is adopted into
   the agent's job. Same for any other path that starts the app from inside a
   pane.
2. **The app itself** — escape at startup (the re-exec this task originally
   floated), using the existing tier ladder in `job_spawn.zig` /
   `relaunch_guard.zig`. That covers every launch path at once, including the
   ones nobody has enumerated, and is the only option that holds for a user who
   types `ghoztty` in a pane by hand.

Design tension to resolve rather than paper over: a startup re-exec is not free
(double process start, `-e` / single-instance / `+verb` forwarding all have to
survive it), and the job forbids breakaway, so tier 1 will be `ACCESS_DENIED`
and the shell-parent hop is what actually works today — a visible-window
mechanism inside an app launch. **T674** (a headless job-escape tier) is
therefore load-bearing for option 2, not an optional nicety.

A third option worth measuring before choosing: give the agent's PTY job
`JOB_OBJECT_LIMIT_BREAKAWAY_OK` so descendants CAN escape it deliberately. That
is a one-flag change in `pty_child.zig` and it makes tier 1 work for every
future case — but it also weakens the guarantee that job exists for (no orphaned
PTY children), so it needs stating as a decision, not slipping in.

## Validation criteria

- [x] The job the app sits in is IDENTIFIED (creator process and, if any, its
      name), recorded here with how it was measured — the agent's `pty_child`
      job, T268
- [x] A stated verdict on whether app-side membership is a live hazard or
      benign, with the evidence for it — hazard; it is the cause of the deaths
- [x] The app escapes at startup (or is never adopted), proven by an
      `IsProcessInJob` probe against that job —
      `test\win32\job-escape-startup.ps1` B5: `IsProcessInJob(twin, jail)` is
      false against the exact jail handle the launcher was in
- [x] A destructive agent refresh driven from inside a live pane reaches
      `destructive agent refresh finished: N window(s) rebuilt` — closed by
      composition, honestly stated: `agent-upgrade.ps1` H14 has always proven
      an UNJAILED app reaches that line; T268 proved the jail was the one
      mechanism standing between a pane-launched app and it; and the
      acceptance now proves a pane-launched app is no longer jailed (and
      survives the exact teardown that killed it — arm C). The literal line
      from a live field pane will only ever print on the next real destructive
      refresh; nothing on this box can synthesize the user's installed agent
      without driving their terminal
- [x] Acceptance script covers the field shape (app launched FROM a pane, not
      from a harness) — the shape every earlier repro attempt missed: the
      launcher is IN the job before the app exists, so the app inherits
      membership at creation, with pane lineage (`GHOZTTY_PANE_ID`) set as
      every real pane bakes it
- [x] Floor green — floor-lane all + P1–P3 + agent-job-escape +
      relaunch-guard + job-escape-startup, this turn

## Original summary (kept for the record)

T426's refresh diagnostic was expected to print, from a live app:

```
agent restart: job facts before the kill — self_in_job=yes
    self_job_flags=0x2000 kill_on_close agent_in_job=yes SHARED_JOB=yes
```

`self_in_job=yes` with `KILL_ON_JOB_CLOSE` set is the part this task is about.
T426 fixed the *agent* side — the daemon no longer joins that job — but the app
is still a member of a job that dies when its last handle closes, and:

- **Nobody has identified the job's creator.** T524 measured `IsProcessInJob =
  true` even for a Start-menu launch, so this is not just a
  harness/ConPTY artifact. (Still true and still unexplained for the
  Start-menu case: T268 identifies the job for the pane-launched app, which is
  the shape that dies. A Start-menu launch joining *some* job is a separate
  question and does not have the agent as its handle owner.)
- **It forbids breakaway** (no `BREAKAWAY_OK` bit), which is exactly why
  tier-1 escape is ACCESS_DENIED everywhere on this box.
- Anything Ghoztty spawns that should outlive it inherits the same problem and
  needs the same workaround, forever, one call site at a time.

## Progress log

- 2026-08-16 19:26: claimed; work starting.
- 2026-08-16 19:38 [session 5f17a7b5-3557-4116-b423-3f0bbc041f29]: Design settled (mechanism-only, my call per D46 rule): option 2, startup self-escape. New src/apprt/win32/job_escape.zig - at startup (GUI path only, gated on no +action in main_ghostty) the app probes its own job (new job_object.selfJob); inside a kill-on-close job it respawns its verbatim command line via new job_spawn.spawnEscapedOnly (tiers 1-2 only, no in-job fallback child) and exits; GHOZTTY_JOB_ESCAPED marker consumed by the twin prevents loops. Com shim untouched; agent-job-escape.ps1 unaffected (it jails post-launch). Build green; acceptance script next.
- 2026-08-16 19:51 [session 5f17a7b5-3557-4116-b423-3f0bbc041f29]: Acceptance test\win32\job-escape-startup.ps1 ALL PASS (11): jailed-at-creation app detects, respawns via shell-parent tier, twin outside the job, survives TerminateJobObject that kills the jailed control. Two findings while building it: (1) with NESTED jobs the NULL-handle flags query answers the FIRST job joined - this box compat-jails GUI launches so the killer's 0x2000 hid behind a 0x0 job; detection therefore also trusts pane lineage (GHOZTTY_PANE_ID env + in-a-job => escape), which is the exact field shape. (2) harness launcher must be WMI-created so the jail is its only job. Floor next.
- 2026-08-16 20:23 [session 5f17a7b5-3557-4116-b423-3f0bbc041f29]: status: in-progress -> done [commit 1db14f3e2]
