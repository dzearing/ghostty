---
id: T1022
title: "Starting Ghoztty again should open a window in the copy already running, not a second app (D79's answer)"
order: 2
deps: ["T851"]
status: "done"
commits: []
seat: "win"
priority: "P1"
milestone: "M1"
triage-reason: "P1, M1: the user answered D79 on 2026-08-19 and chose the joining behavior; today the app still does the opposite, and the answer landed on a task that was already closed, so nothing was queued to build it."
tags: ["feature"]
---

# T1022 - Starting Ghoztty again should open a window in the copy already running, not a second app (D79's answer)

## In plain terms

Right now, starting Ghoztty when it is already running gives you a second,
separate app: two tray icons, two apps quietly sharing one session list and one
saved layout, and closing "Ghoztty" only closes half your terminals. The user
was asked about this (D79) and chose the other behavior: starting it again
should just open another window in the app you already have, the way it works
on a Mac and the way Windows Terminal behaves. This task builds that.

## Summary

T851 fixed the theft — a second launch no longer takes the first app's
terminals away and freezes them — but deliberately left the two copies
independent, and filed D79 for the wider call. The user answered on
2026-08-19 with option **o1**: *"Starting Ghoztty again opens a new window in
the app you already have (Recommended)"*. The answer was folded into T851,
which was already `done`, so no open task carries it. That is the gap this
file closes.

What the answer requires:

- A **single-instance handoff on launch**: a second launch of the same build
  finds the running instance, asks it to open a window (honouring the launch's
  own arguments — working directory, `--command`, session/restore flags), and
  exits without creating a second app, tray icon, or session-manager client.
- **Same-build only**, which is the mitigation the chosen option carries
  verbatim: two *different* builds must still start independently, because
  side-by-side testing of a debug `zig-out` build against the installed
  release is how this project is developed. The instance identity therefore
  has to key on the same lineage the IPC/agent endpoints already derive
  (build mode + install lineage), not on "an exe named ghoztty.exe is
  running".
- **The shortcut-looks-wrong case**, named as a con on the chosen option: a
  shortcut pointing at the portable copy will open a window from whichever
  same-build copy is already running. Whatever the app does here, it should
  not be silent about it.

Where it lives: the launch path in `src/apprt/win32/` that already resolves the
per-user IPC endpoint, plus the restore/attach gate T851 added.

## Goals

- [x] A second launch of the SAME build opens a window in the running instance
      and exits, with no second tray icon and no second session-manager client
- [x] The second launch's arguments (cwd, command, target flags) are honoured
      by the window that opens
- [x] A launch of a DIFFERENT build still starts its own independent app
- [x] An acceptance script in `test/win32/` covers both arms, with a
      `guard-due` row so it cannot drift
- [x] A same-lineage launch of a different build is not SILENT about whose
      window you got (D79's remaining con)

## Validation criteria

- [x] Launch the debug build twice: the second invocation exits, one process
      remains, and a new window appears in the first (measured by `+list --json`
      instance/window counts, not by eye) — `test\win32\single-instance-join.ps1`
      section A: A2/A5 count app processes by image path, A4 counts windows over
      the pipe, A6 pins it to the SAME pid
- [x] With the debug build running, launch a different lineage: two independent
      apps, both usable — the side-by-side development case. Section B, staged
      with two `GHOZTTY_PIPE_SUFFIX` values rather than by launching the user's
      installed release, which would open a window in the terminal they are
      sitting in (the same isolation rule every script here follows)
- [x] `+list --json` shows a single instance across both launches in the
      same-build arm — A3/A7 (`version` over the pipe answers one pid both times)
- [x] The second launch's cwd and `-e` command reach the window that opens —
      A8/A9, the end-to-end half T487 only had unit tests for
- [x] A build-mismatched handoff answers with a caveat and a balloon; a matching
      one says nothing — section C (C2–C9)
- [x] Floor lanes and P1–P3 stay green; the new acceptance script is ALL PASS
      and stamped

## Progress log

- 2026-08-20 07:20: filed during daily triage. D79 resolved 2026-08-19T22:44Z
  choosing o1; its linked task T851 was already `done`, so the answer had no
  open work item. Deps on T851 for the attach-gate context.
- 2026-08-20 09:10: claimed; work starting.
- 2026-08-20 09:34: measured first: the single-instance JOIN already works (App.init AlreadyRunning forwards new-window and exits; two launches under one GHOZTTY_PIPE_SUFFIX left one process and a new window). What was missing is D79's named con: a same-lineage launch of a DIFFERENT build joins silently. Building the handoff identity (os/ipc_handoff.zig, additive 'handoff' field on the IPC request) + a note/balloon when the builds differ.
- 2026-08-20 09:59: status: in-progress -> done
