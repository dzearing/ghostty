---
id: T185
title: "Windows +list/window-inherit report a pane\u0027s INITIAL cwd, not its live cwd (no OSC-7 from cmd.exe/powershell)"
order: 102
deps: []
status: "done"
priority: "P1"
triage-reason: "parity gap: no OSC 7 from cmd/powershell, so +list and ctrl+n report the pane's starting cwd forever"
tags: [fix]
commits: []
---

# T185 - Windows +list/window-inherit report a pane's INITIAL cwd, not its live cwd (no OSC-7 from cmd.exe/powershell)

## In plain terms

When you change directory inside a terminal pane, Ghoztty should know where
you are now: new windows and tabs should open in that directory, and tools
that ask "where is this pane?" should get the current answer. On Windows,
panes running the default shell (cmd) answer with the directory the pane
*started* in, forever — so pressing ctrl+n after moving into a project still
lands you back where the pane first opened, and automation reading the pane's
location gets a stale path. After this lands, both follow you as you `cd`.

## Goals

- [x] `ghoztty +list --json` reports a cmd pane's CURRENT directory after a `cd`
      (new-window-cwd.ps1 F4)
- [x] ctrl+n (and new tab/split) from a cmd pane opens in the directory the user
      is in now, not the starting one (F11/F12; tab/split share the same
      `newConfig` seam)
- [x] PowerShell panes keep their already-working live reporting: the fallback
      stands down the moment a real OSC 7 arrives (`pwd_reported`), and before
      one arrives it reports the same starting directory the seed did — no
      behavior change possible by construction
- [x] No new locks or RPCs in the `+list` hot path (T111b rule): the fallback
      is two bounded `ReadProcessMemory` calls on the shell pid, no ghoztty
      lock, no agent RPC

## Validation criteria

- [x] Unit tests for the process-cwd read module (own-process round-trip +
      failure paths): `src/os/process_cwd.zig` tests, green via
      `floor-lane.ps1 -Lane none -Filter fromPid` (PASS in 21s)
- [x] Acceptance: open a cmd pane in dir A, `cd` to dir B, `+list --json`
      reports B — `test\win32\new-window-cwd.ps1` section F (F3/F4), ALL PASS
      on the box 2026-08-06
- [x] Acceptance: ctrl+n from that pane opens the new window in dir B — same
      script, F11 (+list) and F12 (the shell itself agrees)
- [x] Floor: `floor-lane.ps1 -Lane all` → none/win32/agent all PASS
      (2026-08-06 03:45), then P1/P2/P3 sequentially → ALL PASS each

## Summary

Found while fixing **T144**. `Surface.pwd()` returns
`self.io.terminal.getPwd()` — the value set by an **OSC 7** report from the
shell, seeded once at bring-up from the pane's starting directory
(`termio/Remote.zig` `t.setPwd(cwd)`, and the exec equivalent). On macOS,
ghostty's shell integration makes zsh/bash emit OSC 7 on every `cd`, so the
value tracks the user. On Windows the default panes are `cmd.exe` /
`powershell.exe` with **no shell integration**, so the pane's reported pwd is
frozen at whatever it started in, forever.

Two user-visible consequences:

1. `ghoztty +list --json`'s `working_directory` is a *starting* directory, not
   a current one. Any agent or script that asks "where is this pane?" gets a
   stale answer the moment the user `cd`s.
2. `window-inherit-working-directory` / `tab-` / `split-` inherit that same
   stale value. T144 made a new window inherit the focused pane's directory
   instead of the agent's, which is the big win — but "the directory you were
   just in" still means "the directory that pane opened in". A user who
   `cd`s into a project and presses ctrl+n does not land in the project.

Note the machinery for the right answer already exists and is used elsewhere:
the agent's `GET_CWD` (`protocol.GetCwd` → `child.queryCwd`) reads the child's
real cwd on demand, and `Window.buildRemoteInherit` already calls
`queryCwdTimeout` for tabs/splits on a remote window. So the shapes to consider
are (a) route `pwd()` through a bounded `GET_CWD` for agent-backed local panes
(and something equivalent for exec panes — `NtQueryInformationProcess` on the
child, the same PEB read T144's harness uses), and/or (b) ship PowerShell/cmd
shell-integration that emits OSC 7, which is the Mac-parity answer and also
fixes the title.

Validation must be measured, not argued: open a pane, `cd` somewhere else, and
assert `+list --json` reports the NEW directory and that ctrl+n lands there.
`test/win32/new-window-cwd.ps1` is the natural home (its section D already
presses the real chord and reads a pane's actual shell cwd with `cd`).

## Progress log

- 2026-08-06 03:24: claimed; work starting.
- 2026-08-06 03:29 [session 1352a96f-9d4b-4c05-a207-8fdc0f6a4646]: Design settled: pwsh is already live via T27 OSC-7 integration; the gap is cmd.exe (no prompt hook) and any non-integrated shell. Fix: (1) new pwd_reported flag on win32 Surface, set only by the .pwd action (real OSC 7); (2) shared PEB-read module (factored from pty_child queryCwdWindows) with a pid wrapper; (3) when pwd_reported is false, +list and the newConfig inherit seam read the shell process's real cwd (cmd/bash SetCurrentDirectory on cd, so it tracks live). Config working-directory override also feeds the agent OPEN (T144 seam), so exec+agent panes both fixed.
- 2026-08-06 03:36 [session 1352a96f-9d4b-4c05-a207-8fdc0f6a4646]: Built: os/process_cwd.zig (shared PEB read, pty_child now delegates), pwd_reported flag on win32 Surface (set only by the .pwd action = real OSC 7), livePwd() fallback wired into +list and the newConfig inherit seam. Unit tests green (none lane, filtered). Acceptance: new-window-cwd.ps1 extended with section F (cd tracking + live ctrl+n inherit) - ALL PASS incl. old sections A-E. Floor lanes next.
- 2026-08-06 03:47 [session 1352a96f-9d4b-4c05-a207-8fdc0f6a4646]: Validation complete: floor-lane -Lane all = none/win32/agent PASS; P1/P2/P3 sequential ALL PASS; new-window-cwd.ps1 A-F ALL PASS (F is the T185 section). Filed T511 (Mac half: live process-cwd fallback for non-integrated shells, seat mac) and T512 (cmd PROMPT \ integration for title/prompt marks). Closing as done.
