---
id: T178
title: "remote-inherit.ps1: the remote-native --command split no longer reaches the agent (4 reds, pre-existing)"
order: 34
deps: []
status: "done"
priority: "P0"
tags: [fix, test]
triage-reason: "A remote --command runs but its output never reaches the pane - measured output loss at the attach/replay seam, 4 reds."
commits: []
---

# T178 - remote-inherit.ps1: the remote-native --command split no longer reaches the agent (4 reds, pre-existing)

## In plain terms

When you open a new pane on a remote machine and tell it to run a specific
command, the command actually runs — but everything it printed is thrown away,
so the pane just says "Process exited" with no output at all. Anyone using a
quick remote command (to check a file, print a status, run a one-liner) sees a
blank pane and can't tell whether the command worked. When this lands, the
output of even a very fast remote command shows up in the pane, every time.

## Goals

- [x] A remote `--command` split shows the command's output in the pane, even when the command finishes in milliseconds (echo exits in ~6ms and its marker lands — ALL PASS ×3 on HEAD)
- [x] All four red assertions in `test/win32/remote-inherit.ps1` pass again
- [x] The fix is understood at the real seam: there was no product defect — the 2026-07-31 red was harness contamination (T248/T267/endpoint isolation), and the client seam is guarded by the T06c prebuffer (measured by A/B against the proven-red build, see Details)

## Validation criteria

- [x] `test/win32/remote-inherit.ps1` ALL PASS x3 — three sequential full runs on 2026-08-06 against a fresh Debug build of HEAD
- [x] `zig build test` (none + win32 lanes) and `zig build test-agent` green via floor-lane.ps1 — `floor-lane.ps1 -Lane all`: none PASS 159s, win32 PASS 179s, agent PASS 190s
- [x] P1-P3 acceptance scripts ALL PASS — run sequentially after the lanes, each ends `ALL PASS`
- [x] `test/win32/host-settings.ps1` section B (same remote split path) ALL PASS — full script `ALL PASS (65 assertions)`

## Summary

`test/win32/remote-inherit.ps1` (the T68 remote-inheritance acceptance, last
recorded ALL PASS on 2026-07-18) is RED with 4 failures, and the root failure is
one: section `== 3`'s `+split --target=rem "--command=echo t68-split-marker"`
exits 0 but the marker never appears in the new pane. Sections 4 and 5 fail only
because they look for that same marker downstream (ctrl+t is expected to re-run
the parent pane's command; `+new-window --from-focused` likewise), so fixing
section 3 should clear all four.

**Proven pre-existing, not a T174 regression** (2026-07-30): a worktree built
from HEAD `f1f973b88` with no working-tree changes fails the identical four
assertions, and T174's only touch on this path (`buildRemoteInherit` now sets
`.shell` from the per-host defaults store) is a byte-for-byte no-op when no
store file exists — `Overrides.Remote.shell` already defaulted to null. Nothing
between 2026-07-18 and now re-ran this script: the standing floor is both test
lanes + `test-agent` + P1-P3, and remote-inherit is not in it.

Failing assertions, verbatim:

```
== 3: +split --target with a remote-native --command stays remote
  PASS split --target exit 0
  FAIL explicit command ran through the agent
== 4: ctrl+t - the new-tab keybind inherits the active pane's command
  FAIL new tab pane appeared
  FAIL new tab pane identified
== 5: +new-window --from-focused dials the SAME agent
  FAIL new window inherited the remote command
```

Everything else in the script passes, including section 2's live-cwd GET_CWD
inheritance and section 6/7's local-fallthrough and dead-agent paths — so the
remote transport, the split path, and the agent are all working; it is
specifically the explicit remote-native COMMAND that goes missing.

## Details

### 2026-07-31 (T217 batch 3): re-measured, and it narrows to OUTPUT delivery

Reproduced on the background test desktop while migrating the script (same
three marker assertions; the old fourth, `new tab pane appeared`, now PASSES
because the harness lands ctrl+t reliably where the old foreground grab raced).
The app's own stderr and the pane contents say what the earlier guesses could
not:

```
warning(surface): surface child exited exit_code=0 runtime=6ms wait_after_command=true
+read --name=remsplit  ->  "Process exited. Press any key to close the terminal."
```

So, measured rather than supposed:

- **The command IS forwarded and DOES run.** A child appeared, ran for 6ms and
  exited 0. An interactive `cmd.exe` with no command never exits - hypothesis
  3 ("not forwarded at all") is OUT.
- **The pane is NOT dying early.** `wait_after_command=true` is already set on
  this path and the pane is held open showing the exit notice - hypothesis 1
  ("pane exits before +read sees it") is OUT.
- **What is missing is the command's OUTPUT.** The terminal holds exactly one
  line, the app's own child-exited notice; `t68-split-marker` never lands in
  the screen or the scrollback.

That points the search at the attach/replay seam for a SHORT-LIVED remote
command: the child writes and exits within ~6ms, plausibly before the app has
attached to that session's output stream, and whatever initial replay the
attach performs is not carrying the pre-attach bytes. Look at the agent's
per-session output ring and what an OPEN-then-ATTACH replays (versus a
re-attach, which does gap-fill and is known to work), rather than at argv
conventions. A command that takes a second to run would likely pass, which is
also a cheap way to confirm the race: re-run section 3 with
`--command="ping -n 3 127.0.0.1 & echo t68-split-marker"` and see whether the
marker appears.

Where to look first (in likelihood order) - written 2026-07-30, and items 1
and 3 are now ruled OUT by the measurement above; kept for the reasoning:

1. **The pane may be exiting before `+read` sees it.** A `--command` OPEN runs
   the command through the agent's shell; if the shell now exits when the
   command finishes, the pane is gone (or its scrollback empty) 3s later, when
   the script reads. That would also explain section 4's "new tab pane appeared"
   failing on a COUNT (the tab's inherited command pane may die immediately
   too). Check whether `waitAfterCommand`/hold semantics regressed — the Mac
   marks an explicit remote command so the agent keeps the session alive
   (`Machine.applyOpenDefaults` sets `waitAfterCommand = true` for exactly this
   reason); win32's `IpcHandlers` remote-split branch sets `.command` with no
   equivalent flag.
2. **The agent's per-shell argv convention for a command OPEN.** `echo
   t68-split-marker` through `cmd /c` prints; through `powershell -Command` it
   also prints. But a shell resolved differently (or an argv joined wrong)
   would silently produce nothing. `pty_child.zig` owns those conventions.
3. **The command may not be forwarded at all.** Confirm on the wire: run the
   loopback agent with logging and check the OPEN's `command` field for the
   split. The T99/T89 series reworked the IPC split's override batons; a
   regression there would drop the command while leaving cwd inheritance
   (section 2) intact — which is exactly the shape of this failure.

Candidate windows for the regression: T99 (IPC splits under the local agent),
T89d/T89f (session-persistence OPEN/ATTACH plumbing), T110/T111a (ring +
lock-cycle changes to the agent path).

### 2026-08-06: closed by measurement — no product defect; the red was the harness

Two experiments, both on this box:

1. **HEAD is green.** `test/win32/remote-inherit.ps1` ALL PASS ×3 against a
   fresh Debug build — all four T178 assertions included, with the same 6ms
   `echo` the red runs measured.
2. **The proven-red build is green too, under the current harness.** Built the
   exact commit the task proved the red against (`f1f973b88`, worktree, Debug)
   and drove it with TODAY'S script. Sections 3 and 5 — the marker
   assertions, i.e. the T178 failure itself — PASS. So the binary never lost
   the output; the OLD harness lost the assertion. What did change since
   2026-07-31 is the harness: T248 (a script could measure the previous run's
   state), T267 (a script's window came from whichever script ran before), and
   the `GHOZTTY_PIPE_SUFFIX` endpoint isolation now at the top of this very
   script (before it, the script's `+verbs` could reach whatever instance
   answered the shared debug pipe).

   The old build DOES still fail section 4 — but on `the window forwarded
   focus to a terminal surface`, a focus-forwarding defect of that old build
   (fixed since; HEAD passes it ×3). Its two downstream ctrl+t assertions fail
   as a cascade. That is a different defect with a different signature, not
   the output loss this task is about.

The seam the task suspected is guarded **by design**: the client's
`ChannelTable` prebuffer (`src/remote/inbound_ring.zig`, T06c) stashes DATA
frames that arrive for a channel before `register` runs and flushes them into
the ring in arrival order on registration — precisely the OPENED→register
window this task hypothesized was dropping a fast command's first bytes. That
guard predates the red measurement (present at `f1f973b88`), which corroborates
the harness verdict.

## Validation

- `test/win32/remote-inherit.ps1` ALL PASS x3.
- Both test lanes + `zig build test-agent` + P1-P3 stay green.
- Re-run `test/win32/host-settings.ps1` (T174) too: its section B drives the
  same remote split path and must stay ALL PASS.

## Progress log

- 2026-08-06 00:31: claimed; work starting.
- 2026-08-06 00:38 [session 321596ee-860e-4353-aa9d-b17a10b4a008]: Repro attempt on HEAD: remote-inherit.ps1 is ALL PASS x3 (all 4 old reds green). Since the 07-31 red, harness-contamination fixes landed (T248 previous-run bleed, T267 wrong-window, T350-family endpoint isolation via GHOZTTY_PIPE_SUFFIX in this script). Now running the CURRENT script against a binary built at the proven-red commit f1f973b88 to decide: real product fix vs harness artifact.
- 2026-08-06 00:42 [session 321596ee-860e-4353-aa9d-b17a10b4a008]: VERDICT (measured): the T178 output loss does not exist as a product defect. A/B: built the proven-red commit f1f973b88 and drove it with the CURRENT harness - sections 3 and 5 (the marker assertions) PASS on the old binary; only section 4 fails there, on 'window forwarded focus to a terminal surface' (a focus bug of that old build, fixed on HEAD). The 07-31 red was harness contamination, fixed by T248 (previous-run bleed), T267 (wrong window), and endpoint isolation (GHOZTTY_PIPE_SUFFIX in the script). The suspected attach/replay seam is guarded by design: ChannelTable's T06c prebuffer stashes DATA that arrives before register and flushes it on register, so a fast command's output is never dropped. HEAD: remote-inherit.ps1 ALL PASS x3. Running the standing floor next.
