---
id: T171
title: "relay-account.ps1 failed once unreproducibly (30 pass / 1 fail, text not captured)"
order: 311
deps: ["T141"]
status: "in-progress"
priority: "P2"
triage-reason: "One unreproducible harness flake with the failure text lost; 5 clean runs since. Keep per-run logs and harden the ports."
tags: ["test", "infra"]
commits: []
---

# T171 - relay-account.ps1 failed once unreproducibly (30 pass / 1 fail, text not captured)

## Summary

Filed so it is not lost, not because the cause is known. During T141's
validation, the FIRST of a three-run loop of `test/win32/relay-account.ps1`
reported `1 FAILURE(S) (pass=30 fail=0... fail=1)` while runs 2 and 3 were
ALL PASS (53). Two more back-to-back runs and a fresh ×3 loop afterwards were
all ALL PASS (53) — **5 consecutive clean runs**, so the shipped state is green
and this is a flake, not a standing red.

The failure text was **not captured**: that loop only printed the last line plus
pass/fail counts. That is the real lesson and the first thing to fix — a
per-run log is worth more than a tidy summary line. Runs after it kept per-run
logs and have shown nothing.

Note the count: 30 passes, i.e. the run stopped producing assertions partway.
A full run is 53. Sections 0+1 are 8 assertions and section 2/3 adds ~22, so 30
lands at the END of the GUI section — the run then produced no section 4-7
assertions at all, without printing their SKIP lines. That shape (assertions
stop, no SKIP) does not match any deliberate skip branch, which is what makes it
worth a task rather than a shrug.

Leading hypotheses, none tested:

1. **Port contention between consecutive runs.** The fake relays bind fixed
   ports (47921/47922/47912). A previous run's `Start-Job` listener can still
   hold them for a moment after its host process is killed, so `Wait-Listening`
   can "succeed" against a DYING listener. Cheap hardening regardless of whether
   it is the cause: derive the ports from `$PID` and assert the port is FREE
   before starting, rather than assert something is listening.
2. **A PowerShell error terminated the script mid-section** (`$ErrorActionPreference`
   is `Continue`, so a non-terminating error prints and moves on — but a
   terminating one from `[AcctDrv]` marshalling or `Invoke-WebRequest` would end
   the run silently from the log's point of view).
3. A GUI from the previous run surviving `Stop-TestProcs` and being found by
   `FindTop`, so the chord went to a window with no fake relay behind it.

*Do:* re-run the script in a tight loop (10+) with per-run logs kept, and
whichever hypothesis the failure text implicates, fix the harness. If 10 clean
runs cannot reproduce it, apply hypothesis 1's hardening anyway (it is a real
latent trap independent of this incident) and close the row.

## In plain terms

An automated test that signs in and out of the relay account through the machine
chooser failed once, months ago, and nobody could ever make it fail again --
because the run threw its own logs away and printed only a tidy summary line.
When this lands, that test picks a fresh port for every run instead of three
fixed numbers, refuses to believe a fake server is up until that server answers
with a secret only this run knows, turns a crash inside the script into a named
failure instead of a run that just stops, and keeps every log when anything goes
wrong. Nobody but the people running the test suite notices -- but the next time
it fails, the failure explains itself instead of becoming a task like this one.

## Goals

- [x] Ports are per-run (OS-assigned), never three fixed numbers
- [x] Each port is asserted FREE immediately before it is bound
- [x] "the fake relay is up" means it answered THIS run's nonce, not that a TCP
      connect succeeded
- [x] A terminating error inside the script is a named FAIL with message + line,
      not a silent end of run
- [x] A failing run keeps its temp directory (both GUIs' stderr, every CLI's
      stdout, both hit logs) and prints the path
- [x] 10 consecutive runs ALL PASS, with a log kept per run

## Validation criteria

- [x] 10 consecutive `relay-account.ps1` runs exit 0 / ALL PASS, one retained
      log per run (`%TEMP%\ghoztty-t171-runs\loop-NN.log`)
- [x] The port setup no longer depends on a fixed port being free -- the run
      prints the ports it drew and asserts each is free before binding
- [x] The nonce assertion has teeth: it caught a real defect on its first run
      (the nonce was not being passed into the `Start-Job` argument list, so the
      fake relay answered `{"probe":""}`), which the old bare-TCP check passed
- [x] Floor green: `floor-lane.ps1 -Lane all`, P1-P3

## Progress log

- 2026-08-10 04:16: claimed; work starting.
- 2026-08-10 04:20: hardening landed in test/win32/relay-account.ps1: per-run OS-assigned ports + assert-free-before-bind; fake relay readiness is now a nonce echo (GET /__probe/<nonce>) not a bare TCP connect; a terminating error is an explicit FAIL with message+line; a failing run keeps its temp dir and prints the path. Next: 10-run loop with per-run logs.
- 2026-08-10 05:20: validation: 10/10 consecutive relay-account.ps1 runs ALL PASS (64 assertions each, ~277s each), per-run logs kept at %TEMP%\ghoztty-t171-runs\loop-NN.log. The nonce probe caught a real defect on its first hardened run (nonce never passed into Start-Job -ArgumentList). Filed T694 for the 11 other scripts still binding fixed ports (3 pairs collide).
