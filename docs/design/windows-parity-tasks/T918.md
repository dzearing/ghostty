---
id: T918
title: "relay-account section 8 skip-guard only checks the legacy macos/ client-id path"
order: null
deps: []
status: "done"
commits: ["c1292fb9f"]
seat: "win"
priority: "P1"
triage-reason: "found 2026-08-17 while configuring relay sign-in on the Windows seat; the suite goes red for following the documented setup, and a lying instrument sends the next turn chasing a defect that does not exist"
tags: ["test"]
---

# T918 - relay-account section 8 skip-guard only checks the legacy macos/ client-id path

## Summary

test/win32/relay-account.ps1 section 8 (the T747 unconfigured-build case) guards itself with a Test-Path on 'macos\google-client-id.txt' at line 706 - the LEGACY path only. src/build/Config.zig:580-589 reads repo-root 'google-client-id.txt' FIRST and its own doc comment (Config.zig:575-579) calls the repo-root spelling 'the spelling to use' for exactly the Windows seat this test runs on. So a Windows checkout that follows the documented setup bakes an id in, section 8 does NOT skip, and the suite reports 6 phantom FAILs ('no account control is visible', 'the row says sign-in is not set up', both hint assertions, 'the hint does NOT point at a button', 'clicking the hidden button starts no browser flow') describing a state the build cannot be in. Observed 2026-08-17 on a build with the repo-root file present; the section's own positive control ('CONTROL: with a client id the sign-in button IS visible') PASSED in the same run, which is the tell. Instruments-that-lie class: a green suite is the point of the T747 coverage and this turns it red for doing the right thing. Fix: the guard must test BOTH paths in the same order Config.zig does (repo-root then macos/), i.e. skip when either exists. Better still, make the section not need a skip at all - if the GUI honored a 'force unconfigured' test override (an env var meaning 'resolve no client id') the unconfigured path would be measurable on every seat instead of only on seats that never set one up, which is what let T747 ship in the first place. Validation: with repo-root google-client-id.txt present the section SKIPs (or passes via the override) and relay-account.ps1 reports 0 failures; with both files absent the six T747 assertions still run and still pass.

## In plain terms

One of our own test runs reports six failures that are not real: it checks how
the app behaves when Google sign-in was never set up, but on a machine where
sign-in *has* been set up it looks in the wrong place, decides the setup is
missing, and then complains that the app is behaving as though sign-in works.
After this, the test creates the never-set-up condition on purpose instead of
guessing at it - so it is honest on every machine, and it keeps checking the
thing it was written to check even here, where sign-in is configured.

## Goals

- [x] The test stops reporting failures that describe a state the app cannot be in
- [x] The never-set-up experience is actually exercised on this machine, not skipped
- [x] The check can never drift out of step with how the build finds a client id again

## Validation criteria

- [x] With repo-root `google-client-id.txt` present, `test\win32\relay-account.ps1` reports **0 failures** - the section PASSES via the force-unconfigured knob rather than skipping. Verified on this box (repo-root file present, `macos/` one absent, id baked into the Debug build): full run ends `ALL PASS`, section 8 prints all six formerly-phantom assertions as PASS.
- [x] With BOTH client-id files absent, the six T747 assertions still run and still pass - the coverage is not silently deleted by the fix. Verified by construction and by unit test: the section no longer branches on file presence at all, and `GHOZTTY_RELAY_NO_CLIENT_ID` reaches the same resolver state an unbaked build has (`resolveClientIdFrom: force-unconfigured hides env id AND bake` covers `baked = ""` as well as a baked id, `none` lane).
- [x] The guard consults the same paths, in the same order, as `src/build/Config.zig:580-589`, so the two cannot drift again - satisfied by REMOVAL, which is the stronger form: there is no path check left to drift. The section constructs the unconfigured state instead of inferring it from the checkout, so `Config.zig` can add, reorder or drop client-id files with no effect on this harness.
- [x] Harness audit rules in `docs/claude/testing.md` satisfied (positive control retained) - the configured relaunch still runs and still asserts the button IS visible, and it now doubles as proof that the knob, not a broken launch, is what removed the button. Section count unchanged, no new SKIP branch (the run reports `ALL PASS` with no SKIPPED tail).

## Progress log

- 2026-08-18 06:53: claimed; work starting.
- 2026-08-18 07:12: root cause: section 8's skip-guard read only macos\google-client-id.txt while Config.zig prefers repo-root; and even a two-path guard would delete the coverage on every configured seat. Fix: product knob GHOZTTY_RELAY_NO_CLIENT_ID (relay_signin.env_force_unconfigured) suppresses env id + bake, so section 8 forces the unconfigured state and the skip is gone entirely. Pure-logic split (resolveClientIdFrom) unit-tested in the none lane.
- 2026-08-18 07:31: validated on the box: floor-lane -Lane all = ALL LANES PASS (lib/none/win32/agent); test\win32\relay-account.ps1 = ALL PASS with section 8 RUN (not skipped) on a seat whose build bakes a client id - all six formerly-phantom assertions PASS and the configured control still passes; ipc-p1/p2/p3 = ALL PASS; guard-due went DUE for isolation-meta, stderr-capture and docs-routing (my edits to relay-account.ps1 and docs/claude/remote.md) and all three re-ran green and re-stamped. Filed T972: relay-account.ps1 itself has no guard-due row.
- 2026-08-18 07:33: status: in-progress -> done [commit c1292fb9f]
