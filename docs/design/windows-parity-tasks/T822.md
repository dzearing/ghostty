---
id: T822
title: "The CLAUDE.md progressive-disclosure split is sitting uncommitted in the working tree"
order: null
deps: []
status: "done"
commits: ["561375f0b"]
seat: "win"
priority: "P1"
triage-reason: "the docs split is uncommitted working-tree state - a real loss risk, but internal to the repo rather than user-facing"
tags: ["docs", "infra"]
---

# T822 - The CLAUDE.md progressive-disclosure split is sitting uncommitted in the working tree

## In plain terms

A big reorganisation of the instructions this project runs on was done and never
saved to the repository. It only exists on one machine's disk right now, so a
crash, a bad `git checkout`, or a clean clone loses it — and nobody else can see
it. It needs to be reviewed and committed, or deliberately thrown away.

## Goals

- [x] the split is committed and pushed (66964bfed, 2026-08-15) rather than
      living on one machine's disk
- [x] every pointer the split created resolves: the routing table's targets, the
      `docs/claude/*.md` paths cited across the tree, and the
      `<doc> "<section>"` citations
- [x] the routing cannot silently rot again - a harness checks it, and re-runs
      itself whenever CLAUDE.md or a partition changes

## Summary

Found while committing T471 on 2026-08-12. `git status` shows a whole
progressive-disclosure split of the root `CLAUDE.md` in the working tree, with no
commit and no task behind it:

- **New, untracked:** `docs/claude/` — `build.md`, `cli.md`, `remote.md`,
  `sessions.md`, `testing.md`, `viewers.md`, `win32-ui.md`.
- **Modified:** `CLAUDE.md` itself (net ~2500 lines removed — it is now a routing
  table plus the standing rules), plus 18 files whose `CLAUDE.md "<section>"`
  references were repointed at the new partitions:
  `macos/Sources/Features/Remote/LocalAgentManager.swift`,
  `scripts/delivery-manifest.ps1`, `scripts/loop-session.ps1`,
  `scripts/publish-windows-release.ps1`, `src/os/ipc_client.zig`, six files under
  `src/apprt/win32/`, and seven `test/win32/*.ps1`.

The content is real and coherent — the go-loop has been reading the split
`CLAUDE.md` all day, since the harness loads the working-tree file — which is
exactly why it is easy to miss: everything behaves as though it were committed.

Why P1: this is the standing-instructions file for every session on both seats,
it is unpushed AND uncommitted (worse than the unpushed-commit case go.md's sync
discipline is written about), and nothing in the tracker owns it.

## What to do

Establish whose work it is before touching it (a `git log`/reflog check and the
other seat), then either commit and push it as one `docs:` change with a message
that explains the routing model, or discard it deliberately. Do NOT fold it into
an unrelated task's commit, and do not revert it without asking — the sessions
running right now are reading it.

## Validation criteria

- [x] `git status` is clean of the split (either committed and pushed, or
      deliberately discarded with the reason recorded here) — committed as
      **66964bfed** on 2026-08-15 ("CLAUDE.md is now a small always-loaded core
      that routes to seven scenario docs under docs/claude/"); `git status` at
      this turn's claim reported a clean tree
- [x] If committed: every `docs/claude/*.md` referenced by the root `CLAUDE.md`
      routing table exists, and no file left in the tree still points at a
      `CLAUDE.md` section that moved — sections B/C/D of
      `test\win32\docs-routing.ps1`, ALL PASS (20). It found three pointers the
      split had left dangling; they are repointed in this task's commit.
- [x] The other seat can see it (pushed), since half the repointed references
      are in `macos/` and the test suite — `66964bfed` is on
      `origin/users/dzearing/windows-amd64` (branch reports 0 commits ahead)

## Outcome (2026-08-17)

The split was already committed and pushed (`66964bfed`, 2026-08-15) by the time
this task came up the queue, so nothing was at risk of being lost any more. What
was still open was the second criterion — whether the ~18 repointed references
actually landed on real destinations — and it was not clean:

- `test\win32\window-name-env.ps1` still cited `CLAUDE.md "Naming"`; the section
  now lives in `docs/claude/cli.md`.
- `docs\design\session-persistence.md` and
  `docs\superpowers\specs\2026-07-23-agent-update-dialog-design.md` both cited
  `CLAUDE.md, "Agent contract & upgrade compatibility"`, which moved to
  `docs/claude/sessions.md`.
- `test\win32\build-locked-artifact.ps1` pointed at a "Windows build section"
  that no longer exists by that name; repointed at
  `docs/claude/build.md, "Build, run & debug"`.

Those pointers ARE the navigation model — a comment saying
`docs/claude/cli.md "Instance addressability"` is how the next reader finds the
contract — and nothing checked them, which is why three survived the split for
two days. `test\win32\docs-routing.ps1` now checks the routing table's targets,
every `docs/claude/<file>.md` path cited in the tracked tree, every
`<doc> "<section>"` citation, and that no partition is orphaned; a
`scripts\guard-due.ps1` row re-arms it whenever `CLAUDE.md` or a partition
changes. The append-only parity record (log, digests, task/decision files) is
excluded from the scan on purpose: it describes what was true on the day it was
written, so a later move must not turn history red.

## Related

- **T471** — the task whose commit surfaced it.

## Progress log

- 2026-08-14 20:41: heads-up from T475: the uncommitted docs/claude/testing.md in this split now also carries the T475 paragraph documenting the new 'lib' floor lane. Commit the split as-is; do not regenerate testing.md from the old CLAUDE.md or that paragraph is lost.
- 2026-08-14 20:55: T475 also touched the uncommitted CLAUDE.md in this split (the floor-lane comment now reads 'lib + none + win32 + agent lanes'). Same rule as the testing.md note: commit the tree as it stands.
- 2026-08-17 07:22: claimed; work starting.
- 2026-08-17 07:31 [session dbc2a732]: the split itself was already committed+pushed as 66964bfed (2026-08-15) - tree clean; this turn closes the audit half of the criteria: found 3 pointers left dangling by the split (test\win32\window-name-env.ps1 'Naming', docs\design\session-persistence.md + docs\superpowers\specs\2026-07-23-agent-update-dialog-design.md 'Agent contract & upgrade compatibility'), repointed them, and added test\win32\docs-routing.ps1 + a guard-due row so the routing cannot rot again
- 2026-08-17 07:52: status: in-progress -> done [commit 561375f0b]
