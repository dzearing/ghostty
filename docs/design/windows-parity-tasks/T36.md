---
id: T36
title: "On-box release install refresh flow (Phase H)"
order: 300
deps: []
status: "done"
priority: "P2"
triage-reason: "Install/refresh flow is already proven end-to-end twice; only a live re-verification of the reset-context path remains."
commits: ["ae71b19b4..", "81857a76e"]
---

# T36 — On-box release install refresh flow (Phase H)

## Summary

Release install refresh flow

## In plain terms

Claude Code sessions on this box run inside Ghoztty itself, and they need to
be able to drive the terminal they are sitting in — list its windows, work out
which pane they are in, and clear their own session to start fresh. That only
works if the Ghoztty on the user's PATH is a real installed build that answers
those commands, not a leftover portable copy. This task set that installation
up and kept a refresh path for it; what was left was proving, on the box, that
a session inside the installed build really can clear itself.

## Goals

- [x] A release build is installed at `%LOCALAPPDATA%\Programs\Ghoztty` with
      its `share\` alongside it, and its directory is on the user PATH
- [x] `ghoztty +list` from a plain shell is answered by that installed instance
- [x] A process inside a pane can name its own pane with `+list --pid`
- [x] A refresh path exists that rebuilds and re-delivers that install
- [x] The skill-driven `/reset-context` clears a session running inside the
      installed release — the workflow this whole task existed to unblock

## Validation criteria

- [x] `ghoztty` on PATH resolves into the install dir, and `+version` reports a
      release build — verified 2026-08-09: `(Get-Command ghoztty).Source` =
      `C:\Users\David\AppData\Local\Programs\Ghoztty\ghoztty.com`,
      `+version` = `1.4.0-users-dzearing-windows-amd64-+df8604ad7`
- [x] `ghoztty +list` from a fresh shell exits 0 and prints the live window
      tree — verified 2026-08-09: exit 0, both open windows listed
- [x] The answering instance is the INSTALLED release, not a dev build —
      verified 2026-08-09: the only `ghoztty.exe` running is
      `…\Programs\Ghoztty\ghoztty.exe` (pid 47940) and this pane's baked
      `$GHOZTTY_IPC_SOCKET` is `\\.\pipe\ghoztty-David` (no `-debug` suffix)
- [x] `+list --pid=$PID` from inside a pane prints that pane's id — verified
      2026-08-09: exit 0, answer matched `$GHOZTTY_PANE_ID`
      (`F08888B0-A73E-4554-86DB-5A11F6BCEFDB`)
- [x] Skill-driven `/reset-context` clears a session in that installed release
      and hands off its continuation — verified 2026-08-09 live:
      `%TEMP%\reset-context-last.log` records 21:02:33→21:02:52 in this pane
      (`cleared input line` → `typed /clear` → `verified: /clear landed` →
      `handed off …cont-8471.txt` → `verified: pane is repainting`), and the
      session writing this entry is the one that came up on that continuation
- [x] The refresh path is live and delivering — verified 2026-08-09: the
      once-a-day `scripts\morning-refresh.ps1` watermark
      (`%LOCALAPPDATA%\ghoztty\morning-refresh`) is stamped 10:56 today and the
      installed exe reports that morning's commit
- [x] Standing floor green at HEAD — 2026-08-09,
      `scripts\floor-lane.ps1 -Lane all`: `none#1=PASS win32#1=PASS
      agent#1=PASS` / `ALL LANES PASS` (325s / 357s / 332s), and
      `ipc-p1.ps1` / `ipc-p2.ps1` / `ipc-p3.ps1` each `ALL PASS`, exit 0

## Details

A locally-installed release build powers daily use so `/reset-context` and
other ghoztty-IPC workflows work from on-box sessions (user directive
2026-07-13). Install: `%LOCALAPPDATA%\Programs\Ghoztty\{ghoztty.exe, share\}`
(exe+share side-by-side, same layout as the portable ZIP; resourcesDir
climbs from the exe), dir on user PATH. **Release builds must use
`-Dtarget=x86_64-windows-gnu`** — native msvc + GUI subsystem fails with
`undefined symbol: WinMain` (GhosttyExe.zig sets .Windows subsystem for
non-Debug; zig's msvc CRT expects WinMain). Refresh = rebuild ReleaseFast
gnu, re-run the copy + PATH script. Release exe speaks the
`ghoztty-<username>` pipe; Debug exes keep `ghoztty-debug-<username>`, so
both run side by side.

*Validation:* `ghoztty +list` from a fresh shell answers via the installed
release instance; `+list --pid` resolves a pane from inside it; skill-driven
`/reset-context` clears a session (the original blocked workflow).

*Evidence (in-progress, ae71b19b4 + follow-ups):* 2026-07-13: merged
origin/main (62 commits); both test lanes + P1–P3 ALL PASS post-merge;
ReleaseFast gnu exe installed + user PATH; verified: cold auto-launch,
+list, in-pane `+list --pid=$PID` → pane name, exit 0; teardown clean.
REMAINING: live skill-driven /reset-context from a session inside the
installed release (attempted at the 2026-07-15 task boundary — see log).
2026-07-15: upgrade script proven end-to-end twice (16:32 and 23:29 runs
in %TEMP%\ghoztty-upgrade.log: kill → swap exe+pdb → mirror share →
relaunch+resume); fixed the resume gap — `claude --continue` without a
prompt idles, ResumeCommand now appends "read go.md and go" so refreshed
sessions re-enter the task loop.
2026-08-09: the REMAINING item is closed. The live `/reset-context` was
observed end to end from inside the installed release — see the Validation
criteria above for the log excerpt and the pane ids. Nothing about the
install flow was changed by this turn; the task was carrying an unverified
claim, and the verification is what it was waiting on. The refresh half has
also outgrown the hand-run copy script this task described: delivery now goes
through `scripts\launch-upgrade.ps1` (which builds the staging release and
refuses stale bits) and the once-a-day `scripts\morning-refresh.ps1` (T525),
both of which are their own tasks.

## Progress log

- 2026-08-09 21:03: claimed; work starting.
- 2026-08-09 21:07: verified live on the box: PATH ghoztty -> installed release (.com), only running ghoztty.exe is %LOCALAPPDATA%\Programs\Ghoztty, pane pipe = ghoztty-David (no -debug); +list exit 0; +list --pid=$PID == $GHOZTTY_PANE_ID; reset-context-last.log records the full /clear + handoff at 21:02:33-21:02:52 and this session came up on that continuation. No code change. Floor lanes running.
- 2026-08-09 21:25: status: in-progress -> done [commit 81857a76e]
