---
id: T1293
title: "Defender quarantines ghoztty.exe and ghoztty.com as Trojan:Script/Wacatac.C!ml on a clean machine"
order: null
deps: ["T1312", "T1313"]
status: "blocked(T1312: no second-scanner result; T1313: no Microsoft determination - both need an account or browser session this box does not have)"
commits: ["375526d7f"]
seat: "win"
priority: "P0"
triage-reason: "P0 and worse than T1203's SmartScreen wall: quarantine REMOVES the files rather than warning about them, so the install does not merely look scary - it is deleted. Reported by the user on 2026-09-03 installing on a second machine."
milestone: "M1"
unblock: "T1312 has an answer from a second scanner AND T1313 has a Microsoft determination. Both need something this box does not have (a VirusTotal key or browser session; a Microsoft account sign-in), so reopening this without one of those answers just re-reads the same evidence. The user-facing half - the page and the READ-ME-FIRST warning - is already shipped, and D89 is with the user."
tags: ["fix", "infra"]
---

# T1293 - Defender quarantines Ghoztty as Wacatac.C!ml

## In plain terms

You install Ghoztty on a new machine and Windows Defender decides it is a
trojan, quarantines the program, and takes the files away. Nothing is left to
run. This is worse than the SmartScreen warning, because there is no "run
anyway" - the files are gone.

## What the user hit (2026-09-03)

> "i installed ghoztty on another machine and defender quarantined saying
> ghoztty.com and ghoztty.exe were compromised with Trojan:Script/Wacatac.C!ml"

## Evidence gathered at the time

**Provenance is clean and auditable.** `win-v1.36.2` was built and published by
`github-actions[bot]` in run 33777624339 from `release-windows.yml` on
`ubuntu-latest`, out of this public repository. There is no local packaging
step, no third-party binary drop, and no hand-uploaded asset in that path.

**The same binaries do not trip Defender on the dev box.** Checked directly:
`Get-MpThreatDetection` reports no ghoztty detections, `Get-MpPreference` shows
**no exclusion** covering the install directory or the repo, and cloud
protection is on (`MAPSReporting = 2`, Advanced). Identical files, full
protection, no exclusion, no detection - so the verdict is machine-specific
rather than a property of the bytes.

**The detection name is Microsoft's generic ML bucket.** The `!ml` suffix marks
a machine-learning verdict rather than a signature match, and `Wacatac.*!ml` is
the bucket that fires on low-reputation, unsigned, recently-created
executables. Every one of those describes our release.

**Two of our own choices make us an ideal false-positive candidate:**

1. **The build is unsigned** (D87 - the user chose to stay unsigned and explain
   the SmartScreen warning). An Authenticode signature plus accumulated
   reputation is the single largest input to these ML verdicts. This report is
   new evidence bearing on that decision: D87 weighed a *warning*; quarantine
   is a *deletion*.
2. **`ghoztty.com` is a PE32+ binary with a `.com` extension** (T245 - it is
   the console-subsystem twin, so PowerShell waits for it and wires
   redirection). That shape - an executable whose extension does not match its
   format - is a long-standing heuristic trigger, independent of signing.

**Not yet established:** independent confirmation from a second scanner. That
is the first thing to do, and this task must not assume the answer. It is now
T1312, because it cannot be done from this box.

## Evidence added 2026-09-03 (this turn)

The PUBLISHED `win-v1.36.2` assets were downloaded from GitHub and scanned
explicitly with Defender's own engine, rather than relying on the absence of a
detection history:

| Artifact | Verdict |
|---|---|
| `Ghoztty-1.36.2-x64.msi` | no threats |
| `Ghoztty-portable-1.36.2-x64.zip` | no threats |
| `ghoztty.exe` | no threats |
| `ghoztty.com` | no threats |
| `ghoztty-agent.exe` | no threats |

Engine 1.1.26080.3, signatures 1.459.28.0 (2026-09-02), MAPS Advanced, real-time
protection on, no exclusion covering the scan path. The download and the unzip
were not intercepted either. SHA-256, for the Microsoft submission and any later
comparison: `ghoztty.exe`
`0A53D5A05A254A5BAF854C80A8EB4997BC6C3C79D3EF8B7FB05E173A8457B669`,
`ghoztty.com`
`DA0071548324B130136EF2B0EF40CDEF3705586920E87E3B0CD28EE4397DF487`,
`ghoztty-agent.exe`
`E7A212A3CD4701EB0677BEB1343D368F7A64DB34A59A81EA5A60FC9272F06772`.

This rules OUT a signature match and rules nothing in: the verdict the user hit
is a cloud machine-learning call that can differ per machine. It is not a
substitute for T1312.

## The hashes to look up (win-v1.36.2)

Recorded here so the second-opinion lookup is a copy-paste rather than a
re-download. SHA-256, from the assets as published:

| File | SHA-256 |
|---|---|
| `Ghoztty-1.36.2-x64.msi` | `29b8f9f09b0cc07014623a95b2f02ba175ceb1f7f9228e75d83843c7f4573bfb` |
| `Ghoztty-portable-1.36.2-x64.zip` | `627de58888a5f78b7f5417f91508b75242cfd73c6380024bd463b701e702a77b` |
| `ghoztty.exe` (in the zip) | `0a53d5a05a254a5baf854c80a8eb4997bc6c3c79d3ef8b7fb05e173a8457b669` |
| `ghoztty.com` (in the zip) | `da0071548324b130136ef2b0ef40cdef3705586920e87e3b0cd28ee4397df487` |
| `ghoztty-agent.exe` (in the zip) | `e7a212a3cd4701eb0677beb1343d368f7a64db34a59a81ea5a60fc9272f06772` |
| `opengl32.dll` (in the zip) | `fc5821c70602ca37924fd1a7941db8d8b169dbf82f6d2cfb603e4dc1d8e16b1b` |

`ghoztty.exe` and `ghoztty.com` are the two the user's machine quarantined.
Paste either into <https://www.virustotal.com/gui/home/search> for the
engine-by-engine answer, or run the scripted version once a key exists:

```
powershell -NoProfile -File scripts/verify-release-clean.ps1 -Tag win-v1.36.2
```

Nothing is uploaded by that path - it is a lookup by hash.

## What to do

1. **Verify rather than assume.** Submit both files to VirusTotal and compare
   engines; a lone Defender ML hit with everything else clean is the signature
   of a false positive, while multiple independent detections would mean
   something else entirely and this task becomes an incident.
2. **Report it to Microsoft.** The Security Intelligence submission portal
   (`microsoft.com/en-us/wdsi/filesubmission`) takes a software-developer
   false-positive report; these are typically corrected within days, and the
   correction reaches every machine. This is the actual remedy and it is free.
3. **Reopen the signing question.** D87 chose unsigned on the strength of "a
   warning most people click through". A quarantine is not clickable-through.
   Re-put the decision to the user with this evidence attached (see T1246,
   which was skipped on that answer).
4. **Consider the `.com` shape.** If submission does not clear it, test whether
   the `.com` twin alone drives the verdict; if so, weigh alternatives that
   keep T245's console behaviour without the extension mismatch.
5. **Tell the user before they meet it.** Until it is cleared, the download
   page should say plainly that Defender may quarantine the files and what to
   do about it - the same honesty the SmartScreen note now has.

## Validation criteria

- [ ] Both binaries are submitted to VirusTotal and the result recorded here -
      engine names and counts, not a summary. **T1312 built the asking machine
      but could not ask**: `scripts/verify-release-clean.ps1` now hashes every
      published asset, queries VirusTotal by hash and names the engines, with a
      harness that proves it can return DETECTED and UNKNOWN rather than only
      CLEAN - but the query needs a free API key this box does not hold, and the
      browser route is still shut. The hashes are recorded above so the answer
      is one command away. The published artifacts were scanned with Defender's
      own engine instead (above), which answers a weaker question.
- [ ] A developer false-positive report is filed with Microsoft, with the
      submission ID recorded. **Filed as T1313**: the portal needs a Microsoft
      account sign-in. The hashes and provenance it asks for are prepared there.
- [ ] A fresh install on a clean machine completes without quarantine, verified
      after Microsoft's correction lands. Waits on T1313.
- [x] The signing decision is re-put to the user with this evidence - **D89**,
      which states the change since D87 plainly: that decision weighed a warning
      with a Run anyway button, and the worst case is now a machine where the
      program has been deleted.
- [x] While the detection stands, the website says so and says what to do -
      the download page carries `#win-defender-note` naming the detection,
      saying the files are removed rather than warned about, giving the Windows
      Security -> Protection history -> Restore path, and linking the public
      release workflow so the provenance can be checked. The portable ZIP's
      READ-ME-FIRST carries the same as step 4, for someone who never visits the
      site. Held by `website-windows-download.ps1` A17/A17b-f and
      `release-artifacts.ps1` B4c/B4c2-4; both demonstrated red at HEAD first.
- [ ] If independent scanners disagree with the false-positive reading, this
      task is escalated as a supply-chain incident and the release is pulled.
      Waits on T1312.

## Progress log

- 2026-09-03 19:10: claimed; work starting.
- 2026-09-03 19:18: On-box evidence: downloaded the PUBLISHED win-v1.36.2 MSI and portable ZIP from GitHub and scanned them explicitly with MpCmdRun (engine 1.1.26080.3, signatures 1.459.28.0 of 2026-09-02, cloud/MAPS Advanced, real-time on, no exclusion covering the scratchpad). All five artifacts - MSI, ZIP, ghoztty.exe, ghoztty.com, ghoztty-agent.exe - found no threats, and the ZIP downloaded and extracted without interception. So the verdict is not a signature match and does not reproduce on this box with current definitions; consistent with a per-machine cloud ML call. Not a substitute for the VirusTotal comparison, which needs an account this box does not have. Landed the user-facing half: the download page and the portable ZIP READ-ME-FIRST now name the quarantine, say the files are removed rather than warned about, give the Protection history -> Restore path, and point at the public release workflow.
- 2026-09-03 19:21: status: in-progress -> blocked(T1312: no second-scanner result; T1313: no Microsoft determination - both need an account or browser session this box does not have)

### Decision D89 - Windows now deletes the download on some machines, not just warns about it. Buy a signing certificate?

Resolved 2026-09-04T19:45:20Z : Buy an EV certificate - the download stops being warned about or removed, starting with the first person (Recommended)
