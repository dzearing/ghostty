---
id: T1461
title: "A Ghoztty pane drains streamed output at ~50 KB/s where conhost does ~1.3 MB/s"
order: 2
deps: ["T1460", "T1463"]
status: "todo"
commits: []
seat: "win"
priority: "P0"
tags: ["perf", "fix"]
---

# T1461 - A Ghoztty pane drains streamed output at ~50 KB/s where conhost does ~1.3 MB/s

## Summary

Measured 2026-09-08 (T1458): a Ghoztty pane whose PTY is held by `ghoztty-agent`
drains a sustained stream at **~50 KB/s**. conhost, on the same desktop and the
same byte-identical 8 MB workload, does it at **~1.3 MB/s** - about 26x faster.
The writer is released after 9.1 s (conhost 6.1 s, the 1.5x everyone quotes) but
the pane is still parsing the backlog more than 20 seconds later, so the number
the user actually lives with is far worse than the writer's clock suggests.

This is the defect behind T1458's report. It is filed separately because T1458 is
the user-facing symptom and this is the specific throughput deficit that has to
close for the symptom to go away.

## In plain terms

When a program prints a lot of text quickly, a Ghoztty pane falls behind and
keeps catching up long after the program has finished - which is what "it feels
laggy, it arrives in lurches" means. The terminal Windows ships with keeps up
about twenty-six times better on the same output. This is that gap.

## What is already ruled out - do not re-investigate

From T1458's capture, all with numbers in that task file:

- **The present is not it.** `swapBuffers` costs 15-194 us and `swap_max_ms` was
  0 across 1,240 samples. The virtual/streamed display theory is dead.
- **The T111b priority handoff is not it.** `yield_ms_per_s` was 0 in every
  sustained second.
- **Slicing granularity is not obviously it.** `slices_per_s` tracks
  `kb_per_s` / 4 KiB exactly, so the slicer is doing what it says.
- **Renderer-mutex starvation is not it** (T1460, 2026-09-08). This was the
  leading reading and it is now measured and dead: `lock_ms_per_s` is **1 ms per
  second** across the 28 sustained seconds of the drain, 0.08% of the cost. The
  "12 fps x ~40 ms held mutex = ~480 ms/s" arithmetic matched the total by
  coincidence. Do not go looking at `drawFrame`.

## Details

**The cost is real work inside `processOutputLocked`**: 99.83% of the
`parse_ms_per_s` T1458 measured is time with the renderer mutex already held.
The thing to explain is why a **4 KiB slice takes ~40 ms to parse** - which is
~10 us per byte, and the sustained shape is `feeds_per_s=3`, `slices_per_s=14`,
`kb_per_s=50`.

**Read T1463 before starting.** Every number above came off a **Debug** build,
which announces in its own log that performance will be very poor. T1460's
*ratio* is build-independent; the *rate* is not, and Zig's Debug mode is
worst-case for exactly this shape of code (a byte-at-a-time state machine, every
safety check live, nothing inlined). Optimising against the Debug rate risks
spending a task recovering overhead that the shipped build never pays. T1463
re-establishes the baseline on an optimized build and is this task's dependency
for that reason.

The reproduction is cheap and already written: the workload emitter and the A/B
runner used for T1458 live in this session's scratchpad and are described in
T1458's measurement section. Fold them into `test\win32\` as part of this task so
the number has a home and can regress.

## Goals

- [x] The dominant cost is identified with T1460's split. **Done**: work under
      the lock (the parser), not lock wait - 99.83% vs 0.08%.
- [ ] Sustained drain rate for the T1458 workload improves materially against
      the 50 KB/s baseline.
- [ ] The A/B harness lives in `test\win32\` rather than in a scratchpad.

## Validation criteria

- [ ] The T1458 400k workload completes with the pane caught up, and the drain
      rate is recorded before and after.
- [ ] `test\win32\resize-flicker.ps1` green (shared present path; T1031, T1392,
      T1393).
- [ ] `powershell -NoProfile -File scripts\floor-lane.ps1 -Lane all` green and
      `test\win32\ipc-p1.ps1` / `ipc-p2.ps1` / `ipc-p3.ps1` green.
- [ ] The reporter confirms by eye that a pane no longer feels heavier than
      conhost. **Needs the user** - the background test desktop cannot host the
      streamed-display condition.
