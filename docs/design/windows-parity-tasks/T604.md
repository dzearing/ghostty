---
id: T604
title: "src/cli/send_keys.zig has diverged from main: main rewrote prompt submission while this branch built bracketed-paste framing"
order: 1.5
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P0"
milestone: "M1"
tags: ["fix"]
triage-reason: "USER DIRECTIVE 2026-08-09 (escalated from P1): 'In the latest mac builds we updated the enter logic recently. Make sure we have 100% parity with the changes, that should be a priority.' The prompt-never-submits failure has wedged the go loop 11+ times in two days; main's a7f7476e1 rewrote submission to be unambiguous and this branch must carry the SAME guarantees"
---

# T604 - src/cli/send_keys.zig has diverged from main: main rewrote prompt submission while this branch built bracketed-paste framing

## User directive (2026-08-09, verbatim)

"In the latest mac builds we updated the enter logic recently. Make sure we
have 100% parity with the changes, that should be a priority."

100% parity means: read main's `a7f7476e1` as the spec for submission
semantics, and this branch's implementation must give every guarantee it
gives - not a merge of convenience, an equivalence. The reconciliation
below stands, with main's design as the reference point where the two
sides disagree.

## In plain terms

Both seats independently fixed the same problem - an agent's prompt arriving in
a pane but never actually submitting - and both rewrote the same file to do it.
Whichever way that gets merged, one of the two fixes could be lost silently, and
the symptom (a prompt sitting unsent in a composer) is the exact failure that
kills the automation loop. This reconciles them into one implementation before
the merge, rather than during it.

## Summary

Upstream: `a7f7476e1` "feat(send-keys): make submitting a prompt unambiguous"
(merged `9c74bdbbb`) - 471 changed lines in `src/cli/send_keys.zig`, 18 in
`CLAUDE.md`, plus the bundled skill doc.

This branch's side: the bracketed-paste run framing documented in `CLAUDE.md`
("Text and keys stay distinguishable"), the single-write frame rule, and the
`Get-LoopSubmitArgs` trailing-space convention from T438.

Do this as a real reconciliation, not a `git checkout --theirs`:

1. Read both implementations and write down what each actually guarantees.
2. Decide one design that keeps every guarantee both sides established.
3. Land it here with tests for both sets of behaviors, and record it so the Mac
   seat can take the same file (file a `seat: mac` follow-up if the Mac needs a
   companion change).

## Validation criteria

- [ ] One implementation of `send_keys` that satisfies both sides' documented
      guarantees, with the differences enumerated in the task's progress log.
- [ ] `test/win32/send-keys-bracketed.ps1` still ALL PASS, including the
      byte-level rounds 6-8.
- [ ] `zig build test -Dapp-runtime=none` covers the new parsing/framing paths.
- [ ] `CLAUDE.md` describes the merged behavior once, not twice.
- [ ] A `seat: mac` follow-up filed if the Mac half needs anything.
