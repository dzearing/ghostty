---
id: T671
title: "Screenshot capture has no keyboard-only path: a region can only be selected by dragging"
order: 1.1
deps: ["T647"]
status: "in-progress"
commits: []
seat: "win"
priority: "P1"
milestone: "M1"
triage-reason: "A new feature reachable only by dragging is an accessibility hole. Same bar that put T639/T640 into M1."
tags: ["feature"]
---

# T671 - Screenshot capture has no keyboard-only path: a region can only be selected by dragging

## In plain terms

The new screenshot button opens a picker you can only use with a mouse: you
have to drag a rectangle. Someone who works from the keyboard can start the
capture (Ctrl+Shift+S) and then has no way to finish it except to press
Escape. This gives the picker a keyboard.

## Goals

- [x] Arrows aim a caret; Ctrl+arrow takes a coarse step
- [x] Shift+arrow starts a selection; Enter pins a corner and Enter captures
- [x] Escape still cancels out of a keyboard-driven state
- [x] The live caret position and selection size are announced, not only drawn
- [x] The caret is visible, so arrows are seen to do something

## Summary

Noticed while building T647. The selector reads its whole gesture out of mouse
messages; the only key it answers is Escape. A capture that can be STARTED
from the keyboard and not FINISHED from it is worse than one with no chord at
all, because the chord promises a keyboard path that is not there.

## Details

- Arrows move a caret point; Shift+arrows extend from the anchor; Enter
  captures; Escape still cancels. Ctrl+arrow as the coarse step (say 32 px vs
  1 px) so crossing a 4K monitor is not a marathon.
- Tab (or Space, if T670 lands first and takes Space) to cycle candidate
  windows is a much better keyboard answer than nudging a rectangle by hand —
  worth doing whichever of the two lands second, and worth noting in the other
  task.
- The current selection rect must be ANNOUNCED, not just drawn: the hint card
  is already there and can carry `x,y  WxH` live, which also helps mouse users
  hit an exact size.
- Everything stays in the existing paint path — this adds no new drawing, only
  new ways for `anchor`/`cursor` to change.

## Validation criteria

- [x] Unit tests (none lane): the key-driven cursor motion is pure — step
      sizes, clamping to the desktop, and Shift-extend from the anchor.
      `region_select.zig` gained `stepPx`/`clampPoint`/`caretStart`/`moveCaret`/
      `dropAnchor`/`statusText`/`caretBox` and six tests over them (17 pass via
      `zig test src\apprt\win32\region_select.zig`; the whole none lane is
      green).
- [x] On-box acceptance: with the selector up and NO mouse messages at all,
      arrows + Shift+arrows + Enter produce a capture of a predicted size
      (posted `WM_KEYDOWN`s only, which is what the harness can do anyway).
      Arm H of `test\win32\viewer-feedback-capture.ps1`: three plain arrows,
      then Shift+Ctrl+arrows, then Enter →
      `capture=done rect=3843,1080 160x96`, and an `[Image #1]` chip.
- [x] On-box acceptance: Escape still cancels from the keyboard-driven state.
      Same arm, before the capture: the cancel tally goes up and the overlay
      goes away.
- [x] On-box acceptance: the hint card shows the live selection size. Asserted
      through the overlay's window text — the same string the card paints, and
      the window's accessible name — read back with `WM_GETTEXT`: it went
      `3840,1080` → `3843,1080` → `3843,1080  160x96` across the arm.

## Progress log

- 2026-08-10 12:42: claimed; work starting.
- 2026-08-10 13:13: built: pure keyboard model in region_select.zig (moveCaret/dropAnchor/clampPoint/caretStart/statusText/caretBox) + RegionSelector wiring (arrows, Ctrl coarse step, Shift-extend, Enter pin/capture, tracked modifiers, visible caret, live status announced via window text). none lane green, win32 lane green, viewer-feedback-capture.ps1 ALL PASS (63) incl. 20 new keyboard arms.
