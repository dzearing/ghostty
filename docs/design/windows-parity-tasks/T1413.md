---
id: T1413
title: "A stray always-on-top bit on a scrollbar or dim wash is no longer cleared when the pane is resized"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P1"
tags: ["fix", "test"]
---

# T1413 - A stray always-on-top bit on a scrollbar or dim wash is no longer cleared when the pane is resized

## Summary

Measured 2026-09-07 under T607: overlay-zorder.ps1 sections F/dim and F/scrollbar are RED. A stray WS_EX_TOPMOST injected onto a dim overlay or a themed scrollbar is not cleared by a window resize any more, so the T142 defect (a background window's chrome floating over another app) has no heal left on the reposition path. T1345 (982d1b8df) is the cause and says so in its own commit message: a live layout pass moves already-visible popups with SWP_NOZORDER and now skips the z-order walk, which is where healOverlayZOrderAfterMove used to run. Sections A-E, G and H still pass, so the banner overlay, the URL bubble and the quick terminal are unaffected - it is the popups a live pass moves. The fix has to restore the heal without giving back the drag cost T1345 bought.

## In plain terms

Sometimes another program leaves a "stay on top" flag stuck on one of the small
floating strips Ghoztty draws over a pane - the dim wash over an inactive split,
or the themed scrollbar. When that happens the strip hovers over whatever app
you switch to, which looks like a stray sliver of Ghoztty painted on top of your
browser. Resizing the Ghoztty window used to knock it back down; a speed fix
last week accidentally removed that, so the only thing left that cleared it was
clicking on the Ghoztty window. This puts the resize cure back without making
dragging a splitter slow again.

## Goals

- [x] a window resize clears a stray always-on-top bit on the dim overlay again
- [x] the same for the themed scrollbar popup
- [x] the splitter-drag saving T1345 bought is kept - the expensive z-order walk
      is still skipped during a live layout pass
- [x] the pure-logic half is unit-tested in the `none` lane
- [x] `test\win32\overlay-zorder.ps1` sections F/dim and F/scrollbar go green

## Validation criteria

- [x] `test\win32\overlay-zorder.ps1` reports F/dim and F/scrollbar PASS, and
      sections A-E/G/H stay green. **Verified 2026-09-07**: `ALL PASS (39
      assertions)` on the background test desktop, with `F/dim: reposition
      cleared the stray WS_EX_TOPMOST` and the same for `F/scrollbar`. Section B
      (the negative control) still reproduces the defect and section E still
      preserves a legitimate float-on-top bit, so the fix stays owner-relative.
- [x] The `none` lane covers `chrome_fanout.healScope`: a suppressed pass over an
      already-shown overlay returns `.stray_only` (walk skipped) and never a
      scope that skips the stray check. **Verified**: two tests in
      `chrome_fanout.zig` - the scope table, and "the stray-topmost check
      survives the suppression (T1413)" over all four input combinations -
      green in `LANE none PASS`.
- [x] The four floor lanes (`scripts\floor-lane.ps1 -Lane all`) are green.
      **Verified**: `FLOOR SUMMARY: lib#1=PASS none#1=PASS win32#1=PASS
      agent#1=PASS` / `ALL LANES PASS`.
- [x] `test\win32\drag-perf.ps1` still parses the drag line and the heal walk is
      still declined during a live pass (`chrome_skipped` non-zero).
      **Verified**: `ALL PASS (12 assertions)`, and the emitted line reads
      `chrome_heals=0 chrome_skipped=26 ... verdict=smooth` at 8 panes - the
      walk is still fully skipped for the drag, which is the saving T1345
      bought.
- [x] The other harnesses covering the touched files are green:
      `test\win32\printclient-audit.ps1` `ALL PASS (8)` and
      `test\win32\test-reach-audit.ps1` `ALL PASS (14)`, both re-stamped.

## Progress log

- 2026-09-07 00:58: claimed; work starting.
- 2026-09-07 01:12: root cause confirmed: T1345's shouldHeal skipped the WHOLE heal during a live layout pass, and T1393 made a whole-window WM_SIZE a live pass - so the resize path lost the cheap stray-WS_EX_TOPMOST check too, not just the expensive z-order walk. Fix: chrome_fanout.shouldHeal -> healScope returning .full/.stray_only; win32.healOverlayZOrderScoped always does the two GetWindowLongW reads and the demotion, and skips only seatedAboveOwner's walk during a suppressed pass (a stray that WAS found still gets the walk, since the demotion moved the popup). Drag saving preserved.
