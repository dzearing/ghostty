---
id: T572
title: "The real balloon click has never been driven end to end - only the routing and the registration are measured"
order: null
deps: ["T448"]
status: "blocked(this box draws no notification banners: Windows notifications are off system-wide)"
commits: []
seat: "win"
priority: "P2"
triage-reason: "the two halves either side of it are measured and the middle is documented behavior; this closes the last inch of evidence, it does not fix a known defect"
unblock: "This box draws no notification banners at all - Settings > System > Notifications is off, and measured on 2026-09-05 nothing renders even with the switch flipped, the notification service restarted and explorer restarted. Turn Notifications on, confirm a stock balloon is visible, then run: powershell -NoProfile -File test\\win32\\notification-click-real.ps1 from a session on the interactive desktop. It exits SKIP ALL with `desktop-toasts` named while there is nothing to click, so a green run IS the observation."
tags: ["test", "polish"]
---

# T572 - The real balloon click has never been driven end to end - only the routing and the registration are measured

## In plain terms

Clicking a Ghoztty notification is supposed to jump you to the pane that
raised it. We now have proof of everything on either side of the click - the
notification is registered correctly with Windows, and once a click arrives
the right pane gets focused - but nobody has yet had a machine actually click
a real notification and watched the window come forward. It almost certainly
works; it has simply never been seen to.

## Goals

- [ ] A real notification is clicked, by a person or by real input, and the
      raising window is observed coming to the foreground
- [x] However it is driven, the recipe is written down where the next person
      will find it — `test/win32/notification-click-real.ps1` drives the whole
      thing, and `notification-click-focus.ps1`'s header points at it

## Summary

Split out of T448 (2026-08-07), which fixed the actual defect: the balloon
icons were never registered with `NIM_SETVERSION`, so the shell kept them on
its pre-5.0 default behavior and `NIN_BALLOONUSERCLICK` was never sent at all.

What T448 left measured:

- **Routing** — `test/win32/notification-click-focus.ps1`, 38 assertions:
  the right pane in the right window is focused, the last raiser wins, a
  timed-out balloon does not steal focus, a closed pane does not crash it.
  It posts `WM_APP_TRAY` itself, so by T240's rule it proves nothing about
  delivery.
- **Registration** — the app logs a warning when the shell refuses
  `NIM_SETVERSION`, and an interactive run on 2026-08-07 produced none, so
  this box accepts `NOTIFYICON_VERSION` against the real notification area.
  (On the background test desktop there is no notification area at all, so
  both `NIM_ADD` and `NIM_SETVERSION` fail there and the script asserts only
  that the failure has that shape.)

What is left is the join between them: that Windows really does deliver
`NIN_BALLOONUSERCLICK` to us when a human clicks the toast. MSDN says it does
for a version-3 icon; nobody here has watched it.

## Details

**Why it is not just a lane.** Balloons render on the interactive desktop, so
`test/win32/lib/TestDesktop.ps1` cannot host this — `SendInput` and screen
reads are dead off the input desktop, and there is no toast there to click.
Any automated version has to take the user's foreground for a few seconds,
which is why T448 filed this rather than wiring it into the floor.

**A first attempt and where it stalled.** T448 wrote a one-off probe
(launch a debug build under a private pipe suffix → two windows → raise OSC
777 from one → focus the other → find the toast → click it → poll
`GetForegroundWindow`). It hung before reporting, most likely in the
`EnumWindows` callback: PowerShell 5.1's scriptblock→delegate conversion is
the usual suspect for that shape. Whoever picks this up should either do the
enumeration in a compiled `Add-Type` class (the way
`test/win32/lib/TestDesktop.ps1` does everything) or skip enumeration and
click by coordinate.

**Where the toast is.** Windows 11 renders a `Shell_NotifyIcon` balloon as a
banner notification, hosted by the shell, class `Windows.UI.Core.CoreWindow`,
near the notification area. A click on its body is what produces
`NIN_BALLOONUSERCLICK`; a click on its dismiss button produces
`NIN_BALLOONHIDE` and must NOT move focus (which is worth asserting too — the
handler already distinguishes them, `tray_notify.classify`).

**The oracle.** The raising window coming to the foreground is enough and
needs no code change: raise from window A, make window B foreground, click the
toast, poll `GetForegroundWindow()` for A. The `--session-persistence=false`
+ `GHOZTTY_PIPE_SUFFIX` isolation from the T448 probe applies unchanged, and
the app must be killed by exact `ExecutablePath` afterwards.

*Validation:* the observation itself, recorded here with the date and the
build; plus whatever script drove it checked in (under `test/win32/` with a
header saying it is interactive-only and not part of the floor) or, if it
stays a human step, written into
`test/win32/notification-click-focus.ps1`'s header where the recipe already
lives.

## Validation criteria

- [ ] A real click on a real Ghoztty notification is performed on the
      interactive desktop and the raising window is observed becoming
      foreground; the date, build commit and method are recorded here.
- [ ] A dismiss (rather than a body click) is confirmed NOT to move focus.
- [x] The method is checked in or written into the acceptance script's header,
      so the next person does not re-derive it. — `test/win32/notification-click-real.ps1`,
      declared interactive-by-design and as a user-desktop launcher in
      `lib/TestDesktop.ps1`; ran green through setup + the case-1 raise on
      2026-09-05 and then declared its skip at the toast wait.

## What landed 2026-09-05, and what is still owed

**The script.** `test/win32/notification-click-real.ps1`: an isolated debug
build, two windows, an OSC 777 raised from a pane in window A, the foreground
parked on window B, the toast located by DIFFERENCE against a pre-raise
snapshot of visible top-level windows (not by a class allowlist — the shell has
moved that class twice), a real `SendInput` click on its body, and
`GetForegroundWindow()` polled for window A. Case 2 clicks the dismiss X and
asserts the foreground does NOT move. `-NegativeControl` inverts case 1.

**Why the observation is still owed.** This box shows no desktop notifications
at all. `HKCU\...\PushNotifications\ToastEnabled` is `0` — Settings > System >
Notifications, off — and with it flipped on, `WpnUserService` restarted and
`explorer` restarted, a stock `System.Windows.Forms.NotifyIcon.ShowBalloonTip`
*still* drew nothing, confirmed by an `EnumWindows` diff and by a
`CopyFromScreen` of the notification corner. The registry was restored to `0`.
So there is no toast to click here, and the script says so rather than going
red: a new `desktop-toasts` capability (`lib/DesktopCapability.ps1`) answers
the question before anything launches, and the toast wait itself calls
`Exit-TestSkip` with the same capability name when the shell draws nothing.

**To take the observation:** turn Notifications on, then run
`powershell -NoProfile -File test\win32\notification-click-real.ps1` from a
session on the interactive desktop.

**A defect found on the way (fixed here).** The `real-input` capability probe
could never say "available" on any box: its `INPUT` struct carried two extra
`int` fields, so `Marshal.SizeOf` was 48 where x64 requires 40, and every
injected event was rejected with `ERROR_INVALID_PARAMETER`. Every script gated
on `real-input` — `context-menu-real-input.ps1` among them — had therefore been
skipping unconditionally with a message that read like a busy box. The padding
is gone, the probe now accepts an event on this box, and
`test-desktop-harness.ps1` asserts the struct is 40 bytes so it cannot regrow.

## Progress log

- 2026-09-05 20:16: claimed; work starting.
- 2026-09-05 20:24: box state measured 2026-09-05: this box renders NO balloon toasts at all. HKCU PushNotifications\ToastEnabled was 0 (notifications off system-wide); flipping it to 1, restarting WpnUserService_f0d17 AND restarting explorer still produced no toast for a stock System.Windows.Forms.NotifyIcon.ShowBalloonTip - verified by both an EnumWindows diff and a CopyFromScreen of the notification-area corner. Registry restored to 0. So the real-click observation cannot be taken here until Notifications are turned on; the deliverable this turn is the end-to-end script plus a capability gate that SKIPs with the reason named.
- 2026-09-05 21:00: landed: test\win32\notification-click-real.ps1 (interactive-only, declared in lib\TestDesktop.ps1 for both the input-desktop and the user-desktop-launch audits); new desktop-toasts capability in lib\DesktopCapability.ps1 with the registry pre-check and the in-situ Exit-TestSkip at the toast wait. Also fixed a defect found on the way: the real-input probe's INPUT struct was 48 bytes where x64 needs 40, so SendInput rejected every event with error 87 and the probe could never say available - context-menu-real-input.ps1 had been skipping unconditionally and now runs ALL PASS (11). test-desktop-harness.ps1 ALL PASS (68) with three new assertions. Filed T1383 for profile-latency.ps1, masked by the same probe.
- 2026-09-05 21:04: status: in-progress -> blocked(this box draws no notification banners: Windows notifications are off system-wide) (by measured 2026-09-05: no toast renders here even with ToastEnabled flipped on, WpnUserService restarted and explorer restarted; the script and the capability gate landed, the observation is owed)
- 2026-09-05 21:05: commit d0ddd2d6b carries the T572 work (script + desktop-toasts capability + the real-input probe fix)
