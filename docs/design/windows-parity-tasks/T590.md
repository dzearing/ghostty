---
id: T590
title: "A launch that restores nothing overwrites the session-layout manifest, losing the recorded layout for good"
order: null
deps: ["T398"]
status: "done"
commits: ["bf408f03e"]
seat: "win"
priority: "P1"
triage-reason: "P1: a single bad launch destroys the recorded layout permanently, and there is no way to get it back"
tags: ["fix"]
---

# T590 - A launch that restores nothing overwrites the session-layout manifest, losing the recorded layout for good

## In plain terms

If Ghoztty starts one time without the background piece that keeps your shells
alive, it opens a single empty window — and then immediately writes that empty
window down as "this is your layout", erasing the record of the windows, tabs
and splits you actually had. Even once the background piece is working again,
they are gone: the last copy was overwritten by the one bad launch.

## Goals

- [x] A launch that cannot restore a window no longer erases its manifest entry
- [x] The kept entry restores normally on the next launch once the agent is back
- [x] Legitimate erase paths (persistence off, closing windows) still work
- [x] Unit tests for the merge rule + an on-box acceptance script

## Summary

`syncSessionLayout` (`src/apprt/win32/App.zig:1261`) captures the LIVE topology
and `session_layout.write`s it wholesale — the manifest is regenerated, never
merged. A launch where `restoreSessionLayout` returns false opens the default
blank window, whose `addTab` calls `markLayoutDirty`
(`src/apprt/win32/Window.zig:1542`), and the debounced sync then replaces the
recorded layout with that one window.

Normally harmless (the agent holds layout blobs as the second source, T194), it
is total loss in exactly the case where the agent is the thing that is missing:
both copies are then unavailable and the only surviving one is overwritten.

**macOS does not have this bug and says so out loud** — on the no-connection
path it logs `"local agent unreachable; keeping N manifest entries for next
launch"` and returns without touching the file
(`macos/Sources/.../SessionLayoutRestore.swift:56-64`), on the stated reasoning
that the agent and its sessions outlive the app so a later retry recovers them.
This is a win32-only divergence.

## Details

Found while landing T398, which fixes the adjacent half: a no-agent launch now
restores the windows that need no agent. This task is about the ones it still
correctly drops — a terminal-only window — which today are also erased from
disk rather than left for the next launch.

Shape of the fix: suppress the capture-and-write while a launch has recorded
windows it did not restore. Options to weigh: (a) skip the sync entirely until
the first user-initiated mutation when restore was suppressed by a missing
agent; (b) merge rather than replace — carry unrestored manifest entries
forward into the next write, which is closer to Mac's per-entry model. Note the
same write also drives `pushLayoutBlobs`, so whichever is chosen must not
delete the agent's blobs either (a moot point when the agent is missing, but
not on the general "restored nothing" path).

Care needed on the legitimate erase paths, which must keep working: persistence
turned off (`session_layout.clear`), and a user genuinely closing every window.

## Validation criteria

- [x] With the agent unspawnable, a relaunch that restores nothing leaves the
      recorded manifest intact on disk (not replaced by the blank window).
      *Verified: `test\win32\session-layout-preserve.ps1` phase B (B1 waits for
      the rewrite — the exact moment the old code destroyed the entry — then
      B2/B3/B3b assert the entry survived with its uuid + session id; B4/B5 are
      the not-restored / no-agent controls). ALL PASS (19) on-box 2026-08-16.*
- [x] With the agent restored, the NEXT launch brings the original windows back.
      *Verified: same script, phase C — the fresh agent materializes the session
      as a relaunchable tombstone and the carried entry restores as a live
      window (C1), agent-running control C2.*
- [x] `session-persistence = off` still clears the manifest.
      *Verified: the `clear` path is untouched and now also drops the carried
      keys (`clearCarriedLayouts`), so nothing can resurrect; covered at unit
      level (merge is bypassed entirely on the persistence-off arm) and by the
      existing `session-persistence.ps1` behavior, unchanged.*
- [x] Closing every window with the agent healthy still shrinks the manifest —
      the fix must not make the manifest immortal.
      *Verified: phase D (+close on the once-carried window shrinks the
      manifest, D1) plus the `mergeCarried` unit test "a carried key that is
      live again is adjudicated, not duplicated" — a restored window leaves the
      carry set, so its close is forgotten normally. A window the agent
      positively disowns is never carried in the first place
      (`positively_adjudicated` gate in `restoreSessionLayout`).*
- [x] An acceptance script covers it (sibling script
      `test/win32/session-layout-preserve.ps1`, phases A–D) plus both zig lanes
      green (floor-lane all, this turn) and 4 new `mergeCarried` unit tests in
      `session_layout.zig` (every-lane).

## Progress log

- 2026-08-16 13:39: claimed; work starting.
- 2026-08-16 13:47 [session 5fbadc0c-5513-4c3c-8130-302eff9a129e]: Design: option (b) merge - Mac's keep-entries rule translated to win32's wholesale-rewrite model. restoreSessionLayout records windowKey of every union-set window dropped WITHOUT positive adjudication (conn!=null AND probe.roster!=null) into carried_layout_windows; syncSessionLayout re-reads the on-disk manifest and appends those entries to every write via new pure session_layout.mergeCarried (unit-tested, none lane). Live key adjudicates (removed, so close still shrinks manifest); persistence-off clears carry. Building now.
- 2026-08-16 14:20 [session 5fbadc0c-5513-4c3c-8130-302eff9a129e]: Validated on-box: session-layout-preserve.ps1 ALL PASS (19, stamped guard); floor-lane all = lib/none/win32/agent PASS; P1 (25), P2 (20), P3 (16) ALL PASS; release-artifacts green w/ 2 Docker-only skips (Docker stays down per standing rule); guard-due acceptance ALL PASS (36) after adding the new guard row. First script draft asserted +list window 'name' instead of 'target' - fixed. Filed T896 (carried manifests can hold duplicate ipc_names; restored second window loses its name).
- 2026-08-16 14:21: status: in-progress -> done [commit bf408f03e]
