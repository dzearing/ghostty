---
id: T647
title: "Viewer feedback: screenshot capture from the + button and Ctrl+Shift+S"
order: 180.7
deps: ["T637"]
status: "done"
commits: ["45a86ae21"]
seat: "win"
priority: "P2"
triage-reason: "Screenshot capture is the + button the composer already draws. Already M1 with the composer arc."
milestone: "M1"
tags: ["feature"]
---

# T647 - Viewer feedback: screenshot capture from the + button and Ctrl+Shift+S

## In plain terms

The feedback box has a `+` button that should let you grab a picture of what
you are complaining about. On Windows it currently does nothing. This makes it
take a screenshot — and, critically, without wiping whatever you had on your
clipboard, which is what every built-in Windows snipping tool does.

## Summary

Split out of T637, which landed the image half of the composer (storage, stable
chip numbering, clipboard paste, images written into the report). What is left
is the CAPTURE primitive behind the `+` action and Mac's ⇧⌘S — on Windows,
Ctrl+Shift+S while the composer has focus.

Everything downstream already exists: a captured PNG goes through
`ViewerFeedbackBar.attachImage`, which allocates the chip number, inserts the
`[Image #N]` chip and puts the file in the report. This task only has to
produce the bytes.

## Details

- **Take D44's answer.** The decision (`docs/design/windows-parity-decisions/D44.md`)
  is filed and open; its recommended option is a ghoztty-drawn region selector
  over a screen snapshot, because it matches what Mac's `screencapture -i -o`
  actually does and touches no clipboard. `ms-screenclip:` / `SnippingTool /clip`
  are ruled out precisely because they publish through the clipboard.
- **The user's clipboard must come back byte-identical.** That is a validation
  criterion, not a footnote — put a known string on the clipboard, capture, and
  read it back.
- The layered-overlay and DPI-aware screen-capture machinery from the pixel-probe
  work is the starting point; multi-monitor and per-monitor DPI are ours to get
  right (a virtual-screen-sized overlay, not a primary-monitor one).
- Escape cancels with nothing added to the composer; a zero-area drag is a
  cancel, not an empty image.
- PNG encoding is already there and is pure: `png_encode.encode` takes top-down
  RGB/RGBA pixels, and `clipboard_image.normalize` is the worked example of
  getting those out of a GDI bitmap.

## Goals

- [x] The `+` button puts a region selector over the whole desktop and a drag
      out of it becomes an `[Image #N]` chip in the composer.
- [x] Ctrl+Shift+S does the same from the keyboard (Mac's ⇧⌘S).
- [x] The user's clipboard is untouched, which is why the Windows snipping
      tools were ruled out in the first place.
- [x] Escape, a right-click, and a click with no drag all cancel cleanly.
- [x] The captured picture reaches the filed report at the size that was
      dragged.

## Validation criteria

- [x] On-box acceptance: capture a screenshot from the `+` button and from
      Ctrl+Shift+S; a chip and an image file result.
      — `test\win32\viewer-feedback-capture.ps1` arms A/B/F/G: the `+` click and
      the chord each raise a `GhozttyRegionSelect` overlay covering the virtual
      screen; a posted drag yields `capture=done rect=120,140 160x120`, an
      `[Image #1]` chip in the RichEdit, and `images/image-1.png` in the
      published report with `pixelWidth/pixelHeight` of 160x120.
- [x] On-box acceptance: the clipboard's contents before and after a capture are
      byte-identical (put a known string on it first and read it back).
      — same script, arm C: a GUID string is set before the first capture and
      read back unchanged after three captures and a send.
- [x] On-box acceptance: Escape during a capture cancels — no chip, no file.
      — same script, arm D (`capture=cancelled`, overlay gone, no image line, no
      chip) and arm E (a zero-area click cancels too, `done=0`).
- [x] Unit tests (none lane): the selector's rect math (drag in any direction
      yields a normalized non-empty rect; a zero-area drag yields none).
      — `region_select.zig`: all four drag directions, the three zero-area
      shapes, negative coordinates, clipping, and `hintBox` asserted at
      1.0/1.25/1.5/2.0. Plus win32-lane tests in `screen_capture.zig` that
      crop the real desktop and check the PNG's dimensions.

## Progress log

- 2026-08-09 16:25: claimed; work starting.
- 2026-08-09 17:02 [session 83971a7d]: Built D44 option 1: screen_capture.zig photographs the virtual screen once (BitBlt+CAPTUREBLT into a top-down 32bpp DIB, no clipboard anywhere), RegionSelector.zig is the full-desktop opaque overlay that dims it and takes the drag, region_select.zig holds the pure rect math (none lane). Wired to the composer's + button and a new ctrl+shift+s composer chord. Acceptance test\win32\viewer-feedback-capture.ps1: ALL PASS (43) first run, incl. the clipboard byte-identity arm.
- 2026-08-09 17:25: status: in-progress -> done [commit 45a86ae21]
