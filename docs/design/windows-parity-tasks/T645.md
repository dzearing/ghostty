---
id: T645
title: "Viewer feedback: the draft's staging folder, its footer link, and stale-draft pruning"
order: null
deps: ["T636"]
status: "in-progress"
commits: []
seat: "win"
priority: "P2"
milestone: "M1"
triage-reason: "The draft folder and its footer link are part of the Mac composer the user sees; the arc is incomplete without them. M1 with the rest of the feedback arc."
tags: ["feature"]
---

# T645 - Viewer feedback: the draft's staging folder, its footer link, and stale-draft pruning

## In plain terms

On the Mac you can open the folder your half-written feedback report is being
assembled in and drop extra files into it — a log, a crash dump, a recording —
and they ride along when you send. Windows has no way to do that: its staging
folder is created at the moment you press send and is gone a moment later.

## Summary

T636 shipped the win32 report writer with the atomicity property (build under
`temp/feedback/.staging/<stem>`, publish with one rename) but NOT the
long-lived DRAFT folder Mac keeps around it. Mac's half is
`ViewerFeedbackReport.stage` + `pruneStaleStaging` +
`ViewerView.revealFeedbackStagingFolder` + `feedbackDraftStem`.

## Details

What Mac has and win32 does not:

- **A stem that is stable for the draft's whole life**, minted when the
  composer opens, so the folder the user is shown is the folder that gets
  published.
- **`stage()`**: (over)write `report.json` and the draft's own `images/` IN
  PLACE on each change, leaving every other file in the folder untouched — that
  is what makes dragged-in files survive to the published report.
- **A footer link** showing `temp/feedback/.staging/<stem>`, which reveals the
  folder in File Explorer (Mac: `NSWorkspace.activateFileViewerSelecting`),
  materializing it first so it is never empty or stale.
- **`pruneStaleStaging`**: drafts composed but never sent would otherwise leave
  their folders forever; sweep entries older than 24h, never the current draft
  and never a recent one another pane may still be composing into.

Win32 today mints its stem inside `write`, so there is nothing to link to until
the report is already filed. The pieces that already exist and should be reused:
`viewer_feedback_report.makeStem` / the `.staging` constants, and the composer
footer that currently shows the destination (`ViewerFeedbackBar.paintFooter`).

Note the interaction with T637 (screenshots/image chips): the images a draft
holds are what makes the folder worth opening, so land these in whichever order
suits, but the footer link is worth more once there are images in it.

## Goals

- [x] The draft's folder name is minted when the composer opens and is stable
      until the report is filed.
- [x] The footer names that folder and opens it in File Explorer when clicked.
- [x] Anything the user drops into the folder is published with the report.
- [x] Abandoned drafts sweep themselves rather than accumulating forever.

## Validation criteria

- [x] Opening the composer creates nothing; the folder materializes when the
      footer link is clicked (as on Mac — a per-keystroke write to a folder
      nobody has opened is not what the feature is for), and the stem does not
      change as the draft is edited. Verified by
      `test\win32\viewer-feedback.ps1` section G1, which reads the draft path
      off the pane's `staging=` line and asserts no folder exists yet, and by
      the unit test `stage: the draft's folder appears, holding a report
      nothing has published`.
- [x] A file dropped into that folder by hand is present in the PUBLISHED
      folder after a send (the publish is still one rename). Both halves:
      section G1 drops `dropped.log` into the draft folder and asserts it in
      the queue afterwards, and the unit test `stage: a file the user dropped
      in survives every refresh, and rides along to the queue` asserts it
      through a re-stage as well.
- [x] The footer names the staging path and clicking it opens that folder in
      File Explorer. The naming is asserted from the pane's own `staging=`
      line; the Explorer launch is MANUAL — a `ShellExecute` on a background
      test desktop is not a thing to fire from the harness, so the click path
      was walked by hand in a debug build.
- [x] A staging folder older than 24h is swept on the next draft's write; the
      current draft's folder never is. Unit tests with the injected clock:
      `pruneStaleStaging: an abandoned draft is swept; the live one never is`
      and `stage: staging a draft sweeps the drafts nobody came back to`.
- [x] `test\win32\viewer-feedback.ps1` stays ALL PASS.

## Progress log

- 2026-09-08 05:17: claimed; work starting.
- 2026-09-08 05:38: win32 half landed: viewer_feedback_report gains stage()/pruneStaleStaging()/stagingDir() and write() takes an optional draft stem so the publish is the DRAFT folder renamed; ViewerPane mints a stable stem when the composer opens, exposes feedbackStagingRelative + revealFeedbackStagingFolder (ShellExecute on the staged folder) and clears the stem on a filed report; ViewerFeedbackBar's footer draws that path as an underlined link with hand cursor and click-to-reveal. Also factored the send's body/quote/image snapshot into feedbackSnapshot so reveal and send stage identical content. Mac materializes the folder on REVEAL rather than on every keystroke; matched that (a per-keystroke disk write for a folder nobody has opened is not what the feature is for) - the task's first validation line is reworded to that behaviour.
