---
id: T265
title: "Merged chrome row has nowhere to paint a pinned window title (T205 follow-up)"
tags: ["fix", "polish"]
order: 116
deps: ["T205"]
status: "done"
priority: "P1"
triage-reason: "documented pinnable window title shows nothing once 2 tabs are up; visible gap in a shipped feature"
commits: ["6a4db23b7"]
---

# T265 - Merged chrome row has nowhere to paint a pinned window title (T205 follow-up)

## Summary

T205 put the tab run inside the caption band. In that mode `caption_layout`
returns an EMPTY `title` rect on purpose — the tabs occupy the row's left half
and painting a window title behind them is the two-rows problem in one row —
so a window showing 2+ tabs (or launched `--window-show-tab-bar=always`) draws
no window title at all.

That matches the reference exactly: Windows Terminal, Edge and Explorer show
no title text once tabs are in the caption. What makes it worth a task anyway
is that **ghoztty documents a window title as a first-class, pinnable thing**:

> `--title`: Set the **window title**. A window title pins the titlebar — it
> wins over any tab or pane title and survives pane focus changes and shell OSC
> title updates — until cleared.
> (CLAUDE.md, `+new-window` / `+rename --title=`; interactive equivalent
> Cmd+Shift+R "Change Window Title".)

So a user or an agent can pin a title and, at two tabs, see nothing. The pin
still works — it is in the taskbar, in `+list`, and it is what the titlebar
would show if the strip went away — but the on-screen affordance is gone in the
state most agent workflows are in.

## In plain terms

You can give a Ghoztty window a permanent name of your own — from the command
line or with Ctrl+Shift+R — and the window is supposed to show it in its title
area no matter what the programs inside are doing. But as soon as a window had
two or more tabs, that name vanished from the window itself: the tabs took
over the whole top row and the name had nowhere to appear. Now the name is
painted in the empty stretch of the top row between the tabs and the window
buttons, so a window you named stays visibly named. Windows that were never
given a name look exactly as before.

## Goals

- [x] A pinned window title is visible in the top row of a window with 2+ tabs.
- [x] Unpinned windows are unchanged — no title text appears from the tab/pane
      fallback chain, matching Windows Terminal's look.
- [x] The title drops (rather than jittering as an ellipsized sliver) when the
      gap between the tabs and the caption buttons is too narrow.
- [x] Pinning, unpinning, and title changes repaint the row immediately.

## Options (decide, do not drift)

1. **Accept and document.** Match the reference; state in CLAUDE.md that on
   Windows the window title is shown only when the strip is not up, and that
   the taskbar carries it otherwise. Cheapest, and defensible.
2. **Paint it in the drag region.** The empty band between the "+" and the "…"
   is exactly the space a title could use. It is variable — it shrinks as tabs
   are added and can reach zero — so it needs a minimum width below which the
   title drops, and it moves whenever a tab title changes (see T249, which is
   about the same reflow). Closest to "the pin is honored".
3. **Only when pinned.** Paint it only when a title was explicitly pinned
   (not the fallback chain), so the common case still reads like WT and the
   documented feature still shows something.

Option 3 is the recommendation: it keeps the reference look for everyone who
never pins a title, and makes the documented pin visible for everyone who does.

## Validation criteria

- [x] Unit tests: `caption_layout.mergedTitleRect` asserted at 1.0/1.25/1.5/2.0
      (placement off the '+', min-width drop, standalone empty, stays inside
      the strip's half) — none-lane `zig build test -Dapp-runtime=none`, part
      of `floor-lane -Lane all`.
- [x] `test/win32/chrome-merged-row.ps1` §7: with `+rename --title=…` on a
      2-tab window, ink appears in the band between the '+' and the seam ONLY
      while pinned (bare→1152 px→bare measured on-box; ALL PASS, 28 assertions).
- [x] The one-tab case unchanged: `caption-bar.ps1` ALL PASS (24 assertions) —
      a standalone band still paints its title.
- [x] Negative control: `chrome-merged-row.ps1 -NegativeControl` exits 1
      (1 FAILURE / 27 passed), and §7 carries its own built-in negative
      (band asserted bare before the pin and after the clear).
- [x] Standing floor: `floor-lane.ps1 -Lane all` PASS ×3, then P1–P3 ALL PASS,
      run sequentially.

## Progress log

- 2026-08-06 10:08: claimed; work starting.
- 2026-08-06 10:14 [session 05013f7d-3432-4b03-bffa-cbd109185cba]: Option 3 built (paint only when pinned): caption_layout.mergedTitleRect (one pad_md off the '+' painted edge to the seam, tab-band vertical extent, min_tab_w floor) + 4 unit tests; paintTabBar draws the composed window text there when mergedChrome && title_override; invalidations in updateWindowTitle (pinned changes) and setTitleOverride (pin/unpin transitions). Validating now.
- 2026-08-06 11:11 [session 05013f7d-3432-4b03-bffa-cbd109185cba]: User requested a context reset before continuing. State: code landed in caption_layout.zig (mergedTitleRect + 4 tests, none-lane filtered run PASS) and Window.zig (paintTabBar draw + invalidations); UNCOMMITTED. Remaining: extend test/win32/chrome-merged-row.ps1 with a pinned-title ink-probe section (pin via +rename --target=window-1, assert ink in [plusRight+padMd, bandLeft) only while pinned, clear and assert gone), run full floor (floor-lane -Lane all) + P1-P3 sequentially, tick Validation criteria, commit+push.
- 2026-08-06 12:01: stale in-progress claim picked up by a new turn; reassessing from this log and git status before resuming.
- 2026-08-06 12:02 [session 07e48330-06b3-4e76-b040-667847dc8b60]: RESUME assessed: uncommitted code matches the 11:11 log entry exactly (mergedTitleRect + 4 tests in caption_layout.zig; paint + invalidations in Window.zig). Keeping and finishing: acceptance-test extension, full floor, close-out.
- 2026-08-06 12:25 [session 07e48330-06b3-4e76-b040-667847dc8b60]: Validation complete: chrome-merged-row.ps1 gained a section-7 pinned-title ink probe (bare/ink=1152/bare across pin+clear) ALL PASS 28; -NegativeControl fails as designed; caption-bar.ps1 ALL PASS 24; floor-lane all three lanes PASS; P1-P3 ALL PASS, all sequential. No new tasks needed: merged row is win32-only chrome (no Mac analog gap), and the min-width drop is by design with unit coverage.
