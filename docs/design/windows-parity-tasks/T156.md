---
id: T156
title: "shift+insert is swallowed on Windows (paste_from_selection can never perform)"
order: 110
deps: [T154]
status: "done"
priority: "P1"
tags: [fix]
triage-reason: "shift+insert is swallowed entirely: no paste, and the key never reaches the shell. Windows users expect it to paste."
commits: ["3db7a3d5b"]
---

# T156 - shift+insert is swallowed on Windows (paste_from_selection can never perform)

## In plain terms

Pressing Shift+Insert in a Ghoztty terminal on Windows did nothing at all —
it neither pasted the clipboard (the way it does in Windows Terminal and
every console window) nor reached the program running in the pane. Anyone
with Shift+Insert muscle memory found the key simply dead. After this lands,
Shift+Insert pastes your clipboard text, and when there is no text to paste
the keystroke goes through to the program instead of vanishing.

## Goals

- [x] Shift+Insert pastes clipboard text on Windows (Windows Terminal parity)
- [x] With no text on the clipboard, the chord reaches the pane instead of being swallowed
- [x] Linux keeps its selection-clipboard binding untouched (fix lives in the `== .windows` mirror block only)

## Validation criteria

- [x] Unit test: windows shift+insert binding is paste_from_clipboard + performable (win32 lane) — "default keybinds: windows shift+insert pastes from the standard clipboard", green in `floor-lane.ps1 -Lane all` (win32 PASS in 341s)
- [x] Acceptance: clipboard-paste.ps1 section E — text clipboard + shift+insert pastes (probe char=90 'Z', ALL PASS 26 assertions, 2026-08-06)
- [x] Acceptance: clipboard-paste.ps1 section F — image-only clipboard + shift+insert reaches the pane (probe ReadKey returned, char=0 = VK_INSERT keystroke; a swallowed chord never prints)
- [x] Standing floor: none + win32 zig lanes, test-agent (FLOOR SUMMARY all PASS), P1–P3 ALL PASS, all sequential per T401

## Summary

Found by T154's audit of the Windows keybind blocks, same defect class, one
block over. `Config.zig` (non-darwin block, `.{ .physical = .insert }` +
shift) binds **shift+insert -> `paste_from_selection`** with a plain `put()`
and no `performable` flag. On win32 `Surface.clipboardRequest`
(`src/apprt/win32/Surface.zig:1199`) returns **false immediately** for any
`clipboard_type != .standard` — Windows has no selection clipboard — so the
action can NEVER perform. Combined with the missing flag, the chord is
swallowed: shift+insert does nothing at all, and the pane never sees the key
either. Windows users expect shift+insert to paste (it is Windows Terminal's
and conhost's paste chord), so today's behavior is worse than either
alternative.

## Details

Two candidate fixes, pick one after checking Mac/Linux parity intent:

1. **Make it performable** (minimal, matches T154): the chord falls through
   to the shell, which is what a Linux user gets when the selection
   clipboard is empty. Does not give Windows users a working paste.
2. **Bind shift+insert to `paste_from_clipboard` on Windows** (Windows
   Terminal parity, likely what the user actually wants), performable, in
   the same win32 mirror block that owns ctrl+v. The generic non-darwin
   `paste_from_selection` binding stays for Linux.

Recommended: (2) plus performable, since (1) alone leaves a documented
binding that visibly does nothing. Check first whether the Mac seat has an
opinion — this is a Windows-only divergence in a shared block.

## Validation

Extend `test/win32/clipboard-paste.ps1` (T154's script — its in-pane
`[Console]::ReadKey` probe is exactly the oracle needed): with text on the
clipboard, shift+insert must paste it; with an image-only clipboard, the
chord must reach the pane rather than being swallowed. Plus the standing
floor.

## Progress log

- 2026-08-06 07:09: claimed; work starting.
- 2026-08-06 07:12: root cause confirmed: non-darwin block rebinds shift+insert to paste_from_selection (Config.zig ~6968) AFTER the shift+insert->paste_from_clipboard put; win32 clipboardRequest returns false for .selection and the binding is non-performable, so the chord is swallowed. Fix = option 2: re-bind in the windows mirror block to paste_from_clipboard + performable (next to ctrl+v). Unit test added; clipboard-paste.ps1 extended with sections E/F.
