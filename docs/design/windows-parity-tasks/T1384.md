---
id: T1384
title: "the Mac build has never compiled the +list readonly field"
order: null
deps: []
status: "todo"
commits: []
seat: "mac"
priority: "P2"
triage-reason: "P2: the Windows half is proved on box; this is the Mac seat compiling and running its own half"
tags: ["test"]
---

# T1384 - the Mac build has never compiled the +list readonly field

## Summary

T574 added an additive readonly field to +list --json on BOTH servers. The win32 half is proved end to end (test\win32\readonly-badge.ps1, 37 assertions). The Swift half - IPCMessage.swift's TerminalData plus IPCServer.swift reading view.readonly - was written from the Windows seat and has never been through a Mac compile, and the T370 drift detector checks the encoder's SOURCE shape rather than that it builds. The Mac seat should build, toggle read-only on a pane, and confirm +list --json reports readonly true for that pane and omits the key everywhere else.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
