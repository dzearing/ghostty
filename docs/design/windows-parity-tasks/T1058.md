---
id: T1058
title: "Ship workflow: merge the working branch back to main, then per-feature worktrees and PRs"
order: null
deps: []
status: "done"
commits: ["3e7caf25c"]
seat: "win"
priority: "P1"
triage-reason: "User directive 2026-08-21: once we are stable and feature complete, merge back to main and move to per-feature worktrees + PRs. Today every turn commits straight onto one long-lived branch, so there is no review point and no way to ship a feature independently. This task defines the transition; T1059 and T1060 build the machinery it needs."
tags: ["infra", "docs"]
---

# T1058 - Ship workflow: merge the working branch back to main, then per-feature worktrees and PRs

## Summary

User directive, 2026-08-21: "when we feel like we're in a stable state and
feature complete, we would get this all merged back into main and continue work
from there or build individual worktrees for new features and make prs."

Today the loop commits straight onto `users/dzearing/windows-amd64`, one long
branch, hundreds of commits deep. That is fine for a solo sprint and wrong for
anything after it: there is no review point, no way to ship one feature without
shipping every other in-flight change, and no boundary at which quality is
checked (T1060) or CI is consulted (T1059).

## In plain terms

Right now everything goes into one big pile on one branch. It works, but nothing
is ever "finished and shipped" - it is just further down the pile. The change is
to draw a line: get the pile onto main once, and after that let each new feature
grow in its own copy of the repo, come back as a proposal that can be looked at
on its own, and merge when it is actually good. That gives us a place to say no,
and a place to say done.

## What this task owns

- The **entry criteria**: what "stable and feature complete" means concretely
  here, so the decision to cut over is a check and not a mood. Candidate floor:
  all lanes green, P1-P3 green, no P0 open, the guard sweep current, the watchdog
  clean over a real session.
- The **cutover itself**: merging the working branch to `main`. Note the prior
  art - T87 (Mac seat: macOS regression build + merge to main) is still `todo`
  and is a real dependency, since the merge cannot be validated from the Windows
  seat alone. T516 and T879 (divergence inventory, merge-back plan) are done and
  should be read first.
- The **after**: per-feature worktrees, one branch per feature, PR per branch,
  `main` as the trunk that new work forks from. Written into go.md so the loop
  actually follows it, including how a turn picks up mid-PR work and how the
  worktree is created and cleaned up.
- The **handoff to the other two**: PR health lands in the digest (T1059), and
  nothing merges without an adversarial review (T1060).

## Goals

- [x] Entry criteria for the cutover are written down and checkable, not
      subjective. -> `scripts\ship-readiness.ps1`, ten criteria, exit code as
      the verdict.
- [x] The merge to main is planned against T87/T516/T879 rather than improvised,
      with the Mac seat's part named. ->
      `docs/design/windows-parity-ship-workflow.md`; T87 is a named criterion
      (`macseat`) reported on every run, and T1074 is dep-linked to it.
- [x] go.md carries the post-cutover turn shape: fork from main, worktree,
      branch, PR, review, merge - and what a turn does when it inherits an open
      PR instead of a fresh task. -> go.md step 6.9.
- [x] Worktree creation and teardown is a scripted, repeatable step, not a
      remembered incantation. -> `scripts\ship-feature.ps1 new|list|pr|done`.

## Validation criteria

- [x] The entry-criteria check runs and reports on the box, naming what is not
      yet satisfied. **Verified**: `ship-readiness.ps1` run on this box reports
      NOT READY with 7 of 10 unmet (tree, behind, p0, macseat, ghrepo, lanes,
      accept), each with a remedy. Section A of `test\win32\ship-workflow.ps1`
      asserts the honesty properties (unmeasured = unmet, the numbers are git's,
      it mutates nothing).
- [x] go.md's turn protocol describes the PR workflow end to end, and a reader
      following only go.md can execute one full feature through it. **Verified**:
      go.md step 6.9 carries the two commands, the inherited-PR rule and the
      `--repo` rule, and links the doc; section D of the harness asserts all
      three references exist.
- [ ] One feature is taken through the new path end to end as the proof - a
      worktree, a branch, a PR, a review, a merge - before the old path is
      retired. **Carried by T1075**, which is dep-linked to the cutover (T1074)
      and the review gate (T1060). It cannot be done from here today: merging a
      real PR to main before the cutover would put Windows work on the trunk
      outside the plan. The lifecycle itself IS proven mechanically - section B
      runs new/list/pr/done, and every refusal, against a real throwaway repo
      pair.
- [x] T87's Mac-seat half is either done or explicitly sequenced with a date, so
      the cutover is not blocked on an unowned step. **Sequenced, not dated**:
      T87 is a `deps:` of T1074 (the cutover cannot be claimed before it) and a
      criterion of `ship-readiness.ps1`, so it is reported at the top of every
      readiness run rather than remembered. A date is the Mac seat's to give and
      is not this seat's to invent.

## What landed

| | |
|---|---|
| `scripts\ship-readiness.ps1` | the cutover gate: ten criteria, `-Json`, `-RunLanes`, exit 0/1/2 as the verdict |
| `scripts\ship-feature.ps1` | the per-feature lifecycle: `new` / `list` / `pr` / `done`, each with its refusals built in |
| `docs/design/windows-parity-ship-workflow.md` | which merge this is, the criteria table, the cutover steps, the post-cutover turn, the open dependencies |
| `go.md` step 6.9 | the turn-facing summary: the two commands, the inherited-PR rule, the `--repo` rule |
| `test\win32\ship-workflow.ps1` | 39 assertions across four sections; registered in `guard-due.ps1` as `ship-workflow` |

**A landmine was found and closed on the way.** This repo has `upstream`
(ghostty-org/ghostty) as a remote and had no `gh repo set-default`, so a bare
`gh pr list` here answered with UPSTREAM's pull requests - which means the first
`gh pr create` under the new workflow would have offered this fork's Windows
work to the public Ghostty project. Three defences now: the box's `gh` default
is set to `dzearing/ghoztty`, `ship-readiness.ps1` re-checks it as the `ghrepo`
criterion (local config travels with no clone or pull), and every gh call in the
family goes through one funnel that passes `--repo` unconditionally, which
section C of the harness enforces by scanning for a second one.

## Progress log

- 2026-08-21: filed from a user directive in the controller chat.
- 2026-08-21 13:17: claimed; work starting.
- 2026-08-21 13:21 [session e605ccfd]: design: two merge axes already distinguished in windows-parity-merge-back-plan.md; T1058 owns branch->fork-main (never done), not upstream->fork. Measured: branch is 1397 ahead / 93 behind origin/main, merge-base cda6e5191. Fork main ALREADY runs the target shape (users/dzearing/* branches merged as 'Merge branch ...', releases v1.31-v1.34) from the mac seat, so the post-cutover protocol is adoption, not invention. LANDMINE found: bare 'gh pr list/create' in this repo resolves to UPSTREAM ghostty-org/ghostty (no default repo set), so an unqualified gh pr create would open a PR against the public upstream project - every script must pass --repo dzearing/ghoztty explicitly. Building: scripts\ship-readiness.ps1 (checkable entry criteria), scripts\ship-feature.ps1 (worktree/branch/PR lifecycle), docs\design\windows-parity-ship-workflow.md, go.md protocol, test\win32\ship-workflow.ps1.
- 2026-08-21 13:47 [session e605ccfd]: built: ship-readiness.ps1 (10 criteria, exit-code verdict, -Json, -RunLanes; UNKNOWN counts as unmet), ship-feature.ps1 (new/list/pr/done, all gh via one --repo funnel), windows-parity-ship-workflow.md, go.md step 6.9, test\win32\ship-workflow.ps1 ALL PASS 39 + guard row ship-workflow. gh default set to dzearing/ghoztty on this box (was resolving to upstream). Five meta-audits made due by the new harness file re-run green. Readiness on this box: NOT READY, 7/10 unmet - behind(93), p0(T30,T330 mac), macseat(T87 todo), lanes/accept unmeasured. Filed T1073 (behind gap), T1074 (the cutover, deps T87), T1075 (end-to-end proof).
- 2026-08-21 13:55 [session e605ccfd]: validated: ship-workflow ALL PASS (39), floor lib/none/win32/agent ALL PASS (win32 390s, agent 365s), P1/P2/P3 ALL PASS (25/20/16), five meta-audits re-run green, guard-due check exit 0.
- 2026-08-21 13:55: status: in-progress -> done [commit 3e7caf25c]
