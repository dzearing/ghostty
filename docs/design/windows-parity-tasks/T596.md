---
id: T596
title: "T400\u0027s end-to-end debounce assertion compares two cache-dependent fetch counts, so the host-floor test flakes red"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P1"
triage-reason: "P1: a floor lane that goes red without a real failure teaches the loop to ignore red, which is worse than the flake"
tags: ["test"]
---

# T596 - T400's end-to-end debounce assertion compares two cache-dependent fetch counts, so the host-floor test flakes red

## In plain terms

One of the automated checks fails at random — roughly every other full test
run — even when nothing is wrong. It is not testing what it thinks it is: it
compares how many times a page was fetched against a number it measured
earlier, and both numbers depend on whether the browser happened to have the
page cached at that moment. A test that cries wolf is worse than no test,
because the next real failure gets waved through as "that one again".

## Goals

- [x] The flaky comparison is gone: the test no longer equates two
      cache-dependent fetch counts
- [x] The regression arm keeps its teeth (restoring T400's bug still turns
      the section red)
- [x] The diagnostic log line survives in a form that still names the
      navigation's cost

## Summary

`ViewerPane.zig`'s "host floor" test ends its T400 section with an end-to-end
companion assertion:

```zig
const nav_cost = reload_page.requests.load(.acquire) - t400_base;   // ~:3963
...
const after_nav = reload_page.requests.load(.acquire) - before_nav; // ~:4010
try testing.expectEqual(nav_cost, after_nav);                       // ~:4015
```

Both sides count HTTP fetches the test server saw for a REPEAT navigation to
the same URL, so both depend on whether WebView2's HTTP cache served that
navigation. They are measured at different points with different cache warmth,
and nothing makes them agree.

**Evidence — one `floor-lane.ps1 -Lane all` run, 2026-08-08, identical source:**

| lane | log line | result |
|---|---|---|
| win32 (03:44) | `a repeat navigation costs 0 fetch(es)` / `window cost 0, expected 0` | PASS |
| agent (03:49) | (nav_cost 1) / `window cost 0 fetch(es), expected 1` | **FAIL** |

Five minutes apart, same binary logic. The failure is non-deterministic by
construction, so it cannot be a code regression — it is the assertion.

## Details

The comment directly above the assertion already records why counting fetches
is the weak oracle here ("a stale timer that fires while the destination is
STILL LOADING hits `reloadPlan`'s `full_load` branch, not `refetch` … the
mutation is invisible at the server in both cache states"). The line above it —
`expectEqual(0, KillTimer(...))`, the timer oracle — is the assertion that
actually proves T400's fix and is fully deterministic. The fetch-count
companion adds the user-visible framing but pays for it with a coin flip.

Options, for whoever takes this:

1. **Make the baseline deterministic.** Warm the cache before measuring
   `nav_cost` (navigate to `reload_url` and settle once more, discarding the
   count) so both measurements are taken in the same cache state.
2. **Assert the bound instead of the equality.** The claim is "the page they
   opened is loaded once, not twice" — `after_nav <= nav_cost` (or
   `after_nav <= 1`) states that without depending on which side the cache
   fell on.
3. **Drop the companion.** The timer oracle above it already turns red when
   the bug is restored, which the comment says explicitly.

Whichever is chosen, keep the log line: it is what made this diagnosable.

## Validation criteria

- [x] The assertion no longer depends on WebView2 cache state — stated in a
      comment and visible in the code. **Verified by construction:** the
      asserted number is the fetch-count delta across `pumpFor` AFTER the
      destination settles; nothing navigates in that window, so the cache
      cannot contribute to it. The navigation's own (cache-dependent) cost is
      logged, never asserted. The old calibration loop is deleted outright.
- [x] The host-floor test passes in every lane and every repeat.
      **Verified:** `floor-lane.ps1` has no `-Repeat` switch, so run as
      3 consecutive filtered runs of `zig build test -Dapp-runtime=win32
      -Dtest-filter="host floor"` (all green, `t400: navigation cost 0
      fetch(es); pumping past the debounce window cost 0, expected 0` each
      time) plus one full `floor-lane.ps1 -Lane all` sweep (lib/none/win32/
      agent — the win32 and agent lanes each run the host-floor test once
      more).
- [x] Restoring T400's bug (drop the `KillTimer` from `syncWatcher`) still
      turns the section red, so the fix did not cost the regression arm.
      **Verified:** with the `KillTimer` commented out, the section fails at
      the timer oracle (`expected 0, found 1`) — note the fetch-count
      companion never even executes with the bug present, because the timer
      oracle's `try` above it fails first; the companion was always framing,
      never the teeth.

## Progress log

- 2026-08-08 17:53: Recurred 2026-08-08 twice in a row during T125's floor (agent lane), then passed on the third run. Same signature: 't400: stale-debounce window cost 0 fetch(es), expected 1'. Also present in this box's 16:04 lane log the same day, so the every-other-run estimate still holds.
- 2026-08-16 15:10: claimed; work starting.
- 2026-08-16 15:12 [session d7a49cb5-5d23-4f63-a13a-8c0c3136e0b5]: Reassessed vs T690 (double calibration, 82cd56f63): no flake sighting in the log since 2026-08-10, but the assertion still equates two cache-dependent measurements - suppressed, not removed. Plan: drop the calibration loop and the equality; after the destination settles, assert pumping past the debounce window costs ZERO additional fetches (no navigation in that window, so no cache involvement); log the navigation's own cost instead of asserting it. The KillTimer oracle keeps the teeth (with the bug present the companion never even runs - the KillTimer try fails first). Will close T597/T615 as dups on completion.
- 2026-08-16 15:28 [session d7a49cb5-5d23-4f63-a13a-8c0c3136e0b5]: Fix landed in ViewerPane.zig: calibration loop deleted; companion now asserts the post-settle delta is 0 (reads the counter after page_loaded, pumps reload_debounce_ms*4, asserts no growth) and logs the navigation's own cost instead of asserting it. Validation so far: 3/3 filtered host-floor runs green (nav cost 0, post-settle 0 each time); teeth check green->red confirmed (KillTimer dropped from syncWatcher turns the section red at the KillTimer oracle, 'expected 0, found 1'). Floor -Lane all running.
