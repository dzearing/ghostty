---
id: T1189
title: "The MSI packaging guard is due after every packaging edit and only Docker can clear it"
order: null
deps: ["T1176"]
status: "done"
commits: []
seat: "win"
priority: "P1"
triage-reason: "Stopped being internal on 2026-08-31: the Docker-only packaging guard is exactly why an unbuildable installer reached a release run (T1218). T1218 added a no-Docker XML gate for the one defect that bit; the general problem - only Docker can prove the package compiles - is still this task."
tags: ["infra"]
---

# T1189 - The MSI packaging guard is due after every packaging edit and only Docker can clear it

## In plain terms

Nothing a user can see. This is about the safety net around the Windows
installer: the check that says "somebody has actually run the installer tests
against the installer script as it stands now". On this machine that check can
only be satisfied with Docker running, so every time the installer script is
edited the check goes red and the only way past it is the manual override. An
override you press every time stops being a decision.

## Summary

`guard-due.ps1`'s `release-artifacts-packaging` row covers
`dist/windows-installer/build-msi.sh`, and its harness section (B in
`test\win32\release-artifacts.ps1`) needs Docker and the `msitools-local`
image. Without Docker the run is `ALL PASS (1 skipped)` and deliberately does
not stamp, so the row stays due. Docker is down on this box by default and
starting it is a user call (a WSL2 backend that has buried the machine before),
so a packaging edit ends with `parity-tasks.ps1 validate -NoGuardDue` — which
is precisely the "this harness cannot run here" case the hatch exists for, but
used routinely it is indistinguishable from the T1165 misuse.

Worth considering, in preference order:

- CI already builds the MSI on every commit (`fork-ci.yml` → msitools 0.106 →
  `build-release-artifacts.sh`). If a green CI run for a sha could WRITE the
  packaging stamp, the guard would be cleared by the machine that can actually
  answer it, and the on-box hatch would go back to being rare.
- Failing that, split the row: the parts of `build-msi.sh` a Docker-less run
  DOES cover (now including `test\win32\install-launch.ps1`, T1176) stamp
  normally, and only the FILEVERSION half stays Docker-gated.
- Failing that, make the hatch cheaper to read: have `validate -NoGuardDue`
  print WHICH guard was excused and why, so a turn cannot excuse a red harness
  while believing it excused an unrunnable one.

## Validation criteria

- [x] A `build-msi.sh` edit on a Docker-less box can reach a clean
      `parity-tasks.ps1 validate` without `-NoGuardDue`. The row is ADVISORY
      now: reported by every claim, counted against nothing. Arm K8 of
      `test\win32\guard-due.ps1` drives the real `validate` against a fixture
      whose only due row is that one and asserts it does not fail; arms K7/K7b
      assert the row still SAYS it is due, in the same words plus `(advisory)`.
- [x] The Docker-only assertions are still gated by something - the change
      must not silently stamp coverage that never ran. Three things, all
      measured: `install-launch` covers the same file and still blocks (K8
      stamps it explicitly to isolate the advisory row); sections B5-B7 of
      `release-artifacts.ps1` parse the generated WXS without Docker, with B7
      as the negative control; and fork-ci's `windows-cross` job compiles the
      MSI on every push while `validate` fails on a red CI verdict (T1219).
      The Docker bar for the stamp itself is unchanged - a skipped section
      still stamps nothing (`ALL PASS (1 skipped)`, packaging stamp not
      updated, observed on this box today).
- [x] Demonstrated failing: a deliberately broken packaging surface still
      makes the guard due / the harness red. K1 (a red build machine), K2/K2b
      (a green run whose `windows-cross` job failed) and K3/K3b/K3c (a green
      job that built OTHER bytes) each stamp nothing and exit 1; K10 refuses a
      row that declares no CI evidence at all.

## Progress log

- 2026-08-31 12:25: priority: P2 -> P1 (Stopped being internal on 2026-08-31: the Docker-only packaging guard is exactly why an unbuildable installer reached a release run (T1218). T1218 added a no-Docker XML gate for the one defect that bit; the general problem - only Docker can prove the package compiles - is still this task.)
- 2026-08-31 12:25 [session 9d2e9ecb]: T1218 (2026-08-31) is the measured cost of this: build-msi.sh carried an XML-illegal comment for 10 hours, no on-box check could see it, and the first thing that noticed was a failed release run. Section B5-B7 of release-artifacts.ps1 now parses the generated WXS without Docker, which closes that one class; the FILEVERSION compile check (B1) is still Docker-only.
- 2026-09-01 01:35: claimed; work starting.
- 2026-09-01 01:49 [session ecd0ee79]: design: the packaging row becomes ADVISORY (reported by claim, never fails validate) because this box cannot answer its question at all; its teeth move to fork-ci (validate already fails on CI RED) plus install-launch/B5-B7 locally. New 'guard-due.ps1 stamp-ci' clears such a row from the green CI run that built byte-identical covered files. -NoGuardDue now names what it excused.
- 2026-09-01 02:15: status: in-progress -> done
