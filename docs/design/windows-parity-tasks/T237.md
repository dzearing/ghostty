---
id: T237
title: "CLOSE_SESSION RPC times out against the local agent (1.5s, no reply) even though both peers negotiate the capability"
order: 130
deps: []
status: "done"
priority: "P1"
milestone: "M1"
triage-reason: "CLOSE_SESSION never replies to the local agent; the chooser's session Kill is built on that RPC and tombstones pile up"
commits: ["c71feb2e5"]
tags: ["test"]
---

# T237 - CLOSE_SESSION RPC times out against the local agent (1.5s, no reply) even though both peers negotiate the capability

## In plain terms

When the app asked the background service to delete a leftover dead session,
the answer never seemed to arrive - the app waited, gave up, and logged a
failure even though the deletion usually worked. That same ask-and-answer is
what the "Kill" button in the machine picker uses, so a kill could look like
it failed when it hadn't. The two underlying defects were fixed by earlier
tasks; this task confirms the fixes cover the original report and closes the
last untested gap: proving that leftover dead sessions really are cleaned up
after a restart, instead of piling up forever.

## Goals

- [x] Establish why the reply never arrived (root cause, with evidence).
- [x] The ask-and-answer is tested: the reply arrives, on time, in every lane
      that can see it.
- [x] Cleanup after a restart is proven on the box: no dead leftover sessions
      accumulate in the service's records.

## Validation criteria

- [x] A test drives `closeSession` and asserts it returns `ok` within a sane
      bound - `connection.zig:5047` ("the agent's CLOSE_SESSION_RESULT wakes
      the RPC", none lane, from T96) plus the agent-lane ordering test
      "CLOSE_SESSION replies BEFORE the child teardown" (T326); end-to-end
      against a real agent, `test\win32\chooser-sessions.ps1` asserts
      `confirmed=true` on the Kill path - ALL PASS (17) on 2026-08-07 (T326's
      run).
- [x] On box: after a `notify` restore, dead tombstones must NOT accumulate -
      new assertion A14 in `test\win32\session-relaunch-notify.ps1` (the
      marker tombstones leave sessions.json while the fresh live sessions
      remain) - full script ALL PASS (92), A14 `live=3`, 2026-08-07.

## Summary

Found 2026-07-31 while building T230. The first implementation of the `notify`
relaunch policy retired the dead tombstone it was replacing with the BOUNDED
RPC `Connection.closeSession(sid, 1500ms)`. On box, against the local
`ghoztty-agent`, it timed out **every time**:

```
info(io_remote): notify policy: could not retire dead session err=error.Timeout (harmless)
info(io_remote): notify policy: could not retire dead session err=error.Timeout (harmless)
```

(two panes, two timeouts, same run — `src/termio/Remote.zig`, since replaced by
the fire-and-forget `closeSessionNoWait`).

`error.Timeout` — not `error.Unsupported` — means the capability gate
(`supportsCloseSession`) said **yes**: both HELLOs advertised `close_session`,
the frame went out, and no `CLOSE_SESSION_RESULT` came back within 1.5 s. So
either the agent is not replying, is replying on a channel the client is not
correlating on, or the reply is slower than 1.5 s.

T230 does not need the answer (it now sends the request fire-and-forget, which
is the right shape for housekeeping on a pane's bring-up path either way), so
this is filed rather than fixed. But it matters on its own: the same RPC is what
the machine chooser's session **Kill** is built on (T146), and there it IS
load-bearing — a Kill that hangs for its whole timeout, or reports failure while
the session actually dies, is a bad affordance.

## What to check

1. Does the agent even reach `handleCloseSession`?
   (`src/remote/agent/server.zig`) It looks correct on inspection — it answers on
   `frame.channel` in every arm, including the malformed-payload one — so
   instrument the agent and confirm the frame ARRIVES.
2. Channel correlation. `closeSession` picks a random `req_channel` and
   `rpcCall`s `.close_session` → `.close_session_result`
   (`src/remote/connection.zig`). Confirm the agent's reply channel is the
   REQUEST channel and that the client's pending-RPC table is keyed the same
   way (`requestSessions`/`relaunch` use the same shape and do work).
3. Timing. `handleCloseSession` does `freeUnlinked` + `persistMeta` +
   `reapLayouts`/`persistLayouts` before replying — all synchronous, all under
   or around the store lock. Measure it; if a sessions.json write can exceed
   1.5 s the fix is to reply first and persist after.
4. Whether the tombstone is nevertheless being freed (i.e. the request works and
   only the REPLY is lost). That distinction decides whether this is a wire bug
   or a liveness bug.

## Validation

- A test that drives `closeSession` against a real agent and asserts it returns
  `ok` within a sane bound, in the none-runtime or agent lane — the existing
  coverage clearly does not exercise the local-agent transport this way.
- On box: after a `notify` restore, `ghoztty +sessions --json` must NOT
  accumulate one dead tombstone per pane per agent restart.

## Resolution (2026-08-07)

Closed as verified-fixed-by T96 + T326, plus the one missing validation arm
added here.

The four "What to check" questions were all answered by T96's on-box
instrumentation (its task file has the traces): the frame DID arrive, the
agent DID reply on the request channel, the whole teardown ran in ~30 ms, and
the tombstone WAS freed — the reply was lost in the CLIENT, because
`close_session_result` was the one A→C reply frame with no dispatch arm in
`handleControlInternal`, so `deliverRpcReply` never woke the parked RPC slot.
T96 added the arm (7ee3a1e16); T326 then hardened the agent side so the reply
is sent before the child teardown (14a4127a4) and cannot go late on a loaded
box.

What this task added: the second Validation bullet had no coverage anywhere —
nothing asserted the notify path's fire-and-forget retirement
(`closeSessionNoWait`, `src/termio/Remote.zig:625`) actually removes the dead
tombstones. `test\win32\session-relaunch-notify.ps1` arm A now asserts it
(A14): after the restore, the marker commands must leave the agent's
sessions.json while the file still lists all the fresh live sessions (so an
empty file can't satisfy it; A5–A8 prove the tombstones existed at restore).
Note the retirement path is *reachable* under the notify default because the
notice arm runs first and the CLOSE is sent on bring-up — A14 measured the
roster at exactly the 3 live fresh sessions with both markers gone.

Floor note: this change is a test script + docs only (no compiled code). The
full floor — both zig lanes, test-agent, P1–P3, chooser-sessions — ran green
earlier the same night on these exact binaries (T326's validation, recorded in
its file); the relevant acceptance for this diff is the edited script itself,
run in full: ALL PASS (92).

## Progress log

- 2026-08-07 00:36: claimed; work starting.
- 2026-08-07 00:37 [session 2aa4b7db-8bc9-4cf8-bc01-a019aca6ae25]: Reassessed: the RPC timeout itself was root-caused and fixed by T96 (missing close_session_result client dispatch arm, commit 7ee3a1e16) and the reply ordering hardened by T326 (reply before teardown, 14a4127a4, today). chooser-sessions.ps1 asserts confirmed=true against a real agent and is ALL PASS. Remaining gap = T237's second validation ask: session-relaunch-notify.ps1 never asserts the notify path actually RETIRES the dead tombstones (closeSessionNoWait, Remote.zig:625). Adding that assertion to arm A.
- 2026-08-07 00:39 [session 2aa4b7db-8bc9-4cf8-bc01-a019aca6ae25]: Coverage audit: closeSession RPC has none-lane tests (connection.zig:5047 wake/miss/timeout/capability, T96), agent-lane reply-ordering test (T326), and chooser-sessions.ps1 asserts confirmed=true on-box. Added A14 to session-relaunch-notify.ps1: after the notify restore, sessions.json must lose the marker tombstones while still listing the fresh live sessions. Running the script now.
