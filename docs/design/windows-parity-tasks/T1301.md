---
id: T1301
title: "Uninstalling a throwaway test product leaves its Start Menu shortcut behind"
order: null
deps: []
status: "done"
commits: ["d7dda7b4a"]
seat: "win"
priority: "P1"
triage-reason: "P1: it silently weakens every harness that installs a real package (T1194, T1209, T1299) - the rewritten package's shortcut component is never registered, so no uninstall can remove it, and each run leaves a dead entry in the box's Start Menu."
milestone: "M1"
tags: ["fix", "infra"]
---

# T1301 - Uninstalling a throwaway test product leaves its Start Menu shortcut behind

## In plain terms

Three test harnesses install a throwaway copy of Ghoztty and then uninstall it,
so that the installer can be exercised without touching the terminal you
actually use. The uninstall does not finish the job: the Start Menu entry it
created stays behind, pointing at a folder that no longer exists. Every run
leaves another one, and nothing was checking.

## What is actually wrong

`scripts\msi-test-identity.ps1` rewrites a published package into a throwaway
product. Measured on `win-v1.36.2` on 2026-09-03:

| Component | ComponentId in the published package | after the rewrite |
|---|---|---|
| `C_UserPathEntry` | `{AE832FF0-BD88-5A1A-98AE-C2877CCAD643}` | remapped, non-empty |
| `C_StartMenuShortcut` | `{E4ED7C37-21EB-5C3D-A455-85C590CB13E3}` | **empty** |

A component with no ComponentId is never registered, so Windows Installer does
not believe it is installed. The uninstall log says so in one line:

```
Component: C_StartMenuShortcut; Installed: Null;   Request: Absent;   Action: Null
Component: C_UserPathEntry;     Installed: Local;  Request: Absent;   Action: Absent
```

The install still works, which is what hides it - it is the UNINSTALL that
quietly does less than it says, exactly the shape the rewriter's own header
describes for the blanked *primary keys* it already repairs
(`Restore-DamagedKeys`). This one is not a primary key, so nothing puts it back
and nothing notices: the script's verification pass checks
`Property.ProductCode`, `Property.ProductVersion` and friends, but never asks
whether every `Component` row still HAS a ComponentId.

Left behind on this box by earlier runs, still there today:
`HKCU\Software\dzearing\GhozttyT1194Test` and `...\GhozttyT1209Test`.

## What to do

- Find which write frees the string (the rewrite order is Component GUIDs ->
  Registry keys -> Shortcut name, all inside one session; the string pool is
  refcounted, and `Shortcut`.`Name` is the write that replaces the last use of
  "Ghoztty"). Restore the cell the way `Restore-DamagedKeys` restores a blanked
  key, or reorder so nothing shares a freed string.
- Add the missing gate to the script's verification pass: **every `Component`
  row has a non-empty ComponentId after the rewrite**, and the count of distinct
  ComponentIds equals the row count. A rewrite that half-registers a product must
  fail loudly rather than produce a package that installs and cannot be removed.
- Then delete the `T1301` skip in `test\win32\install-walkthrough.ps1` (F5) so
  the walk asserts the shortcut is gone again, and drop the two leftover HKCU
  keys above.

## Validation criteria

- [x] A rewritten package has a non-empty, distinct ComponentId on every
      `Component` row, asserted by `msi-test-identity.ps1`'s own verification
      pass (and shown going red against a deliberately blanked row).
      *Verified: the rewriter reports `identity p1: 1 cell(s) re-asserted
      (Component.C_StartMenuShortcut.ComponentId)` and then `verified: 582
      components`; `-NegativeControlBlankComponent C_StartMenuShortcut` exits 1
      with `rewrite verification FAILED (package deleted)` and no package on
      disk. Both are asserted as A4/A5 in `install-walkthrough.ps1`.*
- [x] `test\win32\install-walkthrough.ps1` runs with F5 ENABLED and passes: the
      Start Menu shortcut is gone after uninstall.
      *Verified: ALL PASS (33 assertions, 1 SKIPPED - case D, this published
      package predates the MaintenancePrompt custom action), `F5 the Start Menu
      shortcut is gone` among them.*
- [x] `test\win32\update-real-msi.ps1` and `test\win32\update-graceful.ps1` are
      still green (the rewriter is theirs too), and neither leaves a
      `HKCU\Software\dzearing\Ghoztty*Test` key behind.
      *Verified: `ALL PASS (30 checks)` and `ALL PASS (42 assertions)`, after
      which `HKCU\Software\dzearing` holds only `Ghoztty` and the Start Menu
      only `Ghoztty.lnk`. Getting there needed a second fix - see below.*

## What the fix turned out to be

Two defects, and the second is why the first would have looked fixed while
still shipping:

1. **The rewriter.** `Component|ComponentId` is on `$intendedCells`, so the
   repair pass reads a cell that went BLANK there as "changed on purpose" and
   skips it - the same shape as the `Property.ProductVersion` case session 4
   already handled. Rewriting `Shortcut`.`Name` freed the string-pool entry
   `C_StartMenuShortcut`'s (already remapped) GUID shared. `Update-MsiColumn`
   now records what every intended cell must hold and session 4 re-asserts the
   drifted ones; verification fails on any blank ComponentId, and on a
   `Shortcut.Name` that is not this identity's.
2. **The harness cache.** `Get-ThrowawayPackage` re-rewrote only when the
   PUBLISHED package was newer than the cached rewrite - so a fix to the
   rewriter invalidated nothing. Both harnesses ran green over a pre-fix
   package, installed it, and left the shortcut behind anyway. The rewriter's
   own mtime is now part of that comparison, because its output is what is
   under test.

Also filed: **T1303** (section E's 30s sweep wait goes red under box load and
green solo, minutes apart, over the same code).

## Progress log

- 2026-09-03 11:48: claimed; work starting.
- 2026-09-03 11:54: root cause: Component|ComponentId is on the rewriter's intended-cells list, so the repair pass skips a cell that went BLANK there - same shape as the Property.ProductVersion case session 4 already handles. Rewriting Shortcut.Name freed the pool entry C_StartMenuShortcut's (already-rewritten) GUID shared. Fix: Update-MsiColumn now records what every intended cell must hold, and session 4 re-asserts the drifted ones; verification now fails on any blank ComponentId, with -NegativeControlBlankComponent as the demonstration it can go red. Measured: 'identity p1: 1 cell(s) re-asserted (Component.C_StartMenuShortcut.ComponentId)', and the negative control deletes the package.
- 2026-09-03 12:30: status: in-progress -> done [commit d7dda7b4a]
