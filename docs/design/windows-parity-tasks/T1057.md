---
id: T1057
title: "Make the push non-optional: a turn cannot end with HEAD ahead of origin"
order: null
deps: []
status: "done"
commits: ["bc30ff6d4"]
seat: "win"
priority: "P1"
triage-reason: "User restated the rule on 2026-08-21 ('there is no reason not to push to remote... all commits are pushed'), which is the second time it has had to be said (first: 2026-08-07, go.md step 4). A rule restated is a rule not enforced; the guard is small and removes the whole class."
tags: ["infra"]
---

# T1057 - Make the push non-optional: a turn cannot end with HEAD ahead of origin

## Summary

The rule already exists - go.md step 4 ("push immediately after each commit... an
unpushed commit is invisible to the other seat") - and `git-commit-guard.ps1`
can do it. But `-Push` is an opt-in switch (`scripts/git-commit-guard.ps1:79`,
`:346`), and a plain `git commit` bypasses the guard entirely. So the rule is
honour-system, and the user has now had to state it twice (2026-08-07, and again
2026-08-21). Close it mechanically instead.

## In plain terms

Work that is committed but still sitting on this machine may as well not exist.
The other seat cannot see it, a dead box loses it, and the branch that is
supposed to be the shared record of what has been built is quietly behind what
was actually built. We already agreed to push every time. It should not be
possible to forget.

## Goals

- [x] `-Push` is the default in `git-commit-guard.ps1`; opting OUT is the
      explicit flag, and it says why on the way past.
- [x] Something checks, at the turn boundary, that `HEAD` is not ahead of its
      upstream - and says so loudly enough that the loop cannot narrate a
      finished task over an unpushed commit.
- [x] A rejected push is handled, not just reported: pull/rebase and push, per
      go.md step 4, with the failure surfaced if that does not resolve it.

## Validation criteria

- [x] With a local commit and no push, the guard reports the unpushed state and
      fails; after a push it is clean. Both directions demonstrated on the box.
      *(W23/W26 of `test\win32\go-loop-guard.ps1`: exit 7 naming the commit,
      then `push clean` after the `push` verb.)*
- [x] `git-commit-guard.ps1 commit` (no flags) pushes; the opt-out path is
      covered by a test and prints its reason. *(W19 the default push reaches
      the fixture remote; W21/W22 `-NoPush -Reason` prints the reason and the
      commit demonstrably does NOT reach the remote.)*
- [x] A teeth-check proves the new check can go red - a planted unpushed commit
      is caught, not waved through. *(W31: `parity-tasks.ps1 validate` FAILS
      over a planted local-only commit; W30/W33 are the green directions either
      side of it, and W32 proves the `-NoPushCheck` hatch announces itself.)*
- [x] Floor lanes green. *(`floor-lane.ps1 -Lane all`: lib, none, win32, agent.)*

## Progress log

- 2026-08-21: filed from a user directive in the controller chat.
- 2026-08-21 12:35: claimed; work starting.
- 2026-08-21 13:04 [session d4a5878e]: built: commit pushes by default (-NoPush -Reason opts out and says why), new unpushed/push verbs, rejected push recovered by pull --rebase (no autostash - shared tree); claim reports unpushed, validate FAILS on it (-NoPushCheck hatch). 16 new arms W19-W33 green, parity-tasks-seat green, 4 script audits re-stamped.
- 2026-08-21 13:15: status: in-progress -> done
