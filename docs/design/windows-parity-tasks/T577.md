---
id: T577
title: "release-windows.yml is not on main, so no Windows release has ever been published automatically"
order: 147.5
deps: []
status: "done"
commits: ["e6bbe0460"]
seat: "win"
priority: "P0"
milestone: "M1"
triage-reason: "the automated Windows release has never once run; the public download page offers a build from 2026-07-19 while macOS ships v1.31.0"
tags: ["infra"]
---

# T577 - release-windows.yml is not on main, so no Windows release has ever been published automatically

## In plain terms

Windows users who click Download on the website get a build from July 19th.
Mac users get today's. Every piece of machinery that was supposed to fix
that got built and tested — it just lives on a branch that releases are not
cut from, so it has never actually run. Nothing is broken; nothing is
plugged in.

## Goals

- [x] Find out why the workflow never fired — and the answer turned out not to
      be "it is on the wrong branch" but "main cannot build Windows at all".
- [x] Give the workflow a trigger that a ref carrying the Windows source can
      actually reach.
- [x] Publish a Windows release for the current version, with both the MSI and
      the portable ZIP. — `win-v1.31.0`, 2026-08-08.
- [x] Have the download page move itself to that release, automatically, as
      part of the same run. — gh-pages commit `2b0bb6925`, by the workflow.

## Summary

T38 built the automated Windows release: `.github/workflows/release-windows.yml`
fires on the same `vX.Y.Z` tag push as the macOS release, cross-compiles on
`ubuntu-latest`, publishes `Ghoztty-<version>-x64.msi` +
`Ghoztty-portable-<version>-x64.zip` to a `win-v<version>` release, and then
runs `dist/website/update-windows-links.py` to retarget the landing page at
what it just published (T39).

**None of it has ever executed.** The workflow file exists only on
`users/dzearing/windows-amd64`; `origin/main` does not have it, and release
tags are cut from `main`. Evidence gathered 2026-08-07 while working T349:

```
$ git ls-tree --name-only origin/main .github/workflows/ | grep windows
(nothing)

$ gh run list --repo dzearing/ghoztty --workflow release-windows.yml
HTTP 404: workflow release-windows.yml not found on the default branch

$ gh release list --repo dzearing/ghoztty --limit 200 | grep win-v
Ghoztty for Windows v1.4.1    win-v1.4.1    2026-07-19T22:30:23Z
```

So the newest Windows release is still the hand-published `win-v1.4.1` from
before T38 landed, while macOS is at `v1.31.0` (2026-08-07) — the exact
27-releases-behind gap T38 was written to close, still open, for a different
reason than the one T38 fixed. The website is honest about what exists
(T39's rewrite only runs when a release is published, and T349 added the
unsigned/SmartScreen caveat), which is why this has stayed invisible: every
link answers 200, it just answers with a three-week-old build.

## Details

The fix is not code — the workflow is written and its wiring is asserted by
`test/win32/website-windows-download.ps1` section D. It is a delivery
question, and the honest options differ in how much they front-run the
merge-back:

- **Cherry-pick the release path onto `main`** — the workflow plus the
  scripts it calls (`dist/windows-installer/build-release-artifacts.sh`,
  `build-portable-zip.sh`, `dist/website/update-windows-links.py`). Smallest
  surface that makes the next tag publish Windows artifacts, and it does not
  wait on the whole branch being merge-ready. The artifacts it would build
  come from `main`'s source, which is the honest thing to ship anyway.
- **Wait for the branch merge-back**, and accept that Windows publishes
  nothing until then. Fewest moving parts, but the lag is unbounded and the
  download page keeps offering July's build.
- **Run `scripts/publish-windows-release.ps1` on this box per release** as a
  stopgap. It is the on-box path T38 kept working and it defaults `-Version`
  to the newest `vX.Y.Z` tag. This is precisely the "needs a human to
  remember" process T38 replaced, so it is a bridge, not an answer.

Whichever is chosen, verify it by *observation* rather than by the workflow
file existing: a `win-v<version>` release with both assets, and a gh-pages
commit from the workflow moving `win-msi-link` / `win-zip-link` /
`win-version`.

Note that a Windows build published off `main` today would be missing every
parity feature on this branch. That is a reason to hurry the merge-back, not
a reason to keep shipping a build from July — but it should be a conscious
call, so file it as a decision if the answer is not obvious when this is
picked up.

## Resolution (2026-08-07)

**The first option above cannot work, and the Details section above is kept
verbatim as the hypothesis it was.** `origin/main` does not merely lack the
workflow — it lacks everything the workflow builds. There is no
`src/apprt/win32`, no `dist/windows-installer`, no `dist/website`, and main's
apprt enum is `gtk`/`none`/`embedded`, so `-Dapp-runtime=win32` is not a valid
build option there at all. A workflow runs from the tree of the ref that
triggered it, so a `vX.Y.Z` tag cut from main cannot produce a Windows build
no matter which branch the file lives on. Cherry-picking it would have turned
every macOS release red rather than shipping Windows.

So the trigger set was widened instead: `release-windows.yml` now fires on
`win-v*` as well as `v*`. A `win-vX.Y.Z` tag cut on
`users/dzearing/windows-amd64` is the only tag shape whose tree carries both
this workflow and the source it compiles. `v*` is untouched, so at merge-back
the main-side trigger starts working on its own with nothing to rewire; the
globs are anchored, so they never double-fire and `release.yml`'s own `v*`
never picks up a `win-v` tag. The version parse strips an optional `win-`
before the `v`, so both shapes resolve to the same `X.Y.Z` and publish onto
the same `win-v<version>` release that `update_check.zig` scans. Recorded as
decision D33.

Two things the first run then found, neither of which was code on this branch:

- **CI's msitools was three minor versions behind the MSI it builds.**
  `ubuntu-latest` ships msitools 0.103, whose `wixl` does not know the
  `<Environment>` element and aborts on the component that puts ghoztty on the
  user PATH (T70). The Mac seat's `brew install msitools` and the on-box
  `msitools-local` Debian image are both on 0.106 — the version
  `build-msi.sh`'s post-compile patches are written against.
  `build-release-artifacts.sh` was built so those paths could not produce
  different artifacts, but that only ever covered the script; the tool
  underneath it was whatever the machine had. CI now builds msitools v0.106
  from source and asserts the version it got.
- **The Zig cross-compile itself was fine** — 10 minutes on `ubuntu-latest`,
  producing an exe embedding the right version string, on the very first
  attempt. Nothing was published from the failed run, because the
  `Verify artifacts` gate sits before the publish step.

Follow-ups filed: **T578** (nothing builds the Windows target in CI until
release time, so this class of break is always found while shipping) and
**T579** (a release can still ship macOS-only, silently, if the second tag is
forgotten).

## Validation criteria

The first two criteria were written on the assumption that the fix was "put
the file on main". It is not, and both are rewritten to match what was found
(see the Resolution section) rather than ticked against a mechanism that
cannot work:

- [x] ~~`release-windows.yml` is on `origin/main`~~ → **the workflow triggers
      on a tag shape a ref carrying the Windows source can be tagged with**,
      and `v*` is retained so the main-side trigger takes over untouched at
      merge-back. Asserted statically by D7/D8/D10 of
      `test\win32\website-windows-download.ps1`.
- [x] ~~`gh run list --workflow release-windows.yml` resolves~~ → it cannot
      and should not: `--workflow <file>` resolves against the **default
      branch**, so it 404s by design until merge-back. **`gh run list --branch
      win-v<newest>` shows a completed `Release (Windows)` run** instead.
      Verified: run **31232524541** on tag `win-v1.31.0`, conclusion
      `success`, every step including `Publish win-v release` and
      `Update website Windows download links`.
- [x] `gh release view win-v<newest> --json assets` lists both
      `Ghoztty-<v>-x64.msi` and `Ghoztty-portable-<v>-x64.zip`. Verified:
      `win-v1.31.0` published 2026-08-08T01:30:03Z with
      `Ghoztty-1.31.0-x64.msi` (20 815 360 bytes) and
      `Ghoztty-portable-1.31.0-x64.zip` (20 142 957 bytes). It is NOT marked
      latest — `Ghoztty v1.31.0` still is, so the Sparkle/macOS flow is
      untouched. The tag points at `e6bbe0460` on this branch.
- [x] The live page's `win-msi-link` and `win-zip-link` point at that
      release and both answer 200 —
      `powershell -NoProfile -File test\win32\website-windows-download.ps1
      -RequireNetwork` is ALL PASS. Verified: the workflow's own gh-pages
      commit `2b0bb6925` ("Update website Windows download to win-v1.31.0",
      by github-actions[bot]) moved both links and `win-version` off
      `win-v1.4.1`; the ZIP link now points at the asset rather than the
      release page, because a portable ZIP exists for this release. The
      in-repo mirror was re-synced from the deployed page, so check F passes
      too.

Standing floor, run on the box for this change: `floor-lane.ps1 -Lane all` →
`ALL LANES PASS` (none 289s, win32 347s, agent 328s); `ipc-p1` / `ipc-p2` /
`ipc-p3` each `ALL PASS`.

## Progress log

- 2026-08-07 17:53: claimed; work starting.
- 2026-08-07 17:55: root cause is deeper than the task assumed: origin/main has NO src/apprt/win32 at all (its apprt enum is gtk/none/embedded only), and dist/windows-installer + dist/website do not exist there either. So cherry-picking release-windows.yml onto main would produce a workflow that fails on every tag - 'zig build -Dapp-runtime=win32' is not a valid option on main's source. The Windows frontend exists ONLY on users/dzearing/windows-amd64 (729 commits ahead, main not an ancestor). Therefore a Windows release must be built from a ref that HAS the Windows source. Plan: trigger release-windows.yml on 'win-v*' tags cut from this branch - the tag's own tree carries both the workflow and the win32 source, so the workflow runs without any change to main and without the macOS pipeline being able to break.
- 2026-08-07 18:15: first ever run of Release (Windows) (31231537658): zig cross-compile SUCCEEDED (10 min, exe embeds 1.31.0+bbdbb423d), then wixl aborted with 'unhandled child Component node Environment' (wix.vala:232). ubuntu-latest ships msitools 0.103, whose wixl does not know the <Environment> element; the component is the user-PATH one from T70. Nothing published - the Verify artifacts gate worked. Fix: CI now builds msitools v0.106 from source (the same version brew and the msitools-local Debian image have, and the one build-msi.sh's post-compile patches are written against) and asserts the version. Step moved ahead of the zig build so a msitools failure costs 2 min not 11. Tag moved to 42d5bc09f; re-run 31232193126.
- 2026-08-07 18:23: msitools-from-source on the runner took four iterations, each gated correctly (nothing published): (1) wixl 0.103 has no <Environment>; (2) apt meson is 1.3.2 vs required >=1.4 -> pipx; (3) meson.build needs the bats-core git SUBMODULE and git-describe history -> full clone --recurse-submodules; (4) missing gobject-introspection -> stopped hand-listing -dev packages and switched to 'apt-get build-dep msitools' with deb-src enabled, keeping the explicit list as a complete fallback. Standing floor is green on box throughout: floor-lane -Lane all = ALL LANES PASS (none/win32/agent), and ipc-p1/p2/p3 each ALL PASS. Run 5 = 31232524541.
- 2026-08-07 18:32: status: in-progress -> done (by win-v1.31.0 published and observed live) [commit e6bbe0460]

### Decision D32 - How should Windows releases start actually publishing - cherry-pick the workflow onto main, hand-publish now, or wait for the merge-back?

Resolved 2026-08-08T01:24:25Z : Superseded by D33, on evidence rather than preference. Option 1 (cherry-pick the release path onto main) is not a choice that exists: origin/main has no src/apprt/win32, no dist/windows-installer and no dist/website, and its apprt enum is gtk/none/embedded, so -Dapp-runtime=win32 is not a valid build option there. A workflow runs from the tree of the ref that triggered it, so a vX.Y.Z tag cut from main cannot build a Windows terminal no matter which branch the file lives on - cherry-picking would have made every macOS release red rather than shipping Windows. Option 3 (hand-publish from this box) also lost its premise: a win-v tag cut on the branch drives the SAME CI workflow, so the artifacts come from a tag and are reproducible, which was its main con. What remains genuinely open - whether it is right to publish a Windows build off a branch that has not merged back - is asked directly in D33, so this is resolved to avoid presenting a menu whose top item cannot be built.

Superseded by D33, on evidence rather than preference. Option 1 (cherry-pick the release path onto main) is not a choice that exists: origin/main has no src/apprt/win32, no dist/windows-installer and no dist/website, and its apprt enum is gtk/none/embedded, so -Dapp-runtime=win32 is not a valid build option there. A workflow runs from the tree of the ref that triggered it, so a vX.Y.Z tag cut from main cannot build a Windows terminal no matter which branch the file lives on - cherry-picking would have made every macOS release red rather than shipping Windows. Option 3 (hand-publish from this box) also lost its premise: a win-v tag cut on the branch drives the SAME CI workflow, so the artifacts come from a tag and are reproducible, which was its main con. What remains genuinely open - whether it is right to publish a Windows build off a branch that has not merged back - is asked directly in D33, so this is resolved to avoid presenting a menu whose top item cannot be built.

### Decision D33 - How should a Windows release be cut, given main has no Windows source at all?

Resolved 2026-08-11T15:34:18Z : When we feel we are at a good enough state to merge to main, we will do a merge and then do the windows releases. Until then we should keep working on parity and reliability and readiness for merging. I think m1 goal should be something like full feature parity or at least exhaustive consideration for all features. when we're done with m1 we should hten move on to phasing the merge back to main and the release process. Eventually i would like to have a windows dev process where we work off the release version of ghoztty and use the debug version (in parallel) to build and test features. Then once we've released updates, we check for updates and install the newest release version and resume development on the release version. but on windows. this is currectly how i work with mac.

When we feel we are at a good enough state to merge to main, we will do a merge and then do the windows releases. Until then we should keep working on parity and reliability and readiness for merging. I think m1 goal should be something like full feature parity or at least exhaustive consideration for all features. when we're done with m1 we should hten move on to phasing the merge back to main and the release process. Eventually i would like to have a windows dev process where we work off the release version of ghoztty and use the debug version (in parallel) to build and test features. Then once we've released updates, we check for updates and install the newest release version and resume development on the release version. but on windows. this is currectly how i work with mac.
