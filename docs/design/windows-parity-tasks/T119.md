---
id: T119
title: "`+reload` verb unhandled on win32"
deps: ["T117"]
status: "done"
commits: ["cc11777d9"]
---

# T119 — `+reload` verb unhandled on win32

## Summary

`+reload` verb unhandled on win32 — main's `2daaa98c9` added the action; the CLI/`apprt.ipc` plumbing came in free with T117 (and `wireName` now maps it), but `IpcHandlers.dispatch` has no `"reload"` branch, so the verb falls through to the unknown-action error rather than the documented "is a terminal pane, nothing to reload". Real behavior only lands with viewer panes (T90b–T90h), so the near-term deliverable is the parity ERROR string; wire the actual reload as part of T90e/T90f

## Details

Main's `2daaa98c9` added `ghoztty +reload` to refresh a viewer pane in place.
The CLI + `apprt.ipc` plumbing arrived with T117 (and `wireName` now maps it),
but `IpcHandlers.dispatch` has no `"reload"` branch, so the verb falls through
to the generic unknown-action error.

Real reload behavior cannot exist before viewer panes do (T90b–T90h), so the
near-term deliverable is the **parity error string**: targeting a terminal pane
must fail with `... is a terminal pane, nothing to reload` (exit 1), mirroring
how the terminal-only verbs reject viewer panes. Wire the actual reload as part
of T90e/T90f.

## Done 2026-08-03 — superseded by T390 (`cc11777d9`)

T119 predicted its own ending: it asked for the placeholder error string now
and the real behavior "as part of T90e/T90f". T90f's split put the whole verb
in **T390**, which landed both halves at once, so there is nothing left here to
build. Verified against the tree rather than inferred from T390's write-up:

- `src/apprt/win32/IpcHandlers.zig:82` — the `"reload"` branch exists in
  `dispatch`, so the verb no longer falls through to the unknown-action error.
- `IpcHandlers.zig:1098` / `:1109` — both terminal-target refusals, byte-for-byte
  Mac: `target '<name>' is a terminal pane, nothing to reload` and
  `focused pane of '<name>' is a terminal pane, nothing to reload`. T119 asked
  for one string; the pane-vs-window distinction means there are two.
- `test/win32/viewer-panes.ps1:431,443` — both are asserted on box, alongside
  T390's real reload coverage (web re-fetch from origin, file re-render).

No new code, no new tests: closing a task whose deliverable already shipped
under another id is bookkeeping, and the evidence is the citations above.
