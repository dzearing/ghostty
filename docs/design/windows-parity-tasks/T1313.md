---
id: T1313
title: "The Wacatac false positive has not been reported to Microsoft, so every machine keeps making the same call"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P1"
triage-reason: "P1: this is the free remedy that actually reaches every machine, and the only one that can clear the current detection without buying anything. Not P0 because it cannot be done from this seat - the submission portal needs a Microsoft account - and because the website note now covers the people who meet it meanwhile."
milestone: "M1"
tags: ["infra"]
---

# T1313 - The Wacatac false positive has not been reported to Microsoft

## In plain terms

Windows deleted Ghoztty on someone's machine because Microsoft's malware model
guessed wrong. Microsoft takes reports of exactly that from the people who build
the software, and corrections usually reach every machine within days. Nobody
has filed one, so every new machine keeps making the same guess.

## Summary

T1293 names this as "the actual remedy and it is free". The Security
Intelligence submission portal at `microsoft.com/en-us/wdsi/filesubmission`
takes a software-developer false-positive report: upload the file, say it is
detected incorrectly, point at the public build. A correction there is global
and permanent for those bytes, which is more than any note on the download page
can do.

**It cannot be done from this seat.** The portal requires signing in with a
Microsoft account, and this box has no browser session for one. That is what
makes it a task rather than something the turn that found it finished.

What is ready for whoever files it:

| Field | Value |
|---|---|
| Detection | `Trojan:Script/Wacatac.C!ml` |
| Files | `ghoztty.exe`, `ghoztty.com` from `win-v1.36.2` |
| `ghoztty.exe` SHA-256 | `0A53D5A05A254A5BAF854C80A8EB4997BC6C3C79D3EF8B7FB05E173A8457B669` |
| `ghoztty.com` SHA-256 | `DA0071548324B130136EF2B0EF40CDEF3705586920E87E3B0CD28EE4397DF487` |
| Source | `github.com/dzearing/ghoztty`, built by `release-windows.yml` run 33777624339 on `ubuntu-latest` |
| Download | the `win-v1.36.2` release assets |

## What to do

1. File the developer false-positive report for both files and record the
   submission ID in T1293.
2. Watch it: a submission gets a determination, and the determination is what
   closes T1293, not the act of submitting.
3. If it is upheld rather than corrected, T1293 escalates - that would mean
   Microsoft believes something about these bytes that we do not.
4. Decide whether this becomes a standing release step. Every unsigned release
   is a new set of bytes with no reputation, so the detection can recur on the
   next one; if it recurs twice, the answer is the certificate (D89), not a
   monthly form.

## Validation criteria

- [ ] A submission ID exists and is recorded in T1293.
- [ ] Microsoft's determination is recorded, with its date.
- [ ] A fresh install on a machine that previously quarantined completes
      cleanly, verified rather than assumed.
