---
id: T964
title: "release-artifacts guard can never go current on a Docker-less box, so it reads permanently due"
order: null
deps: []
status: "skipped(duplicate of T953)"
commits: []
seat: "win"
priority: "P2"
tags: ["test", "infra"]
---

# T964 - release-artifacts guard can never go current on a Docker-less box, so it reads permanently due

## Summary

release-artifacts.ps1 only stamps on zero failures AND zero skips, and its packaging arms skip without Docker. On this box the row is therefore due after any covered edit and can only be cleared by starting Docker, which is off-limits unasked. The effect is that validate's teeth get filed down with -NoGuardDue, which is the habit T783 exists to prevent. Decide between a partial stamp keyed to the arms that DID run, or splitting the Docker arms into their own row. Found by T883.

## In plain terms

Nothing a user can see. This is a broken smoke alarm in the workshop: one of
the automatic "has anybody tested this since it changed?" checks got stuck in
the alarm state and cannot be reset on this machine, so every piece of work
now has to walk past a red light. A red light nobody can turn off is one
everybody stops reading.

## Validation criteria

- [ ] `scripts\guard-due.ps1 check` exits 0 on a box with no container runtime,
      after a clean run of whatever the pure half of the harness became
- [ ] the Docker-dependent packaging checks still gate something: they either
      keep their own guard row (DUE when the packaging inputs change) or the
      pure guard's `Covers` no longer claims those inputs — never a silent
      widening that calls unrun checks current
- [ ] a turn no longer needs `validate -NoGuardDue` for this guard

## Details

Closed 2026-08-18 as a duplicate of **T953**, which is the same defect filed a
day earlier, carries the same three options, and is P1. Everything here lives
there; the plain-terms paragraph above was copied over so nothing is lost.

The reason four turns filed this independently (T953, T964, T965, T966) is that
two open tasks for it existed side by side and neither turn's search found the
other — the consolidation is onto T953.

## Progress log

- 2026-08-18 03:21: status: todo -> skipped(duplicate of T953) (by consolidation: T953 is the same defect filed a day earlier, is P1, and carries the same three options; T965/T966 were filed against T964 only because two open tasks existed side by side)
