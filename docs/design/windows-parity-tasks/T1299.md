---
id: T1299
title: "Nobody has ever installed the MSI and clicked through it on this box"
order: null
deps: [T1291]
status: "done"
commits: []
seat: "win"
priority: "P1"
triage-reason: "P1: the installer is the first thing a new user meets, and every claim about how it behaves is currently made at the source or at build time. Three separate installer defects (T1204, T1205, T1291) all reached the user before any harness could reach them, which is the signature of a surface nothing runs end to end."
milestone: "M1"
tags: ["test", "infra"]
---

# T1299 - Nobody has ever installed the MSI and clicked through it on this box

## In plain terms

Everything the installer does is checked by reading the recipe that builds it,
never by running it. So the way we find out that installing is broken is that
you install it and it breaks - which is how the last three installer bugs were
found. There should be a test that actually installs a throwaway copy, runs
through the cases a person hits, and says so when one of them goes wrong.

## Goals

- [x] a harness that installs a real package on this box, with no Docker
- [x] fresh install, upgrade, older-over-newer and uninstall, each asserting the
      outcome a person sees
- [x] the same-version Repair / Cancel case, written and gated on a package that
      carries it (T1302 runs it once one is published)
- [x] proof, on demand, that none of it can reach the user's Ghoztty
- [x] a `guard-due.ps1` row, so an installer edit asks whether it still installs

## Why it is like this today

Two constraints, both real:

- Building the MSI needs `wixl` (GNOME msitools), which on this box lives only
  inside Docker - and starting Docker is the user's call, not the loop's. So
  `test\win32\release-artifacts.ps1`'s packaging half is permanently advisory
  here (`guard-due.ps1` reports it as `GUARD DUE (advisory)`).
- Installing the real product would replace the user's Ghoztty, which is this
  repo's first non-negotiable.

`scripts\msi-test-identity.ps1` and `build-msi.sh --test-identity` already
solve the second constraint: they mint a THROWAWAY product with its own name,
ProductCode, UpgradeCode, install dir and component GUIDs, so an install and an
uninstall cannot touch anything real. What is missing is a package to feed it
without Docker.

## What to do

- **Get a package without wixl on this box.** `release-windows.yml` already
  builds one on every tag and `fork-ci` compiles one on every push; the
  cheapest route is probably to have CI publish the throwaway-identity MSI as a
  build artifact and have the harness download the one built from the current
  commit (skipping cleanly, the way the advisory guards do, when there is no
  network or no matching run).
- **Then drive the cases a person actually hits**, each with an oracle that is
  the outcome and not an exit code:
  - fresh install → files on disk, PATH entry, Start Menu shortcut, a terminal
    opens (T1176);
  - upgrade over an older version → the exe survives (T23), sessions survive
    (T1207), no reboot is demanded (T1204);
  - **re-run at the same version → the Repair / Cancel dialog appears and
    Cancel leaves the install untouched (T1291)** - the case this task was
    filed from, whose MSI half is currently asserted only at its source and at
    the build-time read-back;
  - older package over a newer install → the plain explanation, not 1638;
  - uninstall → nothing left behind.

## Validation criteria

- [x] A harness in `test/win32/` installs a throwaway-identity package on this
      box, with no Docker, and skips cleanly (never fails) when it cannot get
      one. -- `test\win32\install-walkthrough.ps1`; the package is the newest
      PUBLISHED `win-v*` release (`-Msi` takes a local one), and a run that
      cannot obtain one ends in `ASSERTED NOTHING` rather than green.
- [x] It covers fresh install, upgrade, same-version maintenance, older-over-
      newer and uninstall, each asserting the OUTCOME rather than msiexec's
      exit code. -- sections B (files, PATH, shortcut, ARP version, `+version`,
      a real window on a test desktop), C (the exe survives, the version moves,
      no reboot, no duplicated PATH entry), D (the dialog, its buttons, 1602 /
      0), E (the plain-English refusal, not a bare 1638), F (nothing left
      behind). Run 2026-09-03: `ALL PASS (30 assertions, 2 SKIPPED)`.
- [x] It is impossible for it to touch the real product: the assertions name
      the throwaway ProductCode and install dir, and a `-TeethCheck` shows the
      guard that enforces that going red. -- `-TeethCheck` scores 5 assertions,
      three of them refusals; section G measures the user's install (file count
      and exe mtime) before and after every run.
- [x] `guard-due.ps1` carries a row for it, so an edit to `build-msi.sh` asks
      it whether the package still installs. -- the `install-walkthrough` row
      covers `build-msi.sh`, `msi-test-identity.ps1` and the harness. It is
      **advisory** on purpose: the subject is a PACKAGE, this box cannot build
      one, and the newest published release is by definition the world BEFORE
      the edit that makes the row due -- the same shape as
      `release-artifacts-packaging`, and the reason a hatch pressed every turn
      carries no information (T1189).

## Progress log
- 2026-09-03 11:04: claimed; work starting.
- 2026-09-03 11:34: built test\win32\install-walkthrough.ps1: real msiexec against the newest PUBLISHED package rewritten to a throwaway identity (no Docker). ALL PASS (30 assertions, 2 SKIPPED). Two findings on the first run: (1) uninstall left the Start Menu shortcut because msi-test-identity.ps1 blanks C_StartMenuShortcut's ComponentId - filed T1301, F5 gated on it; (2) section D cannot run until a package carrying T1291's MaintenancePrompt is published - filed T1302. Advisory guard-due row added (this box cannot build a package, so the newest release always predates the edit that makes the row due).
- 2026-09-03 11:46: status: in-progress -> done
