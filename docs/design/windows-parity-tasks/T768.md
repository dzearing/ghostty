---
id: T768
title: "persistence-flag.ps1 is red: four GUI launches in agent-attach-refused.ps1 and url-scheme.ps1 never state their session persistence"
order: null
deps: []
status: "skipped(duplicate of T744 - the same four undeclared launches, re-filed by each turn that ran the audit)"
commits: []
seat: "win"
priority: "P2"
tags: ["test"]
---

# T768 - persistence-flag.ps1 is red: four GUI launches in agent-attach-refused.ps1 and url-scheme.ps1 never state their session persistence

## Summary

T158's sweep (test/win32/persistence-flag.ps1) reports 'undeclared: 4 of 175' and exits 1. The sites are agent-attach-refused.ps1:276 (Start-OnTestDesktop with an empty -Arguments) and url-scheme.ps1:359/368/381 (three launches whose only argument is the ghoztty-debug:// activation URL). Persistence is ON by default, so each of those launches restores whatever panes the previous run left behind - which is exactly the self-poisoning T131/T155 cost - and none of them says whether that is wanted. Fix per site: pass --session-persistence=false where the launch wants a clean box, or write a '# persistence: <reason>' marker where it genuinely does not fit (url-scheme's activation launches are the forward-and-exit shape the marker exists for). Found red during T263's floor sweep; T263 changed neither script.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
