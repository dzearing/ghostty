---
id: T135
title: "+new-window --target=<name> silently discards --working-directory/--command when the target…"
order: 111
deps: ["T132"]
status: "done"
priority: "P1"
triage-reason: "+new-window on an existing target reports success while silently dropping --command/--working-directory. Caller cannot tell."
commits: ["cf59b2b41"]
---

# T135 — +new-window --target=<name> silently discards --working-directory/--command when the target…

## Summary

**`+new-window --target=<name>` silently discards `--working-directory`/`--command` when the target already exists.** Found 2026-07-28 while root-causing T132. The idempotent rule ("a named target that already exists is focused, not recreated") is Mac-parity and correct as a rule, but combined with session restore it makes the loop's own relaunch a no-op: `upgrade-ghoztty-windows.ps1` calls `+new-window --target=main --working-directory=… --command="claude … --continue"`, and if restore has already rebuilt a window named `main` (the manifest persists `ipc_name`), the request just focuses it and the command never runs — the pane keeps whatever the tombstone RELAUNCH respawned. T132 makes that respawn land in the right directory, so the loop now survives, but a caller still gets `success:true` for a request that did nothing it asked for. Decide and implement one of: (a) apply the flags to the existing target (re-`cd`/re-run is not generally safe), (b) report a distinct outcome (`focused` vs `created`) in the JSON reply so callers can branch, or (c) an explicit `--recreate`/`--fail-if-exists` flag. Check what Mac does with the same request before diverging. Validation: an on-box case in `auto-launch-cwd.ps1` (or its own script) asserting the chosen contract for an already-registered target

## In plain terms

If you ask Ghoztty to open a window named "main" running a specific command in a
specific folder, and a window named "main" already exists, Ghoztty just brings
that window to the front — your command and folder are thrown away, and the
tool still says "success". A script (like our own upgrade script) has no way to
know its request was ignored. After this lands, the reply says whether the
window was created or merely focused, and the command prints a clear note
naming exactly which options were ignored — so a caller can tell, and a human
sees why their command didn't run.

## Goals

- [x] The IPC reply distinguishes `focused` (already existed) from `created`
- [x] When flags were dropped on a focus, the reply carries a `note` naming them
- [x] The CLI prints that note to stderr (exit code stays 0 — still idempotent)
- [x] A bare `+new-window --target=x` re-focus stays silent (the CLI's
      auto-inserted cwd must not trigger the note)
- [x] Mac half filed as a seat:mac task: T523 (shared CLI already prints the note)

## Validation criteria

- [x] Unit tests in `apprt/ipc/args.zig` cover dropped-flag detection and the
      `--cwd-implicit` marker (none lane; `-Dtest-filter=droppedOnExistingTarget`
      exit 0, then full lanes below)
- [x] On-box acceptance script asserts: focus-with-flags prints the note and
      exits 0; bare re-focus prints nothing; fresh create prints nothing
      (`test/win32/ipc-target-exists-note.ps1` — ALL PASS, 2026-08-06)
- [x] Floor: `floor-lane.ps1 -Lane all` green (none/win32/agent all PASS,
      2026-08-06), P1–P3 ALL PASS — run sequentially after the lanes

## Progress log

- 2026-08-06 07:34: claimed; work starting.
- 2026-08-06 07:45: investigated both servers. Mac (IPCServer.swift:390) has the
  identical silent behavior — focus + bare `.ok`. Decision: option (b) from the
  task — keep idempotent focus, add `outcome: focused|created` + a `note` field
  to the reply; shared CLI (`sendAction`) prints any `note` to stderr. New
  `--cwd-implicit` marker (unknown flags are ignored by both servers, so
  additive) marks the CLI's auto-inserted cwd so it never counts as dropped.
  Exit code stays 0. Mac server half to be filed seat:mac.
- 2026-08-06 07:53 [session 8e6aa6d2-5bc7-45de-add9-4a30e04d67fd]: built + unit tests green (none lane filter); new acceptance test/win32/ipc-target-exists-note.ps1 ALL PASS; Mac half filed as T523; decision receipt D19; floor lanes running
