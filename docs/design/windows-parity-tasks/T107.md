---
id: T107
title: "focus-defer.ps1 tail asserts red on the box"
phase: null
deps: ["T48"]
status: "done"
commits: ["3f74f68cd"]
---

# T107 — focus-defer.ps1 tail asserts red on the box

## Summary

FIX: focus-defer.ps1 tail asserts red on the box — PRE-EXISTING (2026-07-20, identical on pre/post-T105 binaries ×3): after the FD-LOAD flood + 1500-click storm, `+list` times out (>8s; GUI-thread IPC listener busy — the script's own teardown comment already notes the flood keeps it busy) and `focus still moves after storm` fails, while WM_NULL stays responsive; separately the 3-pane setup intermittently yields 1 surface (split race after a fresh agent spawn). Likely harness timing (flood pacing + foreground loss), possibly a real listener-starvation bug under sustained output — decide which and fix script or product

## Details

2026-07-20, found while regression-running T105. PRE-EXISTING: identical
2 failures ×3 on both pre- and post-T105 binaries: after the FD-LOAD
flood (150k echo lines) + 1500-click storm, `+list` (Start-Job, 8s) times
out and `focus still moves after storm` fails — while `Responsive`
(SMTO WM_NULL) still passes. The script's own teardown comment already
concedes the flood keeps the GUI-thread IPC listener busy. Separately the
3-pane setup intermittently finds only 1 surface (the `+split`s race a
fresh agent spawn after `Stop-DebugGhoztty` killed the previous debug
agent lineage — sleeps are fixed at 1–3s).

Decide: harness fix (wait-for-idle before the IPC assert, retry-based
setup) vs product issue (IPC listener starvation under sustained PTY
output — if real, it affects agents driving busy panes). The T53 soak saw
no IPC starvation, so harness-first is the working theory.

## Verdict (2026-08-03): HARNESS, and it was already fixed by T218

The working theory held. Measured on box against today's Debug build, with
the flood running:

| measurement | value |
|---|---|
| flood rate | ~2,500 lines/s, so 150k lines runs ~60s — live across the whole assertion block |
| `+list` under flood | **73 ms** (the assertion allows 8,000) |
| click storm (1500 clicks, unpaced `PostMessageW`) | posts in **21 ms**; app moves focus to the storm target **226 ms** later |
| `focus-defer.ps1` | **ALL PASS ×3** before any change |

So there is no IPC listener starvation to fix: the GUI-thread listener
answers in 73 ms while the pane emits 2.5k lines/s. Both original causes
belonged to the pre-T218 harness and are gone with it — posted input needs
no foreground (so a foreground loss can no longer stall the click/focus
assertions), and `CleanSlate` + `--session-persistence=false` removed the
split-vs-fresh-agent race that intermittently yielded 1 surface.

## What shipped instead: the load shape is now asserted

Closing this as "passes now" would have left the script unable to tell a
green run from a green-and-empty one. Every assertion after the flood is a
claim about behaviour UNDER LOAD, and the load was entirely unmeasured: the
flood's `+send-keys` was `| Out-Null` with its exit code dropped, and the
storm's return value was `[void]`. A flood that never started and a storm
that posted nothing both leave the whole section green — reporting an idle
app's responsiveness as the T48 regression holding.

`test/win32/focus-defer.ps1` now asserts, and `Send-TestClickStorm` returns
the accepted-post count to make it possible:

- `+send-keys` exited 0 and the flood counter is advancing BEFORE the storm;
- the storm posted all 3000 messages;
- the app PROCESSED them (focus lands on the storm's last target, which
  every round ends on) — focus is parked elsewhere first so this cannot be
  true by accident;
- the flood was still producing ACROSS the assertions (`floodStart` →
  `floodEnd`, and short of its 150k end), so the counter can't have been
  read from a flood that had already finished.

Each new assertion was **confirmed able to fail** by breaking it and
re-running (11 → 17 assertions):

| break | result |
|---|---|
| flood sent to `--target=fdNOPE` | 3 FAIL (send-keys exit 1, counter -1, no advance) |
| storm targets `[IntPtr]0xDEAD` | 2 FAIL (0 posted, not processed) |
| storm targets `[IntPtr]::Zero` | **1 FAIL — the count PASSED at 3000** |

That last row is the finding worth keeping, and it is why both oracles are
there: `PostMessageW(NULL, …)` **succeeds**, posting to the calling thread's
own queue. A post count proves messages were accepted, not that anything was
targeted — only the app-acted-on-it oracle catches a storm aimed at nobody.
The note is in `Send-TestClickStorm`'s own header, where the next caller
will read it.

## Follow-up

**T408** — the `+list answers within 8s` bar is ~110× the 73 ms this task
measured, so it detects a wedge and nothing short of one. Filed rather than
folded in: tightening it means measuring in-process (the Start-Job wrapper's
process launch is most of the current variance) and deriving a budget from a
recorded baseline, which is its own piece of work.

**T409** — the floor's `zig build test-agent` lane **hangs** (found while
running it for this task, reproduced twice, both test binaries blocked at
zero CPU). Not caused by this change, which touches only PowerShell: the
agent lane's content was verified instead by running its two test binaries
directly and sequentially — **3707 passed / 0 failed** and **3779 passed /
0 failed** — which is what isolated the hang to the concurrency rather than
the tests. Likely the true root cause of T401.

## Validation

`focus-defer.ps1` ALL PASS (17) ×4, `-NegativeControl` fails as designed
(1 FAILURE / 16 passed), `zig build test -Dapp-runtime=none` and
`-Dapp-runtime=win32` both exit 0, P1/P2/P3 ACCEPTANCE: ALL PASS. Agent lane
run binary-by-binary per T409 above (0 failures in either).
