---
id: T148
title: "Local splits/tabs/windows may re-run the parent's command on Windows"
order: 104
deps: ["T99"]
status: "in-progress"
priority: "P1"
triage-reason: "A new split/tab may re-execute the parent pane's command - Mac fixed exactly this. Verify on the box, port if it reproduces."
tags: [fix]
commits: []
---

# T148 — Local splits/tabs/windows may re-run the parent's command on Windows

## Summary

**Local splits/tabs/windows may re-run the parent's command on Windows.** `cdb689025` (Mac, ZERO references) fixed exactly this: a new local split/tab/window under the agent inherited and re-executed the parent pane's `--command` instead of starting a plain shell. Win32's T99 wired IPC-created panes through the agent without this guard. Verify on the box (open a window with `--command`, then ctrl+n / split from it) and port the fix if it reproduces; if it does not, record the negative result rather than closing silently

## In plain terms

If you opened a terminal window that was told to run a specific program (for
example a window that starts an AI agent), then made a new tab or split pane
in that window, the new pane would silently run that same program again
instead of giving you a normal shell. Running the program twice can have real
side effects — imagine two copies of the same agent working the same files.
After this fix, a new tab or split always opens a plain shell in the same
folder, and only genuinely remote windows keep the "repeat the command"
behavior they were designed to have.

## Goals

- [x] Reproduce on the box: a tab/split of a local `--command` window re-runs the command
- [x] Port the Mac gate: local agent panes never inherit the parent's command
- [x] The split/tab still opens in the parent pane's working directory
- [x] Genuine remote windows keep command inheritance (remote-inherit.ps1 stays green)
- [x] Standing floor green (three zig lanes + P1-P3)

## Validation criteria

- [x] New script `test\win32\local-split-no-command-rerun.ps1` reproduces the bug pre-fix (4 FAILURES: tab + split both re-ran the marker command) and is ALL PASS post-fix
- [x] `test\win32\remote-inherit.ps1` ALL PASS post-fix (remote command inheritance preserved, sections 4/5)
- [x] `scripts\floor-lane.ps1 -Lane all` PASS (none#1=PASS win32#1=PASS agent#1=PASS, 2026-08-06)
- [x] P1-P3 acceptance ALL PASS (2026-08-06)

## Progress log

- 2026-08-06 04:15: claimed; work starting.
- 2026-08-06 04:17 [session af5caf5c-43f9-4782-8a0a-8c06ae4bae3d]: Traced: win32 Window.buildRemoteInherit inherits parent remoteCommand() for BOTH cross-machine AND local-agent flavors (shell inheritance is already gated on !is_local_agent, command is not). Flows via pending_surface_overrides.remote.command -> Surface.init sets config.command + wait-after-command -> core forwards into agent OPEN -> split/tab re-runs the parent's command. Same shape the Mac fixed in cdb689025. Next: on-box repro via +new-window --command then +split, then port the gate.
- 2026-08-06 04:22 [session af5caf5c-43f9-4782-8a0a-8c06ae4bae3d]: REPRODUCED on box: new acceptance script test\win32\local-split-no-command-rerun.ps1 run against unfixed build shows ctrl+t tab AND no-command +split both re-ran the parent's --command (marker found in both new panes); explicit +split --command control passed. Also noted: inherited-cwd assertions failed in the buggy state (new panes did not show the parent's cwd) - will re-check post-fix. Applying the Mac cdb689025 gate to Window.buildRemoteInherit: inherit command only when !is_local_agent.
- 2026-08-06 04:40 [session af5caf5c-43f9-4782-8a0a-8c06ae4bae3d]: Fix landed (one-line gate + comment in Window.buildRemoteInherit). Post-fix: local-split-no-command-rerun.ps1 ALL PASS (and the cwd inheritance now works too - the buggy command-carrying OPEN had been losing the parent cwd as well); remote-inherit.ps1 ALL PASS (remote command inheritance preserved); floor-lane all three lanes PASS; P1-P3 ALL PASS. New-window path verified clean by inspection: openRemoteWindowFrom gates on remote_machine (null for local windows) and a new window's initial surface passes parent=null. Filed T515: explicit local +split --command opens in the agent default cwd, not the parent pane's.
