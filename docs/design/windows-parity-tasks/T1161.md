---
id: T1161
title: "The fork-identity ratchet slipped from 28 to 27 and nothing noticed until the guard was run by hand"
order: null
deps: []
status: "skipped(duplicate of T1134)"
commits: []
seat: "win"
priority: "P1"
tags: ["test", "infra"]
---

# T1161 - The fork-identity ratchet slipped from 28 to 27 and nothing noticed until the guard was run by hand

## Summary

Found while doing T1158. 	est\win32\fork-identity.ps1 scores 1 FAILURE(S) / 48 passed on HEAD: C4 at least 28 of the U-files reproduce from the fork point, reproduced=27 baseline=28. The guard was already reported DUE at the start of the 2026-08-23 turn - scripts/fork-identity.ps1 and test/win32/fork-identity.ps1 had both changed and neither had been run since - so this is exactly the T783 shape: a harness edited, never re-run, and quietly red underneath. One of the 52 upstream-identity files stopped reproducing byte-for-byte from the fork point (063ac3ecc). Either a U-file gained real work (in which case the residual list is right and the baseline needs a reasoned lowering, with the file named), or an identity rule in scripts/fork-identity.ps1 regressed and stopped reproducing a file it used to (in which case the fix is the rule and the baseline stays at 28). The 25-file residual list the run prints is the starting point: diff it against the list the last green run produced to find the newcomer. NOT done as part of T1158, which is a session-leak P0 in a different subsystem; T1158's commit used the -NoGuardDue hatch and said so.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-08-23 10:28: status: todo -> skipped(duplicate of T1134) (by T1158 turn: filed before finding T1134 in the log, which already names the same C4 baseline decay and is P1 for it. T1161's only addition is the current residual list (27 of 52, 2026-08-23), copied into T1134's progress log.)
