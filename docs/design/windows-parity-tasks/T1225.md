---
id: T1225
title: "The MSI packaging harness has been due since the installer fix, and only Docker can clear it"
order: null
deps: []
status: "skipped(duplicate -\u003e T1189)"
commits: []
seat: "win"
priority: "P2"
triage-reason: "Pre-existing guard debt from 501752ab3, not blocking user-facing work; needs the Docker daemon up, which the loop does not start unasked."
tags: ["infra", "test"]
---

# T1225 - The MSI packaging harness has been due since the installer fix, and only Docker can clear it

## Summary

scripts\guard-due.ps1 reports release-artifacts-packaging DUE because dist/windows-installer/build-msi.sh changed in 501752ab3 (2026-08-31) and test\win32\release-artifacts.ps1 -RequireDocker has not run since. The daemon is down on this box and the standing rule is that Docker-down is a SKIP path, never a prerequisite the loop starts on its own - so every turn since has had to take the -NoGuardDue hatch for a guard that is nobody's current work. Run the harness once with Docker up, or give it a stamping path that does not need the daemon.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.

## Progress log

- 2026-08-31 17:31: status: todo -> skipped(duplicate -> T1189) (by T1189 already tracks the Docker-gated MSI packaging guard; filed T1225 before reading the log tail that names it.)
