---
id: T397
title: "Viewer panes: hero-mode carousel tile for a viewer pane (Mac snapshots viewers now; win32 paints a hole)"
order: 137
deps: ["T90f"]
status: "done"
priority: "P1"
milestone: "M1"
triage-reason: "hero carousel paints an empty hole where a viewer tile should be; visible glitch plus a Mac-parity decision"
commits: ["e9c04e654"]
seat: "win"
---

# T397 - Viewer panes: hero-mode carousel tile for a viewer pane (Mac snapshots viewers now; win32 paints a hole)

## In plain terms

Ghoztty can show a document or a web page in a pane instead of a terminal.
When you press the shortcut that blows one pane up big and puts small
previews of the others in a strip down the side, those previews used to
simply not draw for a document pane — you got an empty gap in the strip
where a preview should be, even though the gap was still clickable and still
counted. Now every pane gets a preview, and a document pane's preview shows
the actual page.

## Goals

- [x] No pane kind leaves a hole in the preview strip — every slot is drawn
- [x] A document/web pane's preview shows its real content, not a blank card
- [x] Selecting a document pane from the strip works and lands on it
- [x] The picture costs the app almost nothing when nothing is changing

## Validation criteria

- [x] `test/win32/hero-viewer-tile.ps1` ALL PASS — a terminal + a markdown
      viewer in hero mode: both tiles paint a border stroke (the hole
      oracle), the viewer tile carries 4+ distinct colors (the picture
      oracle), the debug log carries a `kind=viewer` snapshot commit, and
      arrow navigation makes the viewer the hero pane at the same rect.
- [x] Same script `-NegativeControl` FAILS exactly the inverted claim, so
      the pixel oracle is known to be able to detect the hole.
- [x] `hero_math.zig` pure test "every counted leaf gets a slot the strip
      can address" — `count` distinct, equal-sized, gap-separated,
      hit-testable slots for every count/selection, at scale 1.0/1.25/1.5/2.0.
- [x] `com.zig` pure test "a one-parameter Invoke is the interface's shape"
      — the arity generalization does not pad the two-parameter handlers.
- [x] `webview2_iface.zig` `@offsetOf` pins `CapturePreview` at vtable slot
      30, so naming the slot did not move `Reload` (31) underneath it.
- [x] Floor green: `zig build test` on the `none` and `win32` lanes,
      `zig build test-agent`, and P1–P3.
- [x] `test/win32/hero-mode.ps1` still ALL PASS (the terminal-only hero
      oracle this change reroutes through a kind-generic accessor).

## Summary

Split out of T90g (2026-08-02), and it is a **decision task before it is a
code task**: T90a §13 said viewer leaves are EXCLUDED from hero mode "Mac
parity — `compactMap(\.surfaceView)`". **Mac has since changed.**
`HeroModeView.swift:11` now reads "Every pane participates: terminals and
viewers alike. This must stay the full leaf list", and
`HeroCarouselView.swift:398` snapshots a viewer with
`WKWebView.takeSnapshot` (async, with an in-flight guard so the repeating
timer cannot stack captures). So the exclusion T90a pinned is now the
divergence.

## Details

Where win32 stands today (`HeroCarousel.zig`):

- `geometry()` counts EVERY leaf (line 90-93), so a viewer occupies a tile
  slot and is selectable.
- the paint loop does `const surface = entry.view.surface() orelse continue`
  (line 206), so that slot paints **nothing** — a hole in the strip, not an
  exclusion.

Either answer is coherent; the current state is neither. Decide and make the
code say it:

1. **Participate (Mac parity, preferred).** Windows analog of
   `takeSnapshot` is `ICoreWebView2::CapturePreview` (async, PNG/JPEG into a
   stream) → decode → DIB, with the same in-flight guard. Needs measuring:
   cost per capture, and whether `PrintWindow` with `PW_RENDERFULLCONTENT`
   over the WebView2 host is a cheaper path (T211's lesson says PrintWindow
   got chrome, not the GL surface, for a TERMINAL pane — a Chromium child
   may differ, so measure rather than assume).
2. **Exclude properly.** `geometry()` counts terminal leaves only, tile
   index maps through the filtered list, hero selection skips viewers, and
   entering hero in a viewers-only tab is a no-op (T90a §13's own words).

Whichever lands, the hole is the bug to close.

*Validation:* `test/win32/hero-mode.ps1` grown — a tab with a terminal and a
viewer: every carousel tile paints non-background pixels (the hole is what
this catches), and arrow navigation lands somewhere coherent. Pure half in
`hero_math`/the count derivation, asserted at 1.0/1.25/1.5/2.0.

## Progress log

- 2026-08-07 04:55: claimed; work starting.
- 2026-08-07 05:00: Decision: option 1 (participate) - Mac changed to 'every pane participates', so excluding viewers is the divergence. Plan: (a) HeroCarousel paints tile chrome for EVERY leaf via a kind-generic PaneView.heroSnapshot() so the hole is closed regardless of snapshot availability; (b) real viewer thumbnails via ICoreWebView2::CapturePreview (slot 30, already reserved) -> CreateStreamOnHGlobal -> GDI+ decode -> HBITMAP, captured LAZILY (hero activation + navigation) rather than on the 150ms heartbeat, because CapturePreview is a full-size PNG round trip where Mac's takeSnapshot is a direct bitmap. (b) is empirically risky (hidden HWND + Chromium occlusion); if it does not produce pixels on the box, (a) still lands and (b) gets filed.
- 2026-08-07 05:34: Built and validated. CapturePreview DOES work on a hidden viewer host window (hero mode keeps controller.IsVisible true while SW_HIDE-ing the host), so option 1 (participate, with a real picture) landed in full rather than degrading to a placeholder. New: test/win32/hero-viewer-tile.ps1 (ALL PASS, 18 checks; -NegativeControl fails exactly the inverted claim). Viewer tile measured 131 distinct colors vs the band backdrop's 1. Filed T560 (hero strip order is node-array order, not tree leaf order - found because the test's first index assumption was inverted) and D25 (the 2s viewer capture cadence vs the terminal's 150ms).
