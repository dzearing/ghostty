---
id: T487
title: "A second launch of ghoztty -e \u003ccmd\u003e against an already-running instance drops the command"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P1"
milestone: "M1"
---

# T487 - A second launch of ghoztty -e <cmd> against an already-running instance drops the command

## In plain terms

If Ghoztty is already open and you run `ghoztty -e some-command`, you get a new
empty window and your command never runs. Nothing tells you it was ignored. The
same line typed when Ghoztty is *closed* works fine, so it looks random.

## Goals

- [x] A launch command reaches the running instance's new window
- [x] `--working-directory` on the same launch reaches it too
- [x] An acceptance section that fails without the fix

## Summary

Sibling of T406, different code path. When another process already owns the IPC
pipe, win32 `App.init` forwards a bare `new-window` and exits:

```zig
// src/apprt/win32/App.zig — the AlreadyRunning arm
const ok = internal_os.ipc_client.sendAction(alloc, "new-window", null) catch false;
std.process.exit(if (ok) 0 else 1);
```

The payload is `null`, so `initial-command` (`-e …`, `--command=…`) and
`--working-directory` are parsed, then thrown away. Exit code 0, nothing logged
— the same silent-drop shape T406 fixed for the restore path, which is why it is
worth fixing in the same family.

Verified by reading the code (2026-08-04, while landing T406), not yet
reproduced on the box.

## Fix sketch

Build the forwarded `new-window` payload from the parsed config rather than
passing `null`: `command` from `initial-command` (joined the way
`IpcHandlers.joinArgv` already joins `+new-window -e`), plus
`working-directory`. The IPC verb already accepts both, so this is a payload,
not a new protocol.

Check the Mac seat's behaviour before deciding whether a second launch should
also *activate* the existing app — file the Mac half if it diverges.

## Validation

A section E in `test/win32/gui-launch-command.ps1`: launch once, then launch
again with `-e <marker>` at the same private endpoint, and assert the marker
runs in the second window. Must fail with the payload left as `null`.

## Validation criteria

- [x] Section E fails WITHOUT the fix — verified by a negative-control build
      with the payload forced back to `null`: exactly E2 (command ran) and E4
      (cwd reached the pane) fail, everything else passes (2026-08-15 run).
- [x] Section E passes WITH the fix — full `gui-launch-command.ps1` ALL PASS
      (sections A–E) against the fixed Debug build, private endpoint.
- [x] Unit tests for the payload builder — 3 `forwardedNewWindowArgs` tests in
      `App.zig` (bare verb when nothing to say; `-e` argv + cwd mapping;
      shell-form → `--command=`), run by the win32 test lane.
- [x] Standing floor green — `floor-lane.ps1 -Lane all`: lib/none/win32/agent
      all PASS (2026-08-15); P1 ALL PASS (25), P2 ALL PASS (20), P3 ALL PASS
      (16), run sequentially after the lanes.

## Notes

Activation parity: the win32 `new-window` handler already calls
`SetForegroundWindow` on the created window (unless `--no-activate`), which
matches the Mac's LaunchServices activating the running app on a second open —
no divergence to file for the Mac seat.

The forwarded cwd is the launch's RESOLVED working directory (config finalize
pins the Windows CLI default to the launching directory, T506), so a second
launch from a shell opens its window where that shell is — the same answer
`+new-window`'s auto-inserted `--working-directory` gives, and the same
directory a FIRST launch would have used.

## Progress log

- 2026-08-15 05:21: claimed; work starting.
- 2026-08-15 05:26: Fix in: AlreadyRunning arm now forwards a rebuilt +new-window argv (forwardedNewWindowArgs: -e/.direct -> -e argv, .shell -> --command=, resolved working-directory -> --working-directory=). 3 unit tests added in App.zig. Debug build green. Next: section E in test\win32\gui-launch-command.ps1
- 2026-08-15 05:31: Validation: negative control (payload forced null) fails exactly E2/E4; fix build ALL PASS on gui-launch-command.ps1 (sections A-E). Floor lanes next.
