---
id: T525
title: "Every-morning client refresh: deliver the latest build AND cleanly reboot the running app + agent, no manual relaunch"
order: 4
deps: ["T524"]
status: "in-progress"
commits: []
seat: "win"
tags: ["feature", "infra"]
priority: "P0"
milestone: "M1"
triage-reason: "user directive 2026-08-06: 'I would like to at least have my client updated every morning, and if that requires a subagent reset, that is fine, but the app needs to reboot correctly'"
---

# T525 - Every-morning client refresh: deliver the latest build AND cleanly reboot the running app + agent

## User directive (2026-08-06, verbatim)

"I would like to at least have my client updated every morning, and if that
requires a subagent reset, that is fine, but the app needs to reboot
correctly."

## Refined directive (2026-08-07, supersedes the shape above where they differ)

"is there a way to update the client without interrupting the agent? I'd
love to have the latest bits when i come look at the digest, maybe when a
task completes a push and it's after 5am, that should signal that we need
to update the latest client code (but avoid an agent update because that
will shut down the loop.)"

So the design is now **app-only, push-triggered**:

- **Trigger**: the FIRST task-completion push after 5am local (the loop
  pushes at every task boundary, go.md step 7) signals the refresh - not a
  clock alone. Later pushes the same day do not re-trigger.
- **App-only, agent untouched**: swap ghoztty.exe/.com + assets, restart the
  app, sessions re-attach to the STILL-RUNNING agent. Never kill, restart,
  or prompt about the agent - an agent update shuts down the loop, which is
  exactly what the user forbids.
- **The dialog trap to design around**: today's delivery also swaps
  `ghoztty-agent.exe` ON DISK, so the freshly restarted app immediately sees
  "bundled agent newer than running, N live sessions" and raises the
  mandatory confirmation - an interruption by another name. App-only mode
  must either skip the agent-exe swap (stage it for the next deliberate
  agent update) or downgrade the stale-agent dialog to a passive notice
  while sessions are live.
- Blocked on **T529** (frozen re-attach after app restart): an automatic
  morning restart that freezes every window is worse than staleness. T562
  (Enter-submission gate) is the other half of unattended reliability.
- **Second staleness incident, 2026-08-07**: the user reported "ctrl-d/
  ctrl-w don't work in the html pane" and "still no address bar" - both
  ALREADY BUILT at HEAD (T394's viewer accelerator forwarding, the
  ViewerNavBar) and simply undelivered, because no delivery had run since
  2026-08-06 09:19. Every day this task stays open, shipped work reads as
  missing features to the person it was shipped for. That is the cost being
  weighed against T529's risk.

## What already exists vs what is missing

- The go-loop DOES deliver most mornings (`%TEMP%\ghoztty-upgrade.log` shows
  05:56 today: exe + com + agent swapped in all three install locations,
  POST-SWAP VERIFY OK). Delivery is not the gap.
- The gap is the REBOOT: the user's open windows keep running the old exe, and
  the path that is supposed to hand them the new one - the mandatory
  agent-upgrade confirmation -> destructive refresh -> windows come back - has
  killed the app without recovery four times (T229 x2, T421, 2026-08-06).
  T524 fixes the recovery; this task makes the morning flow use it.

## In plain terms

You work all day in one terminal window, and that window keeps running the
copy of the program it was started with. So a feature that was finished and
shipped days ago is still not there for you — twice now you have reported
something as missing when it had already been built. After this, the first
time the work loop finishes a task after 5am, your terminal quietly updates
itself: the window closes and comes straight back on the new version with all
your panes and their running programs still there. Nothing asks you anything,
and the background process that keeps your sessions alive is never touched,
so the loop that does the work keeps running through it.

## Goals

- [x] Once per morning (~5-6am, alongside the existing delivery), the user's
      running Ghoztty is restarted onto the freshly delivered build: sessions
      snapshot, app exits, new exe starts, windows/panes restore. An agent
      reset (killing pane processes) is explicitly acceptable per the
      directive - prefer session preservation when the agent protocol allows,
      fall back to reset when it does not. **Session preservation is what this
      does**: the app is killed and restarted, the agent is not, so the panes
      re-attach to their still-running sessions. `scripts\morning-refresh.ps1`
      (trigger + watermark) driving `upgrade-ghoztty-windows.ps1 -AppOnly`
      (delivery), wired into go.md as step 6.5.
- [x] The reboot is verified, not assumed: the flow confirms a live window
      exists on the new build afterwards (+list answers and +version reports
      the delivered sha) and logs the outcome; a failed comeback triggers the
      T524 guard or an explicit relaunch, never a silent empty desktop. One
      greppable line, `APP-REFRESH OK` / `APP-REFRESH UNVERIFIED`, joining the
      two halves of that evidence (D12b/D12c assert the honest negative).
- [x] The user starting their day sees a current client without ever touching
      the upgrade dialog. The agent exe is not swapped at all in app-only mode,
      and the confirmation that a PREVIOUS full delivery's on-disk agent would
      still raise is deferred past the unattended restart.

## Validation criteria

- [x] The due decision has all four arms and cannot re-fire: before 5am, first
      push of the day, second push of the same day, and the next morning.
      (`test\win32\morning-refresh.ps1` A1–A14, plus B8 end to end.)
- [x] The watermark is stamped BEFORE the launch, so a failed refresh cannot
      restart the user's terminal on every subsequent push that day. (B4–B6,
      B8; the `-Check` arm proves a check does not consume the day, B2.)
- [x] An `-AppOnly` delivery swaps the app and leaves `ghoztty-agent.exe`
      byte-for-byte untouched — in the primary install AND in the portable
      copies — with an ordinary delivery as the positive control that it
      otherwise IS swapped. (D5–D7 against D13–D15.)
- [x] The deferral marker is written BEFORE the kill (the app cannot beat it),
      carries a future deadline, and has no BOM. (D8–D12.)
- [x] The app defers only a CONFIRMATION — both the staleness route and the
      protocol-skew route — never the free idle refresh, and a deferral is not
      a decline (the next quiet moment still asks). (Unit: `applyDeferral`,
      `deferralActive`, `parseDeferDeadline` in `agent_upgrade.zig`, win32
      lane; wiring asserted by E1–E4.)
- [x] A refresh with no `$GHOZTTY_PANE_ID` is refused before it burns the day.
      (C1–C3.)
- [x] Standing floor green: both zig lanes, `test-agent`, P1–P3. (2026-08-09:
      none PASS 294s, win32 PASS 179s, agent PASS 332s via
      `scripts\floor-lane.ps1 -Lane all`; P1/P2/P3 ALL PASS; plus
      `upgrade-staleness`, `upgrade-no-fork`, `upgrade-resume-readiness`
      ALL PASS, since this changed the delivery scripts they cover.)

## Notes

- Depends on T524 (guard survives the app's death) - without it a failed
  morning reboot recreates the empty-desktop incident unattended, which is
  worse at 5am than interactively.
- The upgrade script's RESUME-REUSE FAIL streak (3 consecutive deliveries,
  see %TEMP%\ghoztty-upgrade.log; the resume prompt text arrives with '>'
  characters stripped) is the loop-side half of the morning flow and needs
  its own look - file separately if not absorbed here.

## Progress log

- 2026-08-09 10:09: claimed; work starting.
- 2026-08-09 10:12: design settled: (a) new -AppOnly mode in upgrade-ghoztty-windows.ps1 that swaps exe/com/pdb/share but NEVER touches ghoztty-agent.exe on disk (staged for the next deliberate full delivery); (b) a defer MARKER file (%LOCALAPPDATA%\ghoztty\agent-upgrade-defer[-debug], unix-seconds deadline) written just before the app restart, which the app composes into the agent-upgrade policy so a confirm_first (stale_live OR skew_agent_older) degrades to a logged no-op instead of a modal - deliberately NOT setting agent_upgrade_declined, so the next genuinely quiet moment still self-heals; (c) scripts\morning-refresh.ps1 - first task-boundary push after 5am local, watermarked by date, launches the app-only delivery and tells the caller to end the turn. Both (a) and (b) are needed: skipping the swap alone cannot help because the on-disk agent is ALREADY newer than the running one from earlier full deliveries.
- 2026-08-09 10:28: built: agent_upgrade.applyDeferral + parseDeferDeadline/deferralActive (pure, 4 new tests); App.unattendedRefreshActive() folded into BOTH the staleness and the skew check before their log line; upgrade script -AppOnly/-DeferMarkerPath/-DeferMinutes; scripts\morning-refresh.ps1; go.md step 6.5; test\win32\morning-refresh.ps1 ALL PASS (A-G). Also fixed a latent landmine found on the way: launch-upgrade.ps1 defaulted -UpgradeScript from \, which is EMPTY during param binding under 'powershell -File' - it would have died in the binder with nothing logged. Section G guards all three delivery scripts against that shape.
