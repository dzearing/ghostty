---
id: T1104
title: "Ctrl+T from a viewer pane no longer opens a tab"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P1"
triage-reason: "User-facing keyboard regression, and it is the single red in an otherwise 185-assertion green script."
tags: ["fix"]
---

# T1104 - Ctrl+T from a viewer pane no longer opens a tab

## In plain terms

Pressing Ctrl+T should open a new tab, whichever pane you are in. If the pane you are focused on is a document or web viewer rather than a terminal, nothing happens. The shortcut just does not work there.

## Summary

`test\win32\viewer-panes.ps1` scored `1 FAILURE(S) (185 passed)` in the T1094 sweep:

```
FAIL  T394 ctrl+t from a viewer added a tab
```

One red out of 186 assertions, so the viewer surface at large is healthy - this is a narrow, specific regression in keyboard routing from a focused viewer pane. T394 is the task that put the behaviour in, so the shape to look for is a viewer pane swallowing the accelerator, or the tab action resolving a target window from a pane that is not a terminal.

Worth checking whether other window-level chords have the same hole from a viewer - the script only measures Ctrl+T, so a pass here would not prove Ctrl+W, Ctrl+Shift+T and friends work.

## Where

- `src/apprt/win32/` - key routing from a focused viewer pane to window-level actions
- `test/win32/viewer-panes.ps1` - the T394 section

## Goals

- [ ] Reproduce by hand: focus a viewer pane, press Ctrl+T.
- [ ] Trace where the accelerator is lost between the viewer's host window and the window-level keybind handler.
- [ ] Fix, then AUDIT the neighbours - every window-level chord should work from a viewer pane, and if others are broken they belong in this fix and in the script.
- [ ] Extend `viewer-panes.ps1` to cover whichever additional chords the audit turns up.

## Validation criteria

- [ ] Ctrl+T from a focused viewer pane opens a tab, verified by hand
- [ ] the other window-level chords were audited from a viewer pane and the result recorded here
- [ ] `test\win32\viewer-panes.ps1` is ALL PASS

## Progress log

- 2026-08-22: filed by T1094's full acceptance sweep (242 scripts, forward order, Debug build at 9a764deea). Evidence quoted above is from that run's log in `temp\suite-runs\fwd1\`.
