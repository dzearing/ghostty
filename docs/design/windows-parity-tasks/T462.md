---
id: T462
title: "Chooser session rows show no CPU because win32 never subscribes to the agent's session_cpu stream"
order: null
deps: ["T461"]
status: "done"
commits: ["b292be762"]
seat: "win"
priority: "P1"
milestone: "M1"
triage-reason: "the agent already pushes the numbers and the Mac chooser already shows them; Windows is the only side with a blank column"
---

# T462 - Chooser session rows show no CPU because win32 never subscribes to the agent's session_cpu stream

## In plain terms

The machine chooser lists a machine's sessions but never says which of them
is actually doing anything. On the Mac the same list carries a live CPU
meter per session, so you can tell the build that is chewing a core from the
five shells sitting at a prompt. The agent already measures and pushes those
numbers — Windows just never asks for them. When this lands, each session row
carries the same live meter.

## Goals

- [x] The win32 chooser subscribes to `SESSION_CPU_SUB` for the selected
      machine and unsubscribes when the selection moves or the chooser closes
- [x] Each session row renders a CPU meter, laid out per
      `docs/design/win32-design-system.md` (spacing scale, contrast floors,
      asserted at 1.0 / 1.25 / 1.5 / 2.0)
- [x] The subscription is gated on the negotiated HELLO capability, and an
      agent that does not advertise it degrades to no meter rather than a
      wedge or a wrong number
- [x] The `cpu_units` capability is honored, not assumed

## Evidence

`session_cpu` appears in exactly four files —
`src/remote/protocol.zig`, `src/remote/agent/server.zig`,
`src/remote/connection.zig` and `src/apprt/embedded.zig` (the C API the
Swift app consumes). Nothing under `src/apprt/win32/` references it, so the
whole producer side is present and unused here. The win32 chooser's own CPU
vocabulary (`activity_rows.zig:193-217`, `formatCpu`/`normalizedCpu`/
`formatHostCpu`) already exists for the Activity Monitor and is the natural
formatting to reuse so the two surfaces agree.

## Two protocol details that are easy to get wrong

- **`cpu_units`** (`protocol.zig`, near `cpu_pct`): the chooser's session
  meter needs no separate gate, but the Activity Monitor's process table —
  whose `cpu_pct` predates it — does. Read the comment at the capability
  before deciding which of the two surfaces you are gating.
- **The interval is a hint.** `Server.throttledIntervalMs` clamps the
  client's `interval_ms` to a floor, stretches it under host load and caps
  it, and tells the client the real interval in every frame. The UI must
  read the interval it is given rather than assume the one it asked for —
  that is how a slow stream stays distinguishable from a dead one.

## Depends on

T461. Without a warm per-machine connection there is nowhere to hang a
subscription: the roster's remote path frees its dial at the end of every
fetch.

## Validation

- Unit tests in the `none` lane for the row layout + meter formatting at all
  four scale factors, and for the capability gate (absent capability ⇒ no
  meter, no subscribe).
- An acceptance script in `test/win32/` that opens the chooser against the
  local agent, waits for at least two pushed frames, and asserts a numeric
  meter on a busy session and a quiet one on an idle session.

## Validation criteria

- [x] The chooser subscribes on the selected machine's warm connection, and the
      stream really pushes — `chooser cpu: subscribed` plus three or more
      `chooser cpu: frame` lines (section B of
      `test\win32\chooser-session-cpu.ps1`).
- [x] The cadence used is the AGENT's, read out of every frame rather than
      assumed: inside the 500..10000ms its own throttle can produce (C).
- [x] The numbers are per session and differ: a pinned session reads ~99% and an
      idle one ~0%, matched BY SESSION ID against `+sessions --json`, an
      independent source that dials the agent directly (D).
- [x] A meter is really painted: an unbroken run of warn/danger-toned pixels
      spans the meter column (E), against a control where the same probe returns
      a 1px ClearType fringe (F).
- [x] The capability gate degrades to NO meter:
      `GHOSTTY_AGENT_SUPPRESS_CAPS=session_cpu` ⇒ `chooser cpu: no per-session
      CPU stream`, zero frames, no column, no bar, and a chooser that still
      answers (F).
- [x] Geometry and formatting asserted at 1.0 / 1.25 / 1.5 / 2.0 in the `none`
      lane, contrast floors included — `src/apprt/win32/chooser_cpu.zig` and the
      new column test in `chooser_sessions.zig`.
- [x] Standing floor green: the three zig lanes through `floor-lane.ps1`, P1-P3, and the
      chooser acceptance family (`chooser-sessions`, `chooser-roster-refetch`,
      `chooser-resume`, `chooser-conn-pool`) which share the row geometry this
      changed.

## Progress log

- 2026-08-12 18:44: claimed; work starting.
- 2026-08-12 18:51: design settled: new pure chooser_cpu.zig (column metrics on the 4 DIP scale, fill fraction, tone, formatting) + SessionCpuProbe.zig (subscribe on the LOCAL warm connection / the pooled remote one, reader-thread handler copies into a mutex-guarded store and posts WM_APP_CHOOSER_SESSION_CPU to app.msg_hwnd, routed by chooser id like the roster). chooser_sessions.rowLayout reserves the column between the dot and the text; SessionRoster.VisibleRow gains cpu and paints the meter. Gate: subscribeSessionCpu returns error.Unsupported when the peer never advertised session_cpu -> no column, no meter. Interval read from every frame (agent throttles), never assumed.
- 2026-08-12 20:00: validated on the box: chooser-session-cpu.ps1 ALL PASS (19), none+win32+agent lanes PASS (the agent lane STALLed once under -Lane all and passed alone at 326s -> T815), P1-P3 ALL PASS, chooser-sessions/roster-refetch/conn-pool ALL PASS, five suite audits ALL PASS. chooser-resume.ps1 fails at SETUP and it is NOT this change: crash-orphan recovery (80fa0aa12, 2026-08-04) re-attaches the sessions its fixture tries to orphan -> T816. Fixed mid-task: syncPoolLease released the machine's lease BEFORE the CPU probe let go of that connection, so a selection change could unsubscribe through a freed transport; the stop now happens inside syncPoolLease, ahead of the release. Filed T812 (no tooltip), T813 (a replaced LOCAL agent connection freezes the meter silently), T814 (unsubscribe* returning does not really prove no handler is in flight).
- 2026-08-12 20:01: status: in-progress -> done
