---
id: T852
title: "The forwarding CLI verbs (+close, +split, +rename, ...) still pass unknown flags to the server, which silently drops them"
order: null
deps: ["T489"]
status: "in-progress"
commits: []
seat: "win"
triage-reason: "P1: silent flag-dropping on the forwarding verbs is a correctness gap in the agent-facing CLI surface; sibling of T489 which fixed the self-parsing verbs"
milestone: "M1"
priority: "P1"
tags: ["fix"]
---

# T852 - The forwarding CLI verbs (+close, +split, +rename, ...) still pass unknown flags to the server, which silently drops them

## In plain terms

Mistype a flag on `ghoztty +split` or `+close` and the command still runs -
the part you typed wrong just doesn't happen, with no message. T489 fixed
this for the verbs that parse their own flags; the ones that hand their
whole command line to the running terminal still have the gap.

After this, every one of those verbs answers a typo the same way the rest of
the CLI already does — it names the flag, offers the spelling you probably
meant, and stops — so a script never again half-runs a command whose flag
went nowhere.

## Goals

- [x] every forwarding verb (`+close`, `+rename`, `+rearrange`, `+read`,
      `+set-banner`, `+set-state`, `+reload`, `+split`, `+new-window`,
      `+new-remote-window`) rejects a flag it does not accept
- [x] the message is T489's: verb, flag, nearest spelling, `--help` pointer
- [x] every spelling that worked before still works, aliases included
- [x] literal text starting with `--` is still sendable (bare `--`)

## Summary

Follow-up carved out of T489. The verbs built on `parseManuallyHook` -
`+close`, `+rename`, `+rearrange`, `+read`, `+set-banner`, `+set-state`,
`+split`, `+reload`, `+new-window`, `+new-remote-window` - collect every
argument into `_arguments` and forward them over IPC. The server parses them
with `src/apprt/ipc/args.zig parseVerbArgs`, which deliberately ignores
unknown flags: that tolerance is the app<->agent/CLI compatibility contract
(an older server must not hard-fail on a newer client's flag, see
`cwd_implicit`), so the SERVER must stay tolerant.

The fix therefore belongs in the CLIENT: "hard error on an unknown flag is
the CLI's rule, not the server's" (`src/apprt/win32/pane_capture.zig:74`).
`+send-keys` is the pattern-setter - its `checkArg` enumerates the flags the
verb accepts, errors on any other `--flag`, and lets non-flag text through
(T489's `unknown_flag` field there). Each forwarding verb needs the same:
an explicit allowlist of the flags it supports, kept in sync with what the
handlers actually read (`VerbArgs` is the superset; each verb uses a
subset), erroring through the T489 message shape
(`cli/args.zig reportCliDiagnostics` or the same wording).

Watch out for:

- **The Mac seat's server must not be the excuse.** The allowlist lives in
  the shared CLI (`src/cli/*.zig`), so it lands for both platforms at once;
  the servers stay untouched.
- **`+new-window` forwards config-style args on purpose** (its `-e` tail,
  `--working-directory`, `--color`...); its allowlist question is the
  fuzziest - decide whether unknown `--flags` before `-e` are errors, and
  record the call.
- **Do not break `--` and positional semantics** where a verb has them
  (`+send-keys` text, `+read --lines`).

## The `+new-window` call, recorded

Unknown `--flags` before `-e` ARE errors on `+new-window`, same as everywhere
else. The doubt was whether it forwards config-style arguments open-endedly;
it does not. Every server that receives them reads a fixed table — win32
`handleNewWindow`, macOS `handleNewWindow`, and the GTK `actionNewWindow`,
which binds only `--working-directory`, `--title`, `--command` and the `-e`
argv — and the verb's own doc comment has always said so ("Currently, only
`--working-directory`, `--command`, and `--title` are supported"). So a
`--font-size=13` on `+new-window` has never done anything on any platform,
and saying so is the honest answer. What IS forwarded open-endedly is the
`-e` tail, which is a command and is never checked.

## The allowlists came from the handlers, not the docs

Each list is what a server handler actually READS, taken from win32's
`IpcHandlers.zig` and macOS's `IPCServer.swift` together, so a spelling that
works today keeps working even where the doc comment never mentioned it: the
`--split=`/`--direction=` and `--split-percent=`/`--percent=` aliases that
`parseVerbArgs` maps are accepted on both `+split` and `+new-window`. That
matters concretely — the shipped ghoztty skill drives `+split
--split-percent=` in six places, and a doc-driven allowlist would have broken
every one of them.

## Validation criteria

- [x] each forwarding verb rejects `--bogus-flag=1` with a message naming
      the verb and the flag, exit nonzero (extend
      `test\win32\cli-unknown-flag.ps1`) — section 13, all 10 verbs, plus
      section 14's near-miss suggestion per verb
- [x] every flag each verb's server handler actually reads stays accepted
      (positive controls per verb) — section 15's SENTINEL control: every
      real flag first, one certainly-bogus flag last, and the assertion is
      that the message names the sentinel, which can only happen if every
      flag ahead of it was accepted. Section 16 adds "a correct command line
      still reaches the server" per verb
- [x] none-lane unit tests for at least one converted verb's allowlist —
      `src/cli/verb_flags.zig` carries 9 tests (name matching, per-verb
      isolation, the bare `--`, late `--help`, the message shape, and a
      spec-hygiene test); `zig build test -Dapp-runtime=none` PASS
- [x] P1-P3 still ALL PASS (they script these verbs heavily) — P1 25, P2 20,
      P3 16 assertions, all ALL PASS; floor lanes lib/none/win32/agent all
      PASS; `cli-unknown-flag.ps1` ALL PASS (110 assertions)

## Progress log

- 2026-08-17 19:39: claimed; work starting.
- 2026-08-17 19:59: root cause + fix: the forwarding verbs collect argv and the server's parseVerbArgs ignores unknowns by contract, so the hard error belongs in the CLI. New src\cli\verb_flags.zig holds a per-verb allowlist (built from what the win32 AND macOS handlers actually read, so every spelling that worked keeps working, aliases included) plus a Checker each verb's Options embeds; args.zig's nearest-spelling suggester was generalised to nearestName so the message shape is T489's. A bare -- stops flag checking, which is what keeps dashed +set-banner text sendable. All 10 verbs wired; none lane green; cli-unknown-flag.ps1 grew sections 13-18 (107 assertions ALL PASS).
- 2026-08-17 20:36: validated on box: floor lanes lib/none/win32/agent all PASS; P1 (25) / P2 (20) / P3 (16) ALL PASS; cli-unknown-flag.ps1 ALL PASS (110 assertions, sections 13-19). Late --help now prints help instead of reading as a typo (args.parse only looks for it at the front). Filed T950 (a flag written without its value is still dropped silently - the name check landed, the value-presence check did not) and T951 (the release-artifacts guard can never stamp on this box while Docker is down, so validate needs -NoGuardDue every turn).
