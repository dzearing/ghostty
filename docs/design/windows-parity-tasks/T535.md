---
id: T535
title: "viewer-panes.ps1 second red at HEAD: +reload on a terminal-focused window does not report the window-flavored Mac string"
order: null
deps: []
status: "done"
commits: ["0fc6facf1"]
seat: "win"
priority: "P1"
milestone: "M1"
tags: ["test", "fix"]
---

# T535 - viewer-panes.ps1 second red at HEAD: +reload on a terminal-focused window does not report the window-flavored Mac string

## In plain terms

One more check in the viewer test suite fails on every run, independent of
any new work: asking a window full of terminals to "reload" is supposed to be
refused with a message that names the window, and the message that comes back
is a different one. Until the message (or whatever produces it) is fixed, the
viewer suite reports red even when nothing new broke — the same problem T526
describes for its sibling assert.

## Summary

`test\win32\viewer-panes.ps1` section 8b fails one assert at git HEAD,
deterministically, with no local changes (verified 2026-08-06 against a clean
Debug build of 8d2d2a918 — `2 FAILURE(S) (115 passed)`, the other being
T526's):

```
FAIL  +reload on a terminal-focused window reports the OTHER Mac string
```

The assert creates a fresh single-terminal window (`vpterms`) and expects
`+reload --target=vpterms` to exit nonzero with
`focused pane of 'vpterms' is a terminal pane, nothing to reload`. The exit
code IS nonzero (that assert passes); only the STRING differs, so the server
is refusing with some other message. T526's root cause is likely the same
family — the T105/T89f2 deferred-focus guard never grants focus inside a
non-active window on the background test desktop, so "the focused pane of
this window" may resolve to nothing (or to another window's pane) and take a
different error path. First step is to capture the actual string (the script
discards `$r.Out` on pass); then decide whether the defect is the resolution
or the message.

Note T526 was filed at `1 FAILURE(S) (110 passed)` on the same day, so either
this assert flaked green then, or something between that run and 8d2d2a918
turned it red. Both reds were reproduced back-to-back on 2026-08-06 during
T159 validation (clean-HEAD control run).

## Goals

- [x] Find out what the reload refusal actually says on the test desktop,
      word for word, instead of guessing from the failing check.
- [x] Make the viewer test suite's reload check pass without loosening what
      it demands (the message must stay identical to the Mac's).
- [x] Get the whole viewer suite back to a trustworthy all-green, so a red
      run means something new broke.

## Resolution

The server was right all along; the test was mis-measuring. Verbatim capture
on the background test desktop at HEAD 8fa989d96 (fresh Debug build,
isolated `-t535cap` pipe, same launch shape as the harness):

```
EXIT-CODE: 1
STDERR-VERBATIM: [focused pane of 'vpterms' is a terminal pane, nothing to reload
+reload failed.]
```

That is exactly the Mac string (`IPCServer.swift:1177` /
`IpcHandlers.zig:1266`). The historical red was T526's root cause hitting
this assert too: under the loop's consoleless PowerShell host, `2>&1 |
Out-String` formatted native-stderr ErrorRecords as BLANK lines, so the
string assert compared against `''` and failed while the exit-code assert
(which needs no text) passed — precisely this task's symptom. T526's
`Invoke-Verb` fix (stringify each record before `Out-String`, commit
0fc6facf1) repaired all 14 stderr-text asserts including this one. No
product change was needed; the assert is unweakened. T537 filed the same
red separately ("fails in harness, passes live") and is closed as a
duplicate.

## Validation criteria

- [x] The actual refusal string `+reload --target=<terminal-window>` returns
      on the test desktop is captured and recorded here — see Resolution:
      `focused pane of 'vpterms' is a terminal pane, nothing to reload`,
      exit 1 (captured 2026-08-16 on the background test desktop).
- [x] `viewer-panes.ps1` section 8b's window-flavored assert passes, without
      weakening the assert (the Mac string is the contract) — assert text
      untouched since T526's capture fix; PASS in the 2026-08-16 run.
- [x] Full `viewer-panes.ps1` is ALL PASS once T526 also lands (the two reds
      are tracked separately but the suite is only green together) —
      `ALL PASS (185)` at HEAD 8fa989d96, 2026-08-16, from the loop's own
      consoleless session (plus twice during T526's close-out).

## Progress log

- 2026-08-16 04:10: claimed; work starting.
- 2026-08-16 04:14 [session fc40f699-a19d-482d-87d2-927f283cd8b3]: Verbatim capture on the background test desktop at HEAD 8fa989d96: +reload --target=vpterms exits 1 with stderr 'focused pane of vpterms is a terminal pane, nothing to reload' + '+reload failed.' - the Mac string, correct all along. Root cause of the historical red = T526's consoleless stderr capture bug (ErrorRecords formatted blank via Out-String); fixed in Invoke-Verb 0fc6facf1. Running full suite to confirm.
- 2026-08-16 04:36: status: in-progress -> done [commit 0fc6facf1]
