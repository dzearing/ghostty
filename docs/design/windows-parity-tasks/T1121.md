---
id: T1121
title: "msi-upgrade.ps1 compares two different DisplayVersions as if they were equal"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
triage-reason: "A single assertion in an otherwise 34-assertion green script; likely a stale expected value."
tags: ["test"]
---

# T1121 - msi-upgrade.ps1 compares two different DisplayVersions as if they were equal

## In plain terms

A check on the Windows installer expects the version recorded in Add/Remove Programs to be one number and finds another. Either the installer is writing the wrong version, or the check is comparing against an out-of-date number.

## Summary

`test\win32\msi-upgrade.ps1` scored `1 FAILURE(S) (34 passed)` in the T1094 sweep:

```
FAIL  v2: DisplayVersion 26.7.2002 == 26.7.1902
```

The two values differ in the last component (`2002` vs `1902`), which reads like a version that moved on while the expected value stayed put - but that is a guess, and the alternative (the upgrade genuinely failing to update the recorded version) is the more serious one and must be ruled out rather than assumed.

34 assertions pass, so the MSI upgrade path is broadly working.

Note for whoever picks this up: this box has a registered ghost product from an earlier MSI experiment that must NOT be uninstalled, and per-user MSI ARP entries land under HKLM here. Read the surrounding script comments before touching the registry.

## Reproduced alone

Re-run on its own with `scripts\suite-run.ps1 confirm` (T1137), against a Debug
`zig-out` at HEAD. Logs: `temp\suite-runs\fwd1\<script>.alone.log`.

- `msi-upgrade.ps1` - **reproduced alone: yes** (2026-08-23, `1 FAILURE(S) (34 passed)`). Red on its own too, so the failure is not an artefact of the sweep it was found in.

## Where

- `test/win32/msi-upgrade.ps1` - the v2 DisplayVersion assertion
- the MSI's version property and how DisplayVersion is derived

## Goals

- [ ] Determine which side is wrong by reading what the v2 MSI actually declares.
- [ ] If the script's expectation is stale, derive it rather than hard-code it, so it cannot go stale again.
- [ ] If the upgrade is genuinely not updating DisplayVersion, that is a product defect - fix it and say so here.

## Validation criteria

- [ ] which side was wrong is recorded here with evidence
- [ ] the expected version is derived, not hard-coded, if it stays an assertion
- [ ] `test\win32\msi-upgrade.ps1` is ALL PASS

## Progress log

- 2026-08-22: filed by T1094's full acceptance sweep (242 scripts, forward order, Debug build at 9a764deea). Evidence quoted above is from that run's log in `temp\suite-runs\fwd1\`.
- 2026-08-23 12:30: reproduced alone: YES (2026-08-23, T1137's confirm pass against a Debug zig-out at HEAD): msi-upgrade.ps1 (1 FAILURE(S) / 34 passed). Red on its own as well as in the sweep, so the failure is not an artefact of what ran before it - the triage this task carries stands. Log: temp\suite-runs\fwd1\<script>.alone.log.
