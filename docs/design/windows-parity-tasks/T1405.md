---
id: T1405
title: "Nothing scores the accent REPAINT: chrome-theme.ps1 passes 129 of 129 with the color-change redraw removed entirely"
order: null
deps: []
status: "done"
commits: ["5003c9c78"]
seat: "win"
priority: "P1"
triage-reason: "P1: a gate that cannot fail is not a gate - the whole accent live-update claim is currently unguarded, and the defect it would catch is visible to any user who changes their accent colour"
tags: ["test", "infra"]
---

# T1405 - Nothing scores the accent REPAINT: chrome-theme.ps1 passes 129 of 129 with the color-change redraw removed entirely

## In plain terms

When you change your accent colour in Windows settings, Ghoztty is supposed to
recolour whatever is already on screen there and then. There is a test that
claims to check exactly that. It does not: the test takes its photograph by
asking the window to draw a fresh picture, so the picture is always up to date
whether or not the app would have redrawn anything by itself. The app could
stop repainting entirely and every check would still be green, which is what a
measurement proved.

## Summary

Measured while validating T585, not reasoned: `system_colors.repaintForColorChange`
was cut down to a bare cache drop —

```zig
pub fn repaintForColorChange(hwnd: w32.HWND) void {
    invalidate();
    _ = hwnd;   // no RedrawWindow at all
}
```

— the win32 app was rebuilt, and `test\win32\chrome-theme.ps1` still reported
`ALL PASS (129 assertions)`, B4 (labelled "the LIVE-UPDATE claim") included.
An intermediate control — the same function keeping `RedrawWindow` but losing
`RDW_ALLCHILDREN` — also passed everything.

Root cause: **photographing a window makes it paint.** `Get-TestWindowPixels
-Sync` is `WM_PRINTCLIENT`, and the default path is
`PrintWindow(PW_RENDERFULLCONTENT)`; both hand the window a DC and ask for a
frame. That frame reads the accent through the process-global cache the
message has just dropped, so the new colour appears in the capture whether or
not anything invalidated the window. Every accent assertion in the script —
B3, B4, F1, F2 — therefore scores the CACHE and its drop, and the repaint a
user actually sees is scored by nothing.

The same shape defeated the earlier candidate oracle for T307's child half (the
hero carousel repaints itself on a 150ms timer), so this is a class of blind
spot, not one bad assertion.

What T585 landed meanwhile: `chrome-theme.ps1` F3 asserts `RDW_ALLCHILDREN` as
SOURCE TEXT, which does go red on both controls above. That covers the specific
flag; it does not cover "the window redraws when the accent changes" in
general.

## Where

- `test/win32/chrome-theme.ps1` — B3/B4 and the new F1/F2/F3; its header
  records the measurement under "WHAT NO CAPTURE IN THIS SCRIPT CAN CLAIM".
- `test/win32/lib/TestDesktop.ps1` — `Get-TestWindowPixels`, both capture
  paths.
- `src/apprt/win32/system_colors.zig` — `repaintForColorChange`, the code the
  claim is about.

## Options worth weighing

1. Find a read of the on-screen pixels that does NOT ask the window to draw —
   e.g. a BitBlt of the desktop DC on the background desktop (measure it: the
   harness header records `CopyFromScreen` as dead there, and a `CreateDesktopW`
   desktop may have no composited surface to read at all).
2. Count paints instead of reading pixels: a debug-only counter, or an existing
   log line, and assert it advances on the notification and not otherwise. Cheap
   and exact, at the cost of test-only instrumentation in the app.
3. Keep the pixel oracle but remove the cache from under it — e.g. capture,
   then assert against a window that is NOT notified, so the two windows must
   disagree. (Does not work as stated: the cache is process-global, so the
   un-notified window also paints the new accent when photographed. Written
   down so it is not re-derived.)

## What landed

Option 2, in the shape `drag-perf.ps1` already established for a quantity no
cross-process probe can catch: **the app reports it, the script reads it.**

- `src/apprt/win32/paint_probe.zig` — a per-surface `WM_PAINT` counter that
  prints `paint-probe surface=<s> n=<count>` to stderr under
  `GHOZTTY_PAINT_PROBE`. Two surfaces so far: `window` and `viewer_toc`.
  `Window.paintWindow` and `ViewerTOCPanel`'s `WM_PAINT` call it; **the
  `WM_PRINTCLIENT` handlers deliberately do not**, and that asymmetry IS the
  oracle — a frame the camera asked for must not read as the app repainting
  itself.
- `test/win32/chrome-theme.ps1` — section **G**, inside F's viewer session.
  G asserts the probe is live, **G1 measures the card's quiet** across a 1.5s
  idle with no notification and no capture, and **G2** changes the accent,
  notifies ONLY the top-level window, and asserts the card's count advanced.

Two choices worth keeping:

- **The surface is the contents CARD, not the window.** The hero carousel
  refreshes its thumbnails on a 150ms timer, so a counter on the top-level
  window advances unprompted — the same trap that defeated the earlier
  candidate pixel oracle, and it would have defeated a paint counter just as
  thoroughly. G1 exists to prove the chosen surface is not in that class.
- **Only the owner is notified**, as in F2. A child never receives the
  broadcast, so G2 is red on a build that drops `RedrawWindow` AND on one that
  keeps it but drops `RDW_ALLCHILDREN` — the behavioral form of what F3 can
  only assert as source text.

## Validation criteria

- [x] An assertion that FAILS on a build whose `repaintForColorChange` does not
      redraw, and PASSES on one that does — the negative control run, not
      assumed, on a build made for the purpose. **Both controls built and run**:
      the bare cache drop (`_ = hwnd`) gave `FAIL G2 ... (3 -> 3)`, and the
      `RDW_ALLCHILDREN`-less redraw gave the same, each 2 FAILURE(S) of 136 with
      every pixel assertion still green — which is precisely the blind spot
      this task was filed about. The restored build: `ALL PASS (136 assertions)`,
      `G2 ... (4 -> 5)`.
- [x] It runs in the default (non-interactive) invocation of its script.
      G/G1/G2 sit in section F's existing GUI session; no flag, no `-Interactive`.
- [x] `chrome-theme.ps1`'s header stops saying the repaint is unscoreable, and
      B4's label matches what it measures. The header now names G as the section
      that scores it and explains why it counts paints instead of pixels; B4 is
      "an OPEN panel holds no accent of its own", which is what it measures.
- [x] Floor green over the change: `floor-lane.ps1 -Lane all` ALL LANES PASS;
      the fourteen guard rows the edit made due were re-run green, and
      `paint_probe.zig` was added to the chrome-theme coverage row so a change
      to the probe re-arms the section that depends on it.

## Progress log

- 2026-09-06 17:21: claimed; work starting.
- 2026-09-06 18:18: oracle chosen: count paints in the app (paint_probe.zig, GHOZTTY_PAINT_PROBE) rather than read pixels - WM_PRINTCLIENT deliberately does not count, so the camera cannot advance the counter it is scored against. Surface is the viewer contents card, not the top-level window (the hero carousel's 150ms thumbnail timer would defeat a window counter the same way it defeated the pixel oracle). Section G of chrome-theme.ps1: G asserts the probe is live, G1 measures the card's quiet, G2 asserts the repaint.
- 2026-09-06 18:26: status: in-progress -> done
