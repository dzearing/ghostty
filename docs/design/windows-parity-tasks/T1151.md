---
id: T1151
title: "A dev build can still write machine-wide registrations the user's shell obeys"
order: null
deps: [T1146]
status: "done"
commits: ["5a9f38c40"]
seat: "win"
priority: "P1"
triage-reason: "P1: a repo build silently rewriting the user's own machine state is a defect they meet outside the app and cannot attribute to us; T1124 proved it happens for real and T1146 is a second confirmed instance, so the class needs enumerating rather than another incident."
tags: ["fix", "infra"]
---

# T1151 - A dev build can still write machine-wide registrations the user's shell obeys

## In plain terms

Building and testing Ghoztty in this repo can quietly change settings on the
whole computer - which program opens Ghoztty links, what starts when you sign
in, and possibly more. It has already happened once for real: for days, clicking
a Ghoztty link would have tried to run a half-built file in a scratch folder.
When this lands, nothing built inside the source folder can change those settings
at all, and there is a list of every such setting rather than a new surprise each
time one is found.

## Summary

T1124 fixed one instance and revealed the class. The app's build-mode isolation
rule (T350) covers **endpoints** - the IPC pipe, the agent pipe, the state
directory - so a debug build cannot drive the user's terminal. It says nothing
about **registrations**: writes to HKCU, the filesystem outside the checkout, or
anything else the shell reads on the user's behalf. Those are invisible to every
lane, because a unit test can see the key name and the command string but never
the decision to write them.

Two instances are known:

- **URL scheme** (T1124, fixed): the staging release under `zig-out-release\`
  rewrote `HKCU\Software\Classes\ghoztty\shell\open\command` to a scratch path.
  Fixed with a LOCATION gate - an exe with a `build.zig` in any ancestor
  directory registers nothing.
- **Agent autostart** (T1146, open): the HKCU `Run` entry has the same
  build-mode-only gate and a worse blast radius, since the entry survives a
  reboot.

What is unenumerated: file associations, PATH edits, Start menu / shortcut
writes, jump list entries, per-user scheduled tasks, and anything else written
under `%APPDATA%`/`%LOCALAPPDATA%` outside the build's own state directory.

The fix has two halves: an **inventory** of every out-of-checkout write the app
and its scripts perform, and the **same location gate** applied to each (the
`build.zig`-ancestor marker T1124 established, not a new mechanism per site), so
the rule is "a build that lives in a source checkout registers nothing" rather
than a list of exceptions.

## Goals

- [x] Enumerate every write the app, the agent and the repo scripts make outside
      the checkout and outside the build-mode state directory.
      (`docs/design/windows-registration-sites.md`)
- [x] Each such site either carries the location gate or is documented as
      deliberately ungated with the reason. The one real gap found was
      `src/remote/agent/adopt.zig`, which now asks the same location question.
- [x] One acceptance harness asserts the whole set, so a new registration added
      later without a gate goes red. (`test/win32/registration-sites.ps1`)
- [x] A row in the `guard-due` coverage table so the harness re-runs when any of
      those sites change.

## Validation criteria

- [x] The inventory names every site, each with its gate status — and section A
      of the harness re-derives the set from the source, so the doc cannot go
      stale silently (verified: `test/win32/registration-sites.ps1`, A1-A3).
- [x] A launch of the repo build with the release location gate applied writes
      none of the enumerated keys, with a control launch (gate seam off)
      proving the assertion has teeth (D2/D3 control, D5 gated).
      A *release* build was deliberately not launched for this: applying the
      release gate to a debug build is the seam T1124/T1146 established
      precisely so no acceptance run has to point release bytes at the user's
      endpoints (T350).
- [x] The user's real registrations are byte-identical before and after the run
      (D6, release lineage only — the `-debug` names are the harness's own).
- [x] `guard-due` reports the new harness CURRENT after a clean run (STAMPED,
      9 files).

## Progress log

- 2026-08-31 15:57: claimed; work starting.
- 2026-08-31 15:59: inventory pass: registration-shaped writes are url_scheme (gated T1124), LocalAgent Run key (gated T1146), PathInstaller HKCU Environment Path (canonical-install gate), AgentIntegration hook/skill installs under HOME (canonical-install gate), and remote/agent/adopt.zig -- the ONE unguarded site: its gate is build-mode only, so a repo-built RELEASE agent uninstalls the user's standalone MSI and rewrites HKCU Run GhozttyAgent from a scratch path. Fix: hoist inSourceCheckout to src/os/ and add it to adopt's pure gate; then an inventory doc + a class-level harness so a new ungated registration goes red.
- 2026-08-31 16:44: validated: floor all four lanes PASS (none 318s, win32 387s, agent 206s, lib ok); registration-sites ALL PASS (19) incl. section D against the real registry, -NegativeControl scores its 2 planted failures; agent-adopt ALL PASS (26) with the new gate in place - its both-overrides signature is what keeps it running from zig-out; url-scheme (39), agent-autostart, and the seven meta-audits all ALL PASS and re-stamped; ipc-p1/p2/p3 ALL PASS.
- 2026-08-31 16:45: status: in-progress -> done
