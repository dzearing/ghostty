---
id: T1295
title: "A red win-v release build leaves a health-line colour and nothing else - no task, no retry"
order: null
deps: []
status: "todo"
commits: []
seat: "win"
priority: "P2"
tags: ["infra", "fix"]
---

# T1295 - A red win-v release build leaves a health-line colour and nothing else - no task, no retry

## Summary

T1292 follow-up. daily-publish.ps1 now reconciles a pushed tag against the release CI created and stamps 'failed', and go-loop-health.ps1 reports publish=failed. But nothing files a task, nothing retries, and nothing tells the next turn which run to open - so a red release build is a light that stays on until a human reads the line. The publish is the only delivery path this repo has (D85), so its failure deserves the same treatment as a red CI verdict at claim time.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
