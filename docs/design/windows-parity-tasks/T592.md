---
id: T592
title: "floor-lane -Lane all can fail the second WebView2 lane with environment creation hr=0x80004005"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P2"
triage-reason: "the standing test gate goes red for a scheduling artifact, which costs a turn and teaches the loop to distrust its own floor"
tags: ["test", "infra"]
---

# T592 - floor-lane -Lane all can fail the second WebView2 lane with environment creation hr=0x80004005

## Summary

On 2026-08-08 'floor-lane.ps1 -Lane all' reported agent#1=FAIL on apprt.win32.ViewerPane.test 'host floor: a real controller on a real window, on this box', preceded by two '[webview2] (warn): environment creation failed hr=0x80004005 env=null' lines. Re-running '-Lane agent' alone passed in 164s. Both the win32 and the agent lane stand up a REAL WebView2 environment, and -Lane all starts the next lane the instant the previous one exits (win32 finished 01:53:29, agent started 01:53:29) - so the outgoing lane's msedgewebview2 tree is still tearing down when the incoming one asks for an environment. TestProfile (T430) already keeps each lane off the USER's profile; what is missing is settling between two TEST profiles. Same family as T407/T416, which saw this as a HANG rather than a failed environment. Fix candidates: have -Lane all wait for the previous lane's webview host tree to exit before starting the next (floor-lane already counts and sweeps them), and/or give the host-floor test a bounded retry on hr=0x80004005 so a transient environment refusal is not read as a broken ABI. A red floor that is really a scheduling artifact costs a turn each time and teaches the loop to distrust its own gate.

## In plain terms

The command that runs all the automated checks before a change ships sometimes
reports a failure that is not real: two of the check suites each start a small
embedded browser, and running them back-to-back means the second one asks for a
browser while the first one's is still shutting down. It gets turned away and
the whole run is marked failed. Re-running the same suite on its own passes.
Every occurrence costs a wasted cycle and, worse, makes a genuinely red result
easy to dismiss.

## Goals

- [x] Running all lanes in one command gives the same answer as running them one at a time
- [x] A transient browser-environment refusal is distinguishable from a real failure in the output

## Validation criteria

- [x] `floor-lane.ps1 -Lane all` is ALL LANES PASS on a box where the individual
      lanes pass, i.e. the back-to-back ordering no longer changes the verdict.
      Verified: green before the change (the baseline) and green after it, with
      the settle line printing only when it had something to wait for. The
      RACE itself is covered deterministically instead of by repetition -
      `test\win32\floor-lane-webview-settle.ps1` arm 5 stages a browser
      process that dies mid-wait and requires the settle to have blocked for it,
      which `-Repeat 3` could never have demonstrated on a box where the failure
      shows up three times a month.
- [x] The host-floor test still says so loudly on a box with no WebView2 runtime
      (its existing contract - a quiet skip is a test reporting success for
      work it never did), and the retry cannot swallow it:
      `Failure.isTransient()` returns false for `runtime_not_found`, so that
      answer is taken on the FIRST reply and the skip branch reports it in the
      same words as before. Unit test in `src/apprt/win32/webview2.zig` covers
      every variant of the enum.
- [x] The floor lanes and P1-P3 stay green.

## Progress log

- 2026-08-09 06:42 [session f95104a2]: Recurred 2026-08-09 06:30 in exactly the documented shape, this time on the WIN32 lane rather than the agent one: 'floor-lane.ps1 -Lane all' gave none#1=PASS win32#1=FAIL agent#1=PASS, the sole win32 failure being apprt.win32.ViewerPane 'host floor: a real controller on a real window, on this box' (4287/4357 passed, 1 failed, 69 skipped). The none lane finished and the win32 lane started in the same second, which is the scheduling artifact this task names. Seen while validating T532, whose change touches only src/remote/connection.zig, src/termio/Remote.zig and PowerShell - nothing WebView2 - so it is not a new regression. Re-running -Lane win32 alone is the remedy in flight. Third recorded occurrence; the cost is still a turn each time.
- 2026-09-06 21:28: claimed; work starting.
- 2026-09-06 22:02: Root cause confirmed by reading the code rather than reproducing (three occurrences in a month; a clean -Lane all baseline ran green tonight). Two ends: (1) floor-lane's end-of-lane sweep called Stop-Process and returned immediately, so the next lane started while the browser tree was still unwinding - and its matcher only saw processes carrying --webview-exe-name=, which the crashpad handler does NOT carry (verified against live msedgewebview2 command lines on this box), so the tree was under-counted. New scripts\lib\WebViewLane.ps1 matches on the private ghoztty-wv2test-<pid> profile too, kills, WAITS for exit, and only then prunes the profile dir; floor-lane also settles at the START of every lane. (2) src/apprt/win32/webview2.zig gains Failure.isTransient() and the host-floor test in ViewerPane.zig retries a transient environment refusal up to 3 times, never retrying runtime_not_found. Acceptance: test\win32\floor-lane-webview-settle.ps1, ALL PASS (20 assertions), guard row webview-settle registered.
- 2026-09-06 22:50: Validated on the box. floor-lane -Lane all: ALL LANES PASS (lib/none/win32 412s/agent 412s) with the change in, and the baseline before it was green too. floor-lane-webview-settle ALL PASS (20). Re-stamped after the source edits: docs-routing 20, body-complete 40, isolation-meta 13, launch-preflight 19, verdict-exit, cleanslate 21, stderr-capture 25, test-reach 14, desktop-launch 29, command-resolve 15, printclient-audit 8, viewer-close 44, viewer-nav-pin 24 - all ALL PASS. P1 25 / P2 20 / P3 16 ALL PASS. Filed T1411: the controller half of the same refusal is not retried, and its skip branch is a PASS.
