---
id: T125
title: "no agent-update dialog and no \"What's new\" on Windows"
phase: "K"
deps: ["T117"]
status: "todo"
priority: "P1"
triage-reason: "The correctness half shipped in T147; what is left is the What's New UI plus wiring protocol-skew into the existing dialog."
commits: []
---

# T125 — no agent-update dialog and no "What's new" on Windows

## Summary

No agent-update dialog and no "What's new" on Windows — main added a whole feature (`981d18e29` ReleaseNotesStore, `1d7f809b3` WhatsNewTracking, `d28adcec1` WhatsNewNotesView, `f5c75454f`+`1f8f7c302` reframed agent-restart dialog copy, `1a236ecf7` scoping notes to agent-process changes, `047a80c47` bundling the notes JSON) that is entirely macOS Swift. On Windows `update_check.zig` covers only the APP-version check (T24, `win-v*` tags) and `LocalAgent.zig:264` states outright that "the listen-pipe agent never self-updates" — so the CLAUDE.md agent-contract's mandatory-update path (lazy non-destructive upgrade, else an explicit "upgrading will reset all windows" confirmation) has NO Windows implementation. That contract gap is the real risk here, well above the notes UI: file the notes viewer as nice-to-have, but treat the agent-upgrade confirmation as a correctness requirement. Note `release-notes/` JSON is repo-shared, so only the presentation + tracking need porting

## Details

Main built a whole feature that is entirely macOS Swift: `981d18e29`
(ReleaseNotesStore), `1d7f809b3` (WhatsNewTracking — snapshot last-seen version
at launch), `d28adcec1` (WhatsNewNotesView), `65d345cd5` (split
WhatsNewNotesContent), `f5c75454f` + `1f8f7c302` (reframed agent-restart dialog
copy + What's-new accessory), `1a236ecf7` (scope the notes to agent-process
changes), `047a80c47` (bundle the notes JSON into the app Resources).

On Windows, `update_check.zig` covers only the **app**-version check (T24,
`win-v*` tags), and `LocalAgent.zig:264` states outright that "the listen-pipe
agent never self-updates".

**Split this by risk, because the two halves are not equally important.**

- **The notes UI is nice-to-have.** `release-notes/*.json` is repo-shared, so
  only presentation + last-seen tracking need porting.
- **The agent-upgrade confirmation is a correctness requirement.** CLAUDE.md's
  agent-contract section mandates that when a protocol skew means live sessions
  cannot be carried across, the app shows an explicit *"Upgrading will reset all
  windows. Continue?"* before resetting — and prefers a lazy, non-destructive
  upgrade when sessions CAN be carried. Windows has no implementation of either
  path. Since the win32 agent already outlives the app (HKCU Run entry, survives
  app upgrade), the skew this protects against is reachable on Windows today.

Treat the second bullet as the real T125 and the notes viewer as a follow-on.

## Scope note 2026-07-30 (T147 landed the correctness half)

**The "agent-upgrade confirmation is a correctness requirement" bullet above is
DONE** — built by T147, not by this row. Windows now compares the running
agent's HELLO build stamp against the binary the app ships beside and, when a
restart would end live sessions, shows the mandatory `ConfirmDialog` ("Restart
the Ghoztty background terminal process?" / "Update Now" / "Later") before
touching anything; idle refreshes silently. Proven by
`test/win32/agent-upgrade.ps1` (38) ×3 with a real negative control.

What is still T125 after that:

1. **The "What's new" accessory** on that same dialog (Mac `makeUpgradeAlert`
   hangs a `WhatsNewNotesView` off it) plus the tabbed Client/Agent notes
   window and last-seen tracking — the nice-to-have half, now the *only*
   remaining half of the notes work.
2. **Protocol-SKEW gating, which T147 deliberately did NOT cover.** T147 answers
   "is the running agent an older BUILD?"; it does not answer "did the HELLO
   handshake negotiate an INCOMPATIBLE protocol?". CLAUDE.md requires that on an
   incompatible skew the app must not replay across it and must take the
   mandatory-update path. The dialog now exists, so this is wiring a second
   trigger into it (`Negotiated` version/capability mismatch ⇒ same
   confirmation, different copy) rather than building a mechanism.

## Scope note 2026-07-29 (found while working T127)

`origin/main` is 12 commits ahead of this branch and 10 of them extend this same
feature area to CLIENT-scoped release notes: `066388006` (ReleaseNotesStore
client dir + appcast-description parse), `c01b5d5a8` (bundle
`release-notes/client` into Resources), `c5f597297` (parse offered-version
client notes from the appcast item), `a6739efce` (inline client notes +
Cancel/Update in the pre-update dialog), `a089b95b9` (What's New menu WINDOW
with Client/Agent tabs), plus `0a57c523a` / `83b436ac6` (CI + docs), merged as
`68d9299a9`.

That lands here rather than in a new row: this task is already "no agent-update
dialog and no What's new on Windows", and the Mac side now has TWO note scopes
(client and agent) shown in one tabbed window. Re-read the Mac implementation
after the merge and scope both scopes together -- shipping only the agent half
would leave the tabbed window half-empty on Windows.

The 12th commit, `d6f1c1de5` (banner link hover + action menu), is a different
subsystem and is filed as **T165**.
