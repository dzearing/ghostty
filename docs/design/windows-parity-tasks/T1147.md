---
id: T1147
title: "The soak\u0027s \u0027median fps under load\u0027 assertion counts idle panes, so the first completed soak fails at fps 1"
order: null
deps: ["T1125"]
status: "done"
commits: []
seat: "win"
priority: "P1"
tags: ["test"]
---

# T1147 - The soak's 'median fps under load' assertion counts idle panes, so the first completed soak fails at fps 1

## In plain terms

The stability check now runs to the end for the first time, and it reports the
terminal drawing at one frame a second. It is almost certainly not: the number
is an average taken over every pane in the window, and three quarters of those
samples come from panes that were sitting idle with nothing to draw. Until the
measurement counts only the panes actually doing work, nobody can tell a real
slowdown from this.

## Summary

`test\win32\soak.ps1` was scored `stall` on every sweep until T1125 gave it its
own timeout. Its first completed run (2026-08-23, 30m 53s, HEAD staging release
at 820193367) came back `1 FAILURE(S) / 10 passed`, and the one failure is:

```
renderer telemetry: 10925 windows, median fps 1, worst frame gap 622ms
FAIL  median fps under load >= 20 (got 1)
```

The distribution in that run's own `log-slice.txt` is sharply bimodal:

| fps | windows |
|---|---|
| 1 | 7375 |
| 21-28 | ~1750 |
| 59-60 | ~1789 |

So two thirds of the samples are `fps=1` and the rest are 21-60. Four panes were
open: three under deliberate load (an endless `type` of an 8MB file, an
alt-screen churn loop, a 150k-line grow) and one idle probe pane touched once a
minute. An idle unfocused pane legitimately redraws about once a second, so it
reports `fps=1` every window - and there are far more of those windows than
loaded ones, which is what the median is reading.

The script ALREADY knows this shape and says so, two lines above the failing
assertion:

```
# max_gap is informational only: an IDLE unfocused pane legitimately
# reports gaps equal to the time between its redraws ... so a global
# stall assertion false-fails.
```

The same reasoning applies to fps and was not applied. Leading reading: the
assertion's denominator is wrong, not the renderer. It must be confirmed rather
than assumed - the loaded panes' own numbers (21-60) are what should be graded,
and if a loaded pane is genuinely sitting at 21 fps that is worth knowing too.

Second, smaller finding from the same run, worth a look while in here: the grow
pane did not finish (`grow finished: False` - 150k `echo` lines in 30 minutes),
and the baseline `+read` against the tiny-write storm took 11.9 seconds (the
known T62 renderer-mutex starvation, already unasserted here).

## Where

- `test/win32/soak.ps1` - the telemetry slice and the `median fps` assertion
- the `perf fps=` line's producer, for whether a window can say which pane it is

## Goals

- [x] Establish whether `fps=1` windows are idle panes, from the telemetry itself rather than by inference.
- [x] Grade what the assertion's name claims - panes under load - so the verdict answers the question it appears to.
- [x] Keep a real renderer regression detectable: a loaded pane that drops must still turn this red.

## Validation criteria

- [x] the fps=1 population is identified with evidence, not assumed to be idle panes -
      every `perf` sample now carries `pane=<uuid>`, and the 30-minute run's own
      breakdown says where the fps=1 mass lives: the idle probe pane 1487 samples at
      98% fps<=1, a second idle pane 1486 samples at 100%, `soak-grow` idle after it
      finished; the loaded `soak-stream` pane 1790 samples with min 59.
- [x] a completed soak against a healthy build reports ALL PASS - full 30-minute run
      2026-08-31, `%TEMP%\ghoztty-soak\20260831-103409\report.txt`, **ALL PASS (13
      assertions)**; the 5-minute smoke (`20260831-102702`) is ALL PASS too.
- [x] a negative control shows the assertion still goes red when a loaded pane's fps
      really drops - `soak.ps1 -SelfTest`, 7 checks: a loaded pane at 8 fps scores RED
      and is named; the 60-fps stream pane falling to 25 scores RED even though 25
      clears the altscr floor; a loaded pane with no telemetry at all is a failure;
      400 idle fps=1 samples do not move the verdict.

## Progress log

- 2026-08-23: filed from T1125's validation run - the first soak ever to reach its own verdict. Report: `%TEMP%\ghoztty-soak\20260823-010503\report.txt`.
- 2026-08-31 09:40: claimed; work starting.
- 2026-08-31 09:48: root cause confirmed by design: the perf sampler logs once a second PER PANE with no pane identity, so any pooled statistic (median, and equally T1158's p90) describes how many panes were idle rather than the renderer. Fix: OpenGL.zig's perf sampler now emits 'perf pane=<uuid> fps=... max_gap_ms=...' (threadlocal surface captured in threadEnter), and soak.ps1 groups by pane, maps ids to the names it gave them, reports a per-pane breakdown as evidence, and grades only the panes it deliberately loaded. soak.ps1 -SelfTest is the negative control: 6/6 green including a loaded pane stuck at 12 fps scoring RED. profile-latency.ps1's regex made pane= optional.
- 2026-08-31 11:07: validated on the box. Full 30-minute soak against the release build: ALL PASS (13 assertions), report 20260831-103409. The per-pane breakdown is the evidence the task asked for - idle probe 1487 samples 98% fps<=1, loaded soak-stream 1790 samples min 59. Two things the run turned up, filed: T1214 (a fifth idle pane in a window the harness never meant to open) and T1215 (soak-grow now finishes early, so it is never graded). Floors are per pane: altscr measured p90 29 on a healthy build, so a flat 30 would have gone red - it grades at 15, stream/grow at 30. Also fixed here because they made the soak unrunnable: the setup +list died on the client's 'Waiting for Ghoztty to answer' progress message (twice - once as bad JSON, once as a terminating NativeCommandError from 2>&1), and an aborted setup exited past the teardown leaving three load generators pinning the box.
- 2026-08-31 11:12: status: in-progress -> done
