---
id: T871
title: "win32 agent integrations (7/8): there is no Agent Integrations window on Windows - build the win32 dialog with per-agent state rows and Set Up / Update / Remove actions"
order: 7
deps: ["T869"]
status: "done"
commits: ["c5e3318e2"]
seat: "win"
triage-reason: "P0: T598 split (agent-integrations port), user directive 2026-08-15 - copilot integration is the priority; closed same-day"
priority: "P0"
tags: ["feature"]
---

# T871 - win32 agent integrations (7/8): there is no Agent Integrations window on Windows - build the win32 dialog with per-agent state rows and Set Up / Update / Remove actions

## In plain terms

The Mac has a small "Agent Integrations" window: one row per coding agent
showing whether it is detected and whether its Ghoztty setup is current,
with Set Up / Update / Remove buttons. Windows has nothing — you cannot see
the state or fix it without hand-editing files. This builds that window,
Windows-native.

## Goals

- [x] A window that lists each coding agent with whether it is installed
      and whether Ghoztty's setup is current, filling in without freezing
      the app
- [x] Set Up / Update / Uninstall on each row, with a confirmation that
      says exactly what will be removed and what stays
- [x] Opened from the command palette and the Help menu, like the Mac
- [x] Proven by the GUI acceptance harness and green floor lanes

## Summary

Seventh slice of the T598 split. Windows counterpart of
`AgentIntegrationsView/ViewModel/Controller` on the T869 service layer.

- One row per runtime (Claude Code, Copilot CLI): detected?, integration
  state (not installed / installed / update available), per-row action
  button (Set Up / Update / Remove), busy indicator while working, inline
  error text on failure. States refresh after every action and on window
  open, OFF the GUI thread (probes touch disk/PATH; the Mac freezes were
  caused by inline probes — same trap here).
- Honest copy for the special states, from the Mac model: plugin-managed
  Claude ("managed by the ghoztty plugin" — offer nothing destructive);
  shared banner ("removing this leaves shared scripts used by <other>").
- Windows-native construction per the win32 design system
  (`docs/design/win32-design-system.md` + `docs/claude/win32-ui.md` are
  REQUIRED reading before any pixel): follow the existing dialog patterns
  (HostSettingsDialog is the closest sibling — rows, buttons, theming,
  DPI). Entry points: the palette (replacing the old "Install Claude Code
  Integration" entry per T870) and wherever the Mac menu parity slot is.
- No jq row: the Mac shows a "jq missing" warning; Windows' T866 decision
  removes the dependency, so this row must NOT exist here. If T866 landed
  a dependency of its own, surface that instead, same pattern.

## Validation criteria

- [x] Window opens from the palette, lists both runtimes with correct
      detected/state values on this box, and never blocks the GUI thread
      while probing. — `test\win32\claude-integration.ps1` case 4 (ALL
      PASS, 83 assertions, 2026-08-15): the window appears immediately
      with "Checking…" rows and fills in from the off-thread probe
      (workers post WM_APP+32 to the message-only window; nothing blocks
      the pump — the same run drives the palette and dialogs throughout).
- [x] Set Up on a sandboxed home installs and the row flips to installed;
      corrupting a managed file flips it to Update; Remove clears it. —
      Same harness run, GUI-driven end to end: Remove (after an honest
      confirm, with a cancel negative control) clears skills + banner
      scripts and flips the row; Set Up reinstalls; a marker-stamped stale
      banner script reads "Update available" on reopen and Update rewrites
      it. No claude CLI spawned anywhere in the flow.
- [x] Plugin-managed and shared-banner rows show the honest copy;
      destructive gating follows the CURRENT Mac (26f4ea625 "make Agent
      Integrations rows and uninstall copy tell the truth"): uninstall
      stays available on a plugin-managed row and the confirm copy says
      hooks are plugin-managed and won't be touched — the criterion as
      first written ("disable the destructive action") predates that Mac
      correction. — `agent_integrations_vm.zig` tests pin all three copy
      variants, the plugin-note line, and the precedence.
- [x] View-model logic (row derivation from statuses, action gating) unit
      tested in every lane (`agent_integrations_vm.zig`, 8 tests;
      dialog layout pinned by `AgentIntegrationsDialog.zig` tests at 1.0/
      1.25/1.5/2.0); ALL LANES PASS via `scripts\floor-lane.ps1 -Lane all`
      2026-08-15 20:25–20:47 (lib 52s, none 321s, win32 379s, agent 355s),
      plus P1 (25), P2 (20), P3 (16) ALL PASS.

## Progress log

- 2026-08-15 19:50: claimed; work starting.
- 2026-08-15 19:57 [session 6e0e9ec3]: Design settled after reading Mac AgentIntegrationsView/ViewModel/Controller (origin/main), win32 design system + siblings (HostSettingsDialog, ConfirmDialog, AgentIntegration). Two new modules: agent_integrations_vm.zig (pure row derivation: status labels, action gating, honest uninstall copy variants - every lane) and AgentIntegrationsDialog.zig (modal dark dialog, nested-pump T48-safe shape, per-agent rows name/status/detail + Set Up/Update/Uninstall buttons, Done; refresh + actions on detached threads posting WM_APP+32 to msg_hwnd, routed to the active dialog so a destroyed dialog can never be posted into). Actions serialized (shared banner refcount; matches install_running). Wiring: palette .claude kind + Help menu item open the dialog (Mac: both route to AgentIntegrationsController.show), names become 'Set Up Agent Integrations...'; AgentIntegration loses the palette blind-install source. No jq row (T866). Modal-vs-modeless is user-feelable: filing a decision, assuming modal. claude-integration.ps1 cases 4/7 rewritten to drive the dialog.
- 2026-08-15 20:13 [session 6e0e9ec3]: Implementation complete: agent_integrations_vm.zig (pure row derivation + honest uninstall copy, 8 tests), AgentIntegrationsDialog.zig (modal dark dialog, pure layoutFor + tests, off-thread workers posting WM_APP+32 to msg_hwnd routed via App.msgWndProc to the active dialog), wiring in Surface/commands/menu_bar/App/apprt, AgentIntegration slimmed to first-run-only install (palette source removed, home-failure now reports failed per agent). claude-integration.ps1 cases 4/7 rewritten to drive the dialog (posted WM_COMMAND for row buttons - a sent BM_CLICK would deadlock on the nested confirm); guard-due covers the two new files. win32 build green; floor lanes next.
- 2026-08-15 21:01: status: in-progress -> done [commit c5e3318e2]

### Decision D76 - Should the Agent Integrations window let you keep using the terminal while it is open?

Resolved 2026-08-16T16:26:34Z : Modal dialog, like Ghoztty's other Windows dialogs (Recommended)
