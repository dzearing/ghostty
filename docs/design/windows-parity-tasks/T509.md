---
id: T509
title: "test-binary-soak.ps1 loses the first binary\u0027s per-run logs when a lane soaks two binaries - the second reuses the same log filenames"
order: null
deps: []
status: "done"
commits: ["4d3ff33cf"]
seat: "win"
priority: "P1"
milestone: "M1"
tags: ["fix", "infra"]
---

# T509 - test-binary-soak.ps1 loses the first binary's per-run logs when a lane soaks two binaries - the second reuses the same log filenames

## In plain terms

The script that re-runs a test program many times to catch rare crashes keeps
a log file for every run — but when it soaks two programs back to back, the
second program's logs silently overwrite the first's. If the first program had
a failure, the evidence of *what* failed is gone by the time anyone looks.
That exact loss happened during the T238 investigation and cost an extra
20-run soak just to re-learn what a kept log would have said.

## Summary

`scripts\test-binary-soak.ps1 -Lane agent` soaks both agent test binaries "in
turn", but the per-run log name is `{label}-{stamp}-{NN}.log` where `$stamp`
is taken ONCE per invocation (line ~85) and `NN` restarts at 1 for each
binary (`$idx = $started + $k` inside the per-target loop). So run 07 of
binary 2 overwrites run 07 of binary 1.

Observed 2026-08-06 during T238: `-Lane agent -Runs 20 -Concurrency 5`
reported `runs=40 ... fail=3`, but only 20 log files existed afterwards — the
two FAILs from `ghoztty-agent-test.exe` were unrecoverable and the binary had
to be re-soaked alone to identify them.

Fix shape: include the exe basename (or a per-target index) in the log
filename, e.g. `{label}-{stamp}-{exeName}-{NN}.log`. The final summary should
also name which binary each FAIL/CRASH belonged to.

## Goals

- [x] A two-binary soak keeps every run's log — nothing overwritten
- [x] A failure in the first binary is still readable after the soak ends
- [x] The summary says which binary each fail/crash belonged to
- [x] The acceptance harness pins all three so they cannot regress

## Validation criteria

- [x] `-Lane agent -Runs 2` produces distinct, surviving log files for BOTH
      binaries (4 files total) — harness section 5 fixtures a fake repo with
      two agent-named binaries and asserts 4 log files, each name carrying the
      exe basename (`{label}-{stamp}-{exeBase}-{NN}.log`); ALL PASS 2026-08-15
- [x] A forced failure in the first binary's run is still readable after the
      second binary finishes — the fixture's first binary exits 1 echoing its
      own path; the harness reads `...-ghoztty-agent-test-01.log` after the
      soak and finds that path (and not the second binary's)
- [x] The end-of-soak summary attributes fails/crashes per binary — new
      per-exe lines (`<exe>: runs=N pass=... fail=... crash=...`) printed when
      more than one binary was soaked; asserted for both binaries, plus a
      single-binary negative control ensuring no breakdown line appears

## Progress log

- 2026-08-15 11:33: claimed; work starting.
- 2026-08-15 11:41: Fix in: exe basename now in standalone per-run log names; per-binary summary lines when >1 exe. New harness section 5 fixtures a two-binary agent lane (first binary fails) - all 6 new assertions pass. Harness turned up an unrelated pre-existing teardown race in Stop-SoakProcessTree (depth-first kill lets the worker log a phantom iteration); filing + fixing it this turn.
- 2026-08-15 12:03: status: in-progress -> done [commit 4d3ff33cf]
