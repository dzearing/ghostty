---
id: T799
title: "Mac: assert a banner table\u0027s value column uses the pane width (T745\u0027s horizontal oracle)"
order: null
deps: []
status: "todo"
commits: []
seat: "mac"
priority: "P2"
tags: ["test"]
---

# T799 - Mac: assert a banner table's value column uses the pane width (T745's horizontal oracle)

## Summary

T745 found that every existing banner-table oracle measures the band HEIGHT, which a column granted only half the pane still satisfies (the long value simply wraps one line later). The win32 suite now measures the horizontal extent directly (pane-banner.ps1 6g2: the text's own right edge vs the content column, 96.6% shipped / 33% with the pre-T123 fixed cap). The macOS banner renderer shares the same columnWidths(natural:available:) design and has no such assertion, so the Mac half of the guard is owed.

## Validation criteria

- [ ] TODO: the observable checks that prove this is done, ticked with evidence of how each was validated.
