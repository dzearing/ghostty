---
id: T328
title: "Kill on a REMOTE roster row loses its refetch: CLOSE_SESSION and the LIST behind it both fail on the relay connection"
order: 39
deps: ["T319"]
status: "in-progress"
priority: "P0"
triage-reason: "remote Kill fails both RPCs on every run; the roster the user is reading disagrees with the machine"
tags: [fix, test]
commits: []
---

# T328 - Kill on a REMOTE roster row loses its refetch: CLOSE_SESSION and the LIST behind it both fail on the relay connection

## In plain terms

When you killed a terminal session on another machine from the machine
chooser, the kill itself went through, but the list on screen never updated —
it kept showing the session you had just killed, and the session count stayed
wrong until you left the machine and came back. The underlying cause turned
out to be memory corruption inside the background agent, which has since been
fixed; this task verified that the list now updates correctly every time and
locked that behavior into the test so it cannot silently regress.

## Goals

- [x] Find out who was closing the connection during a remote Kill
- [x] The roster refetch after a Kill lands, so the on-screen list matches the machine
- [x] The test asserts the refetch (the old deliberate NOTE is an assertion again)
- [x] The agent's own stderr is captured by the test, so the next mid-flight RPC death has a witness

## Summary

Found while validating T319, on the box, on every run. Killing a session from
the chooser's roster works **locally** (T318's `chooser-sessions.ps1` proves the
agent really ends it and stops listing it). Over a **relay** connection the same
worker's two RPCs both fail:

```
info(win32):    chooser roster: loaded 2 session(s) target=remote device=dev-remote
info(win32):    chooser roster: ending session id=52184a6228108267a2d3be0131af6130
info(win32):    chooser roster: relay dialled device=dev-remote
warning(win32): chooser roster: close session failed err=error.ConnectionClosed
warning(win32): chooser roster: LIST_SESSIONS failed err=error.ConnectionClosed
```

The error is **not stable across runs**: `error.ConnectionClosed` on four runs,
`error.Timeout` on the fifth. Both are the same event seen from two sides of a
race, and `error.Timeout` is precisely **T326** — the agent terminates the child
and persists to disk BEFORE replying, so a Kill that plainly worked reports a
timeout. On a relay-bridged link that window is evidently long enough that the
connection sometimes dies inside it.

## Why it matters

The refetch is the roster's only source of truth. Losing it means the row the
user just killed is only hidden by the **optimistic hide** (`markKilled`), and
the count in the detail subtitle — and `owned.sessions.len` behind it — stays at
the pre-kill number until the user leaves the machine and comes back. The user
is looking at a machine's roster that disagrees with the machine.

## What has already been ruled out

- **Not the fake relay's bridge lifecycle.** `BRIDGE down` is never logged at
  the time of the failure; the bridge for the Kill's dial comes up and stays up.
- **Not stale bridges piling up.** Reaping them made no difference. (Two reaping
  probes were tried and BOTH are wrong, which is worth keeping: `TcpClient.Connected`
  reports the state as of the last I/O, and `Poll(SelectRead) && Available == 0`
  races the loop's own reads — it tore down healthy bridges ~30 ms after the
  upgrade. `lib\FakeRelay.ps1` carries that note.)
- **Not the 8-byte WebSocket length.** It WAS broken (`[int] -shr 56` is
  silently `-shr 24` in .NET) and is fixed, and the failure survived the fix.
- **Not the dial.** `chooser roster: relay dialled device=…` is logged, so the
  WebSocket upgraded and the HELLO handshake completed. The connection dies
  after that and before (or during) the first RPC.

## What was tried and must NOT be shipped as-is

A retry on a fresh dial — `if (roster == null and conn != null) re-dial, list
again` — **wedged the worker thread**: no `re-dialled` line, no `re-dial failed`
line, no reply ever posted. Blocking in `relay_dial.dial` (the WebSocket upgrade
read has no deadline) or in the previous `Dialed.deinit` are the two candidates.
That is a worse failure than the stale count and it was reverted; the comment in
`SessionRoster.worker` points here.

## The work

1. Decide WHO closes the connection — instrument both sides. The agent's own
   log during `CLOSE_SESSION` is the first place to look, then the client's
   reader/heartbeat threads, then the relay bridge.
2. Settle whether this is relay-specific or just SLOWER on a relay (T326 says
   the same window exists locally). If it is T326's window, fixing T326 — reply
   before persisting — may close this too.
3. Make the roster truthful after a Kill whatever the answer is: either the RPC
   survives, or the refetch happens on a connection that does. Any retry needs a
   BOUNDED dial (see above).
4. Re-enable the assertion that is deliberately not asserted today:
   `test/win32/chooser-sessions-remote.ps1` prints
   `NOTE T328: the post-Kill refetch does not land - …` where it should assert
   `the refetched remote roster is down to 1`.

## Resolution (2026-08-06)

The connection was being killed by **agent-side memory corruption**, not by
anything in the roster worker, the relay, or the RPC path — and the corruption
was fixed by work that landed between filing and pickup:

- **`449a112b4` (2026-08-04, filed as part of the T420/T442 hunt)** is the
  cause-shaped fix: the agent's three per-connection push pumps (metrics,
  session-cpu, **roster**) assigned their thread handle outside the mutex
  guarding their stop flag, so a connection torn down while its control reader
  still ran could (a) skip the join and leave the pump **writing into freed
  memory for as long as the process lived**, or (b) double-join the handle
  (the NtClose crash). The chooser roster's dial-per-request pattern — dial,
  LIST, deinit, on every row visit — armed exactly that window on every
  chooser interaction, and each dial's roster subscription started a fresh
  pump. Heap corruption in the agent then killed *later* connections
  unpredictably, which is why the Kill's freshly-dialled connection died
  mid-RPC and why the error was `ConnectionClosed` on some runs and
  `Timeout` on others.
- **`0b295032c` (2026-08-05, T183)** removed a second corruption on the same
  close path: every persistence module staged atomic writes at a fixed
  `<path>.tmp`, so `handleCloseSession`'s `persistMeta` racing the child-exit
  watcher's save could tear `sessions.json`.

With both in, the failure no longer reproduces: **5/5** runs of the remote
Kill land the refetch (`loaded 1 session(s)` + `close session confirmed=true`).

What this task shipped on top:

- `test/win32/chooser-sessions-remote.ps1` asserts the post-Kill refetch
  (work item 4): the deliberate `NOTE T328` became
  `E the post-Kill refetch lands and is down to 1`. The wait counts from a
  baseline taken **before** the confirm's Enter — the first draft used a fixed
  ordinal and matched a pre-Kill `loaded 2` line (earlier steps log the same
  line), a false red worth remembering.
- The script now captures the remote agent's stderr
  (`-RedirectStandardError`) and, on a refetch failure, prints agent liveness
  plus both logs' tails — the witness this investigation initially lacked.
- The stale `NOTE (T328)` comment in `SessionRoster.zig` now records the real
  cause and keeps the do-not-retry warning (the unbounded
  `relay_dial.dial` wedge) for the next reader.

T326 (agent replies to CLOSE_SESSION only after terminate+persist) is real and
stays open — it is a latency defect on the same RPC, not this connection
killer.

## Validation

- `test/win32/chooser-sessions-remote.ps1` with the NOTE turned back into an
  assertion, ALL PASS, plus its `-NegativeControl` run.
- `test/win32/chooser-sessions.ps1` (the local Kill) must stay ALL PASS.

## Validation criteria

- [x] `chooser-sessions-remote.ps1` ALL PASS with the refetch asserted — 5
      consecutive runs on box (runs 1-5, 2026-08-06; run 1's red was the
      assertion's own fixed-ordinal bug, its log shows `loaded 1` +
      `confirmed=true`)
- [x] `chooser-sessions-remote.ps1 -NegativeControl` ALL PASS (13) on box
- [x] `chooser-sessions.ps1` (local Kill) ALL PASS (17) on box
- [x] Floor: `floor-lane.ps1 -Lane all` — none PASS (159s), win32 PASS (180s),
      agent PASS (190s), ALL LANES PASS (2026-08-06 ~03:08)
- [x] P1-P3 acceptance — all three `ALL PASS`, run sequentially after the
      lanes (2026-08-06)

## Progress log

- 2026-08-06 02:49: claimed; work starting.
- 2026-08-06 02:53 [session 7c171ee0-fe7d-4a82-bd15-36dae1152d14]: Read both sides: handleCloseSession unlinks before terminate (no obvious UAF via onChildOutput); client RPC bound 5s; ConnectionClosed arrives faster than heartbeat math allows, so prime suspect is the agent dying/wedging during freeUnlinked on a bridged session. Next: capture agent stderr in chooser-sessions-remote.ps1 and check agent liveness after Kill.
- 2026-08-06 02:59 [session 7c171ee0-fe7d-4a82-bd15-36dae1152d14]: Root cause identified as already-fixed: 449a112b4 (per-connection push pumps - incl. the roster pump every chooser dial starts - could outlive their connection and write freed memory; double-join NtClose crash) corrupted the agent, killing later connections unpredictably = the ConnectionClosed/Timeout mix. T183's 0b295032c removed a second concurrent-persist corruption on the same close path. Validated: turned the NOTE back into a real assertion (baseline-counted; the old fixed-ordinal wait matched a pre-Kill load), added agent stderr capture + liveness DIAG to the script. 5/5 remote runs ALL PASS, -NegativeControl PASS, local chooser-sessions PASS. Floor next.
