---
id: T1100
title: "The background test desktop cannot capture pixels or deliver real input, so scripts that need them are red for the environment"
order: null
deps: ["T1115"]
status: "done"
commits: []
seat: "win"
priority: "P1"
triage-reason: "Umbrella for a shared cause behind several sweep reds; until it is settled, each dependent script's redness cannot be attributed to the product."
tags: ["test", "infra"]
---

# T1100 - The background test desktop cannot capture pixels or deliver real input, so scripts that need them are red for the environment

## In plain terms

Automated UI checks now run on a hidden second desktop so they do not steal the mouse and keyboard while you work. That hidden desktop turns out not to support taking a picture of a window, and in at least one case not to accept synthetic keystrokes either. Several checks that look at colours or press keys therefore fail there no matter whether the app is correct, which makes them useless as a signal.

## Summary

The T1094 full sweep produced a cluster of reds that all sit on the same two capabilities rather than on any product behaviour.

**Pixel capture is dead.** `test-desktop-spike.ps1` is the spike written to measure exactly this, and it says so directly:

```
FAIL  PrintWindow(PW_RENDERFULLCONTENT) returns real content (meanLum=51, distinct=60)
```

`caption-bar.ps1` fails the same way from the other end:

```
Get-TestWindowPixels: the capture is UNIFORM - 3858x2118, one color over its
whole interior, still uniform after 11 attempt(s) across 2000ms.
```

and `split-dim.ps1` / `split-dim-viewer.ps1` fail with an EMPTY measured colour (`got ''`), which is the same absence one layer up.

**Real input may be dead too.** `context-menu-real-input.ps1` never got started:

```
SETUP FAIL (plain): could not take the foreground (a fullscreen app or an input lock owns it)
```

and `chooser-close-chord.ps1` fails its own POSITIVE CONTROL (`FAIL Ctrl+W closed one pane (2 -> 2)`), which is the shape of a chord that never arrived rather than a chord that was mishandled.

This is the decision this task exists to force: either the test desktop is made able to do these things, or the scripts that need them are marked as requiring the interactive desktop and the suite reports them as SKIP with a reason - never as a silent FAIL that reads like a product defect. What must not happen is leaving them red, because a permanently-red script teaches everyone to ignore the colour.

## Reproduced alone

This task has no red row of its own - it is the umbrella over what the desktop
cannot do. Its evidence script was re-run on its own on 2026-08-23
(`scripts\suite-run.ps1 confirm`, T1137):

- `test-desktop-spike.ps1` - **reproduced alone: yes** (`1 FAILED / 11 passed`).
  The capability gap is real and is not an artefact of the sweep.

## Where

- `test/win32/lib/TestDesktop.ps1`
- `test/win32/lib/` pixel helpers (`Get-TestWindowPixels`, ChromeGeometry)
- `test/win32/test-desktop-spike.ps1` - the measurement
- `scripts/suite-run.ps1` - where a requires-interactive script would be classified

## Goals

- [x] Settle the pixel question with evidence: whether a window on a non-interactive desktop can be captured at all (PrintWindow vs BitBlt from the desktop DC vs DWM thumbnail), and record the answer in the spike so nobody re-derives it. **Done by T1115, 2026-09-02** - the answer is a chrome/surface split, recorded in the spike's header and in the section above.
- [x] Settle the input question the same way - whether SendInput can reach a window on a background desktop given the right desktop/window-station attachment. **Answered, and it was already measured**: `test-desktop-spike.ps1`'s INPUT-1 asserts SendInput is ACCESS_DENIED there (0 of 12 events accepted), with the posted-message replacement landing beside it, and no desktop/window-station attachment changes that. What was missing was not the measurement but the DECLARATION, which is the next goal.
- [x] Whatever the answers, give the harness a declared capability check, and have scripts that need a missing capability report SKIP with the reason rather than FAIL. **Done 2026-09-02** - `test\win32\lib\DesktopCapability.ps1`; `context-menu-real-input.ps1` and `profile-latency.ps1` wired.
- [x] Make `scripts\suite-run.ps1` count those separately from `fail`, so the suite's colour stays meaningful. **Done 2026-09-02** - a `skip` verdict kind, its own count, its own table, and no confirm-pass re-run.
- [x] Re-run the dependent scripts and re-attribute each red: environment or product. The two whose cause is settled are re-attributed here (both ENVIRONMENT, both now SKIP); the three whose cause is not are filed as T1282 (caption-bar), T1283 (split-dim / split-dim-viewer) and T1284 (chooser-close-chord) rather than guessed at.

## Validation criteria

- [x] `test\win32\test-desktop-spike.ps1` is ALL PASS, or states the measured limit as a passing documented finding - done by T1115 (2026-09-02): the spike measures all three capture routes by region and passes expressing the finding.
- [x] the capability table is asserted, and its SKIP path is DEMONSTRATED firing rather than assumed - section Z of `test\win32\test-desktop-harness.ps1`, ALL PASS (44 assertions): it forces a capability missing via `GHOZTTY_TEST_FORCE_MISSING_CAPS` and checks the SKIP ALL line, the exit code, and that the run really stopped.
- [x] the runner scores a skip apart from a pass AND apart from a failure - section S of `test\win32\suite-run.ps1`, ALL PASS (97 assertions): S1 the kind, S2/S3 a skip does not redden a run, S4/S5 it is counted and named, S7 the confirm pass leaves it alone, S8 a real failure still reddens a run that also skipped.
- [x] the two dependent scripts whose cause is settled report SKIP with the capability named - `context-menu-real-input.ps1` and `profile-latency.ps1`, both driven through the missing-capability path, both ending `SKIP ALL: real-input is not available here - ...` at exit 0.
- [ ] a full `suite-run.ps1` pass reports zero FAILs caused purely by the desktop the run happened on - not asserted here: the three unattributed reds are T1282/T1283/T1284, and that claim belongs to the sweep that follows them.

## The pixel answer, settled (T1115, 2026-09-02)

The spike now measures all three capture routes BY REGION and is ALL PASS
expressing the finding. The answer is a SPLIT, and the split is what this task
needs:

| route | chrome band | terminal surface |
|---|---|---|
| BitBlt off the desktop DC (== `CopyFromScreen`) | returns FALSE - nothing at all | - |
| `PrintWindow(PW_RENDERFULLCONTENT)` | mean 79, **103 distinct** | mean 41, **2 distinct** |
| `PrintWindow(flags=0)` | mean ~80, ~100 distinct | mean ~40, ~2 distinct |

The interactive-desktop control is what makes it conclusive. Same build, same
call (PrintWindow on the top-level window, the child's rect sampled out of that
bitmap), same sampler: over the **surface rect** it returns **102 distinct**
colors on the interactive desktop against **1-2** on the background one. So the
flat surface is DESKTOP-dependent rather than an unconditional property of
PrintWindow, and `PW_RENDERFULLCONTENT` is not the deciding factor either -
`flags=0` reads identically on both.

This does not overturn `lib\TestDesktop.ps1`, which is the source of truth and
already carried the operative conclusion (its CAPTURE LIMIT, 2026-07-30, and
T303's "PrintWindow sees GDI, not composition"). It measured PrintWindow on the
GhozttyTerminal **child** hwnd - a different call from the top-level capture
above - and both agree on what matters here: on a background desktop the
terminal surface cannot be asserted. What T1115 actually fixed is that the spike
had never been reconciled with that finding and spent a month asserting the
answer the library had already revised.

**What that means for the dependent scripts:**

- A probe asserting **terminal content** pixels (glyph colors, cursor,
  selection highlight - anything the renderer draws into the terminal surface)
  **cannot run on a background desktop**. There is no capture route; all three
  were measured. `split-dim.ps1` and `split-dim-viewer.ps1` are in this class -
  their empty measured color is this flat surface one layer up.
- A probe asserting **chrome** pixels (title bar, tab strip, banner, the
  window's own painted controls) **can** run there, via `PrintWindow` into a
  memory DC - it just cannot use `CopyFromScreen`. `caption-bar.ps1` is in this
  class, so its "capture is UNIFORM" is a fixable helper problem
  (`Get-TestWindowPixels` reaches for `CopyFromScreen`), not a dead end.

So the capability this task has to declare is not one flag but two:
`chrome-pixels` (available via PrintWindow) and `surface-pixels` (unavailable).
Note also that the earlier reading of this spike was not wrong about the box -
it was scoring whole-window brightness, an oracle that cannot tell "chrome over
a flat client area" from "the whole window", which is why it read green in July
and red later with no behavior change in between.

## Progress log

- 2026-09-02: T1115 settled the pixel question - see the section above. The
  answer splits by region: chrome IS capturable on the background desktop via
  PrintWindow, the terminal surface is NOT, and CopyFromScreen is dead there
  outright. The interactive-desktop control makes it conclusive. Goal 1 of this
  task is met; the remaining goals (the capability declaration, the SKIP
  classification, suite-run's separate count, re-attributing each red) are
  unchanged and now have the fact they were waiting on.
- 2026-08-22: filed by T1094's full acceptance sweep (242 scripts, forward order, Debug build at 9a764deea). Evidence quoted above is from that run's log in `temp\suite-runs\fwd1\`.
- 2026-09-02 07:29: claimed; work starting.
- 2026-09-02 07:57: built the capability declaration: test\win32\lib\DesktopCapability.ps1 (5 named capabilities, background answers from the measured facts, the two input answers probed on the box), Assert-TestDesktopCapability + Exit-TestSkip printing SKIP ALL at exit 0, suite-run scoring a new 'skip' verdict kind counted apart from pass and fail. Wired context-menu-real-input.ps1 and profile-latency.ps1 (both re-attributed ENVIRONMENT). Acceptance: section Z of test-desktop-harness.ps1 (44 assertions, forces the missing case) and section S of test\win32\suite-run.ps1 (97 assertions). Filed T1282/T1283/T1284 for the three reds whose cause is not settled.
- 2026-09-02 08:19: floor green: four zig lanes ALL LANES PASS, P1 (25) / P2 (20) ALL PASS, P3 ALL PASS (16) on its own - P3 was 9-red when run third in one shell and filed as T1285 (order-dependent, not this change: nothing here touches product code or any lib P3 sources). Guards: test-desktop (44), suite-run (97), and the seven audits guard-due named all green; only the two pre-existing advisories remain.
- 2026-09-02 08:21: status: in-progress -> done

## The input answer, settled (2026-09-02)

Nothing new had to be measured. `test-desktop-spike.ps1` already asserts it, as
INPUT-1:

```
SendInput is blocked on a background desktop (accepted=0, err=5 = ACCESS_DENIED)
```

with the replacement asserted beside it - posted WM_KEYDOWN/WM_KEYUP over an
AttachThreadInput-shared input queue, modifiers set with SetKeyboardState,
chords included. No desktop or window-station attachment changes the answer: the
block is that the desktop is not the INPUT desktop, which is the whole point of
running there.

So the gap this task existed to close was never the measurement. It was that
nothing DECLARED the fact, so a script needing real input failed on a background
desktop for a reason that had been known for a month, and that failure was
indistinguishable from a broken feature.

## What landed (2026-09-02)

`test\win32\lib\DesktopCapability.ps1` - five named capabilities
(`chrome-pixels`, `surface-pixels`, `screen-pixels`, `real-input`,
`foreground`), each with the answer for a background desktop and for the input
desktop, and each answer carrying the REASON a skip line is built out of. The
background answers are the measured facts above; the two input answers are
PROBED on the box, because an input lock can take real input away on the
interactive desktop too - which is precisely the second half of the sweep's
reds.

Two entry points: `Assert-TestDesktopCapability` at the top of a script, and
`Exit-TestSkip` for a capability that can only be discovered during setup (the
foreground grab that comes back false after the run has started). Both print
`SKIP ALL: <capability> is not available here - <reason>` and exit 0.

`scripts\suite-run.ps1` scores that as a new verdict kind, `skip`: not a pass,
not red, counted on its own line, listed by name with its reason, and never
re-run by the confirm pass - which exists to separate a product defect from an
isolation artefact, and a skip poses neither question.

Re-attributed, both ENVIRONMENT: `context-menu-real-input.ps1` (its
`SETUP FAIL ... could not take the foreground`) and `profile-latency.ps1` (its
`ABORT: cannot acquire focus`) now skip with the capability named.
