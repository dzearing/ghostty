---
id: T332
title: "+list --json never reports which agent session a pane is bound to"
order: 127
deps: []
status: "in-progress"
priority: "P1"
milestone: "M1"
triage-reason: "no CLI way to join a pane to its agent session; an agent scripting a layout across a restart cannot map them"
commits: []
---

# T332 - +list --json never reports which agent session a pane is bound to

## In plain terms

Ghoztty can keep a terminal's shell alive even when the app is closed, and
lets you list those background sessions. But until now there was no way to
ask, from a script, "which window pane is showing that session?" — the panes
and the sessions were listed by two different commands that shared no common
key. Now `ghoztty +list --json` reports each pane's session id, so a script
(or an AI agent rebuilding its layout after a restart) can match panes to
sessions directly.

## Goals

- [x] `+list --json` reports a `session_id` on every agent-backed terminal pane
- [x] Plain panes and viewers omit the field entirely (no noise, old parsers safe)
- [x] The reported id matches the `+sessions --json` row for that session

## Summary

A pane backed by the session-persistence agent knows its session id
(`Surface.remote_session_id`, set from the ATTACH/OPEN override and read back by
`core_surface.remoteSessionId()`), but nothing on the CLI ever says it.
`+list --json` reports the pane id, type, title and — for viewers — the url;
`+sessions --json` reports the session and whether SOMEONE has it attached. The
join between the two lives only inside the app.

The consequence is that the obvious question has no CLI answer: *which pane is
this session open in* / *which session is this pane on*. T320's own validation
line assumed `+list --json` could report it and had to fall back to
`+sessions`'s `attached` flag plus an app log line.

Concretely, without it:

- a resume/attach test can prove a session became attached, but not WHICH pane
  took it (two panes attaching in the same second are indistinguishable);
- `session already open, focusing its pane` (T320's short-circuit) is only
  assertable through a log line, which is the app talking about itself rather
  than an independent reading;
- an agent scripting a layout across a restart cannot map its panes back to the
  sessions it knows by id.

## The work

Add the session id to `+list --json`'s terminal-pane object — absent when the
pane is a plain local ConPTY (no session), present for local-agent and
cross-machine panes alike. Consider `attached`-side symmetry: `+sessions --json`
could carry the pane id when the session is open in THIS app.

Keep the human tree unchanged unless there is a reason; the id is script data.
Additive fields only — `+list --json` is consumed by scripts on this box.

## Validation criteria

- [x] Unit: the serializer emits `session_id` when set and omits it when null
      (golden test `List: session_id is additive (T332)` in
      `src/apprt/ipc/list.zig`; the four pre-existing golden tests double as
      the omit-when-null case — none emit the field)
- [x] On box: a persistent pane's `session_id` from `+list --json` matches an
      `alive` row in `+sessions --json` (new acceptance script
      `test\win32\ipc-list-session-id.ps1`, ALL PASS — the row is also
      `attached`)
- [x] On box: a viewer pane reports no `session_id` field (same script,
      section 3)
- [x] Floor: `floor-lane.ps1 -Lane all` → none/win32/agent all PASS; P1–P3
      ALL PASS, run sequentially after the lanes

## Validation

- Unit: the serializer emits the field when set and omits it when null.
- On box: open a persistent pane, read its id from `+list --json`, and match it
  against the `+sessions --json` row that reports `attached`.

## Progress log

- 2026-08-06 23:23: claimed; work starting.
- 2026-08-06 23:25 [session e1f81a35-1e9f-480f-8f1f-d0c10e40b07c]: design: additive session_id field on List.Terminal (src/apprt/ipc/list.zig), emitted only when non-null; win32 buildNode fills it from core_surface.remoteSessionId() (lock-free, duped into arena — T111b no-terminal-lock rule holds). CLI parses with ignore_unknown_fields so old CLIs are safe. +sessions-side symmetry (pane id on the agent row) needs agent protocol awareness — assessing for a follow-up task.
- 2026-08-06 23:38 [session e1f81a35-1e9f-480f-8f1f-d0c10e40b07c]: built: session_id field added to List.Terminal + golden unit test; win32 buildNode fills it from remoteSessionId(). Filed T552 (+sessions-side pane_id symmetry, P2) and T553 (Mac server half, seat mac). CLAUDE.md +list section documents the field. Floor lanes running.
