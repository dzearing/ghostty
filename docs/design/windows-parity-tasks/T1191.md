---
id: T1191
title: "A win32 module\u0027s unit tests can sit un-run forever because nothing checks the lane\u0027s hand-written import list"
order: null
deps: []
status: "in-progress"
commits: []
seat: "win"
priority: "P1"
triage-reason: "P1: found by accident during T1177 - a module's tests were compiled and never run, and the only reason anyone noticed was a deliberately broken assertion. Every other win32 module missing from the list has the same silence, and nothing measures how many."
tags: ["test", "infra"]
---

# T1191 - A win32 module's unit tests can sit un-run forever because nothing checks the lane's hand-written import list

## In plain terms

Some of the tests in this project never run, and nothing says so. They compile,
they look like tests, they sit in the file next to the code they check - and
the test suite quietly skips them, so the code they cover is as untested as
code with no tests at all, while reading as covered.

## Summary

`src/apprt/win32.zig` has a hand-written `test { _ = @import("win32/X.zig"); }`
block, and reachability through THAT CHAIN of test-block imports - not the
ordinary import graph - is what pulls a win32 module's tests into the lane. A
module can be imported and used by `App.zig`, compiled by every lane, and still
have every one of its `test` blocks silently skipped.

The evidence, from T1177: `src/apprt/win32/startup_error.zig` was imported and
used by `App.zig` and carried five unit tests. A deliberately broken assertion
inside one of them was run through `zig build test -Dapp-runtime=win32` -
`-Dtest-filter` and the FULL lane both - and both went **green**. Adding one
line to the list in `win32.zig` made the identical tree go red, naming the
test. So the failure mode is not theoretical and it is not loud: the only
reason it was caught is that the test was deliberately broken first, which
nobody does routinely.

**Measure it correctly, or the number will be wrong.** A naive "has tests but
is not named in `win32.zig`'s list" grep reports ~130 files and is
MISLEADING: a listed module carries its own `test { _ = @import(...) }` block,
so plenty of unlisted files are reached transitively and do run - the same lane
run that went green on the broken assertion also reported a failure inside
`claude_plugin_migration.zig`, which is not on the list. The real question is
reachability through the test-block chain, and the sweep has to compute it.

Two things to build:

1. **A sweep that fails.** Compute the set of files reachable from the lane's
   test roots through `test { _ = @import(...) }` blocks, transitively;
   enumerate every `src/apprt/win32/*.zig` that contains a `test` block; and
   FAIL on the difference. It belongs in the `none` lane as a unit test if it
   can read the tree from there, otherwise as an acceptance script beside the
   other meta-audits (`asserted-nothing`, `body-complete`), with a negative
   control that scores red on a file removed from the chain.
2. **The backlog it finds.** The count is unknown until the sweep runs, and
   every file it names is a module whose tests have never executed. Each one
   gets its tests actually run, and anything they turn up gets filed.

Worth deciding while building it: whether the list stays hand-written with a
sweep behind it, or whether the block is generated. The list carries real
commentary today (several entries exist to force a caller-less module to
COMPILE, and say so), which a generated block would lose - so the sweep is the
recommended shape and the comments stay.

## Validation criteria

- [x] A sweep enumerates every win32 module with tests and every module the
      lane actually reaches, and fails on the difference.
      `test/win32/test-reach-audit.ps1` section B, ALL PASS: "174 win32
      modules carry tests; 174 of them run in the lane". It does NOT model
      Zig's reachability - see below.
- [x] Its negative control: removing one known-good entry from the list makes
      the sweep go red, naming that file. Section C (`-TeethCheck`): it
      deletes `_ = @import("win32/provenance.zig");`, rebuilds, requires the
      sweep to name `provenance`, and restores the file byte for byte. C3
      asserts the wounded lane still PASSES, which is the defect itself.
- [x] The current gap is reported as a NUMBER: **4 modules, 16 tests, never
      once executed** - `AgentIntegrationsDialog` (8), `ipc_agent_integration`
      (3), `provenance` (2), `restore_retry` (3).
- [x] Every module the sweep named has its tests running and green: all four
      are listed in `src/apprt/win32.zig` now and all 16 tests pass in
      `zig build test -Dapp-runtime=win32`. Nothing they turned up needed
      filing - they were correct, merely unexecuted.

## What was actually built, and one thing that was tried and rejected

The sweep asks the COMPILER rather than modelling it. Zig names every test
after the file it came from (`apprt.win32.<module>.test.<name>`), those names
are string data in the test binary, and a test that was never analyzed
contributes no name - so scanning the binary the lane just ran answers the
question exactly.

The first draft did model it, in PowerShell, walking `test { _ = @import(...) }`
blocks from `src/main.zig`. It scored **174 of 174 modules unreachable**,
because `src/main.zig` references its entrypoint by IDENTIFIER
(`test { _ = entrypoint; }`), not by a literal `@import`, and
`refAllDecls(@This())` is a third shape again. A model that drifts from the
compiler fails in exactly the way this task exists to stop, so it was thrown
away.

The negative control found a second thing worth knowing: wounding
`ConfirmDialog.zig` - the oldest entry in the list - did NOT make the sweep go
red, because it is also reached transitively through another listed module's
test block. Several entries in that list are redundant and there is no way to
tell which by reading it. The control therefore uses `provenance.zig`, whose
absence from the binary was measured before its line existed.

Staleness is covered by a `guard-due` row (`test-reach`) that covers
`src/apprt/win32.zig` AND `src/apprt/win32/*.zig`, so a NEW win32 module
with tests makes the guard due and the sweep names it.

## Progress log

- 2026-09-01 02:18: claimed; work starting.
- 2026-09-01 03:04: measured: the oracle is the compiled test binary (zig names every test apprt.win32.<module>.test.<name>; an unanalyzed test contributes no string). 174 win32 modules carry test blocks; 170 were in the binary. THE NUMBER IS 4 - AgentIntegrationsDialog (8 tests), ipc_agent_integration (3), provenance (2), restore_retry (3) = 16 tests that had never executed. All 16 pass once listed. A PowerShell model of Zig reachability was tried first and scored 174/174 unreachable (src/main.zig references its entrypoint by identifier, and refAllDecls is a third shape) - rejected in favour of asking the compiler. Sweep: test\win32\test-reach-audit.ps1 + lib\TestReachAudit.ps1, guard-due row test-reach covering src\apprt\win32.zig and src\apprt\win32\*.zig.
- 2026-09-01 04:04: validated: test-reach-audit.ps1 ALL PASS (174 of 174) and -TeethCheck ALL PASS (18 assertions, C2 names provenance, C3 confirms the wounded lane still passes). Floor lib/none/win32/agent ALL LANES PASS; ipc-p1/p2/p3 ALL PASS. Six guards its new file made due re-run and re-stamped; stderr-capture caught the sweep's own zig --verbose redirect (stderr formatted as host-width ErrorRecords on the way to a file, then read back as a text oracle) - moved to cmd, T883's proven-good shape.
