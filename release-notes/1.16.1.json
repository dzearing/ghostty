{
  "version": "1.16.1",
  "sections": [
    {
      "title": "Fork Changes",
      "items": [
        {
          "title": "Fixed an intermittent `ghoztty +sessions` crash",
          "text": "Listing your persistent sessions could occasionally crash (SIGSEGV/SIGBUS) on short-lived CLI runs, because the command shared a non-thread-safe memory arena with the background agent-connection threads. Every allocation for the command is now serialized, so `+sessions` is reliable."
        }
      ]
    }
  ]
}
